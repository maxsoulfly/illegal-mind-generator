# AGENTS.md

# Illegal Mind Generator

Local-first React app for generating and managing YouTube content packaging for two music channels. Generates titles, hooks, thumbnails, descriptions, hashtags, and tags. Also manages saved entries, tag library, upload queues, todo tracking, and project settings.

---

# Projects

**Illegal Mind Covers** — post-apocalyptic SIGNAL series. Transformation-based (Darker, Heavier, Hardcore, etc.). Heavy lore and worldbuilding.

**Maxx Dee Covers** — traditional faithful covers. Modernized recordings, performance-focused. Less lore.

---

# Tech Stack

React · JavaScript · Vite · localStorage · JSON config. No backend, no database, no API.

**This line is being actively migrated away from as of 2026-08-31 — do not treat it as settled.** A migration to Neon/PostgreSQL + a thin hand-built API layer is underway (all architecture decisions resolved, a 14-step dependency-audited migration sequence in progress), driven by a real product problem (the user works across multiple PCs; the only sync mechanism today is manual Backup export → Telegram → import) — not primarily for AI convenience, though that's a side benefit. **Step 0 (DB + API foundation) is done**: a local Express server lives in `server/` (started alongside the frontend via `npm run dev`), talking to a real Neon Postgres **dev branch** (never production) over a static-API-key-authenticated `/health` endpoint — verified working end to end. No app data has moved yet — `src/` is untouched, the app still reads/writes only localStorage. Full assessment, all decisions, the verified step-by-step sequence, and exactly which step is next: `C:\Users\Max\.claude\plans\one-signal-many-terminals.md` (see also `docs/current-context.md`'s In Progress section). Read that file before assuming this app is still purely local-first, and before dismissing DB-related work as against project principles.

---

# Development Environment

`npm run dev` is always running. Vite live reload is the workflow. Do not suggest restarting unless there is a real reason.

**As of 2026-08-31, `npm run dev` runs two processes together** (via `concurrently`, labeled `client`/`server`): the Vite frontend (`dev:client`) and a local Express API server (`dev:server`, `server/index.js`) — part of the Postgres migration in progress, see the Tech Stack pointer above. `server/.env` (git-ignored, one per machine — not synced via git) must exist with `DATABASE_URL`/`API_KEY`/`PORT` before `npm run dev` will start cleanly; copy `server/.env.example` and fill in real values (see the persistence plan for where those values come from). If an already-running `npm run dev` predates this change, it's still on the old Vite-only script — restarting it is a real reason, not a casual one.

Workflow: Discuss → Review architecture impact → Small grouped steps → Test → Commit → Continue.

---

# Core Architecture

Config-driven. `src/config/projects.json` + localStorage overrides = resolved project config. The resolved config is the source of truth for all generation.

```
src/
├── components/   UI and feature components
├── config/       projects.json
├── engine/       generation logic (no React)
├── hooks/        custom React hooks
├── pages/        page-level orchestration only
└── utils/        storage helpers
```

**Rules:** Pages orchestrate. Feature logic in feature components. Generation in `src/engine`. Storage in `src/utils`. No generation logic in components. No UI logic in engine files.

---

# Major Systems

- **Generator** — main workflow. Inputs: artist, song, signal number, video type, tags, notes. Outputs: titles, hooks, thumbnails, descriptions, hashtags, tags.
- **Saved Library** — per-project song database. Search, sort, import/export, load into generator.
- **Tag Library** — transformation tag management. Editable phrases, hashtags, hooks, visibility. Deeply connected — tag changes affect titles, descriptions, hooks, hashtags.
- **Shorts Queue** — upload planning. ~20-item queue with randomization, duplicate spacing, exclusions.
- **Upload Calendar** — Phase 1 in progress (see Current Focus). Month-grid view of per-project upload schedule (`projects.json` `uploadSchedule.slots`, weekday+type+time). Each slot independently tracks `plannedEntryId`/`uploadedEntryId`; status (Planned/Missed/Uploaded/Uploaded-differs) is always derived, never stored. Short slots pick exclusively from the Shorts Queue; Long slots search the full saved library.
- **Todo System** — cover planning. Statuses: Wishlist, Needs Re-record, Needs Remaster, Needs Video.
- **Backup System** — full export/import. Must remain reliable — it's the safety net for storage work.
- **Content Setup** (was "Project Settings", `activePage` id still `projectSettings`) — project-level content/config, 4 sections: **Generation** (Titles / Short Hooks / Thumbnails / Hashtags — a drill-down overview -> editor), **Descriptions** (Long / Shorts / Lists / Text / Hook Blocks / Groups / Placeholders / Links, one workspace with a flat leaf strip — Long/Shorts are first-class leaves, not a nested sub-choice), **Workflow** (Shorts Queue + Todo Statuses + Upload Schedule, one page), **Project** (name/id + app-wide Backup). Nav model: `src/config/contentSetupNav.js` + `ProjectSettingsPage.jsx`.
- **UIKit** — living component catalog at the `/uikit` nav route. Browse before building anything new. Covers all reusable UI primitives with interactive examples.

---

# Storage Architecture

**Main key:** `illegalMindGeneratorData`

```js
{
  version,
  savedEntries,        // [projectId] → entries array
  tagOverrides,        // [projectId][tagId] → partial override
  tagVisibilityOverrides, // legacy — do NOT remove
  shortsQueues,        // [projectId] → queue array
  uploadCalendar,      // [projectId][slotKey] → { plannedEntryId, uploadedEntryId }, slotKey = `${isoDate}|${videoType}`
  ui,                  // activePage, selectedProject, panelVisibility, projectSettingsSection
  generator            // formData
}
```

**Rules — these matter:**
- All live app state now reads/writes `illegalMindGeneratorData` exclusively (unification complete, cleanup pass done 2026-08-04 — see Current Focus). The standalone legacy localStorage keys no longer exist; each hook does its own one-time fallback-and-migrate read for any user still on old data, but writes go only to the unified key.
- `tagVisibilityOverrides` as a *field inside* the unified object must stay — that's a data-shape compatibility concern, separate from the now-dead standalone legacy key of the same name.
- No automatic migration — migration is intentionally paused
- Storage changes are high risk: backup → verify shape → add safe fallbacks → test export/import → test refresh persistence
- When migrating a hook off a legacy key, don't assume a populated unified field is authoritative — it may be a stale one-time snapshot. Verify which source actually has live data before preferring it.

---

# UI Primitives (`src/components/ui/`)

- `TemplateGroupCard` — generic collapsible card: label + reset + optional subtitle/slider + `children`. The template list (count line + `HookTemplateEditor`) only renders when `onUpdateTemplates` is passed — omit it to use the card as a plain content shell (see `ProjectSettingsGeneral`'s Project Info/Actions/Backup cards). Optional `placeholders` prop passes through to `HookTemplateEditor` (see below); omit it to keep the default `HOOK_PLACEHOLDERS` autocomplete. The `subtitle` line renders via `.tag-card-subtitle` (dim `--color-text-muted-3`, deliberately distinct from `.tag-category` — that class is for short high-contrast category/section-header labels elsewhere, e.g. `TagHeader.jsx`; reusing it for long helper sentences read too loud). Base for `ShortHookCard` and description cards.
- `ShortHookCard` — adapter over TemplateGroupCard for hook-specific data shape (`hookConfig` object + `hookType`). **TODO:** evaluate collapsing adapter once hook data shape is refactored.
- **TODO:** extract `CardHeader` component (h3 + reset button + optional remove button + count badge) — then refactor TemplateGroupCard and ShortHookCard to use it.
- `HookTemplateEditor` — searchable template list with add/bulk/highlight+scroll support. `noWrapper` prop skips the `<details>` collapse wrapper (used by Hook Blocks tab where the parent card already collapses). The row `.map()` is wrapped in `.phrase-row-list` (shared with `TagPhraseEditor`): past ~5 rows it scrolls internally (`max-height: calc(var(--phrase-row-h) * 5)`, `src/index.css`) so a big pool can't make the card arbitrarily tall — search stays above it; +Add/+Bulk/Copy AI Prompt and the Bulk textarea are siblings below it. Optional `placeholders` prop (default `HOOK_PLACEHOLDERS`) controls the `{placeholder}` autocomplete — pass `[]` to disable it entirely (literal-text fields like Thumbnail words/fallbacks) or a smaller list like `THUMBNAIL_TAG_PLACEHOLDER` (`src/utils/hookPlaceholders.js`) for fields that only support one substitution.
- `SubTabNav` — lightweight tab nav (underline active, no pill borders). Takes `tabs: [{id, label}]`. Scrolls horizontally instead of wrapping when tabs don't fit the container (`overflow-x: auto`, `flex-wrap: nowrap`) — this matters in narrow contexts like a `tag-library` grid cell, not just small viewports. Used by the Content Setup > Descriptions leaf strip (Long/Shorts are leaves on it now — the old inner Long/Shorts sub-choice is gone) and `DescriptionLayoutBoard`'s mobile [Layout | Available] column switcher (`display:none` on desktop). (The 4 Content Setup section chips are `.tag-filters` buttons, not `SubTabNav`; the Tag Library card is a drill-down.)
- `NavLinkButton` — clickable text for source navigation. `muted` prop for base hooks.
- `PhraseRow` — forwardRef row, onBlur save, `highlighted` prop for scroll target. Optional `placeholders` prop enables the `{placeholder}` autocomplete on its field (see `PlaceholderField`); used by `HookTemplateEditor` (project-level hooks) and `TagShortHooksTab`/`TagPhraseEditor` (per-tag hooks) via the shared `HOOK_PLACEHOLDERS` in `src/utils/hookPlaceholders.js`. `TagPhraseEditor`'s Titles tab passes the narrower `LIVE_PLACEHOLDERS` instead (see `PlaceholderField`'s gotcha below) — Thumbnails now gets the full set (real freeze support added 2026-08-13); Descriptions/Hashtags tabs still don't pass any.
- `PlaceholderField` — input or textarea (`multiline` prop) with a `{placeholder}` autocomplete dropdown: typing `{` starts tracking a query, filters the passed-in `placeholders` array (braces included, e.g. `'{artist}'`), arrow keys + Enter to select, Escape/click-away to dismiss, inserts at cursor. The dropdown `<ul>` is portaled to `<body>` as `position: fixed` (anchored to the field rect captured on the last keystroke — refs can't be read during render) so it escapes `.phrase-row-list`'s `overflow` clip, and it dismisses on any scroll/resize (capture-phase listener). Two save modes — `onBlur` (commit only on blur; `PhraseRow`/`TextBlockEditor`) or `onChange` (live, every keystroke and every insert; `ToggleInputRow`'s prefix/suffix fields, which are fully-controlled). Resyncs on external `defaultValue` change (e.g. reset) without needing a remount. Each caller supplies its own contextually-correct placeholder list — Text Blocks: `artist`/`song`/`year`/`tagLine`/`links.*` (matches `generateCustomBlocks.js`'s `renderTextTemplate`); Hooks: `HOOK_PLACEHOLDERS` from `src/utils/hookPlaceholders.js` (matches `generateShortHooks.js`'s `fillHookTemplate`); Title prefix/suffix: `{num}` only (matches `generateTitles.js`); Tag Library's Titles tab and Project Settings' Thumbnail Patterns: `LIVE_PLACEHOLDERS`/`[]` respectively — Titles' tag phrases have no pick/render freezing yet (see Known Gotchas), Patterns is a keyword field, not display text. Every other thumbnail phrase field (Tag Library's Thumbnails tab, and Words/Fallbacks/Generic Tag Templates) gets the full `buildHookPlaceholders()` set, same as Short Hooks. Keep these in sync if the underlying engine substitutions change.
- `ToggleInputRow` — checkbox + label + input (clicking label toggles checkbox). Input is a `PlaceholderField` in `onChange` (live) mode; optional `placeholders` prop enables the autocomplete (used by Title prefix/suffix fields).
- `LabelInputRow` — label + input, `compact` mode via `form-input--compact`
- `LabelSliderRow` — label + range slider + value display. Passes `--val` CSS custom property to the `<input type="range">` so the global slider theme (amber fill, amber thumb) works via a linear-gradient background.
- `AddBulkRow` — + Add / + Bulk button row
- `BulkTextarea` — textarea + Apply/Cancel. Input is a `PlaceholderField` (`multiline`, `onChange` live mode); optional `placeholders` prop enables the autocomplete (wired in `HookTemplateEditor`/`TagPhraseEditor`).
- `IconButton` — shared shell for every small action button, icon or text (reset ↺, remove ×, lock 🔒/🔓, move ↑/↓, add +, or labeled buttons like "+ Add"/"Apply"/"Cancel"/"Duplicate Project" — `icon` is just rendered as children, no icon required). Takes `icon, title, onClick, disabled, stopPropagation, className`. `onClick` is optional (no-op if omitted — used by not-yet-wired placeholder buttons). Default `className` is `tag-reset-button`; pass `button-secondary` (or another) to reuse the click/disabled wiring with a different look.
- `MoveControls` — up/down reorder button pair built on `IconButton`. Used by list items and description block reordering; pass `className` for context-specific layout (see `.desc-block-move-controls`).
- `SortableActiveBlock` (`src/components/projectSettings/descriptions/SortableActiveBlock.jsx`) — dnd-kit-aware row shell for any drag-reorderable list of blocks: drag handle (`⠿`) + `MoveControls` + `BlockInfoCard`, all three always together. Generic props (`id`, `label`, `onRemove`, `onNavigate`, `disabledUp`/`disabledDown`, `onMoveUp`/`onMoveDown`) — used by `LongDescriptionSettings.jsx`, `ShortsDescriptionSettings.jsx`, and `ProjectSettingsBlockGroups.jsx`'s per-group children list. Caller owns the `DndContext`/`SortableContext`/`onDragEnd` wiring (each has a different underlying array to reorder); this component only owns the per-row rendering, so it can't drift between call sites. Don't hand-roll the `.desc-block-wrapper` + `MoveControls` + `BlockInfoCard` combo for a new reorderable-block list — use this instead, even if that list doesn't need drag-and-drop yet (still gives the ↑/↓ buttons for free, matching the app's established row shape).
- `SavedEntryRow` — shared row shape (`saved-entry-row terminal-block` → signal number + artist/song title button + tags) used by `SavedLibraryItem`, `ShortsQueueItem`, `TodoItem`. `middle` slot for extra content between the main row and actions (Todo's note preview); `actions`/`actionsClassName` for the differing per-page controls. Optional `badges` slot (ReactNode, rendered inline right after the `tags`/`hidden` badges inside `.saved-entry-main`) — currently only `SavedLibraryItem` uses it, for the red `year`/`genre` missing-data indicators (`.saved-entry-missing-badge` CSS, built on `IconButton`, click copies a Google-search string via `src/utils/searchQuery.js`'s `buildSearchQuery`). Kept as a plain ReactNode slot (not a data+callback prop) so `SavedEntryRow` itself stays free of clipboard/copy logic — the caller owns what goes in it. Don't hand-roll this markup — extend the props if a new caller needs something different.
- `OutputItem` (`src/components/ui/OutputItem.jsx`) — generated output block: text + `CopyButton`, used by `HashtagsPanel`, `YouTubeTagsPanel`, `DescriptionsPanel`. Default `textClassName='output-text'` gives pre-wrap monospace styling (single-line output like hashtags/tags). `DescriptionsPanel` passes `textClassName={undefined}` + `textStyle={{whiteSpace:'pre-line'}}` — deliberately different styling for multi-paragraph descriptions, not an oversight, don't "fix" it to match the default. `copyText` lets the copied text differ from displayed text (Descriptions appends a footer via `renderCopyFooter()`; `HashtagsPanel`/`YouTubeTagsPanel` join their `{text,source}` entry arrays into the correct separator-joined string). `children` (falls back to `text` when omitted — `` {children ?? text} ``) lets a caller render structured per-item content instead of a flat string — `DescriptionsPanel` and `HashtagsPanel`/`YouTubeTagsPanel` both do this, mapping entries through their own per-item link component.
- `HashtagLink` (`src/components/output/HashtagLink.jsx`) — shared per-entry nav-link renderer for `HashtagsPanel`/`YouTubeTagsPanel`, modeled on `DescriptionLineLink`/`ThumbnailNavLink`'s branch-on-`source.type` pattern. Takes `entry: {text, source}` + `onOpenSourceTag`/`onOpenSourceHashtag`/`onOpenSongOverride`. `source.type`: `'tag'` (per-tag hashtag phrase or bare-tag-name fallback, `phrase:null` for the latter) → `onOpenSourceTag({tagName, field:'hashtags', phraseText})`; `'base'` (project preset pool, `card:'hashtagsBase'|'youtubeTagsBase'`) → `onOpenSourceHashtag({card, template})`; `'override'` (per-song `customHashtags` field) → `onOpenSongOverride({blockKey:'customHashtags'})`; no source (artist/song-derived dynamic tags) → plain non-interactive `<span>`. Default `className='hashtag-chip-link'` (CSS: transparent border, amber border+glow on hover, sized to a short inline token rather than `.description-block-link`'s full-width box).
- `CollapsiblePanel` (`src/components/ui/CollapsiblePanel.jsx`) — the `panel`/`panel-collapsed` + `panel-header` + `ToggleButton` shell shared by every Generator output panel (`HashtagsPanel`, `YouTubeTagsPanel`, `TitlesPanel`, `ThumbnailsPanel`, `ShortHooksPanel`, `DescriptionsPanel`). Takes `label, visible, onToggle, onNavigate?, headerExtra?, children`. `onNavigate` swaps the plain `<h2 className="panel-title">` for a clickable `panel-title--nav` button (Titles/Descriptions/Thumbnails link back to Project Settings). `headerExtra` renders between the title and the `ToggleButton` — currently unused by any panel (Titles moved its Hooks toggle out of the header into a body-level options row instead, see below); the slot still exists for a future header-level control. Plain `.panel` (no header/toggle — `ProjectSettingsPage`, `GeneratorPage`, `GeneratorResultsPanel`, `SavedLibrary`) is a separate, still-valid bare-wrapper usage — not every `.panel` needs to become a `CollapsiblePanel`. **Wrong choice for a low-frequency admin/utility action** — its full panel-header chrome gives secondary content the same visual weight as primary page content even while collapsed (a real case: Calendar's "Import from YouTube" originally used this, sat above the month nav as a permanent bordered bar, and visually competed with the calendar grid for top billing — see `Modal` below, which replaced it).
- `Modal` (`src/components/ui/Modal.jsx`) — this app's first overlay modal, added 2026-08-05 specifically for the case `CollapsiblePanel`/every inline-panel precedent doesn't fit: a trigger that must stay fully compact with zero permanent page footprint. Takes `title, onClose, children` — no visibility prop of its own, mount it conditionally (`{show && <Modal>...}`) same as every other panel in this app. Backdrop click and Escape both call `onClose`; clicking inside `.modal-content` doesn't propagate to the backdrop; body scroll locks while mounted (restores the previous value on unmount, not a hardcoded `''`). **Default to an inline expandable panel instead** (`AddTagPanel`, `CalendarEntryPicker`, `MissingDataTools` are all inline, not modal — this remains the norm) — only reach for `Modal` when the trigger genuinely can't afford any permanent footprint. One usage so far: Calendar page's "Import from YouTube" button opens `CalendarImportPanel` inside a `Modal`.
- `Toast` (`src/components/ui/Toast.jsx` + `src/hooks/useToast.js`, added 2026-08-06) — replaces `window.alert()` for fire-and-forget success/info messages (Add to Calendar, Sync Tags, Copy tag from another project). `useToast()` returns `{ toast, showToast }`; `showToast(message)` clears any pending auto-dismiss timeout via a `useRef` before scheduling a new one (3s), so a second toast triggered before the first clears doesn't get cut short by the earlier timer. Unlike `Modal`, `<Toast toast={toast} />` is always mounted (in `App.jsx`, at the root, sibling to the page switch) rather than conditionally rendered by the caller — it's cheap and returns `null` internally when there's no toast. `key={toast.id}` forces a remount (and CSS fade-in/out animation restart) on every new toast, including back-to-back ones with identical text. Composed with `.terminal-block` for the shared background/border look; CSS deliberately placed *after* `.terminal-block` in `index.css` so `.toast`'s `border-color` override isn't reset by `.terminal-block`'s `border` shorthand (equal specificity, source order decides). Fixed bottom-center, `z-index: 700` (above `.modal-overlay`'s 500, below the CRT scanline/vignette overlays at 9998/9999). **Not for confirmations** — the 7 existing `window.confirm()` destructive-delete guards (Tag/Block editors) need a synchronous yes/no answer a toast can't provide, and stay as native confirms.
- `Tooltip` (`data-tooltip` attribute + CSS, `index.css`, added 2026-08-07) — replaces native `title=` app-wide (the OS's own tooltip, unstyleable and clashing with the CRT theme) with a themed CSS-only reveal on hover/`:focus-visible`. No wrapper component — any element with `data-tooltip="..."` gets it automatically via `[data-tooltip]::after { content: attr(data-tooltip); ... }`. `white-space: pre-line` on the pseudo-element preserves real `\n` characters for multi-line tooltips (e.g. Todo's notes preview) without any special-casing, since dynamically-set DOM attributes can contain literal newlines. `z-index: 800` (above `.modal-overlay`'s 500 and `.toast`'s 700, below the CRT scanline/vignette overlays at 9998/9999); a `0.4s` `transition-delay` on reveal mimics native tooltips' hover delay, hiding instantly on mouseout. **`IconButton`/`NavLinkButton`/`ToggleField` already forward their `title` prop as `data-tooltip` internally** — the vast majority of tooltips in the app go through these 3 components, so most callers needed zero changes; only ~15 direct native `title=` usages elsewhere needed a one-line swap (plus `aria-label` added where the element had no other visible/accessible text, e.g. icon-only buttons and drag-handle spans — `data-tooltip` alone isn't reliably read by screen readers). **A handful of components have a prop that happens to also be named `title` but renders as visible heading/label text, not a native attribute** — `Modal`, `ShortHookTitles`, `TagPhraseEditor`, `BulkSongEntry`, `ShortsQueueEmptyState`, `GeneratedTitle` (whose `title` prop is actually a title *data object*, not a string) — these must never be converted to `data-tooltip`, that would break real functionality, not fix a tooltip. Catalogued in `/uikit`.
- `FormSelect` — the standard `form-select` dropdown (same look as `TodoStatusSelect`) wrapped with `stopPropagation` for use inside clickable card headers. Takes `value, onChange(value), options: [{value, label}]`.
- `FormField` (`src/components/ui/FormField.jsx`) — `{ label, children, className? }` wrapper around the `form-group`/`form-label` pattern. Used in `InputForm`, `BasicSongFields`, `AdvancedDescriptionFields`, `TagBasicsTab`, `TagPhraseEditor`, `ProjectTextField`, `TodoFields`. Don't hand-roll `<div className="form-group"><label className="form-label">`. Optional `className` is appended to `form-group` for scoped tweaks (e.g. `BasicSongFields`' `basic-song-fields__artist-group` tightens spacing across the Artist/toggle/Artist-Short cluster) — don't use it to reimplement spacing that a shared descendant rule should own instead.
- `ToggleField` (`src/components/ui/ToggleField.jsx`) — `{ label, checked, onChange, title? }` controlled checkbox + label (clicking the label toggles it too). Use for any settings toggle, including ones backed by state that can change out from under the component (e.g. loading a different saved entry) — it's controlled (`checked`, not `defaultChecked`) specifically so that resyncs correctly. Optional `title` forwards to the wrapping `<label>` as a native tooltip (e.g. Titles panel's Hooks toggle: "Mix shorts hooks into long title candidates"). For a checkbox + label + text input in one row, use `ToggleInputRow` instead (it requires the input; it's not a bare checkbox+label alternative).
- `CopyButton` (`src/components/CopyButton.jsx`) — clipboard copy with a temporary "Copied ✔️" state (500ms). Takes `text`. Used in every output panel (Descriptions, Hashtags, YouTube Tags, titles/hooks).
- `CopyPromptButton` (`src/components/ui/CopyPromptButton.jsx`, added 2026-09-03) — the shared "Copy AI Prompt" button for every phrase-pool editor. `{ getPrompt, disabled?, disabledTooltip?, label? }` — `button-secondary` + copied-state (`Copied ✔️`, 500ms, same timing as `CopyButton`) + `data-tooltip` when disabled. `getPrompt` is a `() => string` called **lazily on click**, so the caller closes over live editor state. Owns only the button chrome; the prompt text comes from a `buildX*Prompt` in `src/utils/authorPromptContexts.js`. Wired into `CoverShortHooksEditor` (its `actionsSlot`), the Tag card's `shortHookPool` leaf action row (`TagEditor.jsx`, per Short Hook category), `ShortHookCard` (new `headerActions` passthrough) via `ShortHooks.jsx`, and `Titles.jsx` (group-card headers). Cataloged in `/uikit` under Buttons. Don't hand-roll a copy-to-clipboard-with-checkmark button — use this or `CopyButton`.
- `BlockEditorCard` — collapsible card shell for all three block type editors (Lists, Text Blocks, Hook Blocks). Props: `label`, `badge`, `scope`, `target`, `onScopeChange`, `onTargetChange`, `hasOverride`, `onReset`, `onDelete`, `onRename`, `open`, `children`. `onRename` enables an inline ✏ rename button in the header — clicking switches to an input; Enter/blur saves, Escape cancels.
- `BlockInfoCard` — non-collapsible card used in Description layouts. Props: `label`, `onRemove`, `onAdd`, `onNavigate`. When `onNavigate` is set, the header becomes a clickable nav shortcut (`tag-card-toggle` style, `→` indicator) that jumps to the block's editor tab and auto-expands it — Hook blocks → Blocks → Hook Blocks, List blocks → Blocks → Lists, Text blocks → Blocks → Text Blocks. `onAdd` renders a `+` button (Available column); `onRemove` renders a `×` button (Active Layout column). Generated blocks (no editor) omit `onNavigate` and are non-interactive. Descriptions tab is layout-only; no inline editing of any block type.
- `AppHeader` (`src/components/AppHeader.jsx`) — sticky app-level header. Contains nav tabs (looped from `PAGE_LABELS`), project selector, page `<h1>` (derived from `activePage` + `projectConfig.name`), and optional `actions` slot for page-specific buttons (currently only the Regenerate button on Generator page). Replaces the deleted `AppMenu.jsx`. Do not add page titles (`<h1 className="app-title">`) in individual pages — they belong here. `uikit` is intentionally excluded from `PAGE_LABELS` (not a top nav tab) — reachable only via the "Open UIKit" button in `ProjectSettingsGeneral`'s Actions card, wired through `onOpenUIKit`.

---

# Known Gotchas

**Storage is sensitive.** Changing storage casually can break saved entries, queues, todos, backup import/export.

**"Copy AI Prompt" authoring prompts live in `src/utils/authorPromptContexts.js` — four adapters that must stay consistent with each other and with the placeholder engine.** `coverShortHooksContext`, `tagShortHooksContext`, `globalShortHookContext`, `titlePoolContext` each return an ordered `sections` array that `authorPrompt.js`'s `buildAuthorPrompt` joins; a `buildX*Prompt` wrapper pairs each. Shared decisions baked into all of them: **(1)** new AI-generated hooks/templates must NOT use `{transformation}` (it only yields repetitive "{song} + connector + {transformation}" lines) — prefer typed category tokens (`{tags.genre}`/`{tags.energy}`/`{tags.tempo}`/`{tags.mood}`/`{tags.production}`) only where the type fits; **(2)** `{primaryTag}` is type-unsafe (resolves to a genre / tempo word / adjective / mood / language / era per cover) so it must never sit in a typed grammatical slot — the global + title adapters carry an explicit "PLACEHOLDER TYPES" section teaching this. These are *authoring-prompt rules only* — the engine and every stored template/hook containing `{transformation}` are untouched. If placeholder semantics change (`hookPlaceholders.js` / `placeholders.js`), re-check all four adapters. `coverPrompt.js` is a 2-line re-export of `buildCoverHookPrompt`; its output is guarded by a lock-step inline reference in `coverPrompt.test.js` (update that copy in the same commit as any deliberate cover-prompt change). One-way only — no parse-back; the editor's Bulk Add box is the return path.

**Tags have an optional flat `promptContext` field** (Tag Basics → "What this tag means (for AI prompts)") — a human-authored definition used only as authoritative semantic context by `tagShortHooksContext`; never shown in generated output. It rides in the tag override's `payload` jsonb (no DB column, no migration — `server/routes/tagOverrides.js`'s `splitOverride` routes any non-scalar key there) and is carried by `resolveTagOverride` (client resolve) and `buildEffectiveTag`/`mergeTagData` (server sync/copy-tag, **target-wins** so a sync never clobbers a project's own authored explanation). Surfaced onto the tag object by `buildTagExplorerData.js`. All 51 existing tags across both projects are populated.

**The Tag Library card is a drill-down, not tabs — `src/utils/tagContentSections.js` owns the nav model.** `TagEditor.jsx` holds one `view` descriptor (`{ level, ... }`) as `manualView ?? getInitialView(tag, sourceTarget)`; `parentOf(view)` drives the single back button; `resolvePoolEditorProps(tag, view)` yields the `field`/`parentField`/`parentValue`/`phrases` every leaf `TagPhraseEditor noWrapper` needs. **Short Hook categories come from `projectConfig.shortHookTypes`** (config-driven — all 7, `Contrast` included), not a hardcoded list; a new `shortHookType` in `projects.json` auto-appears as an editable per-tag category. Overview counts are read live from `tag.maps` via `sectionCount`/`descriptionGroupCount`/`shortHookCategoryCount` — never cache them. `tagFieldTabs.js` is gone; its `isSourceMatch` moved into `tagContentSections.js`. `TagLibraryPage.jsx` keys each `TagCard` as `` `${projectId}:${tag.name}` `` so a project switch remounts same-named cards (clears the drill view + `TagPhraseEditor`/`TagBasicsTab` local state) — don't drop the project prefix.

**Content Setup nav lives in `src/config/contentSetupNav.js`; deep-links route through `resolveContentSetupTarget`.** `ProjectSettingsPage.jsx` renders 4 section chips + a `kind`-specific second level (`drill` = overview -> editor + back, `subnav` = leaf strip, `page` = one stacked page). Each leaf maps to an existing `ProjectSettingsContent` dispatch id via `LEAF_TO_DISPATCH`; `ProjectSettingsContent` + every editor component are otherwise untouched, still writing the same `projectSettingsOverrides.<domain>` key. The 5 Generator-output deep-link target states (`titlesTarget`/`thumbnailsTarget`/`hashtagsTarget`/`shortHooksTarget`/`blocksTarget`) and the `onNavigateToSettings('titles'|'thumbnails'|'hashtags'|'descriptions')` strings all resolve through `resolveContentSetupTarget` — a deep-link opens the leaf directly (skipping the Generation overview) with highlight props forwarded unchanged. `blocksTarget.subTab` (the `BLOCK_TYPE_SUBTABS` ids `lists|text|hooks|groups|placeholders`) IS the Descriptions leaf id. The two description modes are also leaves now — `long` / `shorts`; legacy `descriptions` and the retired `layout` leaf id both resolve to `long` (the default mode) via `LEGACY_SECTION_MAP`. Persisted `ui.projectSettingsSection` now holds a leaf id (or a bare section id for the Generation overview / Workflow / Project pages); `resolveStoredView` migrates old flat values on read. `blocks.activeSubTab` / `description.activeSubTab` in overrides are no longer written — stale but harmless in old data.

**`.phrase-row-list` caps every phrase/template editor at ~5 rows.** `HookTemplateEditor` and `TagPhraseEditor` each wrap *only* their row `.map()` in `<div class="phrase-row-list">` (`max-height: calc(var(--phrase-row-h) * 5); overflow-y: auto`, `src/index.css`). Everything that must stay reachable — the search box, +Add/+Bulk, Copy AI Prompt, the Bulk textarea — is a sibling *outside* that div; keep it outside when editing either component. Because the wrapper clips overflow, `PlaceholderField`'s `{…}` autocomplete is portaled to `<body>` (`position: fixed`, dismiss-on-scroll) — don't move it back inside `.placeholder-field`.

**`handleSaveEntry` / `handleLoadEntry` have explicit field lists.** Adding a new `formData` field does NOT automatically persist it per-entry — you must add it to both functions in `useSavedEntries.js`. Fields currently saved/loaded: `artist`, `song`, `signalNumber`, `originalYear`, `originalGenre`, `useCustomArtistShort`, `artistShort`, `transformationTags`, `customHashtags`, `customCta`, `coverShortHooks`, `songBlockOverrides`, `excludeFromRandomizer`, `todo`. `videoType` is intentionally omitted (session/UI state). `changesMade`/`extraVibeNote` are reserved but unused. **Since the Neon migration the "three functions" are `src/utils/savedEntries.js`'s `buildEntryFromFormData` (save) + `buildFormDataPatchFromEntry` (load), and `server/savedEntryMerge.js`'s `mergeImportedEntry` (import)** — plus a real column on the `saved_entries` table (`server/routes/savedEntries.js`'s `rowToEntry`/`upsertEntry` + a migration) and a `src/constants/defaultFormData.js` default. `coverShortHooks` (a flat `string[]` of cover-specific Short Hooks, added 2026-09-02, migration `0007`, jsonb column `cover_short_hooks`) went through all of these. **`handleImportEntries` (JSON library import, not full app Backup/Restore) hand-builds its own third, independent normalized-entry object** — it does NOT derive from `handleSaveEntry`'s field list, so a field added to save/load isn't automatically covered by Import; check all three functions' field lists together when adding a new per-entry field. Two related bugs found + fixed 2026-07-12: (1) `originalYear`/`originalGenre`/`artistShort`/`useCustomArtistShort` were missing from the import normalization entirely. (2) More severe — even with the field list complete, Import used to **replace a matching existing entry wholesale** (imported entries placed first, matching existing entries filtered out), so importing an older/thinner export (e.g. from another device, per "Laptop is source of truth" below) silently wiped any field that was empty in the imported copy but populated locally — this is what actually destroyed a user's Todo status. Fixed via `mergeImportedEntry(item, existing)`: every field now merges independently — `preferNonEmpty(importedValue, existingValue)` keeps the existing value when the imported one is empty/absent; `songBlockOverrides` is shallow-merged per sub-block key (not swapped wholesale, so an import touching only `storyBlock` can't erase an unrelated `logBlock` override); `useCustomArtistShort`/`excludeFromRandomizer` only take the imported value when that key is actually present (`!== undefined`) in the source JSON, since `false` is a meaningful explicit value for both, not just "absent." Any new per-entry field must get the same non-destructive treatment in `mergeImportedEntry`, not a blind field-list addition.

**Cover-Specific Hooks are the one Input-form field that auto-persists — everything else is explicit-SAVE-only.** `CoverShortHooksEditor.handleUpdate` calls `setFormData` (keeps `formData.coverShortHooks` authoritative) **and** `onPersistCoverHooks(nextArray)`. `GeneratorPage.persistCoverHooks` calls `handleUpdateEntry(currentEntryId, { coverShortHooks })` (column-scoped `PATCH`, never touches other fields) **only when `isCurrentEntrySaved`** — an unsaved song is a no-op and the hooks ride the next explicit SAVE (no implicit row insert). Don't route any other Input field through this. Don't drop the unconditional `setFormData` — the PATCH and a later full SAVE (which reads `formData`) must agree. Related: `useInputFormLogic.js`'s `signalNumber` auto-fill effect bails when `formData.signalNumber` is non-empty (deps include `formData.signalNumber`) — the `savedEntries` array-identity change from `handleUpdateEntry` would otherwise revert an unsaved `signalNumber` edit on every hook change. Keep that guard if you touch that effect.

**Every click-to-navigate `*Target` state must belong to exactly one page, and that page must appear in `App.jsx`'s stale-target-clearing effect.** `titlesTarget`/`thumbnailsTarget`/`hashtagsTarget`/`blocksTarget`/`shortHooksTarget` (Project Settings), `tagLibrarySearchTarget` (Tag Library), `songOverrideTarget` (Generator, same-page) each drive a highlight/expand/scroll-to effect somewhere. A single `useEffect` in `App.jsx` (right after the `useAppShellState()` destructure) clears each target once `activePage` no longer matches that target's home page — this is what makes a clicked link's highlight disappear when you navigate away, instead of staying stuck until a refresh (a real bug, fixed 2026-07-13). It works because every `open*Search`/`openBlocksEditor`/`openTagLibrarySearch` function sets its target *and* calls `setActivePage` to the target's own page in the same handler, so the effect always sees the matching page already in place for a freshly-set target and skips clearing it. **A new target added for a page not already in that effect's three groups needs a new branch added there too** — otherwise it inherits the same "stuck forever" bug. `ProjectSettingsPage.jsx`'s `handleSectionChange` (clears the five Project-Settings-scoped targets on a manual section-tab click) is a separate, still-needed mechanism — it covers switching tabs *within* Project Settings, where `activePage` never changes, so the `App.jsx` effect wouldn't fire.

**Renaming/removing a `todoStatuses` entry has no migration for saved entries.** `ProjectSettingsTodo.jsx`'s Statuses list editor writes a plain string array — any saved entry whose `entry.todo.status` still holds the old string becomes invisible (filtered out of every section on the Todo page, per `TodoPage.jsx`'s `entriesByStatus` grouping) rather than deleted or reassigned. `TemplateGroupCard`'s array editor has no rename-vs-add/remove distinction to hang automatic migration off of. Accepted limitation, not a bug to fix reflexively if reported — document it, don't build a migration system nobody asked for.

**Laptop is source of truth.** Desktop sync testing is incomplete.

**Tag system is deeply connected.** Tags affect titles, descriptions, hooks, hashtags, usage tracking.

**Tag `description`/`shortHooks` must be deep-merged, not shallow-spread, wherever base config meets override.** Both are nested objects (`description: {technical, log, status}`, `shortHooks: {hookType: [...]}`) — a plain `{...baseTag, ...override}` lets the override's key wholesale-replace the base's, silently dropping every sub-key the override doesn't happen to cover (e.g. an override touching only `shortHooks.nostalgia` wipes `emotion`/`transformation`/etc. from display *and* generation, since `buildResolvedProjectConfig.js` feeds `generateShortHooks.js` directly). Both `buildResolvedProjectConfig.js` (tag-override merge loop) and `buildTagExplorerData.js` (`registryTag` construction, feeds the Tag Editor's `tag.maps`) now explicitly merge `description`/`shortHooks` one level deep, matching `useTagOverrides.js`'s `syncProjectTags` (which already did this correctly via `mergeDescription`/`mergeShortHooks` — the bug was only in the two *resolution* points, not the sync logic itself). `title`/`thumbnail`/`hashtags` are flat arrays with no sub-keys, so a plain shallow spread is correct for those — don't add merge logic there. If you add another nested per-field object to a tag, give it the same one-level-deep treatment in both places.

**`{tags.<category>}` placeholders resolve from tag `category`, not tag key.** `{tags.era}`/`{genre}`/`{intent}`/`{mood}`/`{lang}`/`{energy}`/`{production}`/`{tempo}` each map to a tag `category` value (only `lang` is aliased — it maps to the `language` category; all others match the category name exactly). If multiple selected tags share a category, one is picked at random; if none match, resolves to `''`, not the literal placeholder text. Add a new category alias in `TAG_CATEGORY_ALIASES` (`descriptionTagHelpers.js`) and the matching token in `TAG_CATEGORY_PLACEHOLDERS` (`hookPlaceholders.js`) together — they must stay in sync.

**`buildMissingDataPrompt` and `parseMissingDataResponse` (`src/utils/searchQuery.js`) must stay in sync.** The first builds the text asking an AI for `Artist - Song: Year, Genre1, Genre2` lines; the second parses that exact shape back out of a pasted reply to auto-fill Saved Library entries. If the requested format ever changes (wording, line shape, separator), `parseResponseLine`'s parsing must change with it — otherwise a pasted response silently lands entirely in `unmatched` with no update applied, no error surfaced.

**Hook block "Lines" count only works through `resolveHookBlockOutput`.** Every generic hook block (including custom ones) resolves its multi-line output through `pickRandomLines`/`resolveHookBlockOutput` in `generateCustomBlocks.js`, which reads `hookBlockCounts`/`hookBlockMaxLines` for that block's key. `statusLines` and `technicalLines` have their own dedicated functions (`generateBroadcastBlock.js`/`generateTechnicalBlock.js`) because they merge per-tag lines with project-level lines before picking — they reuse `pickRandomLines` for the final random selection, but not `resolveHookBlockOutput`. A new hardcoded multi-line block that needs tag-specific merging should follow that same pattern (own function + `pickRandomLines`), not `resolveHookBlockOutput` (which assumes a flat template array from `hookBlocks` config).

**All placeholder resolution goes through `src/engine/placeholders.js`.** `fillPlaceholders(template, ctx)` / `pickViableTemplate(templates, ctx)` are the single implementation used by hooks, titles, and every description block (custom text/hook blocks, broadcast/technical/log blocks) — don't hand-roll a `.replace()` chain for a new placeholder token, add a resolver to the `REGISTRY` object instead (it auto-propagates to `HOOK_PLACEHOLDERS`, the UI autocomplete list, via `REGISTRY_TOKENS`). **Any candidate whose placeholders would resolve empty is dropped before picking, not shown with a gap** — e.g. a hook/line using `{tags.genre}` simply won't be selected when no genre-category tag is chosen. There is no "fall back to showing it anyway" safety net; if a whole pool is empty-valued, that block/hook/title slot silently produces nothing for that generation. `num` and `transformation` are handled specially (not plain `REGISTRY` entries) — `num`'s fallback text differs per caller (hooks/titles use `'XX'`, descriptions use `'00'`), and `transformation` fills its own nested `transformationBlock` template via `ctx.overrides.transformation` (hooks/titles, computed once per batch) or resolved fresh per-call (descriptions).

**Not every placeholder-supporting field can safely offer the full `HOOK_PLACEHOLDERS` set — check whether it has pick/render freezing first.** Random REGISTRY tokens (`{originalGenre}`, `{tags.*}`) are only safe to re-resolve on every render because the *value* gets frozen at pick time and replayed (`fillPlaceholdersAndFreeze`/`fillFrozenPlaceholders`) — a field with no pick-phase freezing of its own would silently re-roll a random token on every keystroke instead of staying picked. Thumbnails (`generateThumbnails.js`'s `pickThumbnails`/`renderThumbnails` — tag-specific phrases and the project-level Words/Fallbacks/Generic-Tag-Template pools alike) gained real freeze support 2026-08-13 and now safely offers the full `buildHookPlaceholders()` set, same as Short Hooks. **Titles' tag phrases are the one remaining exception** — `resolveTitleRecord.js`'s `record.transformation` still resolves at plain render time with no freezing, so it's restricted to `src/utils/hookPlaceholders.js`'s `LIVE_PLACEHOLDERS` (derived from `placeholders.js`'s exported `ALWAYS_LIVE_TOKENS` — `artist`/`song`/`year`/`years`/`decade`/`signalNumber`/`currentYear`/`fileId`, all pure functions of `formData`, zero randomness). Adding freeze support there is possible (see `freezeTemplate`/`renderFrozenTemplate` in `placeholders.js`, and `generateThumbnails.js`'s `pickThumbnails` for a worked example) but is real engine work, not a one-line prop change — don't casually widen it to `HOOK_PLACEHOLDERS`/`buildHookPlaceholders()` without adding that first.

**Shorts description blocks must carry a `source` to be clickable.** `generateShortDescriptions.js`'s `renderShortLine` returns `{ text, source }` per block — a new block-resolution branch that doesn't set `source` (or sets it to `undefined`) silently renders as inert plain text in `DescriptionsPanel`, no error. `source` is one of: `{ type: 'block', blockKey, blockType: 'hook'|'list'|'text', template?, pickedItem? }` (resolve `blockKey` through `buildHookBlockMaps(...).layoutKeyToBlockKey` for hook blocks, since layout key and config key can differ — e.g. `coverLine` → `shortsHeader`; `template`/`pickedItem` come from `resolveHookBlockOutput`/`renderStructuredBlock`'s enriched return and drive exact-highlighting — omit them and clicking still opens the right block, it just won't highlight a specific row), or the hook object itself (`{ sourceType, sourceTag, hookType, sourceText }`) for hook-pool-sourced text — `sourceText` already doubles as the highlight target, no extra field needed there. This only covers Shorts — Long descriptions don't have this mechanism. **`resolveHookBlockOutput`/`renderStructuredBlock` return `{ text, ... }` objects, not plain strings** — every caller must extract `.text`; a new caller that forgets this will get an always-truthy object where it expects a string/falsy-empty check (this exact bug existed transiently in `generateShortDescriptions.js`'s list-block branch during development). **`source.blockType` (`'hook'|'list'|'text'`) is not the same string as the Blocks tab's subTab id** — `ProjectSettingsBlocks.jsx`'s `SubTabNav` ids are `'hooks'`/`'lists'`/`'text'` (`'list'`→`'lists'` and `'hook'`→`'hooks'` don't match directly). Always go through `BLOCK_TYPE_SUBTABS` (`customBlocks.js`) to convert a `blockType` to its `{ subTab, label }` pair — don't hand-roll the mapping (a `blockType === 'hook' ? 'hooks' : blockType` one-off ternary shipped as a real bug: it silently sent `subTab: 'list'` for list blocks, which matches none of `ProjectSettingsBlocks.jsx`'s three branches and renders an empty Blocks panel on click).

**Title engine key names.** `projects.json` for Illegal Mind uses `longPrefix` (legacy) instead of `prefix`. Engine reads `config.title?.prefix || config.title?.longPrefix`. Do not consolidate — legacy key must stay.

**Short Hooks vs Shorts Titles.** Two separate systems:
- `generateShortHooks.js` — hook-based shorts titles, reads `shortsPrefix`/`shortsSuffix` (fallback: `shortHookSuffix`)
- `generateTitles.js` Shorts mode — transformation-based shorts titles, same keys

Both editable via Project Settings → Titles → Generation card. `shortHookSuffix` in `projects.json` is legacy fallback — do not remove.

**`generateShortHooks.js`'s hook objects carry both `text` (Shorts-wrapped) and `rawText` (unwrapped).** `text` has the Shorts prefix/suffix baked in — that's what the Shorts Hooks panel, Shorts-mode titles (`ShortHookTitles.jsx`), and the Shorts description "hook" block all correctly want. But this same hook array is reused by `generateTitles.js`'s `buildHookTitles` (the "mix hooks into Long titles" feature) — it must use `hook.rawText` and wrap it with the *Long* prefix/suffix instead, or the Shorts suffix ends up trapped inside the Long-wrapped title (double-wrapped). Any new consumer of this hook pool needs to pick the right field depending on which prefix/suffix scheme it's rendering for — don't default to `.text` without checking which context you're in.

**Title object shape.** Generated titles are objects: `{ text, sourceHook, sourceTemplate }`.
- `sourceHook` → `{ sourceType: 'tag'|'base', sourceTag, hookType, sourceText }`
- `sourceTemplate` → `{ template, groupName }`
- Both null for plain text titles.
- `NavLinkButton` in `GeneratedTitle` (`src/components/output/GeneratedTitle.jsx`, renamed from `GeneratedTitlePair` when thumbnails were split into their own panel) handles all four cases.

**`tagVisibilityOverrides` is legacy.** Do not remove. Treat as compatibility-sensitive.

**Block label overrides live in two separate keys** inside `projectSettingsOverrides.description`:
- `blockLabelOverrides[blockKey]` — overrides display label for JSON-default List/Text blocks (from `KNOWN_CUSTOM_BLOCKS`)
- `hookBlockLabelOverrides[blockKey]` — overrides display label for JSON-default Hook blocks (from `projects.json`)
- User-created blocks store their label directly (`blockData.name` for List/Text, `customHookBlocks[i].label` for Hook) — no separate override key needed.
- `LongDescriptionSettings` and `ShortsDescriptionSettings` use `getLayoutBlockLabel(blockKey)` which checks both override maps before falling back to config labels. If you add a new place that displays block labels, make sure it also checks these overrides.

**A new block's key must avoid three namespaces, not one.** `generateBlockKey(name, existingKeys)` only guards against whatever `existingKeys` it's given — it has no built-in awareness of any other namespace. A block's key can collide with: (1) other `customBlocks` keys (List/Text share one namespace), (2) any hook block's own `key`, or (3) any hook block's `descriptionLayoutKey` (its *layout* key, which can differ from `key`). All three "Add block" forms (`ProjectSettingsTextBlocks.jsx`, `ProjectSettingsLists.jsx`, `ProjectSettingsHookBlocks.jsx`) must pass a combined `existingKeys` covering all three. A collision is **silent, not an error, and worse than just a display bug** — `resolveBlockSource`/`makeLayoutLabelResolver` (`descriptionLayout.js`) check the hook-layout-key namespace before `customBlocks` (orphaning the colliding block from the Descriptions layout builder), *and* `generateDescriptions.js`'s `blocks` map spreads `renderedCustomBlocks` last (silently overwriting the hook block's actual generated output) — a real case (Text block "Intro Block" → key `introBlock`, colliding with `introHook`'s `descriptionLayoutKey: 'introBlock'`) shipped exactly this way before being fixed 2026-07-10. Existing colliding data self-repairs via `resolveCustomBlockCollisions` (`customBlocks.js`, run from `App.jsx`) for Project-scoped blocks — Song-scoped collisions still need manual resolution (see Current Focus above).

**Avoid premature database work.** Migration is postponed. Current priority is feature completeness.

---

# Block Type System

The description block system is designed to be fully UI-configurable. The goal: a user should be able to build a new project from scratch — different language, different goals, original songs — without editing any JSON.

`projects.json` is a **bootstrap template only**. All real configuration lives in localStorage overrides (backed up/restored via Backup System), eventually migrated to a DB once feature-complete.

## Block types

| Type | What it is | User-creatable | Examples |
|---|---|---|---|
| **List** | `{ title, items: [{ label, text\|link }], scope, target, itemType, isCore, name, displayMode }` | **Yes — built** | `gearBlock`, `supportBlock`, `playlistBlock` |
| **Text** | Single paragraph, optional `{placeholders}`. Legacy blocks are a plain string; UI-created ones are `{ text, scope, target, isCore, name }` | **Yes — built** | `mixingCtaBlock`, `customCtaBlock` |
| **Hook Block** | Array of strings, one picked at random per generation. ALL phrase-template arrays across Long and Shorts descriptions are now Hook Blocks. Song-scoped ones get per-song override fields in the generator. Edited in Project Settings → Blocks → Hook Blocks. | **Yes — built** | `storyBlock`, `logBlock`, `introHook`, `broadcastHeader`, `statusLines`, `technicalLines`, `closingSignal`, `philosophyLine`, `shortsHeader`, etc. |
| **Group** | `{ key, label, scope, target, children: [{ type: 'block', key }] }` — a shell, not content itself. Each child resolves through the same lookup the top-level Descriptions layout uses, joined tight with `\n`. A block assigned to a group is exclusive to it (no longer independently placeable at the top level, still fully editable wherever it normally lives). Edited in Project Settings → Blocks → Groups. `'generated'` child type (fixed, tag-scoped content) not yet implemented — Phase 2. | **Yes — built (Phase 1 only)** | `closingBlock` (children: `closingSignal` + `philosophyLine`) |
| **Generated** | Engine-driven output, parameters only | No (engine code required) | `supportBlock`, `tagLine` |

All List/Text blocks live together in `customBlocks` under `templates.long` regardless of which description(s) they `target` — `target` decides eligibility for Long/Shorts, not storage location. `src/utils/customBlocks.js` is the shared home for `isListBlock`/`isTextBlock`/`getBlockLabel`/`generateBlockKey`/`updateLongKey`/`removeLongKey` AND `SCOPE_OPTIONS`/`TARGET_OPTIONS` — all block-type editors (List, Text, Hook) import from here. `generateBlockKey` collision checks must see *every* `customBlocks` key regardless of type (List and Text share one namespace).

**List `displayMode`** (`'all'` default, or `'random'`): `renderStructuredBlock` either joins every item (current behavior, unchanged) or picks one item at random per generation. Useful for CTA-style lists where you want variety instead of always showing every line.

**`customCtaBlock` is Text, not List**, as of the migration that combined its two CTA lines into one paragraph (joined with `\n`) — this matches the shape of the per-song override mechanism below, removing a structural mismatch where the project default was a List but the override was always plain text. If a project's stored override for `customCtaBlock` predates this (still List-shaped), it'll keep showing in the Lists tab until "Reset to default" (↺) is clicked once, after which it resolves to the new Text default and moves to the Text Blocks tab.

## Per-song block overrides (`formData.songBlockOverrides`)

Any Text or List block with `scope: 'song'` gets a collapsible override field automatically in the generator's Advanced panel (`AdvancedDescriptionFields.jsx`'s `SongBlockOverrideFields` — a generic loop, not per-block code). Text overrides are plain strings; List overrides are `{ items: [...] }` objects — the mini list editor (`SongListBlockEditor`) initializes from the project-default items so the user edits from a known baseline, and a ↺ button removes the override entirely. Writes to `formData.songBlockOverrides[blockKey]`, persisted with the saved entry (`useSavedEntries.js`) — and since `formData`/`savedEntries` already live in the unified storage that `appBackup.js` exports wholesale, this is automatically included in backups, no separate backup code.

Engine: `renderCustomBlock(block, projectConfig, formData, tagLine, songOverride)` takes the override as its last param — when it is `{ items: [...] }`, renders as a structured block using the project-level block's `title`/`displayMode`; when a non-empty string, renders as plain text — used by both `generateDescriptions.js` (Long) and `generateShortDescriptions.js` (Shorts) via `getEffectiveSongOverrides(formData)`, which merges in one **legacy field that predates this mechanism**: `formData.customCta` (→ `customCtaBlock`). `songBlockOverrides` wins when both are set — `useSavedEntries.js`'s `handleLoadEntry` seeds `songBlockOverrides.customCtaBlock` from legacy `entry.customCta` on load (if not already set), so old saved data shows up in the new field and migrates forward on next save, with no manual migration step.

**Project Settings → Blocks** has three sub-tabs (`ProjectSettingsBlocks.jsx`): Lists, Text Blocks, and Hook Blocks. All three use `BlockEditorCard` (shared card shell: collapse, Scope/Target selects, `BlockActions`). List/Text editors (`StructuredListEditor` / `TextBlockEditor`) use `BlockActions` (Reset for JSON-default blocks; Lock + Delete for user-created ones). Hook Blocks (`ProjectSettingsHookBlocks.jsx`) is fully config-driven from `description.hookBlocks` per project — covers ALL phrase-template arrays across Long and Shorts, plus user-created ones (see below). Each card shows: Target dropdown (Long/Shorts/Long+Shorts), Scope dropdown (Project/Song), ↺ reset (JSON-default blocks) or × delete (user-created). Expanded body: Lines row with slider + max number input (slider only appears when max > 1); template editor via `HookTemplateEditor noWrapper`. All three tabs share `AddBlockForm` (name + scope + target + add button); Lists additionally has a Type dropdown via `AddListBlockForm`.

**Dynamic Hook Blocks** — user-created hook blocks are stored as `description.customHookBlocks: [{ key, label }]` in overrides. Templates stored in `description.templates.long[key]` (same as `path: 'long'` static blocks). `buildResolvedProjectConfig.js` appends these to `projectConfig.description.hookBlocks` during resolution, so all downstream code (engine, Descriptions tab palette, `resolveHookBlockTemplates`) sees them automatically. Collision checking uses both existing hookBlock keys and customBlocks keys.

**`hookBlocks` config shape** (in `projects.json description.hookBlocks`): `{ key, label, path, templateKey, descriptionLayoutKey?, scope?, countMax?, countDefault? }`
- `path`: `'long'` (→ `templates.long[templateKey]`), `'top'` (→ `description[templateKey]`), `'shorts'` (→ `templates.shorts[templateKey]`)
- `descriptionLayoutKey`: layout key when it differs from `key` (e.g. `introHook` → `introBlock`, `broadcastHeader`/`operatorStatuses`/`statusLines` → `broadcastBlock`)
- `scope: true` flag in JSON is legacy — scope is now always shown and stored in `phraseBlockScopes`
- `countMax`/`countDefault`: initial max and default for the Lines slider

## Block scope

Each block has a **scope** field (stored in overrides, user-set in the editor):

- **Project** — same value across all songs (links, playlists, support info)
- **Song** — has a project default, overridable per song (gear, custom notes)

Migration progress for old hardcoded per-song generator fields:
- `customCta` → `customCtaBlock` (Text, `scope: 'song'`) ✓
- `customGear` → `gearBlock` (List, `scope: 'song'`, mini list editor) ✓
- `customStory` → `storyBlock` (Hook Block, `scope: 'song'`) ✓
- `customLogNote` → `logBlock` (Hook Block, `scope: 'song'`) ✓
- `customHashtags` — not a description block, still hardcoded (see Other Active Goals)

Legacy `formData.customStory`/`customLogNote` fields are still supported via `getEffectiveSongOverrides` fallbacks and seeded into `songBlockOverrides` on entry load, so old saved entries migrate forward automatically on next save.

## Hook Block song overrides and scope

Hook Blocks (`storyBlock`, `logBlock`, etc.) are phrase-template arrays — the engine picks one at random per generation. When scope is `'song'`, a per-song override text field appears in the generator's Advanced panel, rendered generically by `songScopeHookBlocks` in `AdvancedDescriptionFields.jsx`'s `SongBlockOverrideFields` — every song-scoped hook block goes through this one loop, no exceptions. (`storyBlock`/`logBlock` used to have their own hardcoded `PHRASE_BLOCK_OVERRIDES` array/render path predating the generic hookBlocks system — removed 2026-07-10; they're now ordinary hookBlocks entries like any other.) The override is a plain string stored in `formData.songBlockOverrides[blockKey]` and wins over the randomly-picked project template.

Scope for Hook Blocks is stored in `description.templates.long.phraseBlockScopes` (a map of `blockKey → 'song'|'project'`). When scope is `'project'`, no override field appears in the generator. Editable in Project Settings → Blocks → Hook Blocks. **Default scope (when no explicit override exists) is data-driven, not hardcoded**: each hookBlocks config entry may declare `"defaultScope": "song"` in `projects.json` (only `storyBlock`/`logBlock` do, matching their historical default — everything else implicitly falls back to `'project'`). Both `ProjectSettingsHookBlocks.jsx`'s scope dropdown and `AdvancedDescriptionFields.jsx`'s override-field visibility check must resolve scope with the exact same precedence (`phraseBlockScopes[key] ?? block.defaultScope ?? 'project'`) — they used to disagree (the dropdown defaulted absent-key display to `'project'` while the override field defaulted `storyBlock`/`logBlock` specifically to `'song'` via a hardcoded key check), which meant the Scope toggle silently didn't control the Generator field until a user explicitly touched it. Fixed by moving the default into config instead of code.

## Block targets

Each block can target **Long Description**, **Shorts Description**, or both, via a `target` field (`long`/`shorts`/`both`). Both `generateDescriptions.js` (Long) and `generateShortDescriptions.js` (Shorts) render List/Text blocks via the shared `renderCustomBlock` (in `generateCustomBlocks.js`) — Shorts list-block output is padded with blank lines so it stands apart from the single-newline-joined surrounding lines.

Hook Block target is stored in `description.hookBlockTargets[key]` in overrides. Default is derived from `block.path` (`'shorts'` → `'shorts'`, anything else → `'long'`). The engine reads this via the Descriptions tab Active Layout — a hook block with target `'shorts'` or `'both'` appears in the Shorts Available palette and can be added to the Shorts layout; the engine resolves its templates via `resolveHookBlockTemplates(layoutKey, projectConfig)` in `generateCustomBlocks.js` (shared helper used by both `generateDescriptions.js` and `generateShortDescriptions.js`). Same helper covers the Long engine for any Shorts-path hook block added to the Long layout.

Dynamic blocks (no JSON default, created from the Blocks tab) have no position in the static layout array — they're appended at the end when added to a layout, and sort to the end on "Reset Order". Both Description settings pages (`LongDescriptionSettings.jsx` / `ShortsDescriptionSettings.jsx`) are now **layout-only** — all block types render as `BlockInfoCard` pointing to their editing location. No inline template editing in the Descriptions tab. Hook blocks are detected by deriving `allHookBlockLayoutKeys` from `projectConfig.description.hookBlocks` at runtime (using `descriptionLayoutKey ?? key` on each entry) — no hardcoded lists.

---

# Current Focus

**Cover-Specific Hooks auto-persist, shipped 2026-09-03.** The rest of the Generator Input form stays explicit-SAVE-only, but the Cover-Specific Hooks editor now persists on the spot: add / bulk-add / delete write immediately, an edit writes on blur. Mechanism — `CoverShortHooksEditor.handleUpdate` still calls `setFormData` unconditionally (so `formData.coverShortHooks` stays authoritative and a later full SAVE can't revert it), then calls a new `onPersistCoverHooks(nextArray)` prop. `GeneratorPage.persistCoverHooks` fires `handleUpdateEntry(currentEntryId, { coverShortHooks: nextArray })` — the existing column-scoped `PATCH /saved-entries/:id` (server reads the existing row, replaces only `cover_short_hooks`) — **only when `isCurrentEntrySaved`**. For an unsaved song it is a deliberate no-op: hooks live in `formData` and are written by the next explicit SAVE (which is when the row is first created — never an implicit insert). 3 files: `GeneratorPage.jsx` (build `persistCoverHooks`, pass it), `InputForm.jsx` (thread `onPersistCoverHooks`), `CoverShortHooksEditor.jsx` (call it in `handleUpdate` when `update.coverShortHooks` is an array). No backend / schema / `TagPhraseEditor` change. **Paired fix in `useInputFormLogic.js`:** the effect that auto-fills `signalNumber` from a matching saved entry now bails when `formData.signalNumber` is non-empty (and `formData.signalNumber` is in its deps) — mirrors the adjacent `artistShort` "only when empty" guard. Without this, the extra `savedEntries` array-identity churn from `handleUpdateEntry` would snap a user's unsaved `signalNumber` edit back to the stored value on every hook change. Verified live (Playwright, 20 checks, 0 console errors, throwaway `ZZZQA` entry created + deleted): unsaved song creates nothing; SAVE then persists the current form incl. hooks; bulk-add / `+ Add`+blur / edit-blur / delete each reach the DB with no SAVE click; a dirty `originalYear` and a dirty `signalNumber` are neither persisted by a hook change nor reverted in the form; a later full SAVE keeps the latest hooks array and also writes the now-intentionally-saved dirty fields. `eslint` (1 pre-existing unrelated error) / `vite build` clean.

**Content Setup › Descriptions — Long/Shorts promoted to first-class leaves, shipped 2026-09-03.** Follow-up to the IA rework below. The `descriptions` `subnav` leaf strip was `Layout · Lists · Text Blocks · Hook Blocks · Groups · Placeholders · Links` — where `Layout` was a stub leaf that only existed to nest a `<Descriptions>` wrapper (`<h2>Descriptions</h2>` + a Long/Shorts `SubTabNav`). That nesting is gone: `Long` and `Shorts` are now their own leaves, so the strip is `Long · Shorts · Lists · Text Blocks · Hook Blocks · Groups · Placeholders · Links`. Clicking `Long`/`Shorts` renders `LongDescriptionSettings`/`ShortsDescriptionSettings` directly (editor bodies, unchanged — no `<h2>`/`<section>`/`SubTabNav` of their own), each wrapped by `DescriptionsWorkspace`'s existing `withHeading` → `<h2>Long Description</h2>` / `<h2>Shorts Description</h2>` (nav *chips* stay `Long`/`Shorts`). `src/components/projectSettings/descriptions/Descriptions.jsx` **deleted** (only `DescriptionsWorkspace` imported it). `contentSetupNav.js`: `descriptions.leaves` swaps `{layout}` → `{long},{shorts}`; `LEGACY_SECTION_MAP.descriptions` → `leaf:'long'`, plus a new `LEGACY_SECTION_MAP.layout → descriptions/long` so a stale persisted `ui.projectSettingsSection === 'layout'` (or any lingering `layout` deep-link string) resolves to Long instead of the unknown-id fallback (which would land on Project). `ProjectSettingsPage.LEAF_TO_DISPATCH`: `layout` → `long`+`shorts` (both dispatch `'descriptions'`). `contentSetupNav.test.js` updated (new leaf list, `descriptions`/`layout` → `long`, `isValidLeaf` long/shorts true & layout false). Data model, editors, reset behavior, block deep-links (`openBlocksEditor`/`blocksTarget.subTab` — always a block-editor leaf, never `layout`/`long`/`shorts`), highlighting and write paths all unchanged. `description.activeSubTab` in stored overrides is now fully inert (joins `blocks.activeSubTab`) — **not migrated/removed** this stage. `DescriptionLayoutBoard`'s internal `mobileTab` state and its `{id:'layout'}` mobile column-switcher, and `templates.*.layout` data arrays, are unrelated and untouched. Verified: `contentSetupNav.test.js` + `tagContentSections.test.js` pass; `eslint` clean (4 pre-existing unrelated errors); `vite build` clean; live Playwright (22 checks, 0 console errors) — 8-leaf strip with no `Layout`, one visible leaf-nav row (board's mobile strip is `display:none` on desktop), Long/Shorts headings + editors + Shorts count slider, Shorts survives reload, a reversible add→remove layout round-trip on Long, Generator DESCRIPTIONS panel → Descriptions/Long, a generated description line → the `groups` block-editor leaf, and stale `ui.projectSettingsSection="layout"` → Descriptions/Long. The dead-code/label cleanup from the prior audit (Stage 7) is still deferred.

**Content Setup IA rework, shipped 2026-09-03.** "Project Settings" (a flat strip of 11 section chips) became **Content Setup** — 4 functional sections, the project-level counterpart to Tag Library's tag-level authoring. Top nav reordered to Generator / Shorts Queue / Calendar / Todo / Tag Library / Content Setup (the `projectSettings` page id is unchanged — only the label). Pure navigation/IA: every editor keeps writing the same `projectSettingsOverrides.<domain>` key; no data/schema/engine change.

- **`src/config/contentSetupNav.js`** (new; pure, Node-tested — `contentSetupNav.test.js`) is the model: `CONTENT_SETUP_SECTIONS` (4 x `{id, label, kind, leaves}`), `resolveContentSetupTarget(legacySection, blocksSubTab?)` (every pre-rework flat section id + the Generator output panels' `onNavigateToSettings` strings + `blocksTarget.subTab` -> `{section, leaf}`), plus `getSection`/`getSectionLeaves`/`isValidLeaf`/`parentOf`.
- **`ProjectSettingsPage.jsx`** rewritten as the two-level nav: 4 section chips + a `kind`-specific second level. `LEAF_TO_DISPATCH` maps each leaf -> an existing `ProjectSettingsContent` dispatch id; `resolveStoredView` normalises the persisted `ui.projectSettingsSection` (leaf id / bare section id / legacy flat id) to `{section, leaf}`.
- **generation** (`kind: 'drill'`) — `GenerationOverview` (4 pool rows with live template counts) -> Titles / Short Hooks / Thumbnails / Hashtags editor + a `<- Generation` back header. A deep-link skips the overview and opens the leaf directly.
- **descriptions** (`kind: 'subnav'`) — one `DescriptionsWorkspace` with a flat leaf strip. Originally 7 leaves with a stub `Layout` leaf nesting a Long/Shorts `SubTabNav`; **a follow-up stage (see top of Current Focus) promoted Long/Shorts to first-class leaves**, so it is now 8: `Long / Shorts / Lists / Text Blocks / Hook Blocks / Groups / Placeholders / Links`. Merges the old separate Descriptions + Blocks + Links + Placeholders sections and drops the stacked Descriptions[Long/Shorts] + Blocks[5-tab] nav. `BLOCK_TYPE_SUBTABS` ids are unchanged so every `openBlocksEditor` deep-link still works.
- **workflow** (`kind: 'page'`) — one `WorkflowWorkspace`: Shorts Queue + Todo Statuses side by side, Upload Schedule full width below (`.workflow-grid`, stacks < 960px). `WorkflowWorkspace` owns the `<section>`/`<h2>`/grid hierarchy; each of the three child editors renders just its `TemplateGroupCard` and takes `heading` for the card label (Stage 7 removed their now-dead standalone-page fallback branches + the `embedded` prop).
- **project** (`kind: 'page'`) — the `ProjectSettingsGeneral` editor, `<h2>` "Project"; dead "Duplicate Project" / "Export Project Config" stubs removed; Backup card subtitle states it is app-wide.
- **`{primaryTag}` moved from Short Hooks into Titles** (same `title.primaryTag` key; `PrimaryTagSection.jsx` git-moved `shortHooks/` -> `titles/`).
- **"Copy AI Prompt" moved out of card headers into the editor action row** (`actionsSlot` -> `AddBulkRow.extra`) for Title pools + global Short Hooks; all four pool editors (also Cover-Specific + Tag Short Hooks, already correct) are now consistent — the button collapses away with the editor.
- **Deleted:** `config/projectSettingsSections.js`, `blocks/Blocks.jsx`, `projectSettings/ProjectSettingsLinks.jsx`. `ProjectSettingsContent`'s "Coming Soon" fallback -> `return null` (unreachable).
- **Intentionally stale, no migration:** `projectSettingsOverrides.blocks.activeSubTab` / `.description.activeSubTab` are no longer written (leaf persistence via `ui.projectSettingsSection` covers it) — they stay in existing stored data harmlessly.
- Stage commits: `1c05267` (nav model) -> `2ebf09e` (top nav) -> `327a18f` (2-level nav) -> `a651ba6` (Descriptions merge) -> `c25aeb1` (Generation drill + primaryTag) -> `2306b5f` (Workflow/Project) -> `3c9969b` (Copy AI Prompt row) -> `844f899` (Workflow layout) -> cleanup+docs. `eslint`/`vite build` clean throughout; verified live per stage via Playwright (0 console errors).

**Tag Library card — drill-down IA rework, shipped 2026-09-03.** Replaces the flat "Edit tag → 6-tab `SubTabNav` → sub-section → phrase editor" with a one-level-at-a-time drill: **Tag → content overview → content type → pool editor**. The card stays compact; only the level being worked on is on screen. Rarely-touched config (Label / Category / `promptContext` / exclusions / Hide-from-Generator / Duplicate / Sync-from-project / Delete) moved behind an "Edit Basics" button on the overview — `TagBasicsTab.jsx` reused verbatim as one drill screen, no modal.

- **`src/utils/tagContentSections.js`** (new; pure, Node-tested — `tagContentSections.test.js`, 55 assertions) owns the model: `TAG_CONTENT_SECTIONS` (the 5 overview rows), `sectionCount`/`descriptionGroupCount`/`shortHookCategoryCount` (counts read live off `tag.maps` — no duplicate state), `getShortHookCategories(projectConfig)`, `getInitialView(tag, sourceTarget)` (click-to-nav landing view; hook match beats field match, same precedence as the old `getInitialTab`), `parentOf(view)` (back-nav), `resolvePoolEditorProps(tag, view)` (field/parentField/parentValue/phrases for any leaf `TagPhraseEditor`), `pickTagPhrasePlaceholders`, and `isSourceMatch` (moved here — `tagFieldTabs.js` is **deleted**).
- **`TagEditor.jsx`** rewritten as a `view` state machine (`view = manualView ?? getInitialView(...)`), no tab bar. Views: `overview` / `basics` / `pool` (titles·thumbnails·hashtags → straight to editor) / `shortHookCategories` → `shortHookPool` / `descriptionGroups` → `descriptionPool`. Outer `<details> "Edit tag"` wrapper + open-on-nav-target behavior unchanged.
- **New components** (`src/components/tags/editor/`): `TagContentOverview`, `TagDrillHeader` (`← backLabel  TITLE  subtitle`), `TagShortHookCategoryList`, `TagDescriptionGroupList`. `TagPhraseEditor noWrapper` is every leaf — the redundant `Titles → LONG TITLE PHRASES (7)` nesting is gone.
- **Short Hook categories are config-driven from `projectConfig.shortHookTypes`** — all 7, incl. `Contrast` (now editable per-tag for the first time; the old hardcoded 6-entry `TAG_FIELD_TABS.shortHooks` list is gone). Add a `shortHookType` to `projects.json` and it auto-appears as an editable per-tag category.
- `CopyPromptButton` + `buildTagShortHooksPrompt` now sit in the `shortHookPool` leaf's action row (was `TagFieldTab`).
- `TagCard` key is now `` `${projectId}:${tag.name}` `` (`TagLibraryPage.jsx`) — a same-named tag in another project remounts on switch, clearing the card's drill view + local editor state (fixes the old `activeTab`-carryover bug).
- Deleted: `TagEditorTabs.jsx`, `TagFieldTab.jsx`, `tagFieldTabs.js`. `eslint`/`vite build` clean (4 pre-existing unrelated errors). Live QA by the user. Commits `7ce92d1`, `9e87d4b`, `d030fec`, `d02f044`, `0323b8e`. **Out of scope / noted:** description-sourced click-to-nav still lands on the overview (no deep-link today either); no `onToggle` added to the outer `<details>` (a same-value re-render doesn't reset it — verified).

**Shared "Copy AI Prompt" authoring system, shipped 2026-09-03.** Generalizes the one-off `buildCoverHookPrompt` (from the Cover-Specific Short Hooks feature below) into a reusable skeleton + per-editor context adapters + one button, so the *"open editor → Copy AI Prompt → external AI → review → Bulk Add"* workflow now exists on **four** phrase-pool editors: Cover-Specific Short Hooks, Tag Short Hooks (per category), global Short Hook types (Project Settings → Short Hooks), and Title template pools (Project Settings → Titles: Standard / But It's / Generic). No new engine or storage behavior; one-way (`buildX*Prompt` convention, no parse-back — the existing Bulk Add box is the return path).

- **`src/utils/authorPrompt.js`** — `buildAuthorPrompt(sections)` (drop empty sections, join with one blank line, `trimEnd`) + `headedList` / `headedBullets` / `contextLines` / `placeholderNote` formatting helpers + a `RULE` object of cross-adapter rule strings. A dumb assembler — zero domain knowledge.
- **`src/utils/authorPromptContexts.js`** — one `xContext(projectConfig, opts)` per editor returning the ordered `sections` array, each paired with a `buildX*Prompt` wrapper: `coverShortHooksContext` / `buildCoverHookPrompt`, `tagShortHooksContext` / `buildTagShortHooksPrompt`, `globalShortHookContext` / `buildGlobalShortHookPrompt`, `titlePoolContext` / `buildTitlePoolPrompt`. See the Known Gotcha above for the shared prompt-design rules (no `{transformation}` in new suggestions; `{primaryTag}` type-safety).
- **`src/utils/coverPrompt.js`** — reduced to a 2-line re-export of `buildCoverHookPrompt` (delegates to `coverShortHooksContext`); kept so `CoverShortHooksEditor` + its tests don't move. Output guarded by a lock-step inline reference in `coverPrompt.test.js`.
- **`src/components/ui/CopyPromptButton.jsx`** — the shared button (see UI Primitives).
- **New per-tag `promptContext` field** — Tag Basics → "What this tag means (for AI prompts)". Authoritative meaning for `tagShortHooksContext` (never inferred from the tag's own phrases, which may be what the user is trying to improve). Rides in the tag override `payload` jsonb (no column/migration). `TagBasicsTab.jsx` field + `buildTagExplorerData.js` passthrough; `.tag-edit-fields__full` / `.tag-options` now span the 2-col grid so the field is full-width below Label/Category. Populated for all 51 tags via `PUT /api/tag-overrides`. 5 values flagged for the user's ongoing review (punk-as-faster, `alt metal` vs `alt. metal` dup, `re-recorded` intent, `modernized`/`faithful` on Maxx Dee).

**Per-adapter specifics:** Tag = tag `promptContext` authoritative + existing phrases only for de-dup / "don't imitate the weak ones" + stay within the stated meaning. Global = existing templates ARE the house voice (match them) + must work for ANY cover + `excludeForFaithful` / `requiresGenre` become boundary lines. Title = write templates not finished titles + per-pool role + signal/channel/hashtags go to the prefix-suffix wrapper + "introduce structures the pool doesn't already have". Cover = cover-specific facts only, prior behavior preserved.

Wiring: `CoverShortHooksEditor.jsx` (`actionsSlot`), `TagEditor.jsx`'s `shortHookPool` leaf (per Short Hook category action row — since 2026-09-03 Tag card drill-down; was `TagFieldTab.jsx`), `ShortHookCard.jsx` (new `headerActions` passthrough) via `ShortHooks.jsx`, `Titles.jsx` (group-card headers; `h3`+`↺` wrapped in `.tag-card-label-row`, count moved to the right grid column). Tests (Node smoke, `rolldown` + `node`): `authorPrompt.test.js` (22), `authorPromptContexts.test.js` (75), `coverPrompt.test.js` A/B lock-step guard (13), `tagPromptContext.test.js` (9). `eslint` / `vite build` clean. Live QA by the user (Darker/Faithful tags, global Transformation, Title pools). Commits `f8c9faf`, `fc6282f`, `773d673`, `59b65a6`, `8e35870`, `6cb5970`, `a98bdfe`. **Deliberately out of scope:** Hook Blocks (Blocks → Hook Blocks — mostly lore blocks, not hook pools), Title prefix/suffix (single-value fields), DRYing the two near-identical "PLACEHOLDER TYPES" strings, wiring `promptContext` into `AddTagPanel`. "Generation V2" stays shelved.

**Cover-specific Short Hooks + an AI prompt for them, shipped 2026-09-02.** Replaces the shelved "Generation V2" composition-engine direction (see `docs/current-context.md` In Progress + the plan file's SHELVED banner — do not resume it; the F1–F7 / D1–D6 findings are kept for a later, separate pass at the existing handwritten hook library). New per-cover field `formData.coverShortHooks: string[]` — a **flat, uncategorized** list of Short Hooks holding info unique to one cover (personal story, why it was covered, an unusual recording/arrangement/song-part detail, an anecdote, an in-joke). Persisted like `transformationTags`: new jsonb column `cover_short_hooks` on `saved_entries` (migration `0007`, applied to Neon), added to `src/constants/defaultFormData.js` + the save/load mappers in `src/utils/savedEntries.js` + `server/savedEntryMerge.js`'s `mergeImportedEntry` (`preferNonEmpty`, non-destructive) + `server/routes/savedEntries.js`'s `rowToEntry`/`upsertEntry` (17 cols, `$17::jsonb`). **Generation:** `generateShortHooks.js`'s `pickShortHooks` appends one internal `{ type: 'cover', label: 'Cover-Specific', hooks: pickRandomLines(viable, 2) }` group after the eligible-type groups — pool = the flat list, no eligibility filters, same `fillPlaceholders` viability check + `pickRandomLines(_, 2)` as every other group, `sourceType: 'cover'`. Flows into `mixedRecords` + `buildHookTitles` like base/tag hooks. No `useGeneratedOutput.js` dep change — picks up on entry load via `entryLoadToken`, mid-session edits apply on next Regenerate (selective-regeneration rule). **Click-to-navigate:** a cover hook in the Titles / Short Hooks output is a clickable `NavLinkButton` (cover branch in `GeneratedTitle.jsx` + `ShortHookTitles.jsx`) → new same-page target `coverHookTarget`/`openCoverHook`/`clearCoverHookTarget` in `useNavigationTargets.js` (force-opens `panelVisibility.coverHooks` + `input`, exactly like `openSongOverride`), threaded `App.jsx`→`GeneratorPage.jsx`→`GeneratorResultsPanel.jsx`→`TitlesPanel.jsx` on the output side and →`InputForm.jsx`→`CoverShortHooksEditor.jsx` on the landing side (raw hook text passed as `TagPhraseEditor`'s `highlightText`, scrolls to + highlights the row, consumed on edit). New `generator` branch in `useStaleTargetClearing.js` clears it on nav-away. The nav key is `sourceHook.sourceText` / `hook.sourceText` — the raw unfilled template, preserved through both `renderShortHooks` and `buildHookTitles`, matching a stored `coverShortHooks` entry verbatim. **UI:** own collapsible section in `InputForm.jsx` — a `ToggleButton` "+ Show Cover-Specific Hooks (N)" placed **below the Advanced Options toggle, above `<InputFormActions>`** (the project selector), wrapped in `.cover-hooks-section` (`margin-top: var(--space-4)` in `index.css`, so the two toggle borders don't touch — `ToggleButton` itself is never modified globally). New `coverHooks: false` in `defaultPanelVisibility` (`useAppShellState.js`). New `src/components/input/CoverShortHooksEditor.jsx` renders `<TagPhraseEditor noWrapper searchable actionsSlot={...}>` — **`TagPhraseEditor` gained 3 optional props** (all default off, ~15 tag-editor call sites unaffected): `noWrapper` (skip its `<details>`/`<summary>` shell, matching `HookTemplateEditor.noWrapper`), `searchable` (its own "Search hooks…" box above the list, filters which rows render by *real index* so edit/remove stay correct), `actionsSlot` (an extra control in the +Add/+Bulk row, via `AddBulkRow`'s new `extra` prop) — with a formData adapter (`onUpdateTag={(_, u) => setFormData(p => ({...p, ...u}))}`; `parentField` undefined → its `buildUpdate` returns `{ coverShortHooks: [...] }`). Add/edit/delete/bulk-add + `buildHookPlaceholders` autocomplete + search, all reused from the tag Short Hooks editor. **AI prompt:** `src/utils/coverPrompt.js`'s `buildCoverHookPrompt(formData, projectConfig)` — one-way (no parse-back, no category parser), same convention as `tagPrompt.js`. Assembles a new per-project `promptContext` string (added to both projects in `projects.json` near `name`) + artist/song/year/genre + selected tags with `category`/`label` + resolved transformation phrase (`buildTagPhrase`) + `signalNumber`/`artistShort`/`customHashtags` + Story/Renovation/Log-Notes text (`songBlockOverrides.storyBlock`/`.renovationBlock`/`.logBlock` + legacy `customStory`/`customLogNote`) + a generic sweep of any other non-empty `songBlockOverrides` string + a compact optional `buildHookPlaceholders` list; empty fields omitted. Assignment text: ONE FLAT LIST of 8–12 hooks specific to THIS cover, one per line, no numbering/categories, **short (5–12 words), no trailing period, no invented facts, no signal/hashtag/channel metadata in a hook**, explicitly NOT generic transformation lines ("X but heavier" etc.) — those already come from the tag/global system. "Copy AI Prompt" button (`button-secondary`, 500ms "Copied ✔️", disabled + `data-tooltip` until artist+song set) sits inline next to +Bulk in the editor. Verified via rolldown smoke tests (`pickShortHooks` cover group 9/9; prompt full + empty-fields output) + `eslint`/`vite build` clean; live QA by the user per standing preference. Commits `dc66a0a`, `cb745b5`, `466975a`, `ca13b00`, `6c1ba31`, `1f4d96e` (click-to-nav), `81b4686` (no trailing period).

**Saved Library: Todo-status badge, clickable to jump to Todo, shipped 2026-08-29.** First real item off the post-audit investigation queue (see `docs/current-context.md`'s Next Recommended Tasks) — the two TagEditor items ahead of it in the queue were explicitly dropped by the user as not worth the review depth ("I'm not interested in those small things... there's a big Tag Library rework we'll need to discuss later"), folded into that future rework conversation instead of fixed standalone. A song's Todo status (Wishlist/Needs Re-record/etc., whatever's configured per-project) was previously only visible on the Todo page — invisible while browsing Saved Library. `SavedLibraryItem.jsx` gained a new badge in its existing `badges` slot (alongside the missing year/genre badges), shown only when `entry.todo?.status` is set. Clicking it jumps to Todo, force-expands that status's section if the user had manually collapsed it, and scrolls to/highlights the row — same click-to-navigate convention as every other cross-page target in the app (`titlesTarget`/`blocksTarget`/etc.): new `todoTarget`/`openTodoSearch`/`clearTodoTarget` in `useNavigationTargets.js`, a new `todo` branch in `useStaleTargetClearing.js`'s clear-on-navigate-away effect, threaded `App.jsx` → `GeneratorPage.jsx` → `SavedLibrary.jsx` → `SavedLibraryItem.jsx` for the click, and `App.jsx` → `TodoPage.jsx` → `TodoStatusSection.jsx` → `TodoItem.jsx` for the landing side. `TodoItem.jsx` gained a `highlighted` prop (scroll-into-view + `.tag-section--highlight`, the same reusable amber-glow class `SongBlockOverrideFields` already uses — no new CSS for the highlight itself) and a stable `id={`todo-entry-${entry.id}`}` wrapper. `buildTodoStatusPanelKey` (originally private to `TodoStatusSection.jsx`) extracted to a new `src/utils/todoPanelKey.js` — needed so `TodoPage.jsx` could also compute it to force-open the right section, and a component file exporting a second non-component function trips this project's `react-refresh` lint rule. **Badge styling went through one visual-feedback round**: the first pass reused `.saved-entry-missing-badge`'s bordered-box/uppercase-mono look (same shell as the existing missing-data badges) — user pushed back live, wanting it to read as metadata/additional-data rather than an action button, "in square brackets... match the Fallout CRT theme," specifically pointing at the adjacent `[queue excluded]` badge (`.saved-entry-hidden`) as the reference. Restyled to match that exactly: bracketed text (`[${entry.todo.status}]`, natural casing, not the missing-badges' uppercase mono), italic, `--color-text-muted-3`, no border/background at rest — the *only* addition is a hover-only glow (`text-shadow` + brighter text, matching `.description-line-link`'s established "no permanent color, CRT glow only on hover" restraint) to signal it's clickable without visually competing with the real action badges. `eslint`/`vite build` clean (only the two pre-existing, unrelated errors — `GeneratorPage.jsx`'s `handleRegenerate` and `TodoPage.jsx`'s `projectConfig`, both confirmed via `git diff` to predate this change). No Playwright pass — verified live by the user directly, per standing QA-ownership preference.

**Shorts descriptions: song-scoped List block overrides now actually apply, shipped 2026-08-29.** Flagged during a general project review, then confirmed as a real bug (not intentional) by walking through the exact repro before touching anything. `generateShortDescriptions.js`'s per-block PICK/RENDER dispatch (`pickShortLine`/`renderShortLine`) sorted every layout block into one of four `kind`s — List blocks got `kind: 'list'`, which rendered straight through `renderStructuredBlockFromPicked` with **no `songOverrides` check at all**, while the adjacent Text-block branch (`kind: 'customBlock'`) correctly called `renderCustomBlock(picked, ctx, links, songOverrides[blockName])`. So a song-scoped List block (e.g. `gearBlock`) got a real, working override field in the Generator's Advanced Options — editable, saved, and correctly applied in **Long** descriptions (`generateDescriptions.js` already routes every customBlocks entry through `renderCustomBlock`) — but the same override was silently ignored in **Shorts** output, always showing the project defaults instead. Root cause was structural, not a typo: `renderCustomBlock` expects its `picked` argument shaped like `pickCustomBlockDefault`'s output (`{ kind, picked, block }`, `block` being the raw config the override branch needs to rebuild an overridden variant) — Shorts' own `pickShortLine` built a differently-shaped `picked` for List blocks that never carried `block`. Fixed by (1) adding `block: customBlocks[blockName]` to `pickShortLine`'s `kind: 'list'` return, and (2) routing `renderShortLine`'s `'list'` branch through the same `renderCustomBlock` call the `'customBlock'` branch already makes, with matching `isSongOverrideActive`-driven source attribution (`{type:'override', blockKey}` vs `{type:'block', blockKey, blockType:'list', pickedItem}`) so click-to-navigate correctly points at the Generator field when an override is active, same as Long already does. No override-storage or UI changes — purely wiring the existing mechanism into the one branch that skipped it. New persisted test: `src/engine/descriptions/generateShortDescriptions.test.js` (a plain-assertion Node smoke test — no test runner installed in this project, run via `rolldown` bundling to ESM + `node`, same pattern as every other engine-level smoke test in this file's history, just kept as a real file this time instead of thrown away) — 12 assertions covering project-default List items, a song-specific override, Shorts output correctly using the overridden items (not merged with defaults), reverting to defaults when the override is cleared, source-attribution flipping between `'block'`/`'override'`, and a Long-description regression guard. Confirmed the test actually catches the regression by temporarily reverting the fix and re-running it (5 of 12 assertions failed, exactly the "with override" ones) before restoring the fix. User also verified live in the running app (`gearBlock` retargeted to Shorts, added to the Shorts Active Layout, edited via Advanced Options, confirmed in the Shorts Descriptions panel). `eslint`/`vite build` clean.

**Upload Calendar — drag-and-drop extended to future-dated uploads, plus an "Import from YouTube" matching gap fixed, shipped 2026-08-13.** User asked to drag not just planned entries but also uploaded videos dated in the future ("I sometimes change the schedule for video") — these represent a YouTube video still "Scheduled," not live yet (`isFutureUpload`, already used for the dashed-border "Scheduled" visual). Extended `CalendarSlotEntry`'s existing `canClear`/drag machinery (see the 2026-08-06 entry below, now superseded) into a wider `canDrag`: still-planned/missed entries stay draggable as before, and so does an uploaded/uploaded-drift entry when `isFutureUpload(isoDate, status)` — a past/today upload is real, confirmed history and stays non-draggable. Each `CalendarSlotEntry` now tags its drag payload with a `kind` (`'planned'`/`'uploaded'`), since a slot's `plannedEntryId`/`uploadedEntryId` are independent fields and a drag only moves one of them; `CalendarSlotDropZone.jsx` reads the active drag's `kind` via `useDndContext()` to decide which occupied flag (`plannedOccupied`/`uploadedOccupied`) blocks the target, replacing the old single `occupied` boolean. `CalendarMonthGrid.jsx`'s `onDragEnd` branches on `kind` to call `setUploadedEntry`/`clearUploadedEntry` instead of the planned-entry pair. `.calendar-slot-entry--clearable`'s padding was split into separate `--clearable` (right, for ×) and `--draggable` (left, for the handle) modifiers, since a future-upload row now shows a handle with no × next to it.

**Real bug found via live use, same day: a "mirrored" uploaded entry split into two on its first drag.** Confirming an upload from a plan (the existing status-click quick-toggle, `confirmUploadedAsPlanned`) stores `plannedEntryId === uploadedEntryId` — two fields holding the same id, not an independent plan plus an unrelated upload. The first drag implementation only moved `uploadedEntryId`, leaving the identical `plannedEntryId` behind as a ghost "Planned" row at the origin day, and the moved copy (now upload-only, no `plannedEntryId`) lost the ability to revert to "planned" via the status quick-toggle — clicking it just deleted it outright, since `clearUploadedEntry` had nothing left to fall back to. Fixed with a `mirrorsPlan` flag (`slot.plannedEntry?.id === slot.uploadedEntry?.id`, only meaningful for kind `'uploaded'`): a mirrored drag now moves both fields together (`onDragEnd` also calls `setPlannedEntry`/`clearPlannedEntry`), and `CalendarSlotDropZone` additionally blocks the target on `plannedOccupied` in this case specifically, so a mirrored move can't silently clobber an unrelated plan already sitting there. A pure log-only upload (no prior plan, e.g. from a YouTube import) is unaffected — it only ever had one field to move. **Known limitation, not fixed**: entries already split by the first (buggy) implementation before this fix don't self-heal — each needs one manual repair (Ctrl+Click the orphaned upload's title → "Change Plan" → re-pick the same song to restore the mirrored shape, then clear the ghost planned entry elsewhere).

**Import from YouTube — two related matching gaps fixed, same day.** Reported via a real paste: `2026-08-28 | Руки Вверх! | Unknown` stayed unmatched despite a real "Руки Вверх!" saved entry existing. `parseCalendarImportResponse` (`calendarImportPrompt.js`) previously only had one fallback tier (song-title-only, for the transliterated-artist case like "t.A.T.u." vs "ТАТУ") — no artist-only fallback existed at all, so an unparseable/placeholder song title with a perfectly good artist match had nowhere to land but `unmatched`. Added a second tier: an exact artist-name match, tried when the song-title tier finds nothing, gated by the same `MIN_FUZZY_MATCH_LENGTH` (3 chars, renamed from `MIN_FUZZY_SONG_LENGTH` since it now guards both tiers) guard. Also added: a literal `"Unknown"` song (case-insensitive, the AI's own improvised placeholder when it can identify the artist from a hashtag/description but genuinely can't read a stylized title) now skips the song-title tier entirely rather than being searched as a real title — needed because that tier runs *first* and returns as soon as it finds anything, so "unknown" coincidentally substring-matching some unrelated short saved song title would have silently consumed the match before the artist tier ever got a chance to run. Verified via Node smoke tests (bundled with `rolldown`): the exact reported case, a regression check that exact matches still bypass both fallback tiers, a too-short-artist guard check, and a synthetic substring-collision case (a saved song titled "Now," a literal substring of "unknown") confirming the artist tier now wins instead of a spurious song-title hit. `eslint`/`vite build` clean throughout all three fixes; no Playwright pass — verified live by the user directly, per standing QA-ownership preference.

**Tag Library Titles/Thumbnails: split into separate tabs, plus real `{placeholder}` support for tag phrases and Thumbnail Templates' Words/Fallbacks/Generic Tag Templates, shipped 2026-08-13.** User asked to (1) separate the combined "Titles" tab (which held both Long title phrases and Thumbnail phrases) into its own Titles/Thumbnails pair, and (2) "make them both use placeholders." Investigation before touching anything found (2) was more than a UI toggle — `generateTitles.js`/`generateThumbnails.js` render tag-specific title/thumbnail phrases as pure literal text, with zero `fillPlaceholders` resolution; a real config scan confirmed no existing phrase anywhere uses `{...}` tokens today, so this was a genuine capability gap, not a latent bug.

**(1) Tab split** — `tagFieldTabs.js`'s combined `titles: [{field:'title'}, {field:'thumbnail'}]` entry became two entries, `titles`/`thumbnails`; `TagEditorTabs.jsx` gained a `Thumbnails` tab next to `Titles`; `TagEditor.jsx`'s `getInitialTab` now routes `sourceTarget.field === 'thumbnail'` clicks to the new tab instead of the shared one, so click-to-navigate from a generated thumbnail still lands correctly.

**(2) Placeholder support, scoped deliberately narrow.** Full `HOOK_PLACEHOLDERS` (the set Short Hooks/Text Blocks use) includes random tokens (`{originalGenre}`, `{tags.*}`) that are normally safe only because the *value* gets frozen at pick time and replayed at render (`fillPlaceholdersAndFreeze`/`fillFrozenPlaceholders`) — Title/Thumbnail tag phrases have no pick-phase freezing of their own, so offering the full set would have re-rolled a random token on every keystroke. Instead added `LIVE_PLACEHOLDERS` (`hookPlaceholders.js`) — derived from `placeholders.js`'s newly-exported `ALWAYS_LIVE_TOKENS` (`artist`/`song`/`year`/`years`/`decade`/`signalNumber`/`currentYear`/`fileId`), all pure functions of `formData`, zero randomness, safe to resolve fresh on every render. See Known Gotchas for the full reasoning — don't casually widen these fields to the full set without adding real freeze support first.

Engine changes, both at render time only (no pick-phase changes, so live-refresh behavior is preserved): `resolveTitleRecord.js` now resolves a tag's own title phrase (`record.transformation`, e.g. `"{artist} finally goes dark"`) against a base ctx *before* it fills `{transformation}` in the outer title template — otherwise an embedded `{artist}` would leak through as literal text, since `fillPlaceholders` only scans a template string once. `generateThumbnails.js`'s `renderThumbnails` now resolves every phrase source uniformly (tag-specific phrases, plus the project-level Words/Fallbacks/Generic Tag Templates pools) through `fillPlaceholders` before uppercasing. `TagFieldTab.jsx` and `ProjectSettingsThumbnails.jsx` (Project Settings → Thumbnail Templates) both wire `LIVE_PLACEHOLDERS` into their editors' autocomplete — Generic Tag Templates keeps its existing separate `{tag}` token alongside it (an unrelated, already-pick-time-resolved literal substitution). Patterns (artistFull/artistShort/song keyword field) deliberately untouched — not a display-text field. `useGeneratedOutput.js`'s Titles/Thumbnails live-refresh memos widened to also depend on `formData.originalYear`/`formData.signalNumber`, since `{year}`/`{signalNumber}` are now resolvable inside these phrases.

Verified via two Node smoke tests (bundled with `rolldown`, this project's bundler) totaling 14 assertions: real project config produces zero leaked `{tokens}` (regression safety — nothing used placeholders before, so output must be unchanged), a synthetic `{artist}` tag phrase resolves correctly in both Titles and Thumbnails, live artist edits re-resolve without re-picking which phrase won, and Words/Fallbacks/Generic-Tag-Template phrases now resolve too while Patterns stays untouched. `eslint`/`vite build` clean throughout (same pre-existing unrelated errors). No Playwright pass per the user's standing QA-ownership preference — handed off with a checklist instead.

**Follow-up, same day: Thumbnails upgraded to real pick/render placeholder freezing, so they now support the full placeholder set, not just `LIVE_PLACEHOLDERS`.** User asked specifically about Generic Tag Templates — its whole purpose is to be a fallback usable across any tag, so it needs the wider vocabulary (`{originalGenre}`, `{tags.*}`, `{primaryTag}`, etc.), not just the deterministic subset. Rather than special-case just that one field, added the real freeze mechanism `generateThumbnails.js` was missing: `pickThumbnails` now calls `fillPlaceholdersAndFreeze(entry.phrase, freezeCtx)` on every candidate phrase at pick time and stores the result's `frozen` map alongside it; `renderThumbnails` (now taking `config` as a 3rd param, matching `renderTitles`'s signature) replays it via `fillFrozenPlaceholders` instead of plain `fillPlaceholders`. This is the same mechanism Descriptions/Short Hooks already use — once in place, it's safe everywhere it applies, not just for one field, so **all** four thumbnail phrase sources (Tag Library's tag-specific phrases, and the project-level Words/Fallbacks/Generic Tag Templates pools) now support the full set uniformly. `ProjectSettingsThumbnails.jsx` and `TagFieldTab.jsx`'s Thumbnails tab both widened their `placeholders` prop from `LIVE_PLACEHOLDERS` to `buildHookPlaceholders(projectConfig)` accordingly (`TagEditor.jsx` needed one added prop — `projectConfig` wasn't previously threaded to the Thumbnails tab call, only Short Hooks'). **Titles' tag phrases were deliberately left on the narrower `LIVE_PLACEHOLDERS` set** — `resolveTitleRecord.js` wasn't touched this round, so adding the same freeze there is a separate follow-up if wanted. `useGeneratedOutput.js`'s thumbnails render memo now also depends on `resolvedProjectConfig` (previously missing entirely from that memo, unlike Titles'). Verified via a 9-assertion Node smoke test: real config still produces no leaked tokens; a synthetic `{originalGenre}`/`{tags.*}` pick stays byte-identical across repeated renders *and* survives a live Artist edit (the actual freeze-safety property, not just "it resolves"); `{artist}`/`{tag}` still resolve correctly alongside the frozen random token in the same phrase. `eslint`/`vite build` clean.

**Generator's Input section is now collapsible and remembers the choice, shipped 2026-08-13.** User asked for a collapse toggle showing only "Artist — Song" on one line when collapsed. Reused the existing `panelVisibility`/`togglePanel` mechanism (`useAppShellState.js`) rather than adding new state — `panelVisibility.input` (default `true`) persists via the same `illegalMindGeneratorData.ui.panelVisibility` storage path every other panel toggle already uses. `InputForm.jsx`'s `<h2>Input</h2>` now sits in a `.panel-header` row next to a compact `ToggleButton`; when collapsed, everything below (Basic Fields, Entry Settings, Todo, Advanced Options, Save/Clear/Add-to-Calendar) is hidden and replaced with a one-line `**Artist** — Song` summary (reusing `SavedEntryRow`'s established artist—song text format), matching the literal ask of "only artist + song name shown in a line." One necessary fix along the way: `useNavigationTargets.js`'s `openSongOverride` (fired when clicking an Artist/Song-sourced hashtag chip, or any song-overridden description block, to jump back to the source field) previously only force-expanded Advanced Options — now also force-expands Input, since every override field lives inside Input's now-collapsible body and wouldn't otherwise mount to be scrolled to. `eslint`/`vite build` clean.

**Upload Schedule slots gained an optional "title" label, shown on both the Settings screen and the Calendar page, shipped 2026-08-07.** Lets a slot be distinguished from the default covers use (e.g. a "Vlog" or "Behind the Scenes" slot) — purely visual, no change to matching/generation logic (`videoType`/`weekday` still drive everything). Confirmed via `AskUserQuestion` the label should reach the Calendar grid, not just Settings. `ProjectSettingsUploadSchedule.jsx`'s slot rows gained a title text input stacked above the existing weekday/type/time/remove row; `useUploadCalendar.js`'s `getMonthGrid` now resolves `title` onto each `calendarSlots` item (previously had no reference back to the scheduled-slot config); `CalendarDayCell.jsx` shows it in both the empty placeholder and `CalendarSlotEntry`'s status line (new `slotTitle` prop) — deliberately not just the empty state, so a "Vlog" slot doesn't lose its label the moment something's actually planned into it. `.calendar-slot-placeholder` gained ellipsis-truncation CSS since it can now hold arbitrary-length text, not just SHORT/LONG. `eslint`/`vite build` clean. **Follow-up, same day**: the title input originally stretched `width:100%` (read like an oversized heading) and the tab's single card used `.tag-library--3col` — the same `auto-fill` repeating-card grid every Project Settings tab uses regardless of card count, which for a genuinely single-card tab just produces whatever width falls out of that math for a given viewport, not a deliberate size. Fixed: title input shrunk to a fixed `200px`, left-aligned, tighter padding; new `.tag-library--half` modifier (`minmax(320px, 50%)`) pins a single-card tab to roughly half its container directly, reusable by other single-card tabs later.

**`window.alert()` replaced with a new `Toast` primitive, shipped 2026-08-06/07.** User reported the native browser popup (screenshotted after an "Add to Calendar" click) visually clashed with the CRT terminal design and required an explicit "OK" click. All 6 `window.alert()` call sites migrated to `showToast(message)` — see UI Primitives above for `Toast`/`useToast`. The 7 existing `window.confirm()` destructive-delete guards were explicitly left untouched (need a synchronous yes/no, which a toast can't provide). New UIKit catalog entry added alongside Modal's. **Follow-up, same feature reported missed**: `AppBackupControls.jsx`'s Import-backup flow used a *bare* `alert(...)` (no `window.` prefix) — invisible to a `window.alert(` grep, so it was missed in the first pass. Migrated too: `showToast` threaded through `App.jsx` → `ProjectSettingsPage.jsx` → `ProjectSettingsContent.jsx` → `ProjectSettingsGeneral.jsx` → `AppBackupControls.jsx`, the same chain `onOpenUIKit` already uses. One necessary behavioral adjustment while doing this — the success-path `setTimeout(..., 300)` before `window.location.reload()` previously only started counting *after* the user dismissed the blocking `alert()`; with a non-blocking toast, that 300ms would elapse almost immediately and reload before the message could be read, so it's bumped to 1500ms. `eslint`/`vite build` clean (two pre-existing unrelated lint errors, `GeneratorPage.jsx`'s unused `handleRegenerate` and `UIKitPage.jsx`'s unused `hex`, both confirmed via `git stash` to predate this change). A repo-wide `\balert\(` grep (not just `window.alert(`) confirms no call sites remain anywhere.

**Upload Calendar — remove-from-queue (×) button added to the picker's Shorts Queue suggestion list, shipped 2026-08-06.** Reused the Shorts Queue page's existing "×" behavior (`ShortsQueueItem` → `useShortsQueue.js`'s `markUploaded`, removes that slot and auto-refills a valid replacement at the bottom) inside the Calendar's "Plan Short" picker. `CalendarPage.jsx` now also destructures `markUploaded` and adds `handleRemoveFromQueue(entryId)` — since the picker's `shortsQueueEntries` is deduped by id, this resolves back to the *first* raw index holding that id (`shortsQueue.findIndex(...)`) before calling `markUploaded`, reusing the function verbatim rather than adding a second removal path. `CalendarEntryPicker.jsx` renders an `IconButton` "×" in each row's `actions` slot, gated on `isShort && !needle` — only while genuinely showing the queue-suggestion list (once search widens to the full library, a result may not be queued at all). No `onClose()` on remove — picker stays open, list re-renders from the parent's already-reactive `shortsQueueEntries`. `eslint`/`vite build` clean.

**Upload Calendar — `addToNextOpenSlot` bug fix + × clear + drag-and-drop between slots, shipped 2026-08-06.** Reported live: "Add to Calendar" assigned a song on top of a Friday slot that already had a real upload logged (no plan yet, but a real `uploadedEntryId`), turning a clean "Uploaded" cell into "Differs" instead of skipping to the next free day. Root cause: `findNextOpenSlot` (`useUploadCalendar.js`) only checked `plannedEntryId`, never `uploadedEntryId`, to decide a slot was open — fixed with one added condition. Two follow-up features scoped via `AskUserQuestion`: a corner × to clear a still-planned entry, and drag-and-drop to move one between slots — both deliberately restricted to *planned* (or missed/unconfirmed) entries only; an uploaded/uploaded-drift entry gets neither, since clearing/moving real upload history should require first reverting it to planned via the existing status-click quick-toggle, not a one-click action. `CalendarSlotEntry` (`CalendarDayCell.jsx`) gained a `canClear` check gating both a corner `IconButton` (×, calls existing `calendar.clearPlannedEntry`) and a `⠿` drag handle (`useDraggable`, disabled otherwise), positioned in opposite top corners via new `.calendar-slot-entry--clearable` padding. New `CalendarSlotDropZone.jsx` wraps every slot row (`useDroppable`, id = the slot's own key), `disabled` when the slot already has a `plannedEntryId` — dropping onto an upload-only slot (no plan) is still allowed, matching what Ctrl+Click "Change Plan" already permits. `CalendarMonthGrid.jsx` wraps the grid in one `DndContext` (`PointerSensor` only, no `KeyboardSensor` — this is cross-cell dragging between arbitrary slots, not a sortable list like this app's existing `@dnd-kit` usage elsewhere, so `sortableKeyboardCoordinates` doesn't apply; Ctrl+Click remains the keyboard-accessible move path). `onDragEnd` rejects same-slot/mismatched-videoType drops, otherwise sets the target's plan then clears the origin's (a move, not a copy). Verified live via Playwright (isolated seeded storage, 3 consecutive runs): next-free-slot skip confirmed past both an upload-only and a planned day; drag via real mouse events correctly moved a plan between same-type slots; × correctly absent on an upload-only entry and present/working on a planned one; 0 console errors. `eslint`/`vite build` clean. One pre-existing bad "Differs" cell from the original bug needs a manual fix in real project data — this session never touched it. **Superseded 2026-08-13** — see the entry near the top of Current Focus: drag-and-drop was widened to also cover future-dated uploaded/uploaded-drift entries, not just planned/missed ones.

**Upload Calendar — "Import from YouTube" paste-parser shipped 2026-08-05.** First Phase 2 sub-feature (see plan `C:\Users\Max\.claude\plans\yes-let-s-do-this-prancy-shell.md`) — bulk-syncs real YouTube upload history into the calendar, since YouTube video titles are stylized and don't match this app's Artist/Song naming (e.g. "Still love what The Offspring did with Defy You" is actually The Offspring's "Defy You"). Same AI-round-trip pattern as Missing Data/Tag creation: the user screenshots their YouTube Studio Content list (Shorts and Videos are separate tabs), feeds it to an external AI with a copied prompt, pastes the reply back into a new "Import from YouTube" `CollapsiblePanel` on `CalendarPage.jsx`.

**Prerequisite bugfix, done first as its own step and verified independently before touching the new feature.** `useUploadCalendar.js`'s `patchSlot`/`updateSlots`/`removeSlot` read `slots` from the render-time closure instead of the latest React state — every existing single-call flow (Shorts Queue/Generator add-to-calendar, the manual picker) never exercised this, since each only makes one calendar-mutating call per action, but the new import Apply handler is the first caller that needs to call `setUploadedEntry` N times in a synchronous loop (once per matched row). Since each call would have computed its patch from the same stale `slots` snapshot, only the *last* call in the loop would have survived — in both React state **and** localStorage (both writes derive from the same stale base object, so this wasn't just a render glitch, it would have silently dropped every row but the last one from storage too). Fixed by switching `updateSlots` to the functional-updater form (`setSlots((prevSlots) => {...})`, matching `useSavedEntries.js`'s `handleAddEntries` precedent) and chaining `patchSlot`/`removeSlot` through it. All public method signatures unchanged. Verified live via Playwright: the existing single-call flows (Shorts Queue add-to-calendar, calendar picker Clear) still work identically post-fix, 0 console errors.

**`src/utils/calendarImportPrompt.js` (new)** — `buildCalendarImportPrompt(videoType)`/`parseCalendarImportResponse(text, savedEntries)`, same contract shape as `searchQuery.js`'s Missing Data pair (parser reports what it found, never decides what to write). The prompt branches its extraction instructions on `videoType` because real YouTube Studio screenshots show the Artist/Song pair lives in different places per tab: **Shorts** titles are stylized hooks/questions with the real source only named in the grey description snippet underneath (patterns: `"Artist - Song (...)"`, `"Song by Artist // ..."`, `"Song // Cover by Artist"`, occasionally only in a hashtag as a last resort); **long-form** titles usually already contain the Artist/Song pair directly (e.g. "Rammstein – Sonne (What If It Was Punk?)"), falling back to the description only if the title doesn't make it clear. Both ask for ISO `YYYY-MM-DD` dates and a `YYYY-MM-DD | Artist | Song` reply line per video, or `SKIP | YYYY-MM-DD | reason` for a video with no identifiable cover source (e.g. an original composition). `parseCalendarImportResponse` reuses `normalizeForMatch` (now `export`ed from `searchQuery.js`, was private — identical case/whitespace/curly-quote matching need) and returns `{ matches, unmatched, skipped, unrecognized }` — deliberately does not dedupe same-day/same-type collisions within one batch, that's left to the caller to detect and report (matches don't get silently merged or dropped at the parser level). Verified via a 20-assertion Node smoke test (bundled with `rolldown`, this project's bundler): prompt branches, real-example anchors present, all four result buckets correct including a curly-quote match and an uncollapsed same-date collision.

**`CalendarImportPanel.jsx` (new)** — same Copy/Paste/Apply/Cancel/summary shape as `MissingDataTools.jsx`, plus a Short/Long `FormSelect` selecting which `videoType` the pasted batch targets (one YouTube Studio tab = one paste, per the user's explicit preference over having the AI label each row's type). `handleApply` always calls `calendar.setUploadedEntry(isoDate, videoType, entry.id)` per match — **confirmed via `AskUserQuestion`: YouTube data is source of truth, so an existing different upload gets overwritten, never skipped** — reading `calendar.getSlot` first only to tally new/updated/unchanged/collision counts for the result summary, not to gate the write. **Also confirmed via `AskUserQuestion`: both "Scheduled" (not live yet) and "Published"/"Premiered" YouTube videos are logged identically, always via `setUploadedEntry`, never `setPlannedEntry`** — the user's own words: "those videos are already uploaded... I'd just color differently the videos from the future/past just to visually differentiate somehow." So the Scheduled-vs-Published distinction is presentational only (see below), not a different write target or a new stored field.

**Follow-up fix, same day: import also resolves stale plan drift, not just the upload.** Real usage surfaced a gap — a slot that already had a *different* `plannedEntryId` from before the import (e.g. an earlier manual guess) showed as `uploaded-drift` ("Differs," both rows visible) after importing the real upload, since the import only ever wrote `uploadedEntryId` and left `plannedEntryId` untouched. Correct behavior for the manual picker (a human deliberately picking a different upload than planned — drift tracking's actual purpose), but wrong for a bulk historical import: a stale pre-import guess shouldn't survive as a "differs" flag on real ground-truth data. Fixed in `CalendarImportPanel.jsx`'s `handleApply` — after `setUploadedEntry`, if `existingSlot.plannedEntryId` is set and differs from the imported entry, `setPlannedEntry` aligns it to match too, so an imported row always lands as a clean "Uploaded," never "Differs." Manual-picker drift is untouched — this only fires inside the import's write loop. Verified live: seeded a slot with a mismatched pre-existing plan, imported a different real upload, confirmed the cell collapsed to a single clean "Uploaded" row (both `plannedEntryId`/`uploadedEntryId` now equal in storage), old planned song gone from the cell, 0 console errors.

**Second follow-up, same day: song-title fallback matching + a confirmation UI for ambiguous/uncertain matches.** Real usage surfaced a real unmatched case: `2026-01-24 | t.A.T.u. | Нас не догонят` failed to match a saved entry stored as `ТАТУ — Нас не догонят` — the song title was identical, only the artist name differed (Latin transliteration vs the Cyrillic stage name). User asked for song-title fallback matching, with a confirmation step when a song title matches more than one saved entry (e.g. "Stay," "Breathe," "Hold On" — generic titles covered by many different artists). `parseCalendarImportResponse` (`calendarImportPrompt.js`) now falls back to a song-title-only search (`songTitleMatches` — exact match or substring containment either direction, reusing `normalizeForMatch`; skipped entirely below `MIN_FUZZY_SONG_LENGTH` = 3 chars to avoid false positives on very short titles) whenever the exact artist+song match fails. Candidates found this way are **never auto-applied** — they go into a new `needsConfirmation` bucket (`{isoDate, artist, song, raw, candidates}`), kept structurally separate from `unmatched` (which now only means "no saved entry has this song title at all"). `CalendarImportPanel.jsx` renders one row per pending item — `{isoDate} — {artist} — {song}` plus a `FormSelect` listing every candidate (`{artist} — {song}` labels) with "Skip" as an explicit option; a single-candidate row auto-pre-selects that candidate (still requires the explicit "Confirm Selected" click, never writes silently), a multi-candidate row defaults to "Skip" so an ambiguous case is never guessed. A shared `applyImportMatch(calendar, videoType, isoDate, entry, tally)` helper (extracted from the original immediate-apply loop) is reused by both the auto-apply path and the confirm-pending path, so both get the same tally/plan-alignment behavior with no duplicated logic. The result summary gained a `confirmedCount` clause, additively merged into the original tally after confirming (not a separate second summary). Verified via a 12-assertion Node smoke test (the exact reported t.A.T.u./ТАТУ case, an ambiguous 2-candidate case, exact-match regression, true-unmatched, partial-substring match, and the short-title-skips-fuzzy-fallback guard) and live via Playwright: single-candidate row auto-pre-selected the correct entry, ambiguous row correctly listed both candidates and defaulted to Skip, picking a specific candidate and confirming wrote exactly that entry to the calendar, confirmation rows cleared after confirming, 0 console errors.

**Trigger UI revised same-day after live user feedback: `CollapsiblePanel` → `Modal`.** First shipped rendered inside a `CollapsiblePanel` on `CalendarPage.jsx`, positioned above the month nav — user feedback after seeing it live: the permanent bordered panel-header bar visually competed with the month grid for top billing (a secondary/occasional action given the same visual weight as primary content), and sitting above the nav pushed the actual calendar down. Fixed by introducing this app's first `Modal` primitive (see UI Primitives above) — the trigger is now one small `.button-secondary` in a `.regenerate-row` (top-right, same right-aligned single-action-button pattern `ShortsQueuePage`'s "Randomize" and the Generator's "Regenerate" already use) sitting beside the month nav, and `CalendarImportPanel` renders inside a `Modal` only while open, with zero permanent footprint when closed. This is `CalendarPage.jsx`'s first page-level toolbar action either way — previously only per-day-cell click targets existed.

**Future-upload visual differentiation (presentational only, no new stored field)** — `CalendarDayCell.jsx` gained `isFutureUpload(isoDate, status)`: true when status is `'uploaded'`/`'uploaded-drift'` and the date is after today. `CalendarSlotEntry` gained an `extraClassName` prop appending `calendar-slot-entry--scheduled` (new CSS: dashed border, same color as its status modifier — matches `.calendar-slot-placeholder`'s existing dashed-border precedent) when true. In the `uploaded-drift` branch (which renders two `CalendarSlotEntry`s side by side), it's attached only to the second (`uploaded-drift`) one, not the first (`planned`). Purely a render-time date comparison, consistent with `deriveSlotStatus` already being "always derived, never stored."

**Verified live via Playwright end-to-end**, isolated seeded `localStorage` (never real project data): pasted 3 matched rows on 3 different dates (past/today/future) → **all 3 landed correctly, not just the last one** (the actual regression test for the stale-closure bugfix); persisted correctly across a page reload; future-dated slot rendered with the dashed `--scheduled` class, past-dated one didn't; re-pasting the identical batch reported "0 new, 3 unchanged"; importing a different song into an already-filled slot correctly overwrote it and reported "1 updated" (confirmed via direct storage read, not just the summary text). 0 console errors throughout. `eslint`/`vite build` clean. **Modal swap separately verified live**: no permanent panel chrome visible before opening; Escape, backdrop click, and the × button all close it; a click inside `.modal-content` does not close it; body scroll locks while open and restores on close; the full Copy/Paste/Apply flow still works identically inside the modal (confirmed by writing a real calendar slot from inside it). 0 console errors.

**Deliberately deferred, unscoped**: a "suggested songs to add" feature, and click-to-navigate-and-highlight from Shorts Queue/Generator to the day just added (both `addToNextOpenSlot` call sites already return the assigned `{isoDate, videoType}`, so this remains cheap to add later).

---

**Upload Calendar — Phase 1 fully shipped 2026-08-05 (all 7 steps).** New multi-iteration feature: a visual month-grid calendar per project showing the weekly upload rhythm, letting songs be assigned to specific days, and tracking planned-vs-actual drift (the calendar never syncs with YouTube, so nothing is assumed — the user must explicitly confirm). Full plan: `C:\Users\Max\.claude\plans\i-think-the-time-wild-floyd.md`.

**Step 7, add-from-Generator, shipped 2026-08-05 — closes out Phase 1.** New "Add to Calendar" button in `InputFormActions.jsx`, beside Save/Clear Form, threaded down from a new `handleAddToCalendar`/`isCurrentEntrySaved` pair computed in `GeneratorPage.jsx` (via `InputForm.jsx`, a plain prop pass-through, no new state there). `GeneratorPage.jsx` instantiates its own `useUploadCalendar(projectId, savedEntries, projectConfig.uploadSchedule)` — a third independent hook instance alongside `CalendarPage.jsx`'s and `ShortsQueuePage.jsx`'s, same established per-page pattern. The plan's flagged edge case (the currently-loaded song must already be a saved entry, i.e. have a real id, before `addToNextOpenSlot` can target it) is handled without adding an `id` field to `formData` — `formData` never carries one (see `buildFormDataPatchFromEntry`/`buildEntryFromFormData` in `useSavedEntries.js`'s Known Gotchas), so `GeneratorPage.jsx` recomputes the candidate id the same way saving does, via `buildEntryId(formData.artist, formData.song)` (`src/utils/savedEntries.js`, already exported), and checks `savedEntries.some(e => e.id === currentEntryId)`. The button is `disabled` with a `"Save this entry first"` tooltip when that's false — matches the plan's explicit-action bias (no silent auto-save) and the codebase's established disabled+tooltip convention. `formData.videoType` (`'Long'`/`'Shorts'`) is mapped to the calendar's lowercase `'long'`/`'short'` domain at the call site, same mapping `CalendarEntryPicker.jsx` already uses elsewhere. Feedback is the same `window.alert` pattern step 6 established. Verified live via Playwright (isolated `localStorage` seed): button correctly disabled on a blank form and on an unsaved new artist/song, correctly enabled once the form matched a saved entry; clicking with Long selected landed on Illegal Mind's actual scheduled Long weekday (Sunday), switching to Shorts and clicking again landed on its scheduled Short weekday (Wednesday) as an independent slot — both written with the correct slot-key shape, no cross-contamination; 0 console errors. `eslint`/`vite build` clean (one pre-existing unrelated lint error in `GeneratorPage.jsx`, `handleRegenerate` unused, confirmed via `git stash` to predate this change).

**Step 6, add-from-Shorts-Queue, shipped 2026-08-05.** `ShortsQueueItem.jsx` gained a second `IconButton` ("📅", title "Add to Calendar") beside the existing "×"; `ShortsQueuePage.jsx` instantiates its own `useUploadCalendar(projectId, savedEntries, projectConfig.uploadSchedule)` (a separate hook instance from `CalendarPage.jsx`'s — same pattern `useShortsQueue` already uses per-page, each instance reads fresh from storage on mount and writes through `updateAppStorage`, so no shared-instance wiring was needed) and calls `calendar.addToNextOpenSlot(entry.id, 'short')` on click. Feedback is a plain `window.alert` (matches the existing precedent in `TagLibraryPage.jsx`'s Sync/Copy actions) — either `"Added to calendar: {formatDayLabel(target.isoDate)}"` or `"No open Short slot found in the next 90 days."` if `addToNextOpenSlot` returns `null`. No new state, no new files — `resolvedProjectConfig` (already carrying `uploadSchedule`) was already being passed into `ShortsQueuePage` as `projectConfig`, so no prop-threading changes were needed anywhere upstream. Verified live via Playwright (isolated `localStorage` seed, not real project data): clicking the button assigned a test entry to the correct scheduled Short weekday, wrote the expected `uploadCalendar` slot shape, and the entry then rendered correctly on the Calendar page grid; 0 console errors. `eslint`/`vite build` clean.

**Data model, confirmed via several rounds of clarifying questions before building anything:**
- New `uploadSchedule.slots` config per project in `projects.json` (`{weekday, videoType, time}`) — Illegal Mind: Mon/Wed/Fri short + Sun long; Maxx Dee: Sun/Tue/Thu short + Sat long, all 18:00. Edited via a new Project Settings "Upload Schedule" tab (`ProjectSettingsUploadSchedule.jsx`), always rewritten as a whole `{slots:[...]}` object on every edit (matches `todoStatuses`' whole-array-replace discipline — confirmed no dedicated merge block was needed in `buildResolvedProjectConfig.js` by checking against `shortsQueue`'s two-field merge block, which exists for the opposite reason).
- New `uploadCalendar` storage slice (`src/utils/storage.js`), one entry per project, keyed by `` `${isoDate}|${videoType}` `` (not just date — a day can hold an ad-hoc slot of a type it isn't scheduled for). Each slot holds two **independent, nullable** fields — `plannedEntryId`/`uploadedEntryId`, not a single song+status pair. Video type is an attribute of the *slot*, never the saved entry (saved entries deliberately gained no `videoType` field).
- Status is **always derived**, never stored: `deriveSlotStatus` (`src/hooks/useUploadCalendar.js`) — empty / **planned** (future, unconfirmed) / **missed** (past, unconfirmed — flagged, never silently assumed) / **uploaded** (confirmed, matches plan or no prior plan) / **uploaded-drift** (confirmed, differs from plan — both the original plan and the actual upload stay visible).
- `useUploadCalendar.js`'s core logic (`deriveSlotStatus`, `findNextOpenSlot`, `getMonthGrid`) is written as pure functions taking explicit arguments, separate from the `useState` wiring — Node-testable without a React runtime, matching this codebase's established pick/render-split precedent. `calendarDates.js` (new) is hand-rolled native-`Date` month-grid math — no new dependency, deliberate choice matching the app's minimal-dependency bias (`@dnd-kit` was the one justified exception).

**Three ways to add a song, one shared by two of them:** manual add-from-calendar (`CalendarEntryPicker.jsx`, an inline search-and-pick panel — no modal exists anywhere in this codebase) is built; add-from-Shorts-Queue and add-from-Generator (steps 6-7, "auto-assign to next open slot" via `addToNextOpenSlot`) are not yet wired.

**Shorts Queue sourcing** (added after live user feedback on step 4, loosened 2026-08-06): Short slots default to the current Shorts Queue as a suggestion list (deduped, empty-queue state points to the Shorts Queue page) when the picker's search box is empty — but typing a search widens to the full saved library, since restricting search itself made it impossible to plan a Short for a song not already queued. Long slots always search the full saved library regardless of the search box, since there's no queue concept for longs.

**Interaction model went through two live-feedback iterations before landing on the final minimal design** — worth reading in full if touching `CalendarDayCell.jsx` again, since the reasoning isn't obvious from the code alone:
1. First pass: two `IconButton`s per entry (✎ change plan, ✓ confirm/set upload) sharing a line with the status text — crowded, and the "✓ stays ✓ or becomes ✗" question had no clean answer.
2. User proposed a "..." kebab menu for minimal design over double-click (double-click can't express "pick a specific different song" for drift, so it would've needed a second mechanism anyway). Built it — then found a real bug along the way: `IconButton`'s `className` prop *replaces* the default `tag-reset-button` class rather than appending to it, so a custom class alone silently lost all button-reset styling and rendered raw browser chrome (a solid white box). Fix: pass both classes (`"tag-reset-button calendar-slot-entry-menu-trigger"`).
3. **Final design, current state**: no visible buttons at all. Reuses this app's existing Ctrl+Click convention (Transformation Tags → Tag Library is the precedent) instead of a menu — title click loads into Generator, Ctrl+Click opens "Change Plan"; status click **directly** confirms-as-planned or clears the upload (no picker, no round-trip — `calendar.confirmUploadedAsPlanned`/`clearUploadedEntry` called straight from `CalendarDayCell`, which now receives the whole `calendar` hook object as a prop alongside `onSlotClick`, matching the precedent `CalendarEntryPicker` already set); Ctrl+Click on status opens the picker in upload mode to search for a *specific* different song (the drift case). Empty placeholders gained the same treatment for free — Ctrl+Click logs an upload with no prior plan, closing a gap from step 5. `CalendarSlotEntry` dropped its `useState` entirely; genuinely simpler code, not just fewer pixels.

Also fixed along the way: a real CSS Grid bug where `.calendar-day-cell` (a grid item) lacked `min-width:0`, so a column with unwrapped long text claimed more than its `1fr` share while the emptiest column absorbed the squeeze — all 7 columns are now always equal width regardless of content. Day-cell entries render as a compact 2-line card (`CalendarSlotEntry`, artist/song on separate lines) rather than reusing `SavedEntryRow`'s horizontal layout, which was crushing titles to invisible in a ~180px cell — every other `SavedEntryRow` context is a full-width list, this one genuinely isn't.

**Deliberately deferred to a later iteration** (confirmed with the user up front, not forgotten): a paste-import parser for YouTube-generated text or bulk-pasted schedules (needs a real text sample to design against, same lesson as the Missing-Data AI round-trip feature); a "suggested songs to add" feature; click-to-navigate-and-highlight from Shorts Queue/Generator to the specific day just added.

**Verified at every step via live Playwright** against the running dev server (isolated browser profile, never touching real project data) — 6 separate test scripts now exist covering: settings tab persistence/add/remove/reset, month grid rendering/nav/schedule markers, the full add/edit/clear/cancel/persist picker flow, all 4 derived statuses, Shorts Queue empty-state/sourcing/dedup, confirm/drift/revert, and the empty+Ctrl+Click upload-logging case. Zero console errors across all of them. `eslint`/`vite build` clean at every commit.

**Storage cleanup pass closed out, shipped 2026-08-04.** The three-item PRIORITY TODO (storage-unification cleanup) is done. `appBackup.js` no longer captures/restores the old standalone legacy keys — `LEGACY_KEYS`/`readLegacyData`/the `legacy` field on `buildAppBackup()`/the legacy write-back loop in `restoreAppBackup()` all removed (63 → 37 lines); backup/restore now only touch the unified `illegalMindGeneratorData` key. `storageMigration.js` deleted entirely, along with its DEV-only `window.previewUnifiedStorageMigration`/`writeUnifiedStorageMigration` exposure in `App.jsx` (and the now-unused `useEffect` import there). Confirmed via grep first that each hook already does its own one-time fallback-and-migrate instead of relying on that utility — `useAppShellState.js`/`savedEntries.js`/`useSavedLibraryFilters.js`/`GeneratorPage.jsx`/`SavedLibrary.jsx` each check the unified key first and only fall back to *reading* (never writing) a legacy key when the unified value is empty; that per-hook fallback code is untouched, it's the real replacement mechanism, not part of this cleanup. The third item (deleting the stale standalone keys from localStorage) needed no code change — user confirmed via DevTools none of the old keys (`savedEntries`, `shortsQueueByProject`, `tagOverrides`, `tagVisibilityOverrides`, `formData`, `panelVisibility`, `activePage`, `selectedProject`, `showSavedLibrary`, `hideQueueHidden`) exist anymore. `eslint`/`vite build` clean.

**Todo Statuses: reorderable, shipped 2026-07-30.** User asked for a way to reorder Todo statuses, since `TodoPage.jsx` already builds its sections by mapping `todoStatuses` in array order (no separate wiring needed — reordering the array alone reorders the page). `ProjectSettingsTodo.jsx`'s Statuses editor previously used `TemplateGroupCard`'s built-in `templates`/`onUpdateTemplates` path (`HookTemplateEditor`, search box, no reorder) — swapped for a hand-rolled row list (`TemplateGroupCard` used as a plain content shell) since order is meaningful here, unlike every other array `HookTemplateEditor` normally edits (random-pick phrase pools). Two-step delivery: first shipped ↑/↓ buttons — `PhraseRow.jsx` gained optional `onMoveUp`/`onMoveDown`/`disabledMoveUp`/`disabledMoveDown` props (rendering a `MoveControls` pair in front of the field only when passed, so existing callers like Hook Blocks/Tag phrase editors are unaffected). User then pointed out buttons alone don't scale ("clicking 15 times to move the bottom one to the top?") — added `@dnd-kit` drag-and-drop on top, reusing the exact pattern `SortableActiveBlock.jsx` already established for Active Layout/Block Group reordering: a `SortableStatusRow` wrapper (drag handle + the same `PhraseRow`, buttons kept as the precise/keyboard-accessible fallback, not replaced). `id` is `` `${status}::${index}` `` (value+index combined) rather than the bare status string, specifically to stay unique even when two blank rows exist mid-add (e.g. clicking "+ Add" twice before typing either label) — safe despite not being a stable per-item identity across renders, since dnd-kit only needs the id to hold for the duration of one drag gesture (state doesn't change until drop). One small CSS addition: `.tag-phrase-row` added to the existing `.desc-block-wrapper > .tag-card, .desc-block-group` flex-fill rule. Verified live via Playwright: buttons — moved "Needs Re-record" above "Wishlist" via a single ↓ click, confirmed the Todo page's section order changed to match, restored; drag — dragged "Needs Video" from the bottom straight to the top in one gesture (real mouse move/down/up events, not a single programmatic jump, since dnd-kit's `PointerSensor` requires actual pointer movement to activate), confirmed Todo page order updated to match, restored; 0 console errors throughout both passes.

**Scroll-to-top on page switch, shipped 2026-07-30.** Small UX fix — navigating between pages left scroll position wherever it was on the previous page. Added `window.scrollTo(0, 0)` to exactly the three places that represent a plain "switch to a different page with no specific target" action: `AppHeader.jsx`'s main nav tab buttons, `App.jsx`'s `loadEntryAndReturnToGenerator`, and the "Open UIKit" button. Deliberately NOT a blanket `activePage`-change effect — this app's extensive click-to-navigate system scrolls to and highlights a *specific* element (title/thumbnail/hook/tag/etc.), and a generic top-level scroll reset would race with and likely break that; scoping it to the three targetless call sites avoids the conflict entirely. Verified via live Playwright: nav-tab switches confirmed `scrollY` resets to 0; a Ctrl+Click tag jump confirmed the click-to-navigate/highlight system is unaffected — still lands on and scrolls to its specific target (non-zero `scrollY`), not overridden; 0 console errors.

**`hooks/useAppShellState.js` split, shipped 2026-07-30: 312 → 183 lines — strongest reduction in the whole refactor pass, eleventh file down, pass now effectively complete.** Unlike the partial wins on `App.jsx`/`BlockGroups.jsx`, found one large, fully self-contained seam: the 7 click-to-navigate target states (`tagLibrarySearchTarget`, `shortHooksTarget`, `titlesTarget`, `thumbnailsTarget`, `hashtagsTarget`, `blocksTarget`, `songOverrideTarget`) plus `activeProjectSettingsSection` (state + its own persistence effect) plus every `open*`/`clear*` handler — one coherent concern, touching only `setActivePage`/`setPanelVisibility`, both already available. Extracted verbatim to new `src/hooks/useNavigationTargets.js` (147 lines), taking those two setters as parameters and returning an object `useAppShellState.js` spreads (`...navigationTargets`) into its own return — public return shape unchanged, `App.jsx` needed zero edits. **One required, deliberate fix, not a pure copy in this one spot**: `openSongOverride`'s `useCallback` had `[]` deps in the original, safe there only because `setPanelVisibility` was a `useState` setter in the *same* component (locally verifiable stable). Once it became a cross-file parameter, React's exhaustive-deps lint correctly flagged a real compiler error — fixed by adding `setPanelVisibility` to the deps array, behaviorally identical at runtime. **Verified via a broad live Playwright pass**: all 6 pages load clean; every one of the 7 navigation targets exercised individually with real DOM/state assertions — Tag Library, Titles (exact template highlighted), Thumbnails, Hashtags, Short Hooks, Blocks, and the same-page Song Override target (field highlighted, clears on edit) — all landed correctly; `activeProjectSettingsSection` persistence survived a full reload; stale-target-clearing (separate hook) still correctly clears after nav-away-and-back; 0 console errors/warnings, including the dependency-array-fix path. Large-file refactor pass status: only `App.jsx` (300 lines) remains flagged, judged a permanent partial-reduction case like `UIKitPage.jsx` — its remaining bulk is inherent six-page routing JSX, both its safe seams already extracted. `engine/placeholders.js`/`hookBlockOverrides.js` remain deliberately skipped.

**`App.jsx` split, shipped 2026-07-30: 351 → 300 lines — root component, honest partial reduction, tenth file down.** First investigated `engine/placeholders.js` (the alternative candidate offered) and found it resists a safe pure-move split for a structural reason: its token resolvers (`resolveLogNoteToken`/`resolveOperatorStatusToken`/`resolveTransformationToken`) call `fillPlaceholders`, which resolves tokens via the `REGISTRY` object containing those same resolvers — genuine mutual recursion, not incidental size. Splitting it needs either a circular import (which this codebase already works hard to avoid — `resolveOverrideText`'s intentional duplication, `shortHookEligibility.js`'s dependency-free-leaf-module precedent) or dependency-injecting `fillPlaceholders` as a parameter (the pattern `pooling.js` already uses to dodge exactly this), which changes signatures throughout — a real refactor, not a pure move. Recommended leaving it alone (like `hookBlockOverrides.js`); user chose `App.jsx`/`useAppShellState.js` instead. `App.jsx` had two genuinely safe seams: two self-contained `useEffect` blocks touching only already-passed-in state/setters — extracted verbatim to new `src/hooks/useStaleTargetClearing.js` (46 lines) and `src/hooks/useBlockCollisionRepair.js` (33 lines). Also deduped a small repeated inline callback: `(entry) => { handleLoadEntry(entry); setActivePage('generator'); }` was identical across 3 call sites (`TagLibraryPage`/`ShortsQueuePage`/`TodoPage`'s `onLoadEntry`) — now one `loadEntryAndReturnToGenerator` defined once. **Discussed upfront via `AskUserQuestion` that this wouldn't fully clear 250** — the remaining bulk is the six-page routing JSX with heavy prop-threading, inherent to being the root component (same situation as `UIKitPage.jsx`/`BlockGroups.jsx`). Separately noted, not touched: `App.jsx` still has the DEV-only `window.*StorageMigration` exposure the PRIORITY TODO already calls obsolete — left alone since that's an already-deliberate "after a few stable sessions" deferral, not something to override mid-refactor without asking. **Verified via a broad live Playwright pass**, appropriate since every page routes through this file: all 6 destinations load with real content; `loadEntryAndReturnToGenerator` confirmed working at all 3 call sites; stale-target-clearing confirmed end-to-end (click → highlighted landing; nav away via main tabs, back manually → highlight correctly gone); block-collision-repair confirmed silent (no spurious `console.warn`); Regenerate and project switching both confirmed working; 0 console errors/warnings.

**`engine/titles/generateTitles.js` split, shipped 2026-07-30: 305 → 196 lines — second engine-file split, same approach as `generateCustomBlocks.js`.** Initially looked like it had no clean seam (helpers tightly wound around one shared function) — re-reading found one: `resolveRecordText`, used by both `pickTitles` (during the pick loop, for text/dedup) and `renderTitles` (the live-render phase), plus its four support helpers (`buildGeneratedArtistShort`, `capitalizeFirst`, `resolveWrapping`, `resolveArtistSong`) form a coherent "resolve a picked record to display text" concern, separable from the five pick-only pool-building helpers (`pickTransformation`, `buildTransformationVariations`, `shuffleArray`, `getTitleTemplateGroups`, `getWeightedTemplates`). Pure move, zero logic changes: the five extracted to new `src/engine/titles/resolveTitleRecord.js` (110 lines), imported back by both `pickTitles` and `renderTitles` — same cross-import shape as `resolveHookOverride`/`renderTextTemplate` in the prior split. Only one importer exists (`useGeneratedOutput.js`), confirmed via grep first. Proposed via `AskUserQuestion` before touching code. **Verified with the same seeded A/B rigor**: a Node harness (`rolldown`) checked out the pre-refactor commit into a `git worktree`, monkey-patched `Math.random` to a seeded PRNG (identical seed old vs. new), and ran `pickTitles`/`renderTitles` against both projects' real config across 6 scenario groups each (no tags / 1 tag / 2 tags / Shorts mode / hook-mixing via `useHooksForLongTitles` / a pick-once-render-twice live-substitution case) — **old and new code produced SHA256-identical output across all 12 runs**, with the hook-mixing scenario confirmed to actually exercise the `kind: 'hook'` branch. Live Playwright confirmed the highest-risk behavior specifically: typing into Artist then Song kept the same 5 title slots in the same order, only the substituted text changing — no reshuffle (this is exactly what the original "Generator pick/render split, part 1" work fixed, so the most likely thing to silently regress); Regenerate still produced a genuinely different set; Shorts mode rendered correctly; 0 console errors. Ninth file down in the large-file refactor pass.

**`engine/descriptions/generateCustomBlocks.js` split, shipped 2026-07-30: 325 → 201 lines — first engine-file split in the refactor pass, highest-stakes file touched so far.** After the settings-tab/form-component tier was exhausted, confirmed via `AskUserQuestion` to continue into engine-file territory. This is the "pick/render freeze" machinery documented at length elsewhere in this file — randomized template/phrase selection frozen at pick-time, pure substitution at render-time, specifically so live form edits reflect instantly without re-rolling; a subtly wrong move here (evaluation order, a dropped parameter, a wrong `ctx` closure) would silently change generation output with no lint/build error to catch it. Proposed the exact split plan to the user before touching code: everything about hook-block-pool output + layout-key resolution + Block Groups (`pickHookBlockOutput`/`renderHookBlockOutput`/`resolveHookBlockOutput`, `pickLayoutHookBlocks`/`renderLayoutKey`/`resolveLayoutKey`, `renderBlockGroups`/`generateBlockGroups`) moved verbatim to new `src/engine/descriptions/resolveLayoutBlocks.js` (126 lines), importing `resolveHookOverride`/`renderTextTemplate` back from `generateCustomBlocks.js` (the one cross-dependency); customBlocks/structured-block/song-override logic stayed. Only `generateDescriptions.js`/`generateShortDescriptions.js` imported the moved functions, both updated; two stale comments referencing the old file location fixed too. **Verification went well beyond lint/build**: a Node A/B harness (bundled with `rolldown`) checked out the pre-refactor commit into a `git worktree` (never touching the main working tree's uncommitted changes), monkey-patched `Math.random` to a seeded PRNG (identical seed old vs. new, for a true deterministic comparison), and ran generation against both projects' real config across 7 scenarios each — **old and new code produced byte-identical, SHA256-matched output across all 14 runs**, including the three Block Group segments that are the exact code that moved. Live Playwright confirmed descriptions render correctly, tag-toggle+Regenerate still works, a live song-override still substitutes and reverts to a byte-identical frozen pick on clear, Shorts mode renders correctly, 0 console errors. **Incidental finding, flagged not fixed**: while reading `generateTitles.js` as an alternative candidate, spotted `pickTitles` destructures a `hasEmpty` field from `resolveRecordText` that the function never actually returns — the empty-candidate filtering this drives may be dead code (see Known Issues in `docs/current-context.md`). Eighth file down in the large-file refactor pass.

**`components/input/AdvancedDescriptionFields.jsx` split, shipped 2026-07-30: 265 → 57 lines.** A pure move, not a rewrite — two fully self-contained sub-components already living in the same file were extracted verbatim, zero logic changes: `SongListBlockEditor` (the mini item-editor used inside a List-type song block override) → new `src/components/input/SongListBlockEditor.jsx` (52 lines); `SongBlockOverrideFields` (renders a per-song override field, text or list, for every song-scoped Text/List/Hook Block, plus the `songOverrideTarget` click-to-scroll effect) → new `src/components/input/SongBlockOverrideFields.jsx` (167 lines). The main file is now just the "Additional Hashtags" input plus a single `<SongBlockOverrideFields>` render call. Seventh file down in the large-file refactor pass — the last one in the "settings/form component" risk tier; every remaining candidate over 250 lines is either a high-risk core file (`App.jsx`, `useAppShellState.js`) or an engine file flagged "go carefully" from a recent heavy rework. Verified via live Playwright against the real Generator Advanced Options panel — the one real risk in a pure move like this is a dropped prop silently breaking the cross-file `songOverrideTarget` wiring, so that was the focus: Additional Hashtags input propagates live to the Hashtags output panel; a Story Block text override propagates live to the Descriptions panel with no save/regenerate needed; a temporary List-type song block (created in an isolated Playwright browser profile, never touching real project data) rendered correctly via `SongListBlockEditor` with working add/remove; Reset (↺) correctly reverted an override; clicking a song-overridden description line correctly scrolled to and highlighted the matching Advanced Options field, confirming the wiring survived the split intact; 0 console errors throughout.

**`projectSettings/blocks/BlockGroups.jsx` split, shipped 2026-07-30: 261 → 234 lines.** Third application of the same patch-builder extraction pattern used for `HookBlocks.jsx` and `Placeholders.jsx`: `patchGroup`/`resetGroup`/`deleteGroup`/`addGroup` moved to new `src/utils/blockGroupOverrides.js` (59 lines: `patchGroupPatch`/`resetGroupPatch`/`deleteGroupPatch`/`addGroupPatch`/`isDynamicGroup`), returning patch objects instead of calling `updateProjectOverride` directly — the component still owns that call plus the `window.confirm`/`isCore` delete guard. `AddChildList` (per-group "add child" search list) stayed inline, matching the precedent of not extracting every small tab-specific sub-component (e.g. `AddPlaceholderRow`). Modest line reduction compared to prior files — most of this file's remaining bulk is drag-and-drop wiring (`@dnd-kit` sensors, `SortableActiveBlock` rendering) and JSX, not duplicated patch logic — but it's now under the 250-line threshold. Sixth file down in the large-file refactor pass; the remaining candidates over 250 lines are all either high-risk (`App.jsx`, `useAppShellState.js`) or engine files flagged "go carefully" from recent heavy rework — no more low-risk settings-tab candidates left in this tier. Verified via live Playwright against Illegal Mind Covers' real `blockGroups` config (31 assertions, 0 console errors): existing group cards (Closing/Broadcast/Log Notes) render correctly; Scope/Target dropdown changes and renames landed correctly in `localStorage`'s `blockGroupOverrides` and persisted across reload; child reorder (move buttons), remove, and re-add via the Add-child list all correctly modified the `children` array; Reset (↺) removed the entire group override cleanly and the UI correctly reverted to defaults; a new test group's add/delete round-tripped through `customBlockGroups` with the exact expected shape; real project data confirmed left in its original state after testing.

**`projectSettings/blocks/Placeholders.jsx` split, shipped 2026-07-29: 274 → 164 lines.** Two seams: (a) `PlaceholderReference` (the collapsible "All Placeholders" reference card) was already a self-contained props-in/JSX-out component — extracted verbatim to new `blocks/PlaceholderReference.jsx` (60 lines), zero logic changes. (b) The patch-builder functions (`patchPlaceholder`/`resetPlaceholder`/`deletePlaceholder`/`addPlaceholder`) plus the pure helpers they depend on (`sourceToFieldValue`, `fieldValueToSource`, `generatePlaceholderKey`, `isDynamicPlaceholder`) — extracted to new `src/utils/placeholderOverrides.js` (88 lines), rewritten to *return* patch objects instead of calling `updateProjectOverride` directly, explicitly matching `hookBlockOverrides.js`'s convention from the `HookBlocks.jsx` split (setters return patches; the calling component still owns the `updateProjectOverride` call plus the `window.confirm`/`isCore` delete guards, exactly as `HookBlocks.jsx` does). Verified via live Playwright, since the calling-convention change (return-patch vs call-directly) was the one place a subtle patch-shape bug could slip in and real project override data was at stake: the reference card renders/expands/copies correctly; a Count edit and a Short Hooks Pool toggle both landed correctly in `localStorage`'s `projectOverrides.<project>.description.placeholderOverrides` and persisted across a real page reload; Reset (↺) removed only the targeted key, leaving an unrelated override untouched (confirms scoped removal, not a blanket wipe); Add produced the exact documented `customPlaceholders` entry shape; Delete cleanly removed it; 0 console errors throughout. Fifth file down in the large-file refactor pass.

**`savedLibrary/SavedLibrary.jsx` split, shipped 2026-07-29: 337 → 161 lines.** Two real seams: (a) filter/sort state (`search`, `sortBySignal`, `hideQueueHidden` + its unified-storage persistence effect, `missingDataOnly`) plus the derived `filteredEntries`/`missingEntries`/`hasMissingData` — pure logic with no JSX — extracted to new `src/hooks/useSavedLibraryFilters.js` (107 lines). (b) The "Missing Data AI round-trip" feature (Copy AI Prompt button, Paste AI Response toggle + textarea + Apply/Cancel + result summary) — fully self-contained with its own private state (`promptCopied`, `showPasteBox`, `pasteText`, `applyResult`) — extracted to new `src/components/savedLibrary/MissingDataTools.jsx` (124 lines), needing only `missingEntries`/`savedEntries`/`onUpdateEntry` as props. One deliberate small layout change: the two AI-round-trip buttons used to render inline in the same flex row as the Sort/Hide/Missing-data-only checkboxes (`.filter-row`); they now render in their own `.button-row` directly below instead (reusing the existing CSS class, no new styles) — a cleaner grouping, confirmed live to render correctly. Verified beyond lint/build given real saved-song data was at stake: live Playwright confirmed toggle/search/sort/shown-count all correct; Hide Queue-Hidden filters and **persists across a real page refresh** (checked raw `localStorage`, not just the UI checkbox); Missing Year/Genre filter and the new button-row layout render cleanly; Copy AI Prompt confirmed via clipboard read-back; Paste AI Response confirmed via a real `localStorage` write to the target entry's `originalYear`/`originalGenre` (not just the on-screen summary text); Export/Import buttons still render; 0 console errors. Fourth file down in the large-file refactor pass.

**`useSavedEntries.js` split, shipped 2026-07-28: 400 → 208 lines.** New `src/utils/savedEntries.js` (220 lines) got the already-standalone pure module functions (`buildEntryId`, `normalizeEntryIds`, `mergeImportedEntry`, `toSlug`) plus two newly-extracted ones: `buildEntryFromFormData` (save field list) and `buildFormDataPatchFromEntry` (load field list) — the two concerns Known Gotchas already flagged as distinct from import-merge. Also extracted `loadInitialSavedEntriesByProject` (the `useState` initializer's legacy-format migration). The 4 tiny reducer-only handlers stayed inline, unchanged. Verified via live Playwright reading `illegalMindGeneratorData` directly: save, load, delete, and a non-destructive import merge all confirmed correct, 0 console errors. See Next Recommended Tasks in `docs/current-context.md` for remaining large-file candidates.

**Large-file refactor pass started, two files down, shipped 2026-07-28.** User asked to begin splitting files over 250 lines app-wide — see `docs/current-context.md`'s Next Recommended Tasks for the full tracked candidate list (re-run its `find ... wc -l` command to refresh, since line counts drift as other work touches these files). Chose to start with genuinely splittable settings-tab components over `App.jsx`/stateful hooks (higher risk) or `pages/UIKitPage.jsx` (a component catalog — length is largely inherent to its purpose, not a real target).

**1. `blocks/HookBlocks.jsx`: 497 → 125 lines.** Two real seams, not an arbitrary chop: (a) `HookBlockEditor` — the presentational card body (lines slider, max input, override-type select, template list) — extracted to its own `blocks/HookBlockEditor.jsx` (112 lines), a pure props-in/JSX-out component with zero closures over parent state. (b) The ~20 getter/setter functions (`getTemplates`, `updateTemplates`, `resetBlock`, `getScope`, `addHookBlock`, `deleteHookBlock`, etc.) were pure closures over `projectConfig`/`projectSettingsOverrides`/`updateProjectOverride` with no internal React state at all — extracted into `src/utils/hookBlockOverrides.js` (331 lines) as plain functions that take the relevant state explicitly and *return* patch objects rather than calling `updateProjectOverride` themselves (the component still owns that call, matching how every other Project Settings tab wires its own patches inline) — a direct match for the architecture rule "storage in `src/utils`." `hookBlockOverrides.js` itself is 331 lines (over 250) but was deliberately left alone: it's ~20 small, independently-readable, mostly-non-interdependent pure functions — long because it has many functions, not because anything is tangled — so a further split (e.g. readers vs. patch-builders) would satisfy the line-count rule without making anything easier to follow. Verified beyond lint/build: live Playwright testing of the actual write path — added a template line, edited it, confirmed the value committed on blur and landed correctly in `localStorage`, confirmed remove/reset still work, all 5 Blocks sub-tabs still render — zero console errors, no behavior change.

**2. `descriptions/LongDescriptionSettings.jsx` (272 → 132) + `ShortsDescriptionSettings.jsx` (273 → 138) — deduped, not just split.** Reading both files first revealed they weren't just long, they were near-duplicates: identical DnD sensor setup, `layoutIndex`/`addToLayout`/`removeFromLayout`/`moveBlock`/`handleDragEnd`/`resetOrder`, `getNavigateHandler`, and the entire two-column Available/Active JSX board — differing only in *where* each reads/writes the layout array (`templates.long` vs `templates.shorts`) plus a couple of small config differences (Shorts has a count slider, different `KNOWN_*_BLOCK_META`, no `supportBlockConfig`). CLAUDE.md itself already flagged this exact pair as a sync-drift risk (see the drag-and-drop reordering and Long-description-tooltip entries below) — this refactor eliminates that risk instead of just working around it. Extracted: `descriptions/useDescriptionLayoutActions.js` (62 lines, a hook owning the DnD sensors + all six reorder/add/remove functions, parameterized by `{ activeKeys, defaultLayout, updateLayout }`); `descriptions/DescriptionLayoutBoard.jsx` (97 lines, the shared mobile-tabs/header/two-column-DnD-board JSX, now also owning its own `mobileTab` state internally since nothing outside it needed that state); and `makeNavigateHandler` added to `src/utils/descriptionLayout.js` (the existing shared home for `resolveBlockSource`/`makeLayoutLabelResolver`/`buildHookBlockMaps` — a natural extension, not a new module) to dedupe `getNavigateHandler` too. Each page now keeps only what's genuinely different: its own `defaultLayout`/`activeKeys`/`availableKeys` computation (reads different config paths) and its own `updateLayout` patch-builder. Verified beyond lint/build: live Playwright testing of the actual interactive paths on both pages — precise remove→re-add round-trips confirmed by exact row count (7→6→7, not just "didn't crash"), move up/down confirmed changing order, Reset Order confirmed non-crashing, all 10 Project Settings tabs + both Description sub-tabs still render — zero console errors, no behavior change.

**`src/components/projectSettings/` reorganized into topic folders, shipped 2026-07-28.** User pointed at the VSCode file explorer — 16 flat `ProjectSettings*.jsx` files sitting alongside 5 existing topic subfolders (`blocks/`, `descriptions/`, `hooks/`, `lists/`, `titles/`) — and asked for a folder reorg. Investigation found `descriptions/` already held the pattern worth generalizing: its two sub-tab widgets (`LongDescriptionSettings.jsx`, `ShortsDescriptionSettings.jsx`) already lived inside it, with only the top `ProjectSettingsDescriptions.jsx` router left stranded flat at the root. Applied that same rule everywhere: **a tab's controller file moves into the same folder as the widgets it renders; a tab with no dedicated widgets stays flat.** 9 controller files moved — `Blocks`/`TextBlocks`/`HookBlocks`/`BlockGroups`/`Placeholders` → `blocks/`, `Lists` → `lists/`, `Descriptions` → `descriptions/`, `Titles` → `titles/`, `ShortHooks` → a **renamed** `shortHooks/` (was `hooks/` — confusing next to the real `src/hooks/` React-hooks directory, worth fixing while touching it anyway since it was about to gain a second file). User then asked, separately, whether to also drop the now-redundant `ProjectSettings` prefix from filenames once the folder already gives context — confirmed via `AskUserQuestion` (chose to drop it), applied only to the folder-nested files (file renamed + internal `export default function` name renamed to match, e.g. `blocks/Blocks.jsx` exports `Blocks`) — the 7 files staying flat at `projectSettings/` root (`ProjectSettingsContent.jsx` + `General`/`Links`/`Thumbnails`/`Hashtags`/`Todo`/`ShortsQueue`) keep the full prefix, since for those it's the only namespacing they get and it matches every other top-level page component's naming elsewhere in the app. Blast radius was small and self-contained by design — only one import from outside the directory tree touches it (`ProjectSettingsPage.jsx` → `ProjectSettingsContent.jsx`, itself unchanged), confirmed via an Explore-agent import-graph audit before starting; the other ~40 fixups were internal `projectSettings/**` relative-path edits (both sibling-reference changes and depth-based `../` increments for every moved file's imports reaching outside the directory), done as 3 separate `eslint`+`vite build`-verified steps (Blocks topic → prefix-drop rename pass → the remaining 4 single-file topics together), each left for review before continuing. No barrel/`index.js` files introduced — kept the existing direct-deep-import convention (confirmed via grep that none exist anywhere in this codebase area already). Pure structural refactor, zero behavior changes. Verified: `eslint`/`vite build` clean after every step (same 4 pre-existing unrelated errors throughout, nothing new); live via Playwright — every one of the 10 top-level Project Settings tabs plus Blocks' 5 sub-tabs and Descriptions' 2 sub-tabs render real content with zero console errors; a whole-tree grep confirmed no stale `ProjectSettingsX`-named import survived the moves.

**Blocks → Placeholders tab: full "All Placeholders" reference list + "Copy AI Prompt" button, shipped 2026-07-28.** The tab only ever listed/edited the project's `{custom.*}` definitions — every other placeholder (`{artist}`, `{song}`, `{tags.*}`, `{links.*}`, `{num}`, etc.) was only discoverable by opening a `{`-autocomplete dropdown somewhere else. New `src/utils/placeholderReference.js`: `buildPlaceholderReference(projectConfig)` returns `{ builtIn, tagCategories, links, custom }` — `builtIn` is `REGISTRY_TOKENS` (`src/engine/placeholders.js`) plus the two `SPECIAL_TOKENS` (`num`/`transformation`) paired against a new static `PLACEHOLDER_DESCRIPTIONS` map (one short sentence per token — add a description here whenever a new resolver is added to `REGISTRY`, or it silently shows blank); `tagCategories` is `TAG_CATEGORY_PLACEHOLDERS` (`hookPlaceholders.js`) with a generated description resolved through `TAG_CATEGORY_ALIASES` (`descriptionTagHelpers.js`) so `{tags.lang}` correctly describes the "language" category, not literally "lang" — caught by the smoke test comparing against real config, would have shipped wrong otherwise. `links`/`custom` read live from `projectConfig.description.links`/`.placeholders`, so they're always exactly this project's current set. New `PlaceholderReference` component in `ProjectSettingsPlaceholders.jsx` renders this as a `TemplateGroupCard` used as a plain content shell (per its documented usage — omit `onUpdateTemplates`), placed above the existing editable custom-placeholder cards, `initialCollapsed` (matches the tab's other collapsed-by-default cards) with the "Copy AI Prompt" button living in `headerActions` so it stays clickable while collapsed. **The copy button intentionally exports a narrower set than the visible list, per user feedback after the first pass**: `buildPlaceholderPrompt()` only formats `BUILT-IN`/`TAG CATEGORIES` (the two groups an AI writing new phrase templates would actually need) — `PROJECT LINKS`/`CUSTOM` are project-specific plumbing, deliberately excluded. Since neither remaining group depends on `projectConfig`, `buildPlaceholderPrompt` takes no arguments. Follows the established one-way `buildXPrompt` convention (`tagPrompt.js`, `searchQuery.js`) — no parse-back counterpart needed, this is reference text only, never sent anywhere by the app itself. Verified: a Node+esbuild smoke test against both projects' real config (token well-formedness, non-empty descriptions, correct tag-category-alias resolution, section-omission logic), then live via Playwright against the running dev server for both projects — correct per-project scoping (Illegal Mind: 4 links/3 custom; Maxx Dee: 9 links/2 custom, no cross-contamination), card collapsed by default with the button still clickable, clipboard content confirmed to include only the two intended sections, zero console errors.

**Generator pick/render split, part 2 — Descriptions (Long + Shorts) and Hashtags/YouTube Tags now live-refresh too, shipped 2026-07-26.** Direct continuation of part 1 below (Titles/Thumbnails/Short Hooks) — user confirmed "full same-depth treatment" for the remaining panels via `AskUserQuestion`, after an initial size estimate (Intro/Story/Technical/Broadcast+Log Block Groups/Custom Blocks/Shorts' multi-variant generation, ~6 files) was surfaced up front so the scope was a deliberate choice, not a surprise mid-implementation.

**New generic mechanism (`placeholders.js`), used instead of hand-rolling a pick/render pair per block type like part 1 did**: `fillPlaceholdersAndFreeze(template, ctx)` resolves a template exactly like `fillPlaceholders`, but also captures a frozen snapshot of every token's resolved value *except* a small `ALWAYS_LIVE_TOKENS` set (`artist`, `song`, `signalNumber`, `year`, `years`, `currentYear`, `decade`, `fileId` — tokens that read `ctx.formData` directly with no randomness) and anything the caller already supplied via `ctx.overrides`. `fillFrozenPlaceholders(template, ctx, frozen)` replays those frozen values at render time while the live tokens resolve fresh. `freezeTemplate`/`renderFrozenTemplate` bundle the common single-template case; `pickViableTemplateFrozen`/`renderViableTemplateFrozen` do the same for "pick one random viable template" call sites. `pooling.js` got matching freeze-aware counterparts (`pickPooledTemplates`/`renderPooledTemplates`) for multi-line pool picks (hook blocks, the Technical block's pooled lines). This was necessary because Descriptions has *far* more independent random-pick sites than Titles/Thumbnails/Short Hooks (transformation phrases, tag-category picks, `{custom.*}` pools, `{logNote}`/`{operatorStatus}` pools, per-block template pools, list-item random picks) — hand-freezing each one individually the way part 1 did for `{originalGenre}` would not have scaled. A key simplification the generic design gets for free: nested random resolution (e.g. a `{custom.tagLogLine}` token that internally pools+picks a line via `resolveCustomPlaceholder`) is captured as one opaque frozen string at the *outer* token level — nothing inside `resolveCustomPlaceholder`/`resolveLogNoteToken`/`resolveOperatorStatusToken`/`buildPlaceholderPool` needed to change at all.

**Files rewritten, each into a `pick*`/`render*` pair plus a `generate*` back-compat wrapper** (matching part 1's pattern): `generateCustomBlocks.js` (List/Text blocks, hook-block pools, Block Groups — `resolveLayoutKey`/`generateBlockGroups` kept as back-compat aliases, new `renderLayoutKey`/`renderBlockGroups` take a precomputed `pickedHookBlocks` map instead of re-picking inline), `generateTechnicalBlock.js` (its two-stage per-tag + remaining-pool pick), `generateDescriptions.js` (Long — composes all of the above, plus freezes `buildTagLine()`'s own randomized result once for the Log Block's wrapper), `generateShortDescriptions.js` (Shorts — the hardest case: generates `count`, default 3, fully independent variants per call, so *each* variant's picks are frozen separately, not shared). `generateHashtags.js` needed **zero changes** — audited and confirmed it has no `Math.random`/pooling anywhere (every entry is a deterministic function of `formData`/config), so it was already safe to move straight into a live-render memo.

**The one deliberate design decision with a real behavioral tradeoff — song-override text is live, not frozen.** Unlike every other "random pick," an active Story/Log/CTA/custom-block override isn't a creative selection to preserve — it's live user data, so `renderCustomBlock` (`generateCustomBlocks.js`) checks `songOverrides` fresh from formData on *every* render call, before falling back to the frozen project-default pool pick. This is what makes typing into an override textarea show up immediately, and clearing it revert to the exact same frozen pool result rather than re-rolling a new one — verified via both a Node smoke test and live Playwright (typed a unique override string with an embedded `{artist}` token, confirmed live substitution, cleared it, confirmed byte-identical reversion to the original render). Accepted, documented edge case from this same decision: a project-default block with `displayMode: 'random'` whose override is List-shaped (not plain text) re-picks one random override item on every render, since a freshly-typed override has no prior frozen pick to replay against — narrow combination, not the common case (most overrides are plain textareas).

**Second accepted simplification: `{originalGenre}`'s comma-pick is frozen as a value in Descriptions, unlike Titles/Short Hooks' index-based freezing.** Part 1 (Titles/Short Hooks) froze the comma-pick *index* specifically so a live genre edit could still flow through. Descriptions only uses `{originalGenre}` in one deeply-nested spot (`transformationBlock`, itself only reached via `{transformation}`'s own resolution) — threading index-freezing through every description ctx builder for that one nested case was judged disproportionate; it gets the same generic value-freeze treatment as every other random token instead. Documented directly in `placeholders.js`'s `ALWAYS_LIVE_TOKENS` comment so it isn't mistaken for an oversight later.

**Verified**: a Node smoke test (bundled with `esbuild`, since raw Node ESM can't resolve this project's extensionless relative imports the way Vite does) against both projects' real config — no leftover `{token}` artifacts, Shorts variant/segment counts stable across a live formData change, and (critically) *byte-identical* re-render output when formData is unchanged, proving nothing re-rolls on a no-op render. A second smoke test confirmed only the segments actually using a changed field differ, and the override-live/revert-to-frozen-pick behavior described above. Live via Playwright against the real dev server: Description panel stable across an Artist edit (byte-identical, since this project's current Active Layout doesn't happen to include a block using `{artist}` directly — a config fact, not a bug, confirmed by the Node test instead); Story Block override textarea (inside its collapsed `<details>`, expanded via its summary) showing live substituted text immediately and reverting cleanly on clear; Hashtags panel updating `#Motorhead` → `#IronMaiden` instantly on an Artist edit with zero regeneration; and a final full-panel sanity pass confirming the three-way trigger split still holds end-to-end: a Transformation Tag toggle and Regenerate both still change output, while an unrelated field (Hide from Queue) leaves every panel byte-identical. `eslint`/`vite build` clean throughout. **Deliberately out of scope, same as part 1**: Titles' "mix hooks into Long titles" feature still uses a one-off frozen render of the Short Hooks pool, not the live path.

**Generator pick/render split, part 1 — Titles, Thumbnails, and Short Hooks now live-refresh placeholder values without re-picking, shipped 2026-07-26.** Direct follow-up to the "selective regeneration" fix below: once regeneration was gated to only fire on Transformation Tags/Regenerate/entry load-clear, the user pointed out the opposite gap — editing Artist/Song/Artist Short/Signal Number/Genre should update the *displayed text* immediately (e.g. typing an Artist Short should show up in already-generated titles), without waiting for Regenerate and without re-rolling which template/hook/tag-phrase was randomly chosen. Confirmed scope via `AskUserQuestion`: pilot on Titles + Thumbnails first (Short Hooks came along for free/by necessity — see below), live-refresh fields = Artist/Song/Artist Short/Signal Number/Year/Genre.

**The core pattern, applied to `generateShortHooks.js`, `generateTitles.js`, and `generateThumbnails.js` identically**: each engine file used to pick a random template/hook/pattern *and* fill in its placeholders in one pass, returning only the final text — nothing was kept to re-fill later. Split each into `pick*(formData, config, ...)` (every random selection — which hook/template/tag-phrase/pattern wins, frozen, called only from `useGeneratedOutput.js`'s narrow-deps "pick" memo) and `render*(picked, formData, config)` (pure placeholder substitution against whatever `formData` is passed in — no randomness, safe to call on every render). A `generate*` back-compat wrapper (`pick` + `render` in one call) is kept in each file for any caller that doesn't need the split. `useGeneratedOutput.js` now has two memo layers: the existing narrow-deps "pick" memo (unchanged trigger set) produces `pickedShortHooks`/`pickedTitles`/`pickedThumbnails`, and three new small memos re-render each of those against live `formData` keyed only on the fields that actually feed their placeholders (Short Hooks: artist/song/artist-short/use-custom-artist-short/signal/genre; Titles: artist/song/artist-short/use-custom-artist-short/signal — genre confirmed unused in Title templates via `projects.json` audit; Thumbnails: artist/song/artist-short/use-custom-artist-short only, no signal/genre used there). `formDataRef` (the ref from the prior fix) is what lets these render-memos read fresh values without needing `formData` itself in their dep arrays.

**Two non-obvious sub-problems found and fixed along the way, both about hidden randomness surviving inside "pure" substitution:**
1. **`{originalGenre}` has its own internal random pick** (`pickOneGenre` in `placeholders.js` picks one comma-separated substring at random) — if left as-is, every live re-render would re-roll which genre word shows, even with genre text unchanged, reintroducing exactly the flicker being fixed. Fixed by freezing the *index* (not the value) at pick time (`pickGenrePartIndex`/`resolveGenrePartAtIndex`, new exports in `placeholders.js`) — a render against a live-edited genre string reproduces the same *choice* (e.g. "still the 1st listed genre") instead of re-rolling. Audited every other REGISTRY token used in Short Hooks/Titles templates (`{tags.*}`, `{primaryTag}`, `{decade}`, `{years}`) against real `projects.json` data — none needed the same treatment: `{tags.*}`/`{primaryTag}` depend only on `transformationTags`, which is already frozen at the outer "pick" level, and both projects configure `primaryTag.order: "selection"` (not `"random"`) anyway; `{decade}`/`{years}` are deterministic given already-frozen inputs. `{transformation}` (Short Hooks' shared per-batch phrase, Titles' per-title-attempt phrase) was already an explicit frozen override in both engines pre-refactor — carried through unchanged.
2. **The "mix 5 mixed-type hooks for display" shuffle was itself a second, undiscovered random selection** — `TitlesPanel.jsx`/`ShortHooksPanel.jsx` each flatten `shortHooks` across every type and `Math.random()`-shuffle+slice to 5 for display (this predates this session). Once `renderShortHooks` started returning a fresh array on every live re-render (by design), this shuffle re-ran too, reshuffling *which 5 of the ~12-14 picked hooks show* on every keystroke — the exact bug being fixed, just one layer up. Caught by live Playwright verification (a hook visibly vanished from the list after an unrelated edit), not by code review. Fixed by moving the shuffle into `pickShortHooks` itself (`mixedRecords`, frozen order) — `renderShortHooks` now returns `{ groups, mixed }`, and both panels just `.slice(0, limit)` the already-shuffled `mixed` array (a plain, non-random truncation), with zero local `Math.random()` left in either component. `generateTitles.js`'s own final pool/shuffle/dedupe/slice-to-`titleCount` step (mixing transformation+generic+hook-derived candidates) got the identical treatment inside `pickTitles`, freezing `order` before `renderTitles` fills text.

**Verified via Playwright against the real running dev server** (scratchpad-only installs, matching prior sessions' precedent), reproducing the user's exact reported scenario (Shorts mode, a Transformation Tag selected, "Motörhead"/"Ace Of Spades", then "30 Seconds to Mars"/"The Kill" with Artist Short "3STM"): typing into Artist/Song leaves every non-matching phrase byte-identical and correctly substitutes the ones that do use `{artist}`/`{song}`; toggling Artist Short updates only `artistShort`-pattern thumbnail slots (slots whose frozen pattern is `artistFull` correctly keep showing the full name — matches pre-refactor semantics, not a bug); Long-mode Titles/Thumbnails behave identically (same 5-slot stability, live substitution); Regenerate still re-picks a genuinely new set; title tooltips still show the raw `sourceTemplate.template`/`sourceHook.sourceText` correctly (click-to-navigate metadata untouched by the refactor); panel-title click-to-navigate still lands on Project Settings; zero console errors throughout. `eslint`/`vite build` clean. **Deliberately out of scope for this pass** (documented, not forgotten): Descriptions and Hashtags/YouTube Tags don't have the pick/render split yet — their block overrides, custom CTA, and per-song hashtags still require Regenerate to reflect an edit. Also out of scope: Titles' "mix hooks into Long titles" feature still uses a one-off frozen render of the hook pool (not live) — mixing that into the live path would need `generateTitles.js`'s hook-record rendering to also re-run against the same live deps as its transformation/generic records, deferred since it wasn't part of the reported scenario.

---

**Generator selective regeneration — output no longer re-rolls on every form edit, shipped 2026-07-26.** User reported the Generator's output panels regenerating (and visibly re-rolling randomized phrase pools) on every single form edit — typing a letter in Artist, toggling "Use custom artist short," editing a block override — when only an explicit action should refresh it. Root cause: `useGeneratedOutput.js`'s single `useMemo` (produces all five outputs — titles, hooks, thumbnails, descriptions, hashtags — in one pass) was keyed on `[formData, resolvedProjectConfig, generationSeed]`; since every `setFormData` call is `{...prev, ...}`, *any* field change produces a new `formData` reference and reruns every engine call, including their internal `pickRandomLines`/`resolvePooledOutput` random picks. Confirmed via `AskUserQuestion` that only a Transformation Tag toggle should auto-regenerate — everything else (Artist/Song text, Signal Number, Year/Genre, Artist Short, Video Type, all block overrides, Additional Hashtags/CTA, Hide from Queue, Todo fields) should wait for the Regenerate button. Fixed by narrowing the memo's dependency array to `[resolvedProjectConfig, generationSeed, formData.transformationTags, formData.entryLoadToken]` and reading `formData` inside the memo body via a `formDataRef` (`useRef`, mutated directly in the render body — deliberately *not* inside a `useEffect`, which would still hold the previous render's value at the exact moment the memo recomputes, since effects run after commit) so a recompute always uses the true latest field values regardless of which dep changed. New ephemeral `formData.entryLoadToken` field (`defaultFormData.js`, alongside `videoType` — same "session-only, not part of any saved entry's field list" treatment) is bumped by `handleClearForm` (`useAppShellState.js`) and `handleLoadEntry` (`useSavedEntries.js`) specifically, since those represent the form's entire context being swapped for a different song rather than a live edit to the current one — without it, loading a different saved entry with the same tags as the currently-loaded one would leave stale output on screen. Project switching needed no equivalent token — `resolvedProjectConfig` is already rebuilt (and already a memo dependency) whenever `projectId` changes. **A second, more severe bug was found while verifying live via Playwright**: the narrowed dependency array didn't actually stop the re-rolling, because `resolvedProjectConfig` itself (`App.jsx`) was never reference-stable to begin with — `useTagOverrides.js`'s `projectOverrides = overrides[projectId] || {}` and `useProjectOverrides.js`'s `projectSettingsOverrides = allProjectSettingsOverrides[projectId] || {}` each returned a **fresh `{}` literal on every render** whenever the current project had no stored overrides yet (a common case — e.g. every project before its first Tag Library edit), so every keystroke's re-render produced a new `resolvedProjectConfig` identity regardless of the memo's other deps. This was a latent bug in both hooks all along; it just had no observable effect until something (this new memo) started depending on referential stability. Fixed by replacing both literals with a shared module-level `EMPTY_OBJECT` constant per file. Verified via Playwright against the real running dev server (11-step script, byte-for-byte `textContent` diffing of every output panel per step, not eyeballing): typing into Artist/Song, toggling Artist Short, editing Signal/Year/Genre, editing Advanced Options fields (Additional Hashtags, Story Block override), and toggling Hide from Queue all left every output panel byte-identical; toggling a Transformation Tag, clicking Regenerate, Clear Form, and Load Entry all correctly changed/refreshed output; zero console errors. `eslint`/`vite build` clean. Supersedes the stale "Generator selective regeneration" line under Post-Upload Session Todos below (that note had assumed artist/song/signal/type should also auto-trigger — this session's actual request narrowed that to tags only).

**Third bug, found from real user screenshots after the above shipped**: in **Shorts** video-type mode specifically, the Titles panel (and the separate Short Hooks panel) still visibly reshuffled on every unrelated edit, and even on toggling **Uppercase** — a display-only toggle that must never touch generation. Root cause was outside `useGeneratedOutput.js` entirely: `TitlesPanel.jsx`'s `buildMixedShortTitles(shortHooks, limit)` and `ShortHooksPanel.jsx`'s `buildMixedShortHooks(shortHooks)` both call `shuffleArray` (`.sort(() => Math.random() - 0.5)`) then `.slice(0, limit)` **directly in the render body, unmemoized** — so every re-render of either component (triggered by any parent state change at all, including `titleUppercase`, which lives outside `formData`) re-shuffled and re-sliced the pool from scratch, independent of whether `shortHooks` itself had actually changed. This is why the first fix's Playwright verification passed (it exercised Long mode, where `titles.map(...)` reads the already-computed, stable array) while the user's real Shorts-mode session kept reshuffling. Fixed by wrapping both in `useMemo` keyed on `[shortHooks, titleCount]` / `[shortHooks]` respectively — now the visible 5-title subset only changes when the underlying `shortHooks` pool itself changes (i.e., on an actual regeneration), not on every render. Verified live via Playwright in Shorts mode with a Transformation Tag selected (the exact reported scenario): typing into Artist left the Titles panel byte-identical; toggling Uppercase changed only casing, not the underlying set/order of titles. `eslint`/`vite build` clean.

**Shorts Description's "Hook" line: fixed a title-suffix leak and made it a genuinely regular Hook Block via a new Custom Placeholder source, shipped 2026-07-25.** User reported two related problems with the "Hook" entry in Project Settings → Descriptions → Shorts → Active Layout. (1) Generated Hook lines carried a trailing suffix meant only for Titles (e.g. "Motörhead still hits different // [SIGNAL 07]") — `generateShortDescriptions.js` was using `hook.text` (wrapped with `title.shortsPrefix`/`.shortsSuffix`/legacy `shortHookSuffix`, correct for Shorts Titles and the Shorts Hooks panel) instead of the unwrapped variant, in a bespoke `blockName === 'hook'` branch that bypassed the standard Hook Block pipeline entirely. (2) Clicking "Hook →" forced all of Project Settings to the top-level Short Hooks section via a hardcoded `key === 'hook'` special case (`ShortsDescriptionSettings.jsx`), with no Lines-count control, no song override, and a hardcoded fallback label (`KNOWN_SHORTS_BLOCK_META.hook`) — no real `hookBlocks` entry existed for it. First implementation (mid-session, since superseded — see below) gave "Hook" its own bespoke `hookBlocks` `path: 'shortHookPool'` that dynamically resolved templates from the shared pool, with a special no-template-editor UI branch in `ProjectSettingsHookBlocks.jsx`. **User then proposed a cleaner design after seeing it live**: expose the Short Hooks pool as a `{placeholder}` token instead (reusing the already-shipped Custom Placeholders system — see below), and make "Hook" a *completely ordinary* Hook Block whose default (single, editable, extendable) template is just that placeholder — "it would be a regular one and I could add more lines there if I'd want." Implemented as the final design: `src/engine/placeholders.js`'s `buildPlaceholderPool` gained a third source kind, `source.shortHookPool: true`, alongside the existing `tagField`/`hookBlockKey` sources — it pools the entire eligible `shortHookTypes` set (base + selected-tag templates, `isFaithful`/`requiresGenre`-filtered) via the same `getEligibleShortHookTypeEntries` (`src/engine/hooks/shortHookEligibility.js`, kept as its own zero-dependency leaf module specifically to dodge a real import cycle: `hookBlockLookup.js` ← `placeholders.js` ← `generateShortHooks.js`). New `shortHooksPool` placeholder definition (`description.placeholders`, both projects): `{key: 'shortHooksPool', label: 'Short Hooks Pool', source: {shortHookPool: true}, count: 1}`. `shortsHookLine` (`description.hookBlocks`) is now a totally standard entry — `path: 'shorts'`, `templateKey: 'hookLine'` — pointing at `templates.shorts.hookLine`, a real, editable, addable template array whose sole default entry is the literal string `"{custom.shortHooksPool}"`. `hookBlockLookup.js`/`generateCustomBlocks.js` needed **zero special-casing** and are back to their exact pre-session shape (two-arg `resolveHookBlockTemplates`, no new `path` branch) — the whole point of routing through the placeholder system is that the block-resolution pipeline never needs to know the pool exists. Deleted `generateShortDescriptions.js`'s special `blockName === 'hook'` branch entirely — the key now falls through to the already-generic `resolveHookOverride`/`resolveHookBlockOutput` path every other hook block uses, which structurally cannot apply title-style wrapping (`resolvePooledOutput`/`fillPlaceholders` never touch `title.shortsPrefix`/`.shortsSuffix`), so fix (1) falls out of fix (2)'s redesign for free. Deleted `ShortsDescriptionSettings.jsx`'s `key === 'hook'` special case and `KNOWN_SHORTS_BLOCK_META.hook` — `resolveBlockSource` now recognizes `'hook'` via the hookBlocks entry's `descriptionLayoutKey` exactly like any other block, landing clicks on one scoped, fully-normal Hook Blocks card. Since `path` is legitimately `'shorts'` (not a new sentinel value), the target-default ternaries in `ProjectSettingsHookBlocks.jsx`/`ShortsDescriptionSettings.jsx`/`LongDescriptionSettings.jsx` needed **no changes at all** — they already handled `'shorts'` correctly. `ProjectSettingsPlaceholders.jsx` gained a "Short Hooks pool" `ToggleField` alongside the existing "Tag field source"/"Hook Block pool" selects; `fieldValueToSource`/`sourceToFieldValue` now carry `shortHookPool` through alongside `hookBlockKey` when the tag-field dropdown changes (any/all three source kinds may combine into one pool). Removed now-dead code from `DescriptionsPanel.jsx`'s `DescriptionLineLink` (the `source?.sourceType === 'tag'`/`'base'` branches existed solely for the old special-cased raw-hook-object `source` shape the deleted branch attached — every description segment now uses the standard `{type, blockKey, blockType}` shape) and the `onOpenSourceTag`/`onOpenSourceHook` props feeding it, scoped to `DescriptionsPanel.jsx`/`GeneratorResultsPanel.jsx`'s `<DescriptionsPanel>` call only — `TitlesPanel`/`ThumbnailsPanel`'s own independent copies of this handling are untouched. Also cleaned up now-dead `openShortHooksSearch` prop-threading left over from the original special case, which had been carried the whole way from `App.jsx` → `ProjectSettingsPage.jsx` → `ProjectSettingsContent.jsx` → `ProjectSettingsDescriptions.jsx` for this one purpose (the function itself stays defined in `useAppShellState.js` and is still genuinely used elsewhere, as `onOpenSourceHook` for `GeneratorResultsPanel`'s base-preset-hook click nav — only this one now-pointless thread was removed). Verified via a Node smoke test (bundled with `rolldown`, this Vite 8 project's bundler — no `esbuild` present) against real `projects.json` for both projects: the placeholder resolves to real hook text with no leftover token, `templates.shorts.hookLine` has exactly the one default template, end-to-end `generateShortDescriptions()` output contains no wrapped-suffix artifacts with a correctly-shaped `{type:'block', blockKey:'shortsHookLine', blockType:'hook'}` source, and `generateShortHooks()`/Shorts Titles output is fully byte-unaffected. `eslint`/`vite build` clean. Live via a throwaway Playwright script (scratchpad-only install, `NODE_PATH` pointed at the cached npx package since this project has no local `playwright` devDependency): confirmed the Hook Blocks card for "Shorts · Hook Line" now looks and behaves exactly like every other card — normal template badge/count, search box, editable `{custom.shortHooksPool}` row with its own × delete, +Add/+Bulk buttons, Lines slider — with zero special UI, and generated Shorts Hook lines render unwrapped.

**Per-tag "Copy from Project" action, shipped 2026-07-25.** User asked for a per-tag dropdown + button (Tag Library → a tag's Basics tab) that pulls just *that* tag's data in from another project — distinct from the existing bulk "Sync Tags" control (copies every tag) and "Duplicate tag" (creates a differently-named, empty-content copy). Clarified via `AskUserQuestion` that "copy and add, not override" means additively merging (Set-union) the source project's title/thumbnail/hashtags/description/shortHooks into the *same* tag, reusing `syncProjectTags`'s exact `mergeTagData`/`mergeDescription`/`mergeShortHooks` machinery (`useTagOverrides.js`) scoped to one tag name instead of every override. New `copyTagFromProject({tagName, sourceProjectId, targetProjectId, sourceBaseTags, targetBaseTags})` in `useTagOverrides.js`; new dropdown+button in `TagBasicsTab.jsx` (reuses `FormSelect` and the existing `.tag-actions` CSS class — no new CSS), threaded through `TagLibraryPage.jsx` (new `handleCopyTagFromProject`) → `TagCard.jsx` → `TagEditor.jsx`. User renamed the button label from "Copy from Project" to "Sync" mid-review. Verified live via a throwaway Playwright script (scratchpad-only install, matching prior sessions' precedent): added a real hashtag to "Heavier" on Maxx Dee Covers via the actual UI, switched to Illegal Mind Covers, clicked the per-tag button, confirmed the hashtag plus shortHooks/title/thumbnail/description merged into Illegal Mind Covers' stored override (both in localStorage and live in its Hashtags tab); re-clicked to confirm dedup/no-op (array stays at one entry); confirmed bulk "Sync Tags" unaffected; 0 console errors. **Found, not fixed (pre-existing, out of scope):** `TagEditor.jsx`'s `<details open={shouldOpenEditor(...)}>` has no `onToggle` handler, so a manually-toggled-open native disclosure can silently re-collapse on the next unrelated re-render. Separately, `TagEditor.jsx`'s `activeTab` local state isn't reset when switching projects — since React reuses the same component instance for same-named tags across projects (`key={tag.name}`), whichever tab was last active (e.g. Hashtags) can carry over when the same tag is viewed under a different project.

**Fast tag creation via AI round-trip ("Add Tag" panel), shipped 2026-07-13.** User asked for a faster way to add a new transformation tag (e.g. "Melodic Hardcore") — today's flow was a bare `window.prompt()` for a name followed by hand-filling every phrase across all 5 Tag Editor tabs one row at a time. Modeled directly on the already-shipped Saved Library "Copy AI Prompt" / "Paste AI Response" round-trip (`searchQuery.js`'s `buildMissingDataPrompt`/`parseMissingDataResponse`) — same shape, new domain. New `src/utils/tagPrompt.js`: `buildTagPrompt(label, category, shortHookTypes)` builds a plain-text prompt with one section header per tag field (`TITLE:`, `THUMBNAIL:`, `DESCRIPTION_TECHNICAL:`, `DESCRIPTION_LOG:`, `DESCRIPTION_STATUS:`, `HASHTAGS:`, plus one `SHORTHOOKS_<KEY>:` per entry in the project's real `shortHookTypes` — read from config, not hardcoded, so a project that adds/removes a hook type stays correct) asking for 4-6 phrases per section and mentioning `{artist}`/`{song}` as usable placeholders; `parseTagResponse(text, shortHookTypes)` is a line-based state machine that walks the pasted reply, opens a new section on any `SECTION_NAME:`-shaped header line, accumulates non-empty lines under it, and collects anything outside a recognized section (preamble, sign-off, bad headers) into `unrecognized` rather than silently dropping it — mirrors `parseMissingDataResponse`'s `unmatched` array precedent. Chosen as plain-text sections over JSON specifically to avoid AI-output JSON fragility (markdown fences, trailing commas) — matches the codebase's existing precedent. New `src/components/tags/AddTagPanel.jsx` — inline panel (no modal primitive exists in this app), Name + Category inputs (Category is a free-text input with a `<datalist>` suggesting `getTagCategories(projectConfig)`'s existing values), Copy AI Prompt button (500ms "Copied ✔️" state, same pattern as `CopyButton`), an always-visible paste textarea, and Create Tag/Cancel. Data model required zero changes — confirmed via `buildResolvedProjectConfig.js`'s tag-override merge loop that a brand-new tag can be created purely as a `tagOverrides[projectId][tagName]` entry with no `projects.json` counterpart, so `AddTagPanel` just builds the same override shape `handleCreateTag`/`handleDuplicateTag` already build and calls `updateTagOverride` verbatim — no new storage, no new merge logic. `TagLibraryPage.jsx`'s `handleCreateTag` changed signature from a self-contained `window.prompt` handler to `(tagName, overrideObject) => updateTagOverride(tagName, overrideObject)`, with the panel now owning shape-construction (same division of responsibility `handleDuplicateTag` already had); the existing "+ Add tag" button (`TagActions.jsx`, unchanged) now opens the panel instead of triggering the prompt directly. Leaving the paste textarea empty still creates a fully-empty tag exactly like the old `window.prompt` flow did — no regression to the quick-manual-fill path. Deliberate non-goal: re-pasting a response into an *already-existing* tag to refill/refresh it — that needs a non-destructive "only fill empty fields" merge policy (like Saved Library's), a materially different feature from creation; `parseTagResponse` is generic enough that adding it later would be straightforward. Verified: a 28-assertion Node smoke test covering prompt structure, well-formed responses, partial/missing sections, comma-joined hashtag lines, stray unrecognized headers/preamble/signoff text, and empty input; `eslint`/`vite build` clean (only pre-existing unrelated issues remain); live via Playwright — the inline panel replaces the native dialog entirely, the copied prompt contains all 7 real `SHORTHOOKS_*` sections for Illegal Mind Covers, a pasted fixture response populates all 5 Tag Editor tabs correctly on the new tag, the tag is selectable in the Generator's Transformation Tags picker, and the no-paste quick-create path still produces a blank editable tag with zero console errors throughout. **Same-day follow-up**: first real use surfaced that the prompt's phrasing was too weak — a real AI reply for "Melodic Hardcore" came back almost entirely genre-agnostic filler ("More emotion. Same song.", "Pure Melodic Hardcore energy.", "Heavier heart") with placeholders used in barely 1 of 6 phrases per Short Hooks section, despite the tag name deliberately being specific. Tightened `buildTagPrompt`: added an explicit anti-genericness instruction naming the exact failure-mode phrases as bad examples, a hard requirement that at least 4 of 6 phrases in every `SHORTHOOKS_*` section include `{artist}`/`{song}`, and per-hook-type placeholder examples now pulled live from that hook type's own `shortHookTypes[key].templates[0]` in `projects.json` (e.g. `"{artist} still hits different"` for Nostalgia) instead of a generic parenthetical — real config data doubles as a few-shot example, so the pattern the AI is shown is always accurate to what the app actually expects. `parseTagResponse` untouched (format unchanged, only wording); 28-assertion smoke test still fully passing. **Second same-day follow-up**: the blanket "at least 4 of 6 phrases must include `{artist}`/`{song}`" rule from the first fix was itself too rigid — it broke the `progress` hook type specifically, whose real angle (confirmed from its actual `projects.json` templates: `"Re-recording this after {years} years"`, `"Past me vs current me"`, `"Studio upgrade test: {song}"`) is about the *musician's own growth*, not the covered song, so forcing `{artist}`/`{song}` conflicted with that angle and the AI dropped placeholders entirely rather than reconcile it (observed output: "Learning melodic hardcore production one cover at a time.", zero placeholders). Fixed by showing up to 2 real example templates per `SHORTHOOKS_<KEY>:` section (not just 1) and replacing the fixed `{artist}`/`{song}` requirement with "match this section's own theme and placeholder pattern" — some angles are song-focused (`{artist}`/`{song}`), others are about personal growth (`{years}`), and the prompt now says to follow whichever the shown examples demonstrate rather than forcing one universal pair. Also tightened `AddTagPanel.jsx`'s layout per user feedback: Name/Category now share a `.form-row` (existing side-by-side pattern, matches `BasicSongFields.jsx`'s Signal Number/Year row) instead of stacking full-width. First attempt at the Copy Prompt button's spacing shrank its top margin to bond it visually with the fields above — user immediately flagged the result as too cramped, specifically the gap below the button before "Paste AI Response." Reverted the top-margin shrink and instead added `.add-tag-copy-row { margin-bottom: var(--space-6) }` (`index.css`) — `.button-row` only ever had `margin-top`, so a button-row sitting directly above a `FormField` (which has no margin-top of its own) had near-zero gap below it; this restores the panel's normal `space-6` rhythm on both sides of the button.

**Todo Settings tab built + new Shorts Queue settings tab, shipped 2026-07-13.** User asked to expose whatever's hardcoded around Todo and "the amount of cover in between similar songs" (Shorts Queue's duplicate-spacing logic). Clarified via two questions first: keep the spacing rule's meaning unchanged (exact-song, not same-artist) and give Shorts Queue its own new tab rather than folding it into Todo Settings, matching every other major system's existing one-tab-per-system pattern. **Todo Settings** (`ProjectSettingsTodo.jsx`, new) — one `TemplateGroupCard` list editor for `todoStatuses` (a flat top-level `projects.json` array, so the existing top-level spread in `buildResolvedProjectConfig.js` already merges an override correctly, no new merge block needed); reset reuses the already-generic `resetProjectOverride(fieldName)` from `useProjectOverrides.js` (was already threaded to `ProjectSettingsContent.jsx` for `ProjectSettingsGeneral`, just needed passing to the new tab too). Real bug found and fixed while scoping this: `TodoBulkAdd.jsx` hardcoded `status: 'Wishlist'` plus its UI copy — renaming the first status via the new editor would've silently assigned bulk-added entries a status matching nothing in the configured list. Fixed: `TodoPage.jsx` already had `todoStatuses` as a prop, just wasn't passing it to `TodoBulkAdd`; now `TodoBulkAdd` uses `todoStatuses[0] || 'Wishlist'` for both the assigned status and the "Bulk Add {status} Covers" / "Add to {status}" copy. **Accepted, documented limitation**: renaming/removing a status has no migration for saved entries already using the old string — they become invisible (filtered out of every Todo section) rather than deleted; `TemplateGroupCard`'s array editor has no rename-vs-add/remove distinction to hang auto-migration off of. **Shorts Queue settings** (`ProjectSettingsShortsQueue.jsx`, new) — this was a completely uncharted area, unlike hashtags which already had partial config wiring before its tab existed: `useShortsQueue.js` read two hardcoded module constants (`QUEUE_LENGTH = 20`, and `isTooClose`'s hardcoded `.slice(-2)` spacing window) with zero config or settings UI. Added `projects.json`'s `shortsQueue: { length: 20, duplicateSpacing: 2 }` to both projects (defaults exactly match prior hardcoded behavior), a matching nested-merge block in `buildResolvedProjectConfig.js` (same shallow shape as `hashtags`/`youtubetags`), and reworked `useShortsQueue(projectId, savedEntries, queueConfig)` to take a third param — `isTooClose`/`buildQueue`/`getValidReplacement` (all module-level pure functions) now take `spacing`/`queueLength` params instead of reading fixed constants. The `maxAttempts` safety-valve multipliers (`*50`, `*20`) stay as internal implementation details, not exposed as settings, but now scale off the *resolved* `queueLength` so a longer configured queue still gets a proportional attempt budget. `ShortsQueuePage.jsx` now actually uses its `projectConfig` prop (was previously unused — a pre-existing lint error this change incidentally resolved) to pass `projectConfig.shortsQueue` through, and its empty-state copy ("Click Randomize to create a N-cover Shorts queue") now reflects the real configured length instead of a hardcoded "20". New tab UI mirrors `ProjectSettingsHashtags.jsx`'s "Generation" card exactly — two `LabelSliderRow`s ("Queue Length" min 5/max 50, "Duplicate Spacing" min 1/max 10) each with its own inline ↺ reset. Verified: a Node smoke test confirmed `buildQueue`'s spacing/length constraints hold correctly at default config, at a tighter pool forcing the constraint to bind, at a wider configured spacing (5), and at a longer configured length (40) — all passing. Live via Playwright (13 assertions, 0 console errors): Todo Settings rename propagates to the Todo page's section headers and Bulk Add copy live; reset reverts correctly; Shorts Queue settings' length change is respected by an actual randomized queue and its page copy; reset reverts correctly.

**Artist/Song hashtag click-to-navigate + fixed stuck navigation-target highlights, shipped 2026-07-13.** Two follow-ups from the per-item hashtag nav work (below), reported together. (1) `#TestArtist`/`#TestSong`-style dynamic hashtag/YouTube-tag chips previously rendered as plain non-interactive text (`source: null` — genuinely no source at the time) — the user pointed out the source IS the Artist/Song input fields at the top of the Generator form. `generateHashtags.js`'s `dynamicEntries`/`dynamicYTEntries` now tag these with `{type:'field', field:'artist'|'song'}` (the combined `` `${artist} ${song}` `` YouTube-tag entry stays `source: null` — doesn't map to one field). `HashtagLink.jsx` gained a `'field'` branch reusing `onOpenSongOverride?.({blockKey: source.field})` verbatim — same mechanism the Additional Hashtags field already uses, no new prop. `BasicSongFields.jsx` gained `songOverrideTarget`/`clearSongOverrideTarget` props (one new hop: `InputForm.jsx` already had both, just wasn't passing them down), `id="song-override-artist"`/`"song-override-song"` + conditional `tag-section--highlight` on each `FormField`, a scroll-into-view `useEffect`, and a `handleFieldChange(field)` wrapper around the existing `handleChange` that clears the target when its own field is edited — matching the "consumed on edit" convention. (2) Separately, the user reported every click-to-navigate highlight (block/title/thumbnail/hashtag/tag-library links) stays stuck indefinitely once set, even after navigating to a completely different page, until a full refresh. Root cause, confirmed by tracing every `setActivePage` call site in `useAppShellState.js`: `titlesTarget`/`thumbnailsTarget`/`hashtagsTarget`/`blocksTarget`/`shortHooksTarget` were only ever cleared by `ProjectSettingsPage.jsx`'s `handleSectionChange` (a manual section-tab click *within* Project Settings) — the main app nav (`AppHeader.jsx`, a bare `setActivePage(id)`) never cleared anything. Same gap for `tagLibrarySearchTarget` (only cleared by in-page search/filter interaction) and `songOverrideTarget` (only cleared by editing the targeted field, never by navigating away and back). No existing precedent for "clear X when the user leaves the page" existed anywhere in the codebase. Fixed with one new `useEffect` in `App.jsx`, placed right after the `useAppShellState()` destructure: whenever `activePage` no longer matches a target group's home page, clear that group (`projectSettings` for the first five, `tags` for `tagLibrarySearchTarget`, `generator` for `songOverrideTarget`). Safe by construction — every `open*Search`/`openBlocksEditor`/`openTagLibrarySearch` call sets its own target *and* navigates to that target's home page in the same synchronous handler, so React batches both updates and the effect sees the matching `activePage` already in place, correctly skipping the clear for a freshly-set target. `ProjectSettingsPage.jsx`'s existing `handleSectionChange` is unaffected — it still owns intra-Project-Settings tab switching (where `activePage` never changes), this new effect only covers the cross-page case it never handled. Verified live via Playwright (25 assertions, 0 console errors): Artist/Song chips bordered + clickable + highlight-and-clear-on-edit; base-pool/tag-sourced/same-page targets all correctly clear after a real nav-away-and-back (not refresh) via the main nav; manual in-Project-Settings tab switching (e.g. Titles → Thumbnail Templates) confirmed unaffected by the new effect.

**Per-item click-to-navigate + hover border for Hashtags & YouTube Tags output, shipped 2026-07-13.** Follow-up to the Hashtags & YouTube Tags Project Settings tab (below) — the user wanted the same click-to-navigate/hover-border treatment Descriptions and Thumbnails already have applied per-hashtag/per-tag, not just at the panel-title level. Required `generateHashtags.js` to stop returning joined strings and instead return `{hashtags: Entry[], youtubeTags: Entry[]}` (`Entry = {text, source}`, mirroring `generateThumbnails.js`'s shape) — rewrote the whole function around a shared `dedupEntries(entries, max)` helper (first-occurrence dedup + cap, verified behaviorally identical to the old `[...new Set(arr)].filter(Boolean).slice(0, max)` via a 15-assertion Node smoke test against real `projects.json` data, including confirming the YouTube-tags branch's redundant inner `Set` was safe to drop since the final dedup already fully covers it). Four distinct `source` origins identified by reading the generation logic itself: `{type:'base', card:'hashtagsBase'|'youtubeTagsBase', phrase}` (project preset pool), `{type:'tag', tagName, phrase}` (per-tag `hashtags` array entry, `phrase:null` for the bare-tag-name fallback case — hashtags fall back to the tag's own name when its `hashtags` array is empty; YouTube tags include the bare tag name *unconditionally*, a genuinely different rule from hashtags', both handled), `{type:'override'}` (per-song `customHashtags` field), and `source: null` (artist/song-derived dynamic tags — no config source to point to, rendered as plain non-interactive text, same "no source" convention Titles/Thumbnails already use). New shared `HashtagLink.jsx` (`src/components/output/`, reused by both `HashtagsPanel.jsx`/`YouTubeTagsPanel.jsx` — same "one component, multiple call sites" reasoning as this session's `SortableActiveBlock`) branches on `source.type`, modeled directly on `DescriptionLineLink`/`ThumbnailNavLink`'s existing pattern. New `.hashtag-chip-link` CSS — inline-block, transparent border, amber border+glow on hover (same tokens `.description-block-link` uses, sized to a short token instead of a full-width paragraph block, since many short items flow inline in one wrapped line here). Three navigation targets, only one genuinely new: (1) tag-sourced clicks reuse `onOpenSourceTag`/`openTagLibrarySearch` verbatim, but surfaced a real pre-existing gap — `TagEditor.jsx`'s `getInitialTab` never checked `sourceTarget.field === 'hashtags'` even though `'hashtags'` was already a registered `TAG_FIELD_TABS` entry, so the tab auto-switch silently stayed on `'basics'`; fixed with one added branch. (2) Base-pool clicks needed a new `hashtagsTarget`/`openHashtagsSearch`/`clearHashtagsTarget` triple in `useAppShellState.js`, built as an exact mirror of `thumbnailsTarget`'s three pieces, threaded through `App.jsx` → `GeneratorPage.jsx`/`ProjectSettingsPage.jsx` → `ProjectSettingsContent.jsx` → `ProjectSettingsHashtags.jsx` (which gained a `hashtagsTarget` prop and `highlightText` on its two base-pool `TemplateGroupCard`s, same pattern `ProjectSettingsThumbnails.jsx` already uses for `words`/`fallbacks`). (3) Per-song custom-hashtag clicks reuse `openSongOverride({blockKey})` verbatim — already fully generic, no new state — just needed a landing spot: `FormField.jsx` gained an optional `id` prop (small, additive), the "Additional Hashtags" field got `id="song-override-customHashtags"` + the existing `tag-section--highlight` class (confirmed generic box styling, not `<details>`-specific, safe to reuse on a plain `FormField` div) + its own small scroll-into-view `useEffect` in `AdvancedDescriptionFields.jsx` (separate from `SongBlockOverrideFields`'s own effect, since this field lives outside that subcomponent and isn't a `<details>` needing `.open = true`) + highlight-cleared-on-edit, matching `updateOverride`'s existing "consumed once edited" behavior. `GeneratorResultsPanel.jsx`'s `renderCopyFooter` updated to join the new array shape (`.map(e => e.text).join(' ')`) for its `{hashtags}` placeholder substitution. Verified live via Playwright (23 assertions, 0 console errors): hover border/tooltip on real-source chips vs. plain text on dynamic ones, base-pool clicks landing on and highlighting the correct card (hashtags vs. YouTube tags routed to different cards), tag-sourced clicks landing on the Hashtags tab specifically (confirming the `getInitialTab` fix), per-song custom-hashtag clicks scrolling to and highlighting the Generator's own field (clearing on edit), and both panels' Copy buttons still producing the correct joined string.

**Hashtags & YouTube Tags Project Settings tab built, shipped 2026-07-13.** The "Hashtags & YouTube Tags" nav tab (`id: 'hashtags'`) and its summary-count function already existed in `projectSettingsSections.js`, but selecting it fell through to "Coming Soon" — no component existed, and the underlying values were fully hardcoded: `generateHashtags.js` read `config.hashtags`/`config.youtubetags` as bare arrays with two literal caps (`MAX_HASHTAGS = 18`, YouTube tags `.slice(0, 20)`), no override mechanism at all. Reshaped both `projects.json` root keys from bare arrays to `{ base: [...], maxCount: N }` (same object-with-count shape `thumbnail` already uses; `maxCount` defaults preserve the old hardcoded 18/20 exactly, so no behavior change until a user edits something) — this is the only breaking shape change; per-tag `tags.<cat>.<tag>.hashtags`/`.excludeFromHashtags` (already fully wired through the Tag Library editor and `resolveTagOverride`) is a separate key, untouched. `generateHashtags.js` now reads `config.hashtags?.base`/`config.hashtags?.maxCount ?? 18` and the youtubetags equivalent. New `ProjectSettingsHashtags.jsx` directly modeled on `ProjectSettingsThumbnails.jsx` (`updateArray`/`resetArray` writing into `projectSettingsOverrides.hashtags`/`.youtubetags`, `tag-library--3col` grid of `TemplateGroupCard`s) — one "Generation" card with two `LabelSliderRow`s (Max Hashtags, Max YouTube Tags, each with its own inline ↺ reset since they're two independent leaf fields under one card, following the same per-subsection-reset pattern Thumbnails' Patterns card already used for Long/Shorts), plus "Base Hashtags"/"Base YouTube Tags" list-editor cards. `buildResolvedProjectConfig.js` gained a `hashtags`/`youtubetags` nested-merge block in `mergeProjectOverrides()`, same one-level-deep shallow-merge shape as `thumbnail`'s. Routed via a new `activeSection === 'hashtags'` branch in `ProjectSettingsContent.jsx`. No per-item click-to-navigate — `generateHashtags.js` returns plain joined strings with no `source` object per hashtag/tag (unlike Titles/Thumbnails), so there's nothing to link a specific base entry back to. Follow-up: added simple panel-level click-to-navigate on the Generator's "HASHTAGS"/"YOUTUBE TAGS" panel titles (`HashtagsPanel.jsx`/`YouTubeTagsPanel.jsx`), reusing the exact same `onNavigate` mechanism Titles/Thumbnails/Descriptions panels already use (`CollapsiblePanel`'s `onNavigate` prop → `onNavigateToSettings('hashtags')` → the existing `openProjectSettings` in `useAppShellState.js`, which just sets `activeProjectSettingsSection` + `activePage` — no new state needed, `onNavigateToSettings` was already threaded down through `GeneratorResultsPanel.jsx`, just not passed to these two panels yet). Also fixed while building this: `TemplateGroupCard`'s `subtitle` line (long descriptive helper sentences, e.g. "Always included, on top of tag-based and per-song hashtags.") was reusing `.tag-category` — a class also used for short, bold, high-contrast category/section-header labels elsewhere (`TagHeader.jsx`'s tag category badge, several `<details>`/`<summary>` section headers) — so the subtitle read too loud/bulky for helper text. Gave it its own `.tag-card-subtitle` class (same font-size/mono/uppercase, dimmer `--color-text-muted-3` instead of `--color-text-muted-2`), fixing every `TemplateGroupCard` subtitle at once, including the pre-existing ones in Thumbnail Templates — not just the new Hashtags cards. Verified live end-to-end via Playwright against the dev server: slider cap changes and base-pool edits/resets flow through to actual generated output, per-tag hashtags unaffected, panel-title nav lands on the right tab, subtitle color reads as a clear three-tier hierarchy (title → status line → dim caption), zero console errors, `eslint`/`vite build` clean.

**Transformation Tags: Ctrl+Click to open in Tag Library + hover tooltip, shipped 2026-07-13.** `TransformationTagSelector.jsx` (Generator's Advanced Options tag picker) — Ctrl+Click (or Cmd+Click) a tag chip to jump straight to it in the Tag Library, reusing the existing `openTagLibrarySearch`/`onOpenSourceTag` navigation mechanism already used by Titles/Hooks/Thumbnails (switches page, filters/highlights the tag) rather than building anything new. Threaded `onOpenSourceTag` through `App.jsx` → `GeneratorPage.jsx` → `InputForm.jsx` → `TransformationTagSelector.jsx` (previously only reached the Generator's *output* side via `GeneratorResultsPanel`, never the input-side tag picker). Every chip also gained a `title` tooltip (`"{category} — Ctrl+Click to open in Tag Library"`) for discoverability, since Ctrl+Click has no other visual affordance. Plain click still just toggles the tag, unchanged.

**Drag-and-drop reordering for Active Layout + Block Group children, shipped 2026-07-13 (using `@dnd-kit`, the app's first runtime dependency beyond React).** User asked for drag-and-drop in Description Settings' Active Layout, then to extend the same to Block Groups' children list. Chose `@dnd-kit/core` + `@dnd-kit/sortable` + `@dnd-kit/utilities` over hand-rolled native HTML5 drag events (explicit user choice, weighed against this being the first non-React dependency) for touch support, keyboard accessibility, and smoother UX. New shared `SortableActiveBlock.jsx` (`src/components/projectSettings/descriptions/`) wraps `useSortable` and renders a drag-handle (`⠿`, `listeners`/`attributes` attached only there — not the whole row — so dragging can't hijack clicks on the existing `MoveControls` ↑/↓ buttons or `BlockInfoCard`'s nav/remove buttons sitting right next to it) plus the unchanged `MoveControls`/`BlockInfoCard` pair; both reorder mechanisms coexist, buttons stay as the precise/keyboard fallback. Reused verbatim (not duplicated) across all three call sites, since it's a fully generic shell (`id`, `label`, `onRemove`, `onNavigate`, `disabledUp`/`disabledDown`, `onMoveUp`/`onMoveDown`): `LongDescriptionSettings.jsx`, `ShortsDescriptionSettings.jsx` (CLAUDE.md already flagged these two as needing to stay in sync — a shared component removes the risk entirely for this piece), and `ProjectSettingsBlockGroups.jsx`'s per-group children list (multiple independent `DndContext`/`SortableContext` pairs, one per group, each with its own `handleChildDragEnd(group, event)` computing `arrayMove` over that group's own `children` array and patching via the existing `patchGroup`). `PointerSensor` uses a 5px `activationConstraint` specifically so a plain click on a button inside the row isn't misread as a drag start; `KeyboardSensor` + `sortableKeyboardCoordinates` gives arrow-key reordering for free. See UI Primitives for `SortableActiveBlock`. Verified: `eslint` clean, `npx vite build` succeeds with the new dependency bundled (479KB total, was ~475KB — @dnd-kit's footprint is small), `npm audit --omit=dev` shows 0 production vulnerabilities.

**Saved Library: AI round-trip for missing Year/Genre data, shipped 2026-07-12.** Follow-up to the "Copy AI Prompt" feature (below) — user asked whether pasting an AI's reply back could auto-update the matching songs, and provided a real example response to test against. Built the reverse direction: new `parseMissingDataResponse(text, savedEntries)` in `src/utils/searchQuery.js` parses `Artist - Song: Year, Genre1, Genre2` lines (the exact format `buildMissingDataPrompt` requests) and matches each to a saved entry by artist+song, tolerant of case/whitespace/curly-vs-straight-quote differences (`normalizeForMatch` — an AI reply commonly uses typographic apostrophes like "Won't" even when the stored title uses a plain one). Deliberately returns only `{matches, unmatched}` — it does NOT decide what to overwrite; `SavedLibrary.jsx`'s new `handleApplyPastedResponse` owns the non-destructive policy (only fills a field that's currently empty, never overwrites existing data, same principle as the Import merge fix's `preferNonEmpty`). New "Paste AI Response" button (next to "Copy AI Prompt", same `hasMissingData` gating) reveals a textarea + Apply/Cancel; after applying, shows a summary (`Updated N songs. M already had data. Couldn't match: ...`). Required threading a new `onUpdateEntry` prop through `App.jsx` → `GeneratorPage.jsx` → `SavedLibrary.jsx` (the `handleUpdateEntry` hook function already existed, from `useSavedEntries.js`, previously only wired to the Todo page). Verified end-to-end via a direct Node smoke test against the user's real 10-song pasted example (all 10 matched, including the curly-quote case) and a non-destructive-fill regression case (an entry with a pre-filled year kept it, only the empty genre field got filled). **Keep `buildMissingDataPrompt`'s requested format and `parseResponseLine`'s parsing regex in sync** — if the prompt wording changes to ask for a different line shape, the parser must change with it, or pasted responses will silently land in `unmatched` instead of updating anything.

**Saved-entry data loss on Import — full fix (field list + non-destructive merge), plus several Saved Library / Tag Library / Generator polish items, shipped 2026-07-12.** Two-round investigation. Round 1: user reported `originalYear`/`originalGenre` disappearing from saved songs — traced to `handleImportEntries` (Saved Library's "Import Library" JSON button, distinct from full app Backup/Restore) missing `originalYear`/`originalGenre`/`artistShort`/`useCustomArtistShort` from its own independent field list. Fixed, plus shipped two riding-along features: Generator form double-click-to-copy a Google-search string on empty Year/Genre fields (`BasicSongFields.jsx`, new shared `src/utils/searchQuery.js`), and Saved Library red `year`/`genre` missing-data badges (`SavedLibraryItem.jsx`, new `SavedEntryRow` `badges` slot) with a "Missing Year/Genre" filter checkbox. Round 2 (same day, reported by user after using the app for real cross-device sync): the *real* root cause was deeper than a field-list gap — `handleImportEntries` was replacing a matching existing entry **wholesale**, so importing an older/thinner export from another device silently wiped any field (notably Todo status) that was empty in the import but populated locally, even after the field-list fix. Fixed via a proper non-destructive field-by-field merge (`mergeImportedEntry`) — see Known Gotchas for full mechanism. Also shipped: Tag Library grid now reuses the existing `.tag-library--3col` modifier (was already used by 4 Project Settings pages, just never applied to the actual Tag Library page) to go from 4 columns to 3; Saved Library header shows a live `(shown / total)` count next to the title; Original Genre field normalizes `/`/`and` separators into `, ` on blur (`normalizeGenreSeparators` in `BasicSongFields.jsx`), protecting a short exceptions list (`Rock and Roll`, `Rhythm and Blues`, `Drum and Bass`) so genuine "X and Y" genre names aren't split — verified the protection holds regardless of where in the string the protected phrase falls (a naive space-padded placeholder scheme breaks at the string's trailing edge because `trim()` strips the padding first; fixed with a non-whitespace control-character sentinel instead).

**Tag Editor: config-driven phrase tabs + override-merge centralization ("Phase 5" of the Custom Placeholders arc), shipped 2026-07-12.** Investigation found the deferred rationale undersold the real duplication: 4-5 distinct tag-override-merge implementations existed, not 2, one with a genuine currently-shipping bug. Three independent steps:

1. **Fixed `mergeTagData`'s scalar-field-drop bug** (`useTagOverrides.js`) — the "Sync Tags" merge only ever spread the destination tag for flat scalar fields (`label`/`category`/`visible`/etc.), silently dropping the source tag's values when syncing created a brand-new tag in the destination project. Fixed by spreading `...sourceTag` first as defaults, `...targetTag` second so an existing tag's own settings still win. Verified via 2 smoke-test cases (new tag inherits source's fields; existing tag keeps its own).
2. **Centralized the tag-override-resolve merge** into new `src/utils/resolveTagOverride.js`, used by both `buildTagExplorerData.js` (what the Tag Editor UI shows) and `buildResolvedProjectConfig.js` (what generation actually uses) — previously hand-duplicated byte-for-byte in both, a real drift risk. Verified via a before/after diff across both projects × 4 override fixtures (none/scalar/nested/brand-new-tag) × both functions — zero real differences; `buildResolvedProjectConfig.js` was byte-identical on every case, `buildTagExplorerData.js` had one harmless type change (`isRegistryDriven` went from a JS `&&`-chain artifact returning the raw description object to a real boolean — confirmed zero consumers anywhere in the codebase).
3. **Extracted the 4 phrase-based tabs** (Titles/Descriptions/Short Hooks/Hashtags) into a shared `TAG_FIELD_TABS` config (`src/utils/tagFieldTabs.js`) + one generic `TagFieldTab.jsx`, deleting `TagTitlesTab.jsx`/`TagDescriptionsTab.jsx`/`TagShortHooksTab.jsx`/`TagHashtagsTab.jsx`. `TagBasicsTab.jsx` (discrete fixed fields, not a phrase list) and `TagEditorTabs.jsx`'s 5-tab list stayed untouched — out of scope, nothing needs a 6th tab. Field names preserved exactly, so all 4 external click-to-navigate call sites (`ThumbnailsPanel.jsx`, `GeneratedTitle.jsx`, `DescriptionsPanel.jsx`, `ShortHookTitles.jsx`) and `TagEditor.jsx`'s `getInitialTab` needed zero changes — `isSourceMatch(sourceTarget, entry)` (`tagFieldTabs.js`) matches both real `sourceTarget` shapes (`{field, phraseText}` for Titles-sourced clicks, `{hookType, hookText}` for Short-Hooks-sourced) uniformly across all 4 tabs, verified via 6 smoke-test cases. The `tag-editor-nested-section` CSS wrapper (real left-border + indent) is preserved per-tab via `entries.some(e => e.parentField)` — Titles/Hashtags render unwrapped, Descriptions/Short Hooks wrapped, matching today exactly. Short Hooks' search box + `buildHookPlaceholders(projectConfig)` autocomplete stay scoped to that one tab via an opt-in `searchable` prop. `TagHashtagsTab`'s old `tag.hashtags` read switched to `tag.maps.hashtags` (confirmed byte-identical value in `buildTagExplorerData.js`, just makes the previously-dead `.maps.hashtags` field a real, exercised field like the other 3 tabs).

Found but deliberately left alone: a 5th, currently-harmless merge implementation (`src/utils/tagRegistry.js`'s `getResolvedTagRegistry`, flat shallow merge, only reads `.visible`); `buildTagExplorerData.js` re-merging already-resolved `tagOverrides` a second time (idempotent, harmless); `isRegistryDriven` being dead code (computed, never read elsewhere).

---

**Custom Placeholders Phase 4 (Broadcast Block) shipped, then superseded by a better design: Broadcast/Log Notes are now real Block Groups with zero hand-written joins (2026-07-12).** First pass: registry-ified `{fileId}`/`{operatorStatus}` in `placeholders.js` (mirrors `{logNote}`'s precedent, with a self-recursion guard on `{operatorStatus}` since its pool is user-editable), added a `tagStatusLine` Custom Placeholder (Illegal Mind only — Maxx Dee has no Broadcast Block), and composed `broadcastBlock` in `generateDescriptions.js` via `[resolveHookBlockOutput('broadcastHeader', ctx)?.text, resolveCustomPlaceholder('tagStatusLine', ctx, statusLineCount)].filter(Boolean).join('\n')` — `statusLineCount` read dynamically from `hookBlockCounts.statusLines` (confirmed still at its default of 2 in the live app before removing the fallback path) so an existing "Broadcast · Status Lines" Lines-slider customization wouldn't silently reset. `generateBroadcastBlock.js` deleted, same treatment `generateLogBlock.js` got in Phase 3.

User then pushed back on the hand-written join itself ("why are we back to hardcode stuff") and found the actual fix: a Group doesn't need a new child type to hold a Custom Placeholder — a trivial single-template Hook Block whose *only* content is `{custom.tagLogLine}` (or `{custom.tagStatusLine}`) can be an ordinary Group child, since "drop this candidate if the placeholder resolves empty" is exactly the right behavior when there's nothing else in that template to lose (unlike embedding the token inside a wrapper template that also has other content, which was the original reason a `{type:'generated'}` child type was considered necessary). Implemented: two new one-template Hook Blocks (`tagLogLineWrapper` both projects, `tagStatusLineWrapper` Illegal Mind only) and two new real Groups reusing the existing layout keys — `logBlock` (both projects, `scope:'song'`, children `logFormat` + `tagLogLineWrapper`) and `broadcastBlock` (Illegal Mind, `scope:'project'`, children `broadcastHeader` + `tagStatusLineWrapper`). `generateBlockGroups` (`generateCustomBlocks.js`) gained one small, generic addition — an optional `groupCtxOverrides` param (keyed by group key) — needed because Log Notes' wrapper references `{tagLine}`, which must resolve via `buildTagLine`'s result, not `buildTagPhrase`'s (the shared `ctx.tagLine` value every other group uses); every other group is unaffected since the param defaults to empty. `generateDescriptions.js` now has **zero hand-written block-composition joins** — `broadcastBlock`/`logBlock` are produced entirely by the same Group mechanism `closingBlock` already used, `baseBlocks` no longer sets either key directly. Removed the now-redundant `descriptionLayoutKey: 'broadcastBlock'`/`'logBlock'` from `broadcastHeader`/`operatorStatuses`/`statusLines`/`logFormat`'s hookBlocks entries, same as Closing Block's migration did for `closingSignal`/`philosophyLine`. Side benefit: click-nav/tooltip attribution for Broadcast is now *more correct* than before this session (previously landed on `statusLines` via a last-wins collision quirk; now correctly attributes to the "Broadcast" Group).

Follow-up: Groups' child rows had no click-to-navigate (`BlockInfoCard` already supported an `onNavigate` prop, just never wired there) — added via the same `resolveBlockSource` dispatcher `LongDescriptionSettings.jsx`'s nav arrows use, routed through the existing `openBlocksEditor` (newly threaded `ProjectSettingsContent.jsx` → `ProjectSettingsBlocks.jsx` → `ProjectSettingsBlockGroups.jsx`, previously only reached Descriptions). Also added 2 more wrapper-text variations to Illegal Mind's "Log · Format" pool (`[SIGNAL_LOG]`/`[ARCHIVE_ENTRY]` openers, matching `broadcastHeader`'s existing multi-variant style) — it was always a genuine Hook Block, just happened to have only one template so far.

Verified via Node + esbuild smoke tests against real `projects.json` data for both projects (21 assertions covering: correct Group attribution, override routing still hits the Generator field not the Group, no token leaks, no duplication, `{tagLine}` still resolves correctly, randomness intact, Maxx Dee fully unaffected by Broadcast changes) plus live browser confirmation.

---

**Custom Placeholders — new engine capability, Phases 0-3 shipped (2026-07-12).** User asked to (re)create a "Log Notes/Format Group"; investigation found Block Group's own planned Phase 2 (Generated child type) would hit a real duplication bug for this exact case, so a more general mechanism was designed instead: user-creatable `{custom.<key>}` placeholder tokens, backed by a tag-scoped field pool and/or an existing hook block's pool — usable in any template alongside `{artist}`/`{tags.*}`/etc. Full design: `C:\Users\Max\.claude\plans\thatnk-you-i-have-ticklish-fern.md`.

**Phase 0 (pure refactor).** The same "pool candidates, fill placeholders, filter empty, pick N" logic was duplicated 4 times (`resolveHookBlockOutput`, `generateBroadcastBlock.js`, `generateTechnicalBlock.js` Stage 2, `generateShortHooks.js`) — extracted into new `src/engine/pooling.js` (`pickRandomLines` + `resolvePooledOutput`) and `src/engine/hookBlockLookup.js` (`findHookBlockByKey` + `resolveHookBlockTemplates`), both dependency-free (`resolvePooledOutput` takes `fillPlaceholders` as a parameter, not an import — this is what lets `placeholders.js` use it without a circular dependency). All 4 sites migrated, output byte-identical.

**Phase 1 (data model + resolver, no UI).** `resolveCustomPlaceholder(key, ctx)` lives inside `placeholders.js` itself (same reasoning as `{transformation}`'s `resolveTransformationToken` — it needs to call `fillPlaceholders` on each pooled candidate, so it can't live in a file `placeholders.js` imports from). Wired into `DYNAMIC_PATTERNS` as `{custom.<key>}`. Config mirrors `blockGroups`/`customBlockGroups`/`blockGroupOverrides` exactly: `description.placeholders` (JSON defaults) + `description.placeholderOverrides[key]` + `description.customPlaceholders`, merged in `buildResolvedProjectConfig.js` identically to the `blockGroups` block. `source: { tagParentField?, tagField?, hookBlockKey? }` — `tagParentField`/`tagField` mirror `TagPhraseEditor`'s own prop names. Unknown keys resolve to `''` not `undefined`, so a stale token drops its candidate template instead of leaking literal text.

**Phase 2 (UI).** New `ProjectSettingsPlaceholders.jsx` (Blocks → Placeholders, 5th `BLOCK_TYPE_SUBTABS` entry) — mirrors `ProjectSettingsBlockGroups.jsx` but simpler (one combined Tag field source dropdown, optional Hook Block pool dropdown, plain Count input; no Scope/Target — `BlockEditorCard`'s Scope/Target selects are now conditional, existing callers unaffected). `hookPlaceholders.js` gained `buildHookPlaceholders(projectConfig)` (static `HOOK_PLACEHOLDERS` unchanged) wired into Hook Blocks editor, Text Blocks editor, Song Override fields, and (closed out same day, `projectConfig` threaded through `TagLibraryPage.jsx`/`TagCard.jsx`/`TagEditor.jsx`) the Tag Short Hooks editor. **Found and fixed while testing:** the lock/unlock button only ever worked for List/Text blocks — Hook Blocks and Groups' user-created entries had a dead lock icon (isCore/onToggleCore never implemented for those two). Fixed consistently across all three plus the new Placeholders tab.

**Third source kind added, `source.shortHookPool` (2026-07-25).** `source` is now `{ tagParentField?, tagField?, hookBlockKey?, shortHookPool? }` — `shortHookPool: true` pools the *entire* eligible `shortHookTypes` set (base + selected-tag templates, `isFaithful`/`requiresGenre`-filtered, same set `generateShortHooks.js` draws Shorts Titles/the Shorts Hooks panel from) via `getEligibleShortHookTypeEntries` (`src/engine/hooks/shortHookEligibility.js`, a dependency-free leaf module — needed because `hookBlockLookup.js` is imported by `placeholders.js`, which `generateShortHooks.js` imports, so a direct import from either would close a cycle). Added specifically so the Shorts Description's "Hook" line (see Current Focus) could become an ordinary Hook Block — its default (and only, until edited) template is literally `{custom.shortHooksPool}` — rather than needing its own bespoke block-resolution path. `ProjectSettingsPlaceholders.jsx` gained a matching "Short Hooks pool" `ToggleField`, and `fieldValueToSource`/`sourceToFieldValue` now carry `shortHookPool` alongside `hookBlockKey` when the tag-field dropdown changes (all three source kinds may be combined at once — their candidates just merge into one pool).

**Two pre-existing placeholder-resolution bugs found and fixed while testing (would affect any placeholder, not just custom ones):** (1) Log Notes' song override (`resolveLogNoteToken`) returned the raw override text verbatim instead of running it through `fillPlaceholders` — fixed with a self-recursion guard for `{logNote}` referencing itself. (2) List block items (`renderStructuredBlock`) never resolved any placeholder in `item.text`/`item.label`/`block.title` at all, only `{links.*}` in the link field — affected every List block (Gear Used, Playlists, Support Block), not just Custom Placeholders. Fixed by threading `ctx` into `renderStructuredBlock` (4 call sites updated).

**Phase 3 (migrate Log Notes, delete `generateLogBlock.js`).** Both projects have only one "Log · Format" wrapper template each, so embedding `{custom.tagLogLine}` directly inside it would make the whole wrapper vanish when a selected tag has no log phrases (any empty placeholder drops its candidate template) — and adding a second token-free fallback template would make the tag line show only ~50% of the time by random chance, a real regression from today's "always append when present." Kept wrapper and tag line as two independently-resolved pieces joined in `generateDescriptions.js` instead: `[resolveHookBlockOutput('logFormat', logCtx)?.text, fillPlaceholders('{custom.tagLogLine}', logCtx).text].filter(Boolean).join('\n')` — byte-identical output, `generateLogBlock.js` still fully deleted since the per-tag pooling itself now goes through shared `resolveCustomPlaceholder` machinery. Caught before shipping: `generateLogBlock.js`'s internal ctx used `buildTagLine`'s result for `{tagLine}`, but the rest of `generateDescriptions.js` uses `buildTagPhrase`'s result (genuinely different values) for the shared `ctx.tagLine` — a separate `logCtx = { ...ctx, tagLine }` preserves the original exactly. Verified against real `projects.json` data for both projects via Node smoke tests (16 assertions).

**This supersedes Block Group's "Phase 2 (Generated child type, migrate Log Notes)" and "Phase 3 (Broadcast Block)"** — `you-write-too-much-spicy-crayon.md` now annotates both as superseded. Not scheduled: Custom Placeholders' own "Phase 4" (Broadcast Block, needs `{fileId}`/`{operatorStatus}` registry-ified first) and "Phase 5" (Tag Editor config-driven tabs + override-merge centralization, deferred since nothing yet needs a new nested tag field).

---

**Block Group Phase 1 follow-up: Add Child search, Log Notes override-nav fix, unified clearable search inputs (2026-07-11).** Three small fixes shipped right after Phase 1 landed. (1) Groups tab's "+ add child" list (`ProjectSettingsBlockGroups.jsx`) was a flat unfiltered list of every addable block — extracted a new `AddChildList` subcomponent (owns its own `searchText` state, since it renders inside a `blockGroups.map()` and can't call `useState` directly there) that filters by `getBlockKeyLabel(key)`, mirroring `HookTemplateEditor.jsx`'s existing search pattern exactly (same `form-input` class, same lowercase-includes filter, no new CSS). (2) Real bug fixed: clicking a generated Log Notes line with an active song override navigated to Project Settings → Blocks → Hook Blocks instead of back to the Generator's own Advanced Options override field. Root cause: `generateDescriptions.js`'s `overridden` flag (see "song-overridden description blocks" entry below) had a hardcoded `blockName !== 'logBlock'` exclusion, on the reasoning that Log Notes' override only fills `{logNote}` inside a still-pool-picked template — but that reasoning didn't hold from the user's perspective, since the override does meaningfully change the visible output. Fixed by removing the exclusion — `isSongOverrideActive(songOverrides, blockName)` alone now decides `overridden` for every block, no special case; no other call site (`generateShortDescriptions.js`) had an equivalent exclusion to fix. (3) Audited every search-filter `<input>` in the app (7 total) and added `type="search"` to the ones missing it (`TagControls.jsx`, `SavedLibrary.jsx`, `LinksRegistryEditor.jsx`, `ProjectSettingsLists.jsx`, `ProjectSettingsTextBlocks.jsx`, `HookTemplateEditor.jsx`, plus the new Groups search) — this is what gives a search box the native "×" clear button; no new component or CSS, matches the two that already had it (`TransformationTagSelector.jsx`, `TagShortHooksTab.jsx`). A fourth ask ("recreate the Log Notes/Format Group in Maxx Dee") turned out to rest on a false premise — no such Group ever existed in either project (git history confirms `blockGroups` has only ever had `closingBlock`) — and naively building it the way `closingBlock` was built (two independent children concatenated) would be wrong: `logBlock`/`logFormat` aren't independent content, `logFormat` is an editor view into the wrapper template `logBlock`'s own merge function (`generateLogBlock.js`) already folds in internally, so concatenating them as siblings would render two independently-random "Production Note: ..." lines instead of one. Deferred to Phase 2 (Generated child type) — see Priority Todo / Current Focus below and `docs/current-context.md`'s Next Recommended Tasks.

**Block Group — Phase 1 shipped and verified (2026-07-11).** First of three phases from the approved plan (`C:\Users\Max\.claude\plans\you-write-too-much-spicy-crayon.md`) to make Group a real 4th block type — see Block Types above. Data model: `description.blockGroups: [{ key, label, scope, target, children: [{ type: 'block', key }] }]`, JSON-default groups in `projects.json` (mirrors `hookBlocks`), edits via a new `blockGroupOverrides[key]` patch map, user-created groups via a new `customBlockGroups` array (mirrors `customHookBlocks`) — both merged in `buildResolvedProjectConfig.js`. Engine: new `resolveLayoutKey`/`generateBlockGroups` in `generateCustomBlocks.js` (`resolveLayoutKey` is the old inline per-key resolution logic extracted from `generateDescriptions.js`'s layout loop, now shared by the top-level loop and group-children resolution so they can't drift apart); `{num}` is now set once via `ctx.overrides.num` instead of a post-hoc `.replace()`, so it resolves correctly as a standalone Group child too. `resolveHookBlockOutput`/`resolveHookBlockTemplates` gained a "prefer the hook block whose own `key` matches" lookup before falling back to `descriptionLayoutKey ?? key` — lets a Group child address one specific sibling even when siblings used to share a `descriptionLayoutKey` (same self-named-entry precedent `buildHookBlockMaps` already used for `logBlock`). Nav/tooltips: new `buildBlockGroupMaps` (`descriptionLayout.js`) — `resolveBlockSource` checks it before the hook-block-layout-key branch (a Group can reuse an existing layout key, as Closing Block does), both label resolvers now check a `blockGroupLabelMap` with top priority. Exclusivity: both Description settings pages exclude any `blockGroups[].children[].key` from their independently-addable Available lists. New `ProjectSettingsBlockGroups.jsx` (Blocks → Groups sub-tab) reuses `BlockEditorCard`/`BlockInfoCard`/`MoveControls`/`AddBlockForm` — no new UI primitives. Migrated `closingBlock` (Illegal Mind only) to a Group reusing its existing layout key; removed the now-redundant shared `descriptionLayoutKey: 'closingBlock'` from `closingSignal`/`philosophyLine` (the Group owns that key now; the exclusivity filter is what keeps them off the top-level Available list, not layout-key coincidence). Verified via a direct engine smoke test (Node + esbuild, bypassing the browser) and then live in the running dev server via a throwaway Playwright script (scratchpad-only install): Groups tab shows one "Closing" card with both children, reorder/remove work, "+ add child" correctly excludes already-grouped keys; Hook Blocks tab still lists both children independently editable; Descriptions → Long layout shows exactly one "Closing" card and clicking it navigates to the Groups tab; generated output joins both children with `{num}` filled, tooltip reads "Blocks → Groups → Closing"; Maxx Dee (no `blockGroups`) unaffected; zero console errors; `vite build` and full-project `eslint` clean. Next: Phase 2 (Generated child type, migrate Log Notes) — see Priority Todo / Block Type System above for the phased rollout and the Log Notes override-scoping risk flagged in the plan.

**Exposed the hidden `templates.long.logBlock` wrapper template + relabeled the misgrouped Tag Line blocks (2026-07-10).** Follow-up question after the collision-overwrite fix (below): is a Log Notes segment's "Modification: ..." line hardcoded, and why does clicking an overridden Log Notes segment still point at "Log · Notes"? `generateLogBlock.js` joins two independently-sourced pieces: (1) `baseLogBlock` — a wrapping template (`templates.long.logBlock`, e.g. `"Production Note:\n {logNote}"`) with `{logNote}` filled from the song override or the `logNotes` pool; (2) `tagLogBlock` — a random line from the *selected tag's own* `description.log` array, **not hardcoded**, editable per-tag in Tag Library → Descriptions, just not yet tooltip-attributed (documented block-level-not-per-line limitation). Piece (1) *was* genuinely hardcoded, though — no hookBlocks entry anywhere referenced `templateKey: 'logBlock'`; the existing "Log · Notes" card only ever edited the fallback pool (`logNotes`), never the wrapper that actually produces the "Production Note:" prefix. Fixed: new hookBlocks entry per project, `{ key: 'logFormat', label: 'Log · Format', path: 'long', templateKey: 'logBlock' }` — no `descriptionLayoutKey` (not joined to the `'logBlock'` collision group; it has no layout slot of its own, same category as the Tag Line blocks; Log Notes segment navigation unchanged). Also relabeled `tagLineFallbacks`/`tagLineTemplates` (feed the shared `{tagLine}` placeholder used across many templates — correctly absent from the Descriptions layout builder, by design, not a bug) from "Log · Tag Fallbacks"/"Log · Tag Templates" to "Tag Line · Fallbacks"/"Tag Line · Templates" — they have nothing specifically to do with Log Notes. Config-only (`projects.json`), no code changed. Follow-up to the key-collision-prevention fix (below): reported as "Intro · Hook" (3 templates) never varying — always the same static line. Root cause, more severe than the earlier navigation-orphaning bug: `generateDescriptions.js`'s `blocks` object is `{ broadcastBlock, introBlock, storyBlock, technicalBlock, logBlock, closingBlock, supportBlock, ...renderedCustomBlocks }` — spreading `renderedCustomBlocks` *last* means a colliding `customBlocks` key silently overwrites the explicitly-computed value in the actual generation map, not just in Settings-tab display. A colliding Text block's static content always won over the Hook Block's random template pick, with no error anywhere. Fixed via `resolveCustomBlockCollisions(projectSettingsOverrides, hookBlocks)` in `customBlocks.js`, run as a `useEffect` in `App.jsx` on every resolved-config change: detects `customBlocks` keys colliding with a hook block's `key`/`descriptionLayoutKey`; Project-scoped ones are renamed automatically (suffixed key + matching `blockLabelOverrides` entry moved together), self-stabilizing after one repair pass. Deliberately leaves the saved `layout` array untouched — a colliding key can never win its own `layout` slot (`dynamicBlockKeys` excludes any key already in `defaultLayout`), so any matching `layout` entry unambiguously belongs to the hook block, not the renamed customBlocks entry; the renamed block just becomes available (not auto-added) like any new block. Song-scoped collisions are deliberately *not* auto-renamed (returned in `skipped`, `console.warn`'d) since a safe rename can't also migrate per-song override data scattered across `formData`/every saved entry — that needs a human. Successful renames also `console.warn`, so this stays visible rather than a silent background data mutation.

**Block system: fixed a real key-collision gap, unified storyBlock/logBlock onto the dynamic hook-block path (2026-07-10).** User flagged that Project Settings → Blocks / Descriptions / Generator's Advanced Options don't always agree on which blocks exist or where they're reachable, and that some Generator override fields were hardcoded rather than config-driven ("as dynamic as possible, not hardcoded"). A research-only Explore investigation confirmed four separate root causes, code-grounded not guessed:

1. **Key-collision gap** — all three "Add block" forms passed an incomplete `existingKeys` set to the shared `generateBlockKey()`; Text/List forms never checked the Hook Blocks layout-key namespace (`key`/`descriptionLayoutKey`) at all. Real case found: a Text block named "Intro Block" generated key `introBlock`, colliding with the Hook block `introHook`'s `descriptionLayoutKey: 'introBlock'` — since `resolveBlockSource`/`makeLayoutLabelResolver` check the hook-layout-key namespace *before* `customBlocks`, and `LongDescriptionSettings.jsx`'s `dynamicBlockKeys` filter excludes any customBlocks key already in `defaultLayout`, the Text block became permanently orphaned from the Descriptions layout builder while staying fully editable (and invisible-collision) in Blocks → Text Blocks. Fixed: all three Add forms now build one combined `existingKeys` set (`customBlocks` keys + hook blocks' `key`s + `descriptionLayoutKey`s). See Known Gotchas below.
2. **Scope-toggle default mismatch** — not a storage-path bug (both sides read/write the identical `description.templates.long.phraseBlockScopes[key]`); the dropdown *displayed* `'project'` when absent while the Advanced panel *treated* `storyBlock`/`logBlock` as `'song'` by default via a hardcoded key check — silently disagreeing until a user explicitly touched the dropdown once.
3. **`PHRASE_BLOCK_OVERRIDES` was genuinely hardcoded** — `storyBlock`/`logBlock` had their own dedicated array + render loop in `AdvancedDescriptionFields.jsx`, excluded from the generic `songScopeHookBlocks` loop via a `hardcodedPhraseKeys` set, despite being ordinary `hookBlocks` entries everywhere else.
4. **"Cover Summary"** (a stale layout entry with no backing block) confirmed as saved-data cruft from before a prior cleanup, not a code bug — `coverSummaryBlock` has zero references anywhere in current `projects.json`/`src/`; a project's already-persisted layout override array is used verbatim forever with nothing pruning stale keys. No code fix — remove via the existing "×" button.

Fixed #1–#3 (④ needed no code, user-actionable). Added an optional `defaultScope` field to hookBlocks config entries in `projects.json` — `storyBlock`/`logBlock` get `"defaultScope": "song"` (both projects, replacing the vestigial unread `"scope": true` legacy flag), everything else implicitly defaults to `'project'`. Both `ProjectSettingsHookBlocks.jsx`'s `getScope(key, defaultScope)` and `AdvancedDescriptionFields.jsx`'s `songScopeHookBlocks` filter now resolve with the identical precedence (`phraseBlockScopes[key] ?? defaultScope ?? 'project'`), closing the mismatch generically. With that in place, `PHRASE_BLOCK_OVERRIDES`/`hardcodedPhraseKeys` were deleted outright (net −47 lines) — `storyBlock`/`logBlock` now flow through the same dynamic render loop as any other song-scoped hook block. Verified safe: `overrideKey` resolves to the same storage keys as before, so no data migration; they also gain the reset-to-default icon, full placeholder autocomplete, and the Override Type control — parity gains, only cosmetic regression is Story's textarea shrinking from `rows={5}` to the generic path's `rows={3}`. See Hook Block song overrides and scope below.

**Fix: song-overridden description blocks — misattribution, a real hook-block collision, and click-to-scroll (2026-07-10).** Reported right after the Long description block tooltip feature (below) shipped, in three rounds:

- **Round 1 — override misattribution.** A block with an active per-song override (e.g. Story Block, overridden via the Generator's Advanced Options panel) still tooltipped/linked to "Blocks → Hook Blocks → Story Block," landing on the project-default template card — which has nothing to do with the override text actually shown. Root cause: `resolveBlockSource()` only read *config shape* (is this key a hook/list/text block?), blind to whether a song override had actually replaced that pool's output. New `isSongOverrideActive(songOverrides, blockKey)` in `generateCustomBlocks.js` mirrors `renderCustomBlock`/`resolveHookOverride`'s exact truthiness. `resolveBlockSource` gained an `overridden` param (default `false` — Long/Shorts *settings* pages' nav-arrow callers don't pass it); when true it returns `{ type: 'override', blockKey }` — deliberately the *layout* key, not the resolved Hook Blocks config key, since `formData.songBlockOverrides` is keyed by layout key (can differ from a hook block's own config key, e.g. `introBlock` vs `introHook`). Both engines pass this per-segment. `logBlock` excluded — its override only fills `{logNote}` inside a still-pool-picked template, so the pool stays accurate. Shorts' list-block branch didn't apply song overrides at all — fixed 2026-08-29, see Current Focus above.
- **Round 2 — a real config collision the tooltip surfaced.** A `logBlock` segment tooltipped to "Log · Tag Templates" instead of "Log · Notes," and the generated text visibly matched a Log · Notes template. Three `hookBlocks` entries (`logBlock`, `tagLineFallbacks`, `tagLineTemplates`) share `descriptionLayoutKey: 'logBlock'`, but `generateLogBlock.js` only ever reads the pool behind the `logBlock` entry — the other two are unused by it entirely. `buildHookBlockMaps`'s plain last-one-wins collision resolution landed on `tagLineTemplates` (array order), a pool with zero relation to the content — flatly wrong, not just imprecise like `broadcastBlock`/`closingBlock`'s multi-pool merges (whose "last wins" picks are at least real contributors). Fixed generically: `buildHookBlockMaps` now prefers the hookBlocks entry whose own `key` equals the shared layout key when one exists (a "self-named" entry); groups with no self-named entry keep the unchanged last-one-wins fallback. This also fixed a pre-existing, previously-unnoticed bug in `LongDescriptionSettings.jsx`'s own `logBlock` nav arrow, which shared the same collision.
- **Round 3 — overrides made clickable after all.** Shipped as tooltip-only first (explicit choice) — after seeing it live, wanted it to lead somewhere and highlight it. Since the override lives inline in the Generator's own Advanced Options panel (same page, not Project Settings), "leading" means scroll-to + auto-expand + highlight. New `songOverrideTarget`/`openSongOverride`/`clearSongOverrideTarget` in `useAppShellState.js` (same `{blockKey}`-target shape as `blocksTarget`, but same-page — no `setActivePage`; `openSongOverride` force-opens `panelVisibility.advanced`). Threaded through `App.jsx` → `GeneratorPage.jsx` → `InputForm.jsx`/`AdvancedDescriptionFields.jsx` (consumer) and `GeneratorResultsPanel.jsx`/`DescriptionsPanel.jsx` (producer, `onClick` on the `'override'` segment). `AdvancedDescriptionFields.jsx` gained a stable `id="song-override-{key}"` per field across its three override-rendering loops, a `useEffect` doing `el.open = true; el.scrollIntoView(...)` on target change (plain `getElementById`, simpler than refs across three separate `.map()` calls), and a `tag-section--highlight` CSS class (same amber-glow tokens as `.description-block-link`) — cleared the moment the user actually edits or resets that field, not on a timer.

**Long description block tooltips + glowing-border hover (2026-07-10).** Extends the Shorts-only source tooltip/click-nav feature (below) to Long descriptions, which previously rendered as one flat unsegmented string with no navigation at all. Granularity is block-level (one segment per `layout` entry) rather than per-line — `broadcastBlock`/`technicalBlock`/`logBlock` are each built by `generateBroadcastBlock.js`/`generateTechnicalBlock.js`/`generateLogBlock.js` merging several tag-specific and project-level pools into one multi-line paragraph, and re-architecting those three engine files for per-line attribution was judged out of proportion to the ask. This matches the granularity `LongDescriptionSettings.jsx`'s existing nav arrows already use, and mirrors how Shorts itself first shipped block-level nav before exact-template highlighting was added as a later, separate pass. New `resolveBlockSource(blockKey, { hookBlockMaps, customBlocks, supportBlockConfig })` in `descriptionLayout.js` is the single place this block→source resolution now lives, extracted from `LongDescriptionSettings.jsx`'s `getNavigateHandler` (previously hardcoded there) so the engine and the settings UI's nav arrows can't drift apart; `ShortsDescriptionSettings.jsx`'s near-identical `getNavigateHandler` was refactored onto the same function (its `key === 'hook'` → Shorts-hook-pool special case, unrelated to block resolution, stays first). `generateDescriptions.js` now builds `longDescriptionSegments` (`{text, source}[]`) in the same pass that produces the joined `longDescription` string, so the two can't disagree. `DescriptionsPanel.jsx`'s `DescriptionLineLink` gained a `className` prop (default `'description-line-link'`, Shorts' inline style) so Long passes a new `'description-block-link'` class — same tooltip/label/click logic as Shorts' block branch, since Long segments never hit the tag/base hook-pool branches. New CSS: `.description-block-link` is `display:block` with a transparent resting border that turns into an amber glow (`border-color` + `box-shadow`, same `--color-border-hover`/`--color-shadow-accent`/`--radius-terminal` tokens as `.button-primary`/`.tag-used`) on hover — a bordered box reads better for paragraph-length blocks than Shorts' inline text-shadow-only treatment. Known limitation, inherited not introduced: blocks built from multiple merged hook-block pools sharing one layout key (`broadcastBlock`, `closingBlock`, `logBlock` — see "Hook Blocks vs description layout keys" in `docs/current-context.md`'s Architectural Notes) resolve their tooltip/link to whichever pool's key wins (last one in the array), identical to what clicking that block's existing nav arrow already did before this change.

**Shorts description line tooltips: source-of-truth navigation labels + a real subTab-id bug fix (2026-07-10).** Follow-up to the existing Shorts description click-to-navigate feature — hovering a generated line now shows a native tooltip describing where it came from, matching the `title=` convention already used by `GeneratedTitle.jsx`/`ThumbnailsPanel.jsx` (`"Blocks → Hook Blocks → Story Log: \"template text\""` for block-sourced lines, `"{tagName} ({hookType}): \"{text}\""` for tag-sourced hooks, `"Project preset ({hookType}): \"{text}\""` for base-preset hooks). Block-sourced tooltips need a human-readable block label that respects user renames — reused the existing override-aware resolution (`hookBlockLabelOverrides`/`blockLabelOverrides`/`getBlockLabel`) rather than hardcoding a parallel label lookup, via a new `makeBlockKeyLabelResolver` in `descriptionLayout.js`. This is a *config-key* resolver, distinct from the existing `makeLayoutLabelResolver` (which resolves *layout-slot* keys for `LongDescriptionSettings`/`ShortsDescriptionSettings`) — `generateShortDescriptions.js`'s `source.blockKey` is already resolved to the config key before it reaches the UI, so the layout-key resolver's `layoutKeyToBlockKey` translation doesn't apply here. `projectConfig`/`projectSettingsOverrides` threaded down through `App.jsx` → `GeneratorPage.jsx` → `GeneratorResultsPanel.jsx` → `DescriptionsPanel.jsx` to build the resolver. Along the way, found and fixed a real latent bug: the block-nav-on-click code computed `subTab: source.blockType === 'hook' ? 'hooks' : source.blockType`, which for list blocks sent `subTab: 'list'` — but `ProjectSettingsBlocks.jsx`'s actual subtab id is `'lists'` (plural), so clicking a list-block-sourced description line navigated to Project Settings but matched none of the three subtab branches, silently rendering an empty Blocks panel. Fixed by extracting the block-type → subtab-id/label mapping into one shared constant, `BLOCK_TYPE_SUBTABS` in `customBlocks.js` (`{ list: {subTab:'lists', label:'Lists'}, text: {...}, hook: {...} }`) — both `ProjectSettingsBlocks.jsx`'s `SubTabNav` tabs array and `DescriptionsPanel.jsx`'s tooltip/nav logic now derive from this single source instead of each hardcoding its own copy, so the two can't drift out of sync again.

**Fix: Hooks toggle duplicated the Shorts suffix into Long titles (2026-07-09).** Reported as titles like `[SIGNAL 11] // Does I Won't Lie Down still hold up? // [SIGNAL 11]` when the Titles panel's Hooks checkbox mixed shorts hooks into Long titles — a trailing `[SIGNAL 11]` leaking in from the Shorts side. Root cause: `generateShortHooks.js` bakes the Shorts prefix/suffix directly into each hook's `.text` field (needed for the Shorts Hooks panel, Shorts-mode titles, and the Shorts description "hook" block, which all want that wrapped text) — but `generateTitles.js`'s `buildHookTitles` reused that same already-wrapped `.text` and wrapped it *again* with the Long prefix/suffix, trapping the Shorts suffix inside. Fix: `generateShortHooks.js` now also keeps the unwrapped text in a new `rawText` field; `buildHookTitles` uses `hook.rawText` instead. See Known Gotchas below. Verified: hook-derived Long titles now read cleanly (e.g. `[SIGNAL XX] // Most underrated Face to Face song? // Illegal Mind Rework`), Shorts mode unaffected, no console errors.

**Fix: tag `shortHooks`/`description` silently dropped by base+override resolution (2026-07-09).** Reported as "Sync Tags doesn't sync hooks between projects" — a shared tag (e.g. "Hardcore Punk") showed 0 phrases in every Short Hooks category after syncing. Traced through `syncProjectTags`/`mergeTagData`/`mergeShortHooks` (`useTagOverrides.js`) first — confirmed that sync logic already merges `shortHooks` correctly (union per hook-type). The actual bug was downstream, in the two places that combine a tag's base `projects.json` config with its stored override: `buildResolvedProjectConfig.js` (feeds `generateShortHooks.js` — a real generation-quality bug, not just cosmetic) and `buildTagExplorerData.js` (feeds the Tag Editor's `tag.maps`, exactly what the screenshot showed as "(0)"). Both did a flat `{...baseTag, ...override}` spread for `shortHooks` — see Known Gotchas below for the full mechanism and fix. Verified via headless-browser test against the real Tag Editor and the actual Sync Tags button: Maxx Dee's "Hardcore Punk" now shows its real counts (4/4/6/4/4/4) before *and* after syncing from Illegal Mind Covers (which has no base data for that tag) — sync no longer wipes it.

**Titles panel: functional Uppercase toggle + Hooks moved to a checkbox (2026-07-09).** The Titles panel's "Hooks: ON/OFF" toggle moved out of the `CollapsiblePanel` header (`headerExtra`) into a new `.titles-options-row` inside the panel body, above the generated titles — and was converted from a `button-secondary` pill to a `ToggleField` checkbox. A new "Uppercase" `ToggleField` sits alongside it (both modes — Long and Shorts — unlike Hooks, which stays Long-only). Uppercase does a real `.toUpperCase()` transform (not CSS `text-transform`) on the text used for both display and `CopyButton`'s `text` prop, in `GeneratedTitle.jsx` (Long) and `ShortHookTitles.jsx` (Shorts) — a first CSS-only pass was tried and rejected because it left Copy producing normal-case text. Fixed a latent bug the uppercase change surfaced: `ShortHookTitles.jsx`'s `onOpenSourceTag`/`onOpenSourceHook` calls fell back to the (now possibly-uppercased) display text when `hook.sourceText` was absent — switched to a separate `rawHookText` for those fallbacks so source-navigation matching isn't affected by the display toggle. `ToggleField` gained an optional `title` prop (forwarded to the wrapping `<label>`) to preserve Hooks' tooltip. `titleUppercase` persists via `illegalMindGeneratorData.ui.titleUppercase` (`useAppShellState.js`, same pattern as `panelVisibility`), threaded through `App.jsx` → `GeneratorPage.jsx` → `GeneratorResultsPanel.jsx` → `TitlesPanel.jsx`.

**Exact-template highlighting for description navigation (2026-07-05).** Follow-up to Shorts description source navigation below — clicking a line now scrolls to and highlights the specific winning template/item, not just its block's editor card. `resolveHookBlockOutput` returns `{ text, template }` (template only set when one line was picked); `renderStructuredBlock` returns `{ text, pickedItem }` (only set for `displayMode:'random'` lists) — both were plain strings before, so their 4 call sites all extract `.text` now. `blocksTarget` gained `highlightText` (string, for hook templates — also prefills `HookTemplateEditor`'s search box, benefiting existing Titles/Thumbnails nav too) and is passed as `highlightItem` (raw item object, content-matched since list items have no stable id) to `ProjectSettingsLists`. `ListItemRow.jsx` is now a `forwardRef` with a `highlighted` prop, mirroring `PhraseRow`. Text blocks excluded (one field, nothing to disambiguate).

**Shorts description line → source navigation (2026-07-05).** Clicking a line in a generated Shorts description now jumps to whatever block/tag produced it, reusing Titles/Thumbnails' existing navigation machinery — no new infrastructure. `generateShortDescriptions.js` returns `{ shortDescriptions, shortDescriptionSegments }`; each block carries a UI-agnostic `source` (`{ type: 'block', blockKey, blockType }`, or the hook object itself for the `'hook'` block — same shape Titles already use). `openBlocksEditor` now also switches pages (previously only worked from within Project Settings). New `.description-line-link` CSS: no permanent color, CRT glow only on hover. Long descriptions untouched — Shorts-only pass. See `docs/current-context.md` for full detail.

**CRT design polish round 3 (2026-07-05), `src/index.css` + `InputFormActions.jsx` + `UIKitPage.jsx`.** `.button-primary`/`.button-secondary`/`.form-select` gained the `font-size: var(--font-size-toggle)` fix round 2 gave other controls. Found a real CSS bug: `content: '\2212 '` (hex escape + space) has the space consumed as the escape terminator per spec, so the open-state `−` marker on `.tag-section`/`.tag-editor-section` rendered with no trailing space — fixed by using the literal `−` character instead of the escape. `.subtab-nav button` referenced `--font-size-sm`, a token never defined in `:root` (silently fell back to default `1rem`) — repointed to `--font-size-toggle`. Scoped (not global) size reductions on `.tag-edit-fields .form-input` and `.tag-phrase-row .form-input` (shared `PhraseRow`). `.tag-map-status span` sharpened+mono'd to match everything else. `InputFormActions.jsx` merged Save + Clear Form onto one row with the project select. See `docs/current-context.md` for full detail.

**CRT design polish round 2 (2026-07-05), `src/index.css` + `TemplateGroupCard.jsx` + `BlockEditorCard.jsx`.** Uppercase + mono now applied to `.button-primary`/`.button-secondary`/`.copy-button`/`.tag-chip`/`.tag-filters button`/`.form-select` (inputs/textareas stay sans for free-text). `.tag-chip`/`.tag-filters button`/`.tag-status`/`.copy-button` also got `font-size: var(--font-size-toggle)` (12px) — they inherited the default `1rem` and read oversized once uppercased. Every disclosure widget (`ToggleButton`, `.tag-section`/`.tag-editor-section` `<summary>` markers, `TemplateGroupCard`/`BlockEditorCard`'s custom collapse icons) now uses the same `+`/`−` convention — no triangle markers left anywhere. Known accepted gap: native `<select>` open-list chrome can't be restyled without a custom dropdown component. See `docs/current-context.md` for the full selector list.

**CRT design refinement: buttons & pills (2026-07-05), `src/index.css` only.** Follow-up to the CRT pass below — `.toggle-button` now uses `--font-size-toggle` (12px) instead of inheriting the default `1rem` (was reading oversized), and nav tabs/tag filter chips/radio options/transformation tag chips/primary+secondary buttons/`.toggle-button` all switched from `--radius-pill`/`--radius-md` to `--radius-terminal` to match the sharpened panels/cards. See `docs/current-context.md` for the full selector list.

**Fallout terminal CRT atmosphere pass shipped (2026-07-05), `src/index.css` only.** Adds the terminal identity around the prior day's sans-serif/lighter-border refresh rather than reverting it: (1) `body::before`/`::after` fixed overlay — scanlines (`repeating-linear-gradient` + `mix-blend-mode: overlay`), vignette (inset `box-shadow`), subtle `crt-flicker` animation disabled under `prefers-reduced-motion: reduce`; (2) monospace reintroduced on headers/nav/labels only (`.app-title`, `.panel-title`, `.form-label`, `.tag-category`, `.tag-card-header h3`, `.tag-selector-label`, `.subtab-nav button`, `.app-menu-pages button`) — inputs/buttons/selects stay sans-serif; (3) new `--radius-terminal: 2px` token sharpens `.panel`/`.tag-card`/`.terminal-block`/`.advanced-panel-content`/`.desc-generation-settings` to rectangular while buttons/chips/inputs/nav pills stay rounded; (4) corner-bracket accents + intensified amber glow on `.button-primary`/`.tag-chip.active`/active nav tab/`.tag-used`. All via shared primitives, so it applies app-wide with no per-page edits. See `docs/current-context.md` for full detail.

**Generator Input form visual refresh shipped (2026-07-05).** Fixed "bulky/90s programmer UI" feel: removed redundant double `.form-group` nesting, replaced hand-rolled checkboxes with `ToggleField` (now controlled — `checked`, not `defaultChecked`), unified Todo/Transformation Tags disclosure onto `ToggleButton` (was native `<details>`), and three global CSS changes — `.form-label` de-emphasized (12px normal-case, new `--font-size-field-label` token), monospace font removed from all UI chrome (buttons/inputs/selects now `--font-ui`; output areas keep their own explicit mono font), and inputs/tag-chips moved to a lighter bottom-border/background-driven style instead of a full 1px box border everywhere. Color palette unchanged. See `docs/current-context.md` for full detail.

**Placeholder engine consolidated (2026-07-04/05).** `src/engine/placeholders.js` is now the single registry for all template placeholder resolution (hooks, titles, descriptions) — see Known Gotchas below. Also added a save-to-any-project dropdown in `InputFormActions.jsx`.

Previously: tag-category placeholders (`{tags.*}`), title polish (no `"Title:"` label, consistent capitalization), and hook blocks generically honoring their configured "Lines" count (2026-07-04) — see `docs/current-context.md` for full detail.

`originalGenre` is now fully done through Stage 3. Stage 2 (2026-07-02): added a new `original` Shorts Hook type to both projects — `requiresGenre: true`, no `excludeForFaithful`, templates `"The best {originalGenre} song of {year}"` / `"{originalGenre} perfection since {year}"` / `"Still the greatest {originalGenre} track from {year}"`. Stage 3 (shipped earlier in `63ce426`): a `contrast` Shorts Hook type (`excludeForFaithful: true`, `requiresGenre: true`) using `{originalGenre}` + `{primaryTag}`, gated in `generateShortHooks.js`.

**Project Settings → Thumbnail Templates built** (2026-07-02) — the tab was registered in `projectSettingsSections.js` but fell through to "Coming Soon". New `ProjectSettingsThumbnails.jsx` edits `thumbnail.count`/`.words`/`.fallbacks`/`.genericTagTemplates`/`.patterns.long`/`.patterns.shorts` — the exact keys `generateThumbnails.js` reads, already correctly merged by `buildResolvedProjectConfig.js`.

**Thumbnails split into their own Generator panel with source nav** (2026-07-02) — thumbnails no longer render embedded in Titles (`GeneratedTitlePair.jsx` renamed to `GeneratedTitle.jsx`, title-only now); new `ThumbnailsPanel.jsx` shows them independently, visible for both Long and Shorts (the engine already generated Shorts-flavored thumbnails, just discarded before). `generateThumbnails.js` now returns `{ text, source }` instead of plain strings — `source.type` is `'words'|'fallbacks'|'genericTagTemplates'|'tag'`, carrying whichever raw phrase/template produced it. Clicking a thumbnail navigates to its source: Project Settings → Thumbnail Templates (via new `thumbnailsTarget`/`openThumbnailsSearch`, mirroring `titlesTarget`) for the first three, or Tag Library → that tag → Titles tab (via existing `onOpenSourceTag`/`openTagLibrarySearch`, now also wired through `TagEditor.jsx`/`TagTitlesTab.jsx` for `field: 'thumbnail'`/`'title'`) for tag-sourced phrases. `TemplateGroupCard` auto-expands when `highlightText` is set (via a derived `isCollapsed = collapsed && !highlightText`, not a `setState`-in-effect, which the project's `react-hooks/set-state-in-effect` lint rule forbids) — fixes a latent gap that also applied to the existing Shorts Hooks nav. Thumbnail count now has its own "Generation" slider (`thumbnail.count`, 1–10) instead of being tied to `titles.length`.

---

## Other Active Goals

- CSS refactor (consolidate index.css, clean up class naming)
- `coverLabel` (Shorts Description) has no settings UI — only editable by hand-editing `projects.json`
- **`customHashtags`** — last remaining hardcoded per-song field in `AdvancedDescriptionFields.jsx`. It's not a description block at all, so it won't fit the block system — investigate separately.
- ~~**Reusable "Copy AI Prompt" authoring helper**~~ — **done, shipped 2026-09-03** (see Current Focus). Shared skeleton (`authorPrompt.js`) + per-editor adapters (`authorPromptContexts.js`) + `CopyPromptButton`, wired into Cover / Tag Short Hooks / global Short Hooks / Title pools. Hook Blocks deliberately left out (mostly lore blocks). New per-tag `promptContext` field feeds the tag adapter.

---

# Post-Upload Session Todos

- **Shorts descriptions tag awareness** — pull tag-specific phrases into shorts description blocks
- ~~**Generator selective regeneration**~~ — done, shipped 2026-07-26, see Current Focus above (final trigger set: Transformation Tags + Regenerate button + entry load/clear — narrower than this note originally proposed).

---

# Long-Term Roadmap

- Content Calendar (upload planning, release scheduling)
- Project Dashboard (covers completed, wishlist size, queue status)
- Metadata Expansion (release year, re-record/remaster flags, recording notes)
- Queue Enhancements (history, upload frequency rules, date tracking)
- Hook System Expansion (metadata-aware, year-based, artist-specific)
- Database Migration (IndexedDB / SQLite / local file) — only after storage is stable
- Desktop App (Electron / Tauri) — better backups, native filesystem

---

# Important Decisions

- **Local-first** — no backend by design
- **Config-driven** — behavior from config, not hardcoded logic
- **Reuse before creating** — before writing ANY new UI markup, CSS class, or component, check `/uikit` (UIKitPage) and the "UI Primitives" section above first. Prefer a small conditional tweak to an existing component over new markup or a new class. This applies every time, not just when reminded.
- **Simple architecture** — single developer, no enterprise patterns, no unnecessary abstraction
- **onBlur saves** — phrase/template edits save on blur, not on keystroke (future DB compatibility)

---

# Development Preferences

- **Step-by-step, one change at a time.** Present each step, implement it, then stop and wait for the user to review and approve before moving to the next. Never chain multiple steps autonomously.
- **Before writing any new UI element, check `/uikit` first.** Card, row, wrapper, CSS class — check the UIKitPage catalog and CLAUDE.md's UI Primitives list before writing new markup. Extend an existing component if it can fit; don't hand-roll a shape that duplicates one.
- Hands-on, file by file, small chunks
- Show the change, explain it, let the user review before moving on
- Do NOT batch into autonomous pass unless user says so
- Work in grouped steps, keep changes focused
- Prefer code examples over theory
- Ask for files instead of guessing structure
- Provide Conventional Commit messages when asked. Keep commit messages as short bullet points, not long prose paragraphs.
- When uncertain: choose the simpler solution

## Session Protocol

**Before every planning session:**
1. Read `docs/current-context.md` — current focus, recently completed, known issues, next tasks, key file pointers. Fastest way to get project state without reading code.
2. Run `graphify query "<question>"` for any codebase question. Use the knowledge graph first — avoids redundant file browsing and surfaces cross-file relationships instantly.
3. Only grep/read raw source files if docs and graphify don't answer the question.

**After each significant step (feature, fix, or refactor):**
1. Update `docs/current-context.md` — move completed items to Recently Completed, update In Progress and Next tasks, refresh Known Issues.
2. Update `CLAUDE.md` — update Current Focus, Known Gotchas, and UI Primitives entries if the step changed any of those.
3. Mirror the same edit into `AGENTS.md`. It's a manually-maintained copy of this file for non-Claude tools (Codex, etc.) that read `AGENTS.md` by convention instead of `CLAUDE.md` — there is no symlink or hook keeping them in sync (attempted a symlink, blocked by lack of admin/Developer Mode on this machine; a hard link was tried and confirmed unreliable — editors replace-on-save, which silently breaks it). Whatever changes in this file's Current Focus / Known Gotchas / UI Primitives / PRIORITY TODO sections must be copied into `AGENTS.md` too (only the top `# CLAUDE.md` vs `# AGENTS.md` header line differs between the two files).
4. Commit source + docs changes together.

**Do NOT run `graphify update .` automatically** — not after a step, not at the end of a session or feature, not as part of the doc-update/commit routine above. Only run it when the user explicitly asks (e.g. "update the graph", "/graphify update"). It used to be batched to end-of-session automatically; the user asked for that to stop — each update dispatches a subagent and the user wants to control when that cost is spent.

## graphify

This project has a knowledge graph at graphify-out/ with god nodes, community structure, and cross-file relationships.

Rules:
- For codebase questions, first run `graphify query "<question>"` when graphify-out/graph.json exists. Use `graphify path "<A>" "<B>"` for relationships and `graphify explain "<concept>"` for focused concepts. These return a scoped subgraph, usually much smaller than GRAPH_REPORT.md or raw grep output.
- If graphify-out/wiki/index.md exists, use it for broad navigation instead of raw source browsing.
- Read graphify-out/GRAPH_REPORT.md only for broad architecture review or when query/path/explain do not surface enough context.
- Do NOT run `graphify update .` on your own initiative, including after modifying code. Only run it when the user explicitly asks.
