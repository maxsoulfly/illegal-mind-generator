# Current Context

Developer handoff file. Updated end of session. Describes what is actually done, in progress, and next — not plans or history.

---

## Current Focus

**Cover-Specific Hooks auto-persist, shipped 2026-09-03.** The Generator Input form stays explicit-SAVE-only, with one exception: the Cover-Specific Hooks editor now persists immediately — add / bulk-add / delete on the spot, edit on blur.

- `CoverShortHooksEditor.handleUpdate` still calls `setFormData` unconditionally (formData stays authoritative; a later full SAVE reads it, so it can't revert the hooks), then calls a new `onPersistCoverHooks(nextArray)` prop.
- `GeneratorPage.persistCoverHooks` → `handleUpdateEntry(currentEntryId, { coverShortHooks: nextArray })` (the existing column-scoped `PATCH /saved-entries/:id` — server reads the row, replaces only `cover_short_hooks`), **guarded by `isCurrentEntrySaved`**.
- **Unsaved song:** no PATCH, no implicit insert — hooks stay in `formData` and are written by the next explicit SAVE (which creates the row).
- 3 files: `GeneratorPage.jsx`, `InputForm.jsx` (thread `onPersistCoverHooks`), `CoverShortHooksEditor.jsx`. No backend/schema/`TagPhraseEditor` change.
- **Paired fix — `useInputFormLogic.js`:** the `signalNumber` auto-fill effect now bails when `formData.signalNumber` is non-empty (deps include `formData.signalNumber`), mirroring the adjacent `artistShort` "only when empty" guard. Without it, the `savedEntries` array-identity churn from `handleUpdateEntry` would revert an unsaved `signalNumber` edit on every hook change.
- Verified live (Playwright, 20 checks, 0 console errors, throwaway `ZZZQA` entry created + deleted): unsaved-song creates nothing → SAVE persists the form incl. hooks; bulk-add / `+ Add`+blur / edit-blur / delete each reach the DB with no SAVE click; dirty `originalYear` + dirty `signalNumber` neither persisted by a hook change nor reverted in the form; later full SAVE keeps the latest hooks array and writes the now-intentional dirty fields. `eslint` (1 pre-existing unrelated) / `vite build` clean.

**Content Setup › Descriptions — Long/Shorts promoted to first-class leaves, shipped 2026-09-03.** Follow-up to the IA rework below, closing an unfinished part of it. The `descriptions` `subnav` leaf strip had a stub `Layout` leaf whose only job was to nest a `<Descriptions>` wrapper (`<h2>Descriptions</h2>` + an inner Long/Shorts `SubTabNav`). That nesting is removed: `Long` and `Shorts` are now leaves on the strip itself — `Long · Shorts · Lists · Text Blocks · Hook Blocks · Groups · Placeholders · Links` (8). Clicking `Long`/`Shorts` renders `LongDescriptionSettings`/`ShortsDescriptionSettings` directly, wrapped by `DescriptionsWorkspace`'s existing `withHeading` → `<h2>Long Description</h2>` / `<h2>Shorts Description</h2>` (nav chips stay bare `Long`/`Shorts`).

- `src/components/projectSettings/descriptions/Descriptions.jsx` **deleted** — only `DescriptionsWorkspace` imported it; its two render branches moved in.
- `contentSetupNav.js`: `descriptions.leaves` swaps `{layout}` → `{long},{shorts}`; `LEGACY_SECTION_MAP.descriptions` → `leaf:'long'`; **new** `LEGACY_SECTION_MAP.layout → descriptions/long` so a stale persisted `ui.projectSettingsSection === 'layout'` resolves to Long, not the unknown-id → Project fallback.
- `ProjectSettingsPage.LEAF_TO_DISPATCH`: `layout` removed, `long`/`shorts` added (both dispatch `'descriptions'`).
- `contentSetupNav.test.js` updated: new 8-leaf `descriptions` registry list, `descriptions`/`layout` → `descriptions/long`.
- Unchanged: description data model, both layout-board editors, reset behavior, block deep-links (`openBlocksEditor` / `blocksTarget.subTab` are always a block-editor leaf — never `layout`/`long`/`shorts`), highlighting, write paths. `description.activeSubTab` in stored overrides is now inert (joins `blocks.activeSubTab`) — **not migrated/removed** this stage per instruction. `DescriptionLayoutBoard`'s internal `mobileTab` + its `{id:'layout'}` mobile column switcher, and `templates.*.layout` data arrays, are unrelated and untouched.
- Verified: both Node tests pass; `eslint` clean (4 pre-existing unrelated errors); `vite build` clean; live Playwright (22 checks, 0 console errors) — 8-leaf strip w/ no `Layout`, one visible leaf-nav row (board mobile strip `display:none` on desktop), Long/Shorts headings + editors + Shorts count slider, Shorts survives reload, reversible add→remove layout round-trip on Long, Generator DESCRIPTIONS panel → Descriptions/Long, generated description line → `groups` block-editor leaf, stale `ui.projectSettingsSection="layout"` → Descriptions/Long.
- The dead-code/label cleanup from the earlier audit (Stage 7) is still **deferred** — not started.

**Content Setup IA rework, shipped 2026-09-03.** "Project Settings" — a flat strip of 11 section chips — is now **Content Setup**, 4 functional sections (the project-level counterpart to Tag Library's tag-level authoring). Top nav reordered: Generator / Shorts Queue / Calendar / Todo / Tag Library / Content Setup. Pure navigation/IA — every editor keeps writing the same `projectSettingsOverrides.<domain>` key; no data/schema/engine change. The `activePage` id stays `projectSettings`.

- `src/config/contentSetupNav.js` (new; pure, Node-tested) — `CONTENT_SETUP_SECTIONS` (4 × `{id, label, kind, leaves}`) + `resolveContentSetupTarget(legacySection, blocksSubTab?)` (maps every pre-rework flat section id, the Generator output panels' `onNavigateToSettings` strings, and `blocksTarget.subTab` → `{section, leaf}`) + `getSection`/`getSectionLeaves`. (`isValidLeaf`/`parentOf` dropped in Stage 7 step 4 — never used by the app.)
- `ProjectSettingsPage.jsx` rewritten: 4 section chips + a `kind`-specific second level. `LEAF_TO_DISPATCH` maps each leaf → an existing `ProjectSettingsContent` dispatch id; `resolveStoredView` normalises the persisted `ui.projectSettingsSection`.
- **Generation** (`drill`): `GenerationOverview` (4 pool rows w/ live counts) → editor + `← Generation` back header; deep-links skip the overview.
- **Descriptions** (`subnav`): one `DescriptionsWorkspace`, flat leaf strip — merges the old Descriptions + Blocks + Links + Placeholders sections; the stacked Descriptions[Long/Shorts] + Blocks[5-tab] nav is gone. **A follow-up stage (see top of Current Focus) promoted Long/Shorts to first-class leaves**, so the strip is now 8: Long / Shorts / Lists / Text Blocks / Hook Blocks / Groups / Placeholders / Links. `BLOCK_TYPE_SUBTABS` ids unchanged → `openBlocksEditor` deep-links unchanged.
- **Workflow** (`page`): one `WorkflowWorkspace` — Shorts Queue + Todo Statuses side by side, Upload Schedule full width below (`.workflow-grid`, stacks < 960px). `WorkflowWorkspace` owns the `<section>`/`<h2>`/grid; the 3 child editors each render just their `TemplateGroupCard`, taking `heading` for the card label (Stage 7 removed the dead standalone-page fallback + the `embedded` prop).
- **Project** (`page`): the `ProjectSettingsProject` editor (renamed from `ProjectSettingsGeneral`, dispatch id `'general'` → `'project'`, Stage 7 step 3), h2 "Project"; "Duplicate Project" / "Export Project Config" stubs deleted; Backup subtitle notes it's app-wide.
- `{primaryTag}` moved Short Hooks → Titles (same `title.primaryTag` key; `PrimaryTagSection.jsx` git-moved). "Copy AI Prompt" moved from card headers into the editor action row (`actionsSlot` → `AddBulkRow.extra`) for Title pools + global Short Hooks — all 4 pool editors now consistent.
- Deleted: `config/projectSettingsSections.js`, `blocks/Blocks.jsx`, `projectSettings/ProjectSettingsLinks.jsx`. `ProjectSettingsContent` "Coming Soon" fallback → `return null` (unreachable).
- Intentionally stale (no migration): `projectSettingsOverrides.blocks.activeSubTab` / `.description.activeSubTab` no longer written (leaf persistence via `ui.projectSettingsSection` covers it).
- Stage commits: `1c05267` (nav model) · `2ebf09e` (top nav) · `327a18f` (2-level nav) · `a651ba6` (Descriptions merge) · `c25aeb1` (Generation drill + `{primaryTag}`) · `2306b5f` (Workflow/Project) · `3c9969b` (Copy AI Prompt → action row) · **`844f899` (Workflow layout cleanup)** · **`1bb0bb5` (Stage 6 cleanup + docs)**.

**STATUS: IA rework complete through Stage 6 + the Descriptions Long/Shorts-leaf follow-up (see top of Current Focus).** The Workflow one-page layout cleanup is also complete. Full verification has passed at each step: `eslint` clean (only the 4 long-standing unrelated errors), `vite build` clean, `contentSetupNav.test.js` + `tagContentSections.test.js` pass, and end-to-end Playwright sweeps (0 console errors) covering the top nav, all 4 sections, Generation drill + back, the Descriptions workspace (now an 8-leaf strip, Long/Shorts as leaves), the Workflow one-page layout, the simplified Project page, `{primaryTag}` in Titles, Copy AI Prompt in the action row, and every Generator deep-link (incl. `blocksTarget` + the legacy `layout` → Long resolution). **Stage 7 — the dead-code/label cleanup from the earlier audit, in progress (one step at a time, user-gated).** ✅ Step 1: removed the dead standalone-page fallback branches + the `embedded` prop from the 3 Workflow children (`ProjectSettingsShortsQueue`/`Todo`/`UploadSchedule`) — `WorkflowWorkspace` was already their only caller, always passing `embedded`. ✅ Step 2: panel `<h2>`s now match the nav labels — `ProjectSettingsThumbnails.jsx` "Thumbnail Templates" → "Thumbnails", `ShortHooks.jsx` "Shorts Hooks" → "Short Hooks" (`ThumbnailsPanel.jsx`'s `"Thumbnail Templates → …"` deep-link tooltips deliberately left as-is). ✅ Step 3: `ProjectSettingsGeneral.jsx` → `ProjectSettingsProject.jsx` (`git mv` + component/export rename); `ProjectSettingsContent` dispatch id + branch `'general'` → `'project'`; `ProjectSettingsPage`'s `LEAF_TO_DISPATCH.projectInfo` and the `?? 'general'` fallback → `'project'`. `'general'` stays alive only as a **legacy input** to `resolveContentSetupTarget` (`LEGACY_SECTION_MAP.general → {section:'project', leaf:'projectInfo'}`, test-guarded) and as the `useNavigationTargets` initial-target fallback — both resolve to the Project page unchanged. No storage-key / nav-structure / Backup / UIKit change. ✅ Step 4: deleted the unused `isValidLeaf` / `parentOf` exports from `contentSetupNav.js` (imported only by its own test; the app never used them — `parentOf` returned an `{section, leaf}` object that never fit `ProjectSettingsPage`'s string-based nav state) + their test blocks. The 8-leaf `descriptions` registry assertion already covers what the removed `isValidLeaf` checks did. ✅ Step 5 (docs-only): the click-to-navigate gotcha in `CLAUDE.md`/`AGENTS.md` no longer names the retired `ProjectSettingsPage.handleSectionChange` — it now describes `clearTargets()` called from `selectLeaf` / `selectSection` / `goToOverview` (every in-workspace Content Setup nav action), which clears the five Content-Setup-scoped targets while `activePage` stays `projectSettings`. Dated 2026-07-13 history entries that mention `handleSectionChange` left as-is (accurate for the code of that date). **Stage 7 complete — nothing outstanding.**

**Phrase/template editors: ~5-row scroll cap, shipped 2026-09-03.** Large phrase/hook/template pools no longer make a card arbitrarily tall. `HookTemplateEditor` and `TagPhraseEditor` each wrap *only* their row `.map()` in a shared `<div class="phrase-row-list">` — `max-height: calc(var(--phrase-row-h) * 5); overflow-y: auto; scrollbar-gutter: stable` (`src/index.css`). `max-height` (not `height`) → a short list reserves no empty space and shows no scrollbar; past ~5 rows the rows scroll internally. Search stays above the wrapper; +Add / +Bulk / Copy AI Prompt / the Bulk textarea are siblings below it (never scroll). Covers every screen transitively (global Short Hooks, Title pools, Hook Blocks, Thumbnail/Hashtag base pools, all Tag drill-down leaves, Cover-Specific Hooks). **Paired fix:** `PlaceholderField`'s `{…}` autocomplete now portals its `<ul>` to `<body>` as `position: fixed` (anchored to the field rect captured on the last keystroke — refs can't be read in render), dismissing on scroll/resize — otherwise the new `overflow` would clip the dropdown for rows near the bottom of the scroll area. `ProjectSettingsTodo` (hand-rolled status list) deliberately left out — never gets long enough to matter. Verified live via Playwright against real data (a 39-row Titles pool): cap at ~5 rows, wrapper holds only rows, dropdown portaled + fully in viewport + closes on Escape, click-to-source still scrolls the highlighted row into view. `eslint`/`vite build` clean (4 pre-existing unrelated errors).

**Tag Library card — drill-down IA rework, shipped 2026-09-03.** The flat "Edit tag → 6-tab `SubTabNav` → sub-section → phrase editor" is replaced by a one-level-at-a-time drill: **Tag → content overview → content type → pool editor**. Only the active level is on screen; the card stays compact. Rarely-touched config (Label / Category / `promptContext` / exclusions / Hide-from-Generator / Duplicate / Sync-from-project / Delete) is behind an "Edit Basics" button on the overview — `TagBasicsTab.jsx` reused verbatim as a drill screen (no modal).

- `src/utils/tagContentSections.js` (new; pure, Node-tested — `tagContentSections.test.js`, 55 assertions) is the model: `TAG_CONTENT_SECTIONS` (5 overview rows), `sectionCount`/`descriptionGroupCount`/`shortHookCategoryCount` (live off `tag.maps`, no duplicate state), `getShortHookCategories(projectConfig)`, `getInitialView(tag, sourceTarget)` (click-to-nav landing view; hook match beats field match), `parentOf(view)`, `resolvePoolEditorProps(tag, view)`, `pickTagPhrasePlaceholders`, `isSourceMatch` (moved here from the now-deleted `tagFieldTabs.js`).
- `TagEditor.jsx` rewritten as a `view` state machine (`view = manualView ?? getInitialView(...)`). Views: `overview` / `basics` / `pool` (titles·thumbnails·hashtags) / `shortHookCategories` → `shortHookPool` / `descriptionGroups` → `descriptionPool`. Outer `<details> "Edit tag"` wrapper + open-on-nav-target behavior unchanged.
- New components in `src/components/tags/editor/`: `TagContentOverview`, `TagDrillHeader` (`← backLabel  TITLE  subtitle`), `TagShortHookCategoryList`, `TagDescriptionGroupList`. Every leaf is `TagPhraseEditor noWrapper` — the redundant `Titles → LONG TITLE PHRASES (7)` nesting is gone.
- Short Hook categories are now config-driven from `projectConfig.shortHookTypes` (all 7, incl. `Contrast` — editable per-tag for the first time). The old hardcoded 6-entry `TAG_FIELD_TABS.shortHooks` list is gone.
- `CopyPromptButton` + `buildTagShortHooksPrompt` moved into the `shortHookPool` leaf's action row.
- `TagLibraryPage.jsx` keys each `TagCard` as `${projectId}:${tag.name}` — a same-named tag in another project remounts on project switch, clearing the drill view + local editor state (fixes the old `activeTab`-carryover bug, queue item 1.2).
- Deleted: `TagEditorTabs.jsx`, `TagFieldTab.jsx`, `tagFieldTabs.js`. `eslint`/`vite build` clean (4 pre-existing unrelated errors). Live QA by the user. Commits `7ce92d1`, `9e87d4b`, `d030fec`, `d02f044`, `0323b8e`. Noted, not done: description-sourced click-to-nav still lands on the overview (no deep-link before either); no `onToggle` on the outer `<details>` (a same-value re-render doesn't reset it — verified, queue item 1.1).

**Shared "Copy AI Prompt" authoring system, shipped 2026-09-03.** Generalized the one-off `buildCoverHookPrompt` into a reusable skeleton + per-editor adapters + one button. The *"open editor → Copy AI Prompt → external AI → review → Bulk Add"* workflow now exists on four phrase-pool editors: Cover-Specific Short Hooks, Tag Short Hooks (per hook category), global Short Hook types (Project Settings → Short Hooks), and Title template pools (Project Settings → Titles). One-way — no parse-back; the editor's existing Bulk Add box is the return path.

- `src/utils/authorPrompt.js` — `buildAuthorPrompt(sections)` + `headedList`/`headedBullets`/`contextLines`/`placeholderNote` helpers + a `RULE` object of shared rule strings. Dumb assembler, no domain knowledge.
- `src/utils/authorPromptContexts.js` — `coverShortHooksContext`/`tagShortHooksContext`/`globalShortHookContext`/`titlePoolContext` return the ordered `sections` array; each has a `buildX*Prompt` wrapper.
- `src/utils/coverPrompt.js` — now a 2-line re-export of `buildCoverHookPrompt`; guarded by a lock-step inline reference in `coverPrompt.test.js` (started as a pre-refactor freeze, now updated in step with deliberate cover-prompt changes).
- `src/components/ui/CopyPromptButton.jsx` — shared `button-secondary` + `Copied ✔️` (500ms) + disabled-tooltip button; `getPrompt` is a lazy `() => string`. In `/uikit` under Buttons.
- **New per-tag `promptContext` field** (Tag Basics → "What this tag means (for AI prompts)") — authoritative meaning for `tagShortHooksContext`; never inferred from the tag's own phrases. Rides in the tag override `payload` jsonb (no column/migration — `splitOverride` routes unknown keys there); carried by `resolveTagOverride` and `buildEffectiveTag`/`mergeTagData` (target-wins on sync). `TagBasicsTab.jsx` field + `buildTagExplorerData.js` passthrough; `.tag-edit-fields__full`/`.tag-options` span the 2-col grid so it's full-width below Label/Category. All 51 existing tags populated via `PUT /api/tag-overrides`; 5 flagged for the user's review (punk-as-faster, `alt metal`/`alt. metal` dup, `re-recorded` intent, `modernized`/`faithful` on Maxx Dee).

**Shared prompt-design decisions baked into every adapter:** (1) new AI-generated hooks/templates must NOT use `{transformation}` — it only yields repetitive "{song} + connector + {transformation}" lines; prefer typed category tokens (`{tags.genre}`/`{tags.energy}`/`{tags.tempo}`/`{tags.mood}`/`{tags.production}`) only where the type fits. (2) `{primaryTag}` is type-unsafe (resolves to a genre/tempo/adjective/mood/language/era per cover) so it must never sit in a typed grammatical slot — the global + title adapters carry a "PLACEHOLDER TYPES" section teaching this. Authoring-prompt rules only — the engine and every stored `{transformation}` template/hook are untouched.

**Per-adapter:** Tag = `promptContext` authoritative, existing phrases only for de-dup / "don't imitate the weak ones", stay within the stated meaning. Global = existing templates are the house voice (match them), lines must work for ANY cover, `excludeForFaithful`/`requiresGenre` → boundary lines. Title = templates not finished titles, per-pool role, signal/channel/hashtags go to the prefix-suffix wrapper, "introduce structures the pool doesn't already have". Cover = cover-specific facts only, prior behavior preserved.

Wiring: `CoverShortHooksEditor` (`actionsSlot`), `TagFieldTab` (per-category `actionsSlot`, Short Hooks tab only), `ShortHookCard` (new `headerActions` passthrough) via `ShortHooks.jsx`, `Titles.jsx` (group-card headers — `h3`+`↺` wrapped in `.tag-card-label-row`, count moved to the right grid column). Tests: `authorPrompt.test.js` (22), `authorPromptContexts.test.js` (75), `coverPrompt.test.js` A/B (13), `tagPromptContext.test.js` (9). `eslint`/`vite build` clean. Live QA by the user. Commits `f8c9faf`, `fc6282f`, `773d673`, `59b65a6`, `8e35870`, `6cb5970`, `a98bdfe`. **Out of scope:** Hook Blocks (mostly lore blocks, not hook pools), Title prefix/suffix (single-value fields), DRYing the two "PLACEHOLDER TYPES" strings, wiring `promptContext` into `AddTagPanel`. Generation V2 stays shelved.

**Saved Library: Todo-status badge, clickable to jump to Todo, shipped 2026-08-29.** First real item off the post-audit investigation queue below — the two TagEditor items ahead of it were dropped by the user without investigation ("not interested in those small things... there's a big Tag Library rework we'll need to discuss later"), folded into that future rework instead. A song's Todo status was previously invisible outside the Todo page — `SavedLibraryItem.jsx` gained a badge (existing `badges` slot, alongside the missing year/genre ones), shown only when `entry.todo?.status` is set, clickable to jump to Todo with the matching status section force-expanded and the row scrolled-to/highlighted. New `todoTarget`/`openTodoSearch`/`clearTodoTarget` in `useNavigationTargets.js` + a `todo` branch in `useStaleTargetClearing.js` — same cross-page click-to-navigate convention every other target in the app uses. `buildTodoStatusPanelKey` extracted from `TodoStatusSection.jsx` to a new `src/utils/todoPanelKey.js` (needed by `TodoPage.jsx` too; a component file can't export a second non-component function under this project's react-refresh lint rule). **One live-feedback round on styling**: first pass copied the bordered/uppercase-mono missing-data-badge look; user wanted it to read as metadata instead, "in square brackets... match the Fallout CRT theme," pointing at the adjacent `[queue excluded]` badge as the reference — restyled to match it exactly (bracketed, italic, muted, no border at rest), with only a hover-only glow added to signal it's clickable. `eslint`/`vite build` clean (two pre-existing unrelated errors only). No Playwright — verified live by the user, per standing QA-ownership preference.

**Shorts descriptions: song-scoped List block overrides now actually apply, shipped 2026-08-29.** Bug found during a general project audit, confirmed real via a walked-through repro before fixing. `generateShortDescriptions.js`'s per-block dispatch (`pickShortLine`/`renderShortLine`) rendered List blocks (`kind: 'list'`) straight through `renderStructuredBlockFromPicked` with no `songOverrides` check at all — the adjacent Text-block branch (`kind: 'customBlock'`) already correctly called `renderCustomBlock(picked, ctx, links, songOverrides[blockName])`. So a song-scoped List block (e.g. `gearBlock`) had a real, working override field in the Generator, applied correctly in Long descriptions, but silently ignored in Shorts. Root cause: `renderCustomBlock` expects a `{ kind, picked, block }`-shaped argument (`block` needed to rebuild the overridden variant) — Shorts' `pickShortLine` never attached `block` for List blocks. Fixed by adding `block: customBlocks[blockName]` to the pick result and routing the `'list'` render branch through `renderCustomBlock`, with `isSongOverrideActive`-driven source attribution matching the Text-block branch (so click-to-navigate correctly points at the Generator field when overridden). No storage/UI changes. New persisted test `src/engine/descriptions/generateShortDescriptions.test.js` (Node smoke test, no test runner installed — run via `rolldown` bundle + `node`) — 12 assertions: project defaults, song override applied, revert-on-clear, source attribution, Long-description regression guard. Confirmed the test catches the regression by reverting the fix and re-running (5/12 failed, exactly the override cases) before restoring it. Verified live by the user (`gearBlock` retargeted to Shorts, added to Shorts layout, overridden via Advanced Options, confirmed in the Shorts panel). `eslint`/`vite build` clean.

**Tag Library Titles/Thumbnails: split into separate tabs, plus real `{placeholder}` support for tag phrases and Thumbnail Templates' Words/Fallbacks/Generic Tag Templates, shipped 2026-08-13.** User asked to (1) separate the combined "Titles" tab (Long title phrases + Thumbnail phrases) into its own Titles/Thumbnails pair, and (2) "make them both use placeholders." Investigation found (2) was a real capability gap, not a UI toggle — `generateTitles.js`/`generateThumbnails.js` render tag-specific title/thumbnail phrases as pure literal text with zero `fillPlaceholders` resolution; a config scan confirmed no existing phrase uses `{...}` tokens today. (1) `tagFieldTabs.js`'s combined `titles` entry split into `titles`/`thumbnails`; `TagEditorTabs.jsx` gained a `Thumbnails` tab; `TagEditor.jsx`'s `getInitialTab` routes `field === 'thumbnail'` clicks to the new tab. (2) Full `HOOK_PLACEHOLDERS` includes random tokens (`{originalGenre}`, `{tags.*}`) that are normally safe only because pick-time freezing (`fillPlaceholdersAndFreeze`/`fillFrozenPlaceholders`) preserves the value across re-renders — Title/Thumbnail tag phrases resolve at plain render time with no freezing of their own, so the full set would have re-rolled a random token on every keystroke. New `LIVE_PLACEHOLDERS` (`hookPlaceholders.js`, derived from `placeholders.js`'s newly-exported `ALWAYS_LIVE_TOKENS` — `artist`/`song`/`year`/`years`/`decade`/`signalNumber`/`currentYear`/`fileId`) is the safe subset. Engine changes at render time only: `resolveTitleRecord.js` resolves a tag's title phrase against a base ctx before it fills `{transformation}` in the outer template (otherwise an embedded `{artist}` would leak through literally, since `fillPlaceholders` only scans a template once); `generateThumbnails.js`'s `renderThumbnails` now resolves every phrase source uniformly (tag phrases plus the project-level Words/Fallbacks/Generic Tag Templates pools, wired via `ProjectSettingsThumbnails.jsx`) — Patterns (keyword field, not display text) stays untouched. `useGeneratedOutput.js`'s Titles/Thumbnails live-refresh memos widened to depend on `formData.originalYear`/`formData.signalNumber` too. Verified via two Node smoke tests (rolldown-bundled, 14 assertions total): real config produces zero leaked `{tokens}` (regression safety), a synthetic `{artist}` tag phrase resolves correctly in both Titles and Thumbnails, live artist edits re-resolve without re-picking, Words/Fallbacks/Generic-Tag-Template phrases now resolve too. `eslint`/`vite build` clean. No Playwright pass per the user's standing QA-ownership preference — handed off with a checklist instead.

**Follow-up, same day: Thumbnails upgraded to real pick/render placeholder freezing — full placeholder set, not just `LIVE_PLACEHOLDERS`.** User pointed out Generic Tag Templates specifically needs the wider vocabulary since it's meant to work across any tag. Rather than special-case one field, added the freeze mechanism `generateThumbnails.js` was missing: `pickThumbnails` now calls `fillPlaceholdersAndFreeze` on every candidate phrase and stores the `frozen` map; `renderThumbnails` (now taking `config` as a 3rd param) replays it via `fillFrozenPlaceholders` — same mechanism Descriptions/Short Hooks already use. Once added it applies uniformly, so all four thumbnail phrase sources (tag-specific + Words/Fallbacks/Generic Tag Templates) now support the full `buildHookPlaceholders()` set. `ProjectSettingsThumbnails.jsx`/`TagFieldTab.jsx`'s Thumbnails tab widened their `placeholders` prop accordingly (`TagEditor.jsx` needed `projectConfig` threaded to the Thumbnails tab call, which it wasn't before). **Titles' tag phrases deliberately left on `LIVE_PLACEHOLDERS`** — `resolveTitleRecord.js` wasn't touched this round; same freeze treatment there is a separate follow-up if wanted. Verified via a 9-assertion Node smoke test: a synthetic `{originalGenre}`/`{tags.*}` pick stays byte-identical across repeated renders *and* survives a live Artist edit (the actual freeze-safety property), while `{artist}`/`{tag}` still resolve fresh/correctly alongside it. `eslint`/`vite build` clean.

**Generator's Input section is now collapsible and remembers the choice, shipped 2026-08-13.** User asked for a collapse toggle showing only "Artist — Song" on one line when collapsed. Reused the existing `panelVisibility`/`togglePanel` mechanism (`useAppShellState.js`) rather than new state — `panelVisibility.input` (default `true`) persists via the same `illegalMindGeneratorData.ui.panelVisibility` path every other panel toggle uses. `InputForm.jsx`'s `<h2>Input</h2>` now sits in a `.panel-header` row next to a compact `ToggleButton`; collapsed, everything below (Basic Fields, Entry Settings, Todo, Advanced Options, Save/Clear/Add-to-Calendar) hides and is replaced with a one-line `**Artist** — Song` summary (reusing `SavedEntryRow`'s established format). One necessary fix: `useNavigationTargets.js`'s `openSongOverride` (fired by clicking an Artist/Song-sourced hashtag chip or a song-overridden description block) previously only force-expanded Advanced Options — now also force-expands Input, since override fields live inside Input's now-collapsible body. `eslint`/`vite build` clean.

---

**Themed tooltips, app-wide — replaces native `title=` everywhere, shipped 2026-08-07.** Every tooltip in the app was a native `title="..."` attribute (the browser's own unstyled OS tooltip, unable to be restyled via CSS and clashing with the CRT design) — surfaced while polishing the Calendar's tooltips. Confirmed via `AskUserQuestion` to go app-wide rather than scope to just Calendar. Scoped the real size first rather than assuming "dozens of files": `IconButton`/`NavLinkButton`/`ToggleField` already forward a `title` prop to a native `title=` attribute, covering the large majority of real tooltip call sites app-wide — fixing those 3 files fixed all of them with zero caller changes. Only ~15 direct native `title=` usages elsewhere needed individual one-line swaps (`CalendarDayCell.jsx`, `HashtagLink.jsx`, `DescriptionsPanel.jsx`, `BasicSongFields.jsx`, `ShortHookTitles.jsx`, `TodoItem.jsx`, `Titles.jsx`, `SortableActiveBlock.jsx`, `ProjectSettingsTodo.jsx`, `ProjectSettingsPage.jsx`, `TransformationTagSelector.jsx`, `InputFormActions.jsx`). Mechanism: `data-tooltip="..."` attribute (not `title`, since a native `title=` left in place would show the browser's own tooltip *on top of* the themed one) + pure CSS `[data-tooltip]::after { content: attr(data-tooltip); ... }` reveal on `:hover`/`:focus-visible` — no wrapper component, no JS. `white-space: pre-line` on the pseudo-element handles the one real multi-line case found (`TodoItem.jsx`'s notes preview, sourced from a free-text `<textarea>`) for free, since dynamically-set DOM attributes can contain literal `\n` characters. Added `aria-label` alongside `data-tooltip` wherever the element had no other visible/accessible text (icon-only buttons, drag-handle spans) — `data-tooltip` alone isn't reliably read by screen readers. **A handful of components have a prop that happens to also be named `title` but renders as visible heading/label text, not a native attribute, and must never be converted**: `Modal`, `ShortHookTitles`, `TagPhraseEditor`, `BulkSongEntry`, `ShortsQueueEmptyState`, and `GeneratedTitle` (whose `title` prop is actually a title *data object*, not a string — pure prop-name coincidence). Verified via a full post-implementation repo sweep (`grep -rn "title=" src/`) that every remaining hit is one of these known non-tooltip cases or a `UIKitPage.jsx` `<Section title>` heading — none were missed. New UIKit catalog entry documents the `data-tooltip` convention for future additions. `eslint`/`vite build` clean (same pre-existing unrelated `hex`/`handleRegenerate` lint errors, confirmed predating this session). No Playwright pass per the user's standing QA-ownership preference — handed off with a checklist instead.

**Follow-up, same day: tooltip uppercase inheritance + Saved Library's real scroll container.** Two more reported issues from live testing. (1) Tooltips on `.button-secondary`/`.tag-chip`-styled triggers (e.g. Shorts Queue's "Add to Calendar", Calendar's status text) rendered in ALL CAPS — `text-transform`/`font-weight`/`letter-spacing` are inherited CSS properties, and those trigger classes set `text-transform: uppercase` for their own button label, which the tooltip's `::after` pseudo-element silently inherited. Fixed by explicitly resetting `text-transform: none; font-weight: normal; letter-spacing: normal;` on `[data-tooltip]::after`. (2) Saved Library's year/genre copy-search badges still showed no tooltip after the earlier `.saved-entry-row` clipping fix — traced to a second, more fundamental blocker: `.saved-library-list` (unlike `.shorts-queue-list`, which is permanently `overflow: visible`) is a genuinely scrollable container (`overflow-y: auto`, `max-height: 500px`). Applying the same "unclip on hover" trick here would stop it being a scroll container for that instant, visually dumping every row in the list at once (ignoring scroll position) until the hover ends — a much worse glitch than the missing tooltip, especially for a large library. Confirmed via `AskUserQuestion`: keep these two specific badges on the plain native `title=` tooltip instead of chasing a themed fix that risks that flash. Added a deliberate escape hatch to `IconButton.jsx` — a new `nativeTooltip` prop (default `false`) that renders a real `title=` attribute instead of `data-tooltip`/`aria-label` when set; used only by these two `SavedLibraryItem.jsx` badges. Every other `IconButton` usage app-wide is unaffected. `eslint`/`vite build` clean.

**Follow-up, same day: tooltip clipping fixed — `overflow:hidden` was hiding the popup in two shapes.** Reported live: Shorts Queue's "Add to Calendar" tooltip didn't show, and Calendar's "S · Uploaded" status tooltip didn't show. Both trace to the same root cause, in two different shapes: (1) *self-clipping* — `.calendar-slot-entry-status`/`.calendar-slot-placeholder`/`.todo-note-preview` all have their own `overflow:hidden` for ellipsis text-truncation, which also clips their own tooltip's `::after` popup (a child of the same box). Fixed generically for any tooltip-bearing element: `[data-tooltip]:hover, [data-tooltip]:focus-visible { overflow: visible; }` — the truncated text may also briefly show its full content while hovering, an acceptable side effect. (2) *ancestor-clipping* — `.saved-entry-row` (shared by `SavedLibraryItem`/`ShortsQueueItem`/`TodoItem`) has `overflow:hidden` for an unrelated `::before` load-sweep animation, which clips a nested action button's tooltip popping up above the row. Fixed with a `:has()` rule scoped to that one class: `.saved-entry-row:has([data-tooltip]:hover), .saved-entry-row:has([data-tooltip]:focus-visible) { overflow: visible; }` — harmless to the sweep, which only plays on load, not hover. `vite build` clean.

**Follow-up, same day: occupied-slot status line reverted to type abbreviation, custom title moved to tooltip.** Live testing surfaced that combining the custom title with the status word on one line (`"S COVERS · PLANNED"`) simply didn't fit a day-cell's width for even a short title — the ellipsis truncated it mid-word (`"S COVERS · PLANN…"`). Presented 3 options via `AskUserQuestion` (own line below the song title, tooltip-only, or drop the status word and keep the title inline) — user chose tooltip-only. `CalendarDayCell.jsx`'s `CalendarSlotEntry` status line reverted to always showing `{VIDEO_TYPE_ABBR} · {STATUS_LABELS}` (e.g. "S · Planned") regardless of `slotTitle`; the custom title is now prepended to the status button's native `title=` tooltip instead (`"{slotTitle} — Click to confirm as uploaded..."`), so it's still discoverable on hover without competing for the same cramped line. The empty-placeholder display (which had no competing status word and was already working fine, e.g. "LONG VIDEOS" fit) is unchanged. Settings-tab title input also moved back inline into the same row as weekday/type/time/remove (was stacked above), per a separate follow-up ask — same line, title as the first (widest) column. `eslint`/`vite build` clean.

**Follow-up, same day: Upload Schedule's "Weekly Slots" card tightened up and widened after live feedback.** The new title input previously stretched `width:100%`, making it read like an oversized heading rather than a compact label — shrunk to a fixed `200px` (left-aligned, not centered) with tighter `.form-input`-overriding padding, and slot-to-slot vertical spacing (`.upload-schedule-slot`'s margin) tightened. Separately, the tab's single-card wrapper reused `.tag-library--3col` (the same `auto-fill, minmax(400px,...)` grid every other Project Settings tab uses, regardless of card count) — for a tab with exactly one card, `auto-fill` reserves empty column tracks sized for a repeating multi-card layout rather than reliably proportioning the one real card, so its on-screen width was really just whatever fell out of that math for a given viewport, not a deliberate size. New `.tag-library--half` modifier (`grid-template-columns: minmax(320px, 50%)`) pins it directly to roughly half its container instead, reusable by any other single-card tab later. `eslint`/`vite build` clean.

**Upload Schedule slots gained an optional "title" label, shown on both the Settings screen and the Calendar page, shipped 2026-08-07.** User wanted a way to distinguish a slot meant for something other than regular covers (e.g. a "Vlog" or "Behind the Scenes" slot) at a glance — purely a visual/organizational label, no change to matching/generation logic (still driven entirely by `videoType`/`weekday`). Confirmed via `AskUserQuestion` that the label should show on the Calendar grid too, not just in Settings. `ProjectSettingsUploadSchedule.jsx`: each slot row gained a new title text input, stacked above the existing weekday/type/time/remove row (new `.upload-schedule-slot` wrapper) rather than squeezed into the already-tight 4-column grid — `updateSlot`/`setSlots` needed no changes, the existing `{...slot, ...patch}` merge already handles an added field. `useUploadCalendar.js`'s `getMonthGrid` previously built `calendarSlots` items with no reference back to the original scheduled-slot config (`{slotKey, videoType, plannedEntry, uploadedEntry, status}` only) — added a `title` field resolved via `scheduledSlots.find(s => s.videoType === videoType)`. `CalendarDayCell.jsx` shows the title in **both** places a slot renders — the empty placeholder (`{(slot.title || slot.videoType).toUpperCase()}`) and `CalendarSlotEntry`'s status line (new `slotTitle` prop, named to avoid confusion with the native `title=` tooltip attribute already used throughout that component) — deliberately not just the empty state, since planning a real video into a "Vlog" slot shouldn't silently drop the label right when it's most useful for telling slots apart. Added ellipsis-truncation CSS to `.calendar-slot-placeholder` (previously assumed a short fixed SHORT/LONG string, now needs to handle arbitrary-length user text) — `.calendar-slot-entry-status` already had this. `eslint`/`vite build` clean. No Playwright pass per the user's standing QA-ownership preference — handed off with a checklist instead.

**Follow-up, 2026-08-07: Import-backup's `alert()` was missed in the Toast migration — bare call, not `window.alert()`.** `AppBackupControls.jsx`'s Import flow called plain `alert(...)` (no `window.` prefix), which the original migration's `window.alert(` grep never matched. Fixed: `showToast` threaded through `App.jsx` → `ProjectSettingsPage.jsx` → `ProjectSettingsContent.jsx` → `ProjectSettingsGeneral.jsx` → `AppBackupControls.jsx`, mirroring the existing `onOpenUIKit` prop chain exactly (same four files already thread it, so no new wiring pattern). Both call sites swapped: the success path (`'Backup imported. Reloading...'`) and the catch path (`'Invalid backup file.'`). One necessary side-effect fix: the success path's `setTimeout(() => window.location.reload(), 300)` used to only start counting *after* the user dismissed the blocking `alert()` — a non-blocking toast doesn't pause execution, so 300ms would elapse and reload the page almost immediately, before the message could be read. Bumped to 1500ms. Verified via a repo-wide `\balert\(` grep (not just `window.alert(`, to catch any other bare calls) — confirms zero remaining call sites anywhere in `src/`. `eslint`/`vite build` clean.

**`window.alert()` replaced with a new `Toast` primitive, shipped 2026-08-06.** User reported (via screenshot after clicking "Add to Calendar") that the native browser popup clashed with the CRT terminal design and needed an explicit "OK" click to dismiss — wanted a small in-app notification near the bottom of the screen that disappears on its own instead. New `src/hooks/useToast.js` (`{ toast, showToast }` — `toast` is `{message, id}` or `null`; `showToast` clears any pending auto-dismiss `setTimeout` via a `useRef` before scheduling a new one, the one deliberate improvement over `CopyButton.jsx`'s simpler "temporary state" precedent, needed since a second toast can be triggered before the first clears) and new `src/components/ui/Toast.jsx` (always mounted in `App.jsx` at the root, unlike `Modal` which is conditionally mounted by its caller — cheap, returns `null` internally when there's no toast; `key={toast.id}` forces a remount + CSS animation restart on every new toast). New CSS section in `index.css`, deliberately placed *after* `.terminal-block` (equal-specificity cascade: `.terminal-block`'s `border` shorthand would otherwise reset `.toast`'s `border-color` override if `.toast` came first) — fixed bottom-center, amber glow border/shadow matching `.button-primary`'s hover treatment, `z-index: 700` (above `.modal-overlay`'s 500, below the CRT scanline/vignette overlays at 9998/9999), a `toast-in-out` keyframe (fade+slide in, hold, fade+slide out) timed to the same 3s as the JS auto-clear. All 6 `window.alert()` call sites migrated (`GeneratorPage.jsx`/`ShortsQueuePage.jsx`'s Add-to-Calendar handlers, `TagLibraryPage.jsx`'s Sync Tags and per-tag copy-from-project actions) — each a direct `window.alert(msg)` → `showToast(msg)` swap, same wording, via a new `showToast` prop threaded from `App.jsx`'s single `useToast()` instance. The 7 existing `window.confirm()` destructive-delete guards (Tag/Block editors) were explicitly left untouched — confirmed via research first that they all need a synchronous yes/no answer a toast structurally can't provide. New UIKit catalog entry (`Section`/`Example`, same shape as `Modal`'s) with a demo button. `eslint`/`vite build` clean — two pre-existing unrelated lint errors (`GeneratorPage.jsx`'s unused `handleRegenerate`, `UIKitPage.jsx`'s unused `hex`) both predate this change. No Playwright pass per the user's standing QA-ownership preference — handed off with a checklist instead.

**Upload Calendar — remove-from-queue (×) button added to the picker's Shorts Queue suggestion list, shipped 2026-08-06.** User asked for the same "×" behavior the Shorts Queue page already has (`ShortsQueueItem`'s remove button → `useShortsQueue.js`'s `markUploaded`, which removes that slot and auto-refills a valid replacement at the bottom) inside the Calendar's "Plan Short" picker too, since its queue-suggestion list previously had no way to drop a song from the queue without leaving the calendar. `CalendarPage.jsx` now also destructures `markUploaded` from its own `useShortsQueue` instance and adds `handleRemoveFromQueue(entryId)` — since the picker's `shortsQueueEntries` is deduped by id (the same song can occupy more than one raw queue slot), this resolves back to the *first* raw index holding that id via `shortsQueue.findIndex(...)` before calling `markUploaded(index)`, reusing the existing function verbatim rather than adding a second removal mechanism. `CalendarEntryPicker.jsx` renders a `SavedEntryRow` `actions` slot with an `IconButton` "×" (same icon/pattern as `ShortsQueueItem`), gated on `isShort && !needle` — only shown while the list is genuinely the queue-suggestion view (matches the picker's Current-Focus-documented "no queue vs. non-queue distinction while searching" decision from the earlier full-library-search feature; once search widens the pool, a result may not be queued at all, so no × there). No `onClose()` call on remove — the picker stays open, matching the Shorts Queue page's in-place behavior, and the row list naturally re-renders from the parent's already-reactive `shortsQueueEntries`. `eslint`/`vite build` clean; no Playwright pass per the user's standing QA-ownership preference — handed off with a checklist instead.

**Upload Calendar — entry picker search widened to the full library for Shorts, shipped 2026-08-06.** Reported live: the "Plan Short" picker's search box only searched the current Shorts Queue, matching its "Short slots pick exclusively from the queue" design — but this made it impossible to plan a Short for any song not already queued. Fix, scoped to `CalendarEntryPicker.jsx` only: the queue stays the default *suggestion* list (empty search box → queue entries only, unchanged), but typing a search now widens the pool to the full saved library, same as Long slots already did unconditionally. Confirmed via `AskUserQuestion`: no visual distinction between queue-sourced and other results once searching — a match is a match. Subtitle now flips between "Showing your Shorts Queue." (empty search) and "Searching your full saved library." (typing), and the placeholder hints at the expanded scope. The empty-queue empty-state message (`shortsQueueEntries.length === 0`) is now checked independently of the active `pool`, so it only shows in the genuine empty-search+empty-queue case rather than misfiring once a search widens the pool to the (non-empty) full library. `CLAUDE.md`'s "Shorts Queue sourcing" note updated to describe the suggestion-vs-search split instead of "exclusively." `eslint`/`vite build` clean; no Playwright pass per the user's standing QA-ownership preference — handed off with a checklist instead.

**Upload Calendar — `addToNextOpenSlot` bug fix + × clear + drag-and-drop between slots, shipped 2026-08-06.** Reported live: clicking "Add to Calendar" from the Shorts Queue for a song assigned it to a Friday slot that already had a real upload logged (no plan, but a real `uploadedEntryId`) instead of skipping to the next genuinely free day — turning a clean "Uploaded" cell into a "Differs" one. Root cause: `findNextOpenSlot` (`useUploadCalendar.js`) only checked `slot?.plannedEntryId` to decide a slot was open, never `uploadedEntryId`. Fixed with one added condition (`!slot?.plannedEntryId && !slot?.uploadedEntryId`) — an upload-only slot is no longer treated as free.

Two follow-up features requested in the same report, scoped via `AskUserQuestion` before building: (1) a small × in the corner of a day-cell card to clear a planned song directly, and (2) drag-and-drop to move a planned song between slots. Confirmed design: × only ever appears on a still-*planned* (or missed/unconfirmed) entry — an uploaded/uploaded-drift entry has no × at all, since clearing real upload history should require first reverting it to planned via the existing status-click quick-toggle, not a one-click delete. Same rule governs the new drag handle — only planned entries are draggable, uploads never move. `CalendarSlotEntry` (`CalendarDayCell.jsx`) gained a `canClear = status === 'planned' || status === 'missed'` check gating both a corner `IconButton` (×, calls the already-existing `calendar.clearPlannedEntry`) and a `⠿` drag handle (`useDraggable` from `@dnd-kit/core`, disabled when `!canClear`) — both absolutely positioned in opposite top corners of the card via new `.calendar-slot-entry--clearable` padding.

Drag-and-drop is cross-cell (moving between arbitrary day slots), not a sortable list, so it's a new pattern for this codebase's existing `@dnd-kit` usage (which until now was all `useSortable`/`arrayMove` for linear lists — Todo Statuses, Active Layout, Block Groups). New `CalendarSlotDropZone.jsx` wraps every slot row (`useDroppable`, id = the slot's own `` `${isoDate}|${videoType}` `` key) with `disabled` set when that slot already has a `plannedEntryId` — dropping onto an upload-only slot (no plan yet) is still allowed, matching what the manual Ctrl+Click "Change Plan" flow already permits there. `CalendarMonthGrid.jsx` now wraps the whole grid in one `DndContext` (`PointerSensor` only, 5px `activationConstraint` — no `KeyboardSensor`/`sortableKeyboardCoordinates`, since that assumes a linear reorderable list; the existing Ctrl+Click "Change Plan" flow remains the keyboard-accessible way to move a plan). `onDragEnd` rejects the drop (no-op) if origin/target `videoType` differ or if the target is the same slot — otherwise calls `setPlannedEntry` on the target then `clearPlannedEntry` on the origin (a move, not a copy).

Verified live via Playwright (isolated seeded `localStorage`, real dev server, 3 consecutive runs for stability): seeded an upload-only Friday slot + a planned Monday slot + a queued song not yet assigned; clicking "Add to Calendar" correctly skipped both occupied days and landed on the next truly free scheduled day; dragging the Monday plan's handle to an empty same-type slot correctly emptied the origin and populated the target (via real `mouse.move`/`down`/`up` events crossing dnd-kit's 5px activation threshold, same technique this app's existing `SortableActiveBlock` drag precedent uses); the × button was absent on the upload-only slot and present on the planned one, and clicking it cleared the plan cleanly; 0 console errors across all runs. `eslint`/`vite build` clean. One known pre-existing data issue flagged to the user, not auto-fixed: the original bug had already written one bad "Differs" cell into real project data before the fix landed — needs a manual Ctrl+Click "Change Plan" correction, since this session never touches real project storage.

**Upload Calendar — "Import from YouTube" paste-parser shipped 2026-08-05.** First Phase 2 sub-feature (see plan `C:\Users\Max\.claude\plans\yes-let-s-do-this-prancy-shell.md`) — bulk-syncs real YouTube upload history into the calendar, since YouTube video titles are stylized and don't match this app's Artist/Song naming (e.g. "Still love what The Offspring did with Defy You" is actually The Offspring's "Defy You"). Same AI-round-trip pattern as Missing Data/Tag creation: the user screenshots their YouTube Studio Content list (Shorts and Videos are separate tabs), feeds it to an external AI with a copied prompt, pastes the reply back into a new "Import from YouTube" `CollapsiblePanel` on `CalendarPage.jsx`.

**Prerequisite bugfix, done first as its own step and verified independently before touching the new feature.** `useUploadCalendar.js`'s `patchSlot`/`updateSlots`/`removeSlot` read `slots` from the render-time closure instead of the latest React state — every existing single-call flow (Shorts Queue/Generator add-to-calendar, the manual picker) never exercised this, since each only makes one calendar-mutating call per action, but the new import Apply handler is the first caller that needs to call `setUploadedEntry` N times in a synchronous loop (once per matched row). Since each call would have computed its patch from the same stale `slots` snapshot, only the *last* call in the loop would have survived — in both React state **and** localStorage (both writes derive from the same stale base object, so this wasn't just a render glitch, it would have silently dropped every row but the last one from storage too). Fixed by switching `updateSlots` to the functional-updater form (`setSlots((prevSlots) => {...})`, matching `useSavedEntries.js`'s `handleAddEntries` precedent) and chaining `patchSlot`/`removeSlot` through it. All public method signatures unchanged. Verified live via Playwright: the existing single-call flows (Shorts Queue add-to-calendar, calendar picker Clear) still work identically post-fix, 0 console errors.

**`src/utils/calendarImportPrompt.js` (new)** — `buildCalendarImportPrompt(videoType)`/`parseCalendarImportResponse(text, savedEntries)`, same contract shape as `searchQuery.js`'s Missing Data pair (parser reports what it found, never decides what to write). The prompt branches its extraction instructions on `videoType` because real YouTube Studio screenshots show the Artist/Song pair lives in different places per tab: **Shorts** titles are stylized hooks/questions with the real source only named in the grey description snippet underneath (patterns: `"Artist - Song (...)"`, `"Song by Artist // ..."`, `"Song // Cover by Artist"`, occasionally only in a hashtag as a last resort); **long-form** titles usually already contain the Artist/Song pair directly (e.g. "Rammstein – Sonne (What If It Was Punk?)"), falling back to the description only if the title doesn't make it clear. Both ask for ISO `YYYY-MM-DD` dates and a `YYYY-MM-DD | Artist | Song` reply line per video, or `SKIP | YYYY-MM-DD | reason` for a video with no identifiable cover source (e.g. an original composition). `parseCalendarImportResponse` reuses `normalizeForMatch` (now `export`ed from `searchQuery.js`, was private — identical case/whitespace/curly-quote matching need) and returns `{ matches, unmatched, skipped, unrecognized }` — deliberately does not dedupe same-day/same-type collisions within one batch, that's left to the caller to detect and report (matches don't get silently merged or dropped at the parser level). Verified via a 20-assertion Node smoke test (bundled with `rolldown`, this project's bundler): prompt branches, real-example anchors present, all four result buckets correct including a curly-quote match and an uncollapsed same-date collision.

**`CalendarImportPanel.jsx` (new)** — same Copy/Paste/Apply/Cancel/summary shape as `MissingDataTools.jsx`, plus a Short/Long `FormSelect` selecting which `videoType` the pasted batch targets (one YouTube Studio tab = one paste, per the user's explicit preference over having the AI label each row's type). `handleApply` always calls `calendar.setUploadedEntry(isoDate, videoType, entry.id)` per match — **confirmed via `AskUserQuestion`: YouTube data is source of truth, so an existing different upload gets overwritten, never skipped** — reading `calendar.getSlot` first only to tally new/updated/unchanged/collision counts for the result summary, not to gate the write. **Also confirmed via `AskUserQuestion`: both "Scheduled" (not live yet) and "Published"/"Premiered" YouTube videos are logged identically, always via `setUploadedEntry`, never `setPlannedEntry`** — the user's own words: "those videos are already uploaded... I'd just color differently the videos from the future/past just to visually differentiate somehow." So the Scheduled-vs-Published distinction is presentational only (see below), not a different write target or a new stored field.

**Follow-up fix, same day: import also resolves stale plan drift, not just the upload.** Real usage surfaced a gap — a slot that already had a *different* `plannedEntryId` from before the import (e.g. an earlier manual guess) showed as `uploaded-drift` ("Differs," both rows visible) after importing the real upload, since the import only ever wrote `uploadedEntryId` and left `plannedEntryId` untouched. Correct behavior for the manual picker (a human deliberately picking a different upload than planned — drift tracking's actual purpose), but wrong for a bulk historical import: a stale pre-import guess shouldn't survive as a "differs" flag on real ground-truth data. Fixed in `CalendarImportPanel.jsx`'s `handleApply` — after `setUploadedEntry`, if `existingSlot.plannedEntryId` is set and differs from the imported entry, `setPlannedEntry` aligns it to match too, so an imported row always lands as a clean "Uploaded," never "Differs." Manual-picker drift is untouched — this only fires inside the import's write loop. Verified live: seeded a slot with a mismatched pre-existing plan, imported a different real upload, confirmed the cell collapsed to a single clean "Uploaded" row (both `plannedEntryId`/`uploadedEntryId` now equal in storage), old planned song gone from the cell, 0 console errors.

**Second follow-up, same day: song-title fallback matching + a confirmation UI for ambiguous/uncertain matches.** Real usage surfaced a real unmatched case: `2026-01-24 | t.A.T.u. | Нас не догонят` failed to match a saved entry stored as `ТАТУ — Нас не догонят` — the song title was identical, only the artist name differed (Latin transliteration vs the Cyrillic stage name). User asked for song-title fallback matching, with a confirmation step when a song title matches more than one saved entry (e.g. "Stay," "Breathe," "Hold On" — generic titles covered by many different artists). `parseCalendarImportResponse` (`calendarImportPrompt.js`) now falls back to a song-title-only search (`songTitleMatches` — exact match or substring containment either direction, reusing `normalizeForMatch`; skipped entirely below `MIN_FUZZY_SONG_LENGTH` = 3 chars to avoid false positives on very short titles) whenever the exact artist+song match fails. Candidates found this way are **never auto-applied** — they go into a new `needsConfirmation` bucket (`{isoDate, artist, song, raw, candidates}`), kept structurally separate from `unmatched` (which now only means "no saved entry has this song title at all"). `CalendarImportPanel.jsx` renders one row per pending item — `{isoDate} — {artist} — {song}` plus a `FormSelect` listing every candidate (`{artist} — {song}` labels) with "Skip" as an explicit option; a single-candidate row auto-pre-selects that candidate (still requires the explicit "Confirm Selected" click, never writes silently), a multi-candidate row defaults to "Skip" so an ambiguous case is never guessed. A shared `applyImportMatch(calendar, videoType, isoDate, entry, tally)` helper (extracted from the original immediate-apply loop) is reused by both the auto-apply path and the confirm-pending path, so both get the same tally/plan-alignment behavior with no duplicated logic. The result summary gained a `confirmedCount` clause, additively merged into the original tally after confirming (not a separate second summary). Verified via a 12-assertion Node smoke test (the exact reported t.A.T.u./ТАТУ case, an ambiguous 2-candidate case, exact-match regression, true-unmatched, partial-substring match, and the short-title-skips-fuzzy-fallback guard) and live via Playwright: single-candidate row auto-pre-selected the correct entry, ambiguous row correctly listed both candidates and defaulted to Skip, picking a specific candidate and confirming wrote exactly that entry to the calendar, confirmation rows cleared after confirming, 0 console errors.

**Trigger UI revised same-day after live user feedback: `CollapsiblePanel` → `Modal`.** First shipped rendered inside a `CollapsiblePanel` on `CalendarPage.jsx`, positioned above the month nav — user feedback after seeing it live: the permanent bordered panel-header bar visually competed with the month grid for top billing (a secondary/occasional action given the same visual weight as primary content), and sitting above the nav pushed the actual calendar down. Fixed by introducing this app's first `Modal` primitive (`src/components/ui/Modal.jsx` — `title`/`onClose`/`children`, backdrop-click + Escape to close, body scroll lock while mounted; catalogued in `/uikit`) — the trigger is now one small `.button-secondary` in a `.regenerate-row` (top-right, same right-aligned single-action-button pattern `ShortsQueuePage`'s "Randomize" and the Generator's "Regenerate" already use) sitting beside the month nav, and `CalendarImportPanel` renders inside a `Modal` only while open, with zero permanent footprint when closed. Default remains an inline expandable panel (`AddTagPanel`/`CalendarEntryPicker`/`MissingDataTools`) for every other case — this is the one exception, deliberately not the new norm. This is `CalendarPage.jsx`'s first page-level toolbar action either way — previously only per-day-cell click targets existed.

**Future-upload visual differentiation (presentational only, no new stored field)** — `CalendarDayCell.jsx` gained `isFutureUpload(isoDate, status)`: true when status is `'uploaded'`/`'uploaded-drift'` and the date is after today. `CalendarSlotEntry` gained an `extraClassName` prop appending `calendar-slot-entry--scheduled` (new CSS: dashed border, same color as its status modifier — matches `.calendar-slot-placeholder`'s existing dashed-border precedent) when true. In the `uploaded-drift` branch (which renders two `CalendarSlotEntry`s side by side), it's attached only to the second (`uploaded-drift`) one, not the first (`planned`). Purely a render-time date comparison, consistent with `deriveSlotStatus` already being "always derived, never stored."

**Verified live via Playwright end-to-end**, isolated seeded `localStorage` (never real project data): pasted 3 matched rows on 3 different dates (past/today/future) → **all 3 landed correctly, not just the last one** (the actual regression test for the stale-closure bugfix); persisted correctly across a page reload; future-dated slot rendered with the dashed `--scheduled` class, past-dated one didn't; re-pasting the identical batch reported "0 new, 3 unchanged"; importing a different song into an already-filled slot correctly overwrote it and reported "1 updated" (confirmed via direct storage read, not just the summary text). 0 console errors throughout. `eslint`/`vite build` clean. **Modal swap separately verified live**: no permanent panel chrome visible before opening; Escape, backdrop click, and the × button all close it; a click inside `.modal-content` does not close it; body scroll locks while open and restores on close; the full Copy/Paste/Apply flow still works identically inside the modal (confirmed by writing a real calendar slot from inside it). 0 console errors.

**Deliberately deferred, unscoped**: a "suggested songs to add" feature, and click-to-navigate-and-highlight from Shorts Queue/Generator to the day just added (both `addToNextOpenSlot` call sites already return the assigned `{isoDate, videoType}`, so this remains cheap to add later).

---

**Upload Calendar — Phase 1 fully shipped 2026-08-05 (all 7 steps).** New multi-iteration feature: a visual month-grid calendar per project showing the weekly upload rhythm, letting songs be assigned to specific days, and tracking planned-vs-actual drift (the calendar never syncs with YouTube, so nothing is assumed — the user must explicitly confirm). Full plan: `C:\Users\Max\.claude\plans\i-think-the-time-wild-floyd.md`.

**Step 7, add-from-Generator, shipped 2026-08-05 — closes out Phase 1.** New "Add to Calendar" button in `InputFormActions.jsx`, beside Save/Clear Form, threaded down from a new `handleAddToCalendar`/`isCurrentEntrySaved` pair computed in `GeneratorPage.jsx` (via `InputForm.jsx`, a plain prop pass-through, no new state there). `GeneratorPage.jsx` instantiates its own `useUploadCalendar(projectId, savedEntries, projectConfig.uploadSchedule)` — a third independent hook instance alongside `CalendarPage.jsx`'s and `ShortsQueuePage.jsx`'s, same established per-page pattern. The plan's flagged edge case (the currently-loaded song must already be a saved entry, i.e. have a real id, before `addToNextOpenSlot` can target it) is handled without adding an `id` field to `formData` — `formData` never carries one (see `buildFormDataPatchFromEntry`/`buildEntryFromFormData` in `useSavedEntries.js`'s Known Gotchas), so `GeneratorPage.jsx` recomputes the candidate id the same way saving does, via `buildEntryId(formData.artist, formData.song)` (`src/utils/savedEntries.js`, already exported), and checks `savedEntries.some(e => e.id === currentEntryId)`. The button is `disabled` with a `"Save this entry first"` tooltip when that's false — matches the plan's explicit-action bias (no silent auto-save) and the codebase's established disabled+tooltip convention. `formData.videoType` (`'Long'`/`'Shorts'`) is mapped to the calendar's lowercase `'long'`/`'short'` domain at the call site, same mapping `CalendarEntryPicker.jsx` already uses elsewhere. Feedback is the same `window.alert` pattern step 6 established. Verified live via Playwright (isolated `localStorage` seed): button correctly disabled on a blank form and on an unsaved new artist/song, correctly enabled once the form matched a saved entry; clicking with Long selected landed on Illegal Mind's actual scheduled Long weekday (Sunday), switching to Shorts and clicking again landed on its scheduled Short weekday (Wednesday) as an independent slot — both written with the correct slot-key shape, no cross-contamination; 0 console errors. `eslint`/`vite build` clean (one pre-existing unrelated lint error in `GeneratorPage.jsx`, `handleRegenerate` unused, confirmed via `git stash` to predate this change).

**Step 6, add-from-Shorts-Queue, shipped 2026-08-05.** `ShortsQueueItem.jsx` gained a second `IconButton` ("📅", title "Add to Calendar") beside the existing "×"; `ShortsQueuePage.jsx` instantiates its own `useUploadCalendar(projectId, savedEntries, projectConfig.uploadSchedule)` (a separate hook instance from `CalendarPage.jsx`'s, same pattern `useShortsQueue` already uses per-page — each instance reads fresh from storage on mount and writes through `updateAppStorage`, so no shared-instance wiring was needed) and calls `calendar.addToNextOpenSlot(entry.id, 'short')` on click. Feedback is a plain `window.alert` (matches the existing precedent in `TagLibraryPage.jsx`'s Sync/Copy actions) — either `"Added to calendar: {formatDayLabel(target.isoDate)}"` or `"No open Short slot found in the next 90 days."` if `addToNextOpenSlot` returns `null`. No new state, no new files — `resolvedProjectConfig` (which already carries `uploadSchedule`) was already being passed into `ShortsQueuePage` as `projectConfig`, so no prop-threading changes were needed anywhere upstream. Verified live via Playwright (isolated `localStorage` seed, not real project data): clicking the button assigned a test entry to the correct scheduled Short weekday (today, since it happened to be one), wrote the expected `uploadCalendar` slot shape, and the entry then rendered correctly on the Calendar page grid; 0 console errors. `eslint`/`vite build` clean.

Data model, confirmed via several rounds of clarifying questions before building anything: new `uploadSchedule.slots` config per project in `projects.json` (`{weekday, videoType, time}` — Illegal Mind: Mon/Wed/Fri short + Sun long; Maxx Dee: Sun/Tue/Thu short + Sat long, all 18:00), edited via a new Project Settings "Upload Schedule" tab. New `uploadCalendar` storage slice keyed by `` `${isoDate}|${videoType}` `` — each slot holds two independent, nullable fields (`plannedEntryId`/`uploadedEntryId`), not a single song+status pair. Video type is an attribute of the *slot*, never the saved entry. Status is always derived, never stored (`deriveSlotStatus`): empty / planned / missed (past + unconfirmed, flagged) / uploaded / uploaded-drift (confirmed upload differs from plan — both stay visible). `useUploadCalendar.js`'s core logic is pure functions taking explicit arguments, Node-testable without React; `calendarDates.js` is hand-rolled native-`Date` month-grid math, no new dependency.

Built: manual add-from-calendar (`CalendarEntryPicker.jsx`, inline search-and-pick, no modal). Not yet built: add-from-Shorts-Queue and add-from-Generator auto-assign buttons (steps 6-7) — see Next Recommended Tasks. Shorts Queue sourcing added after live feedback: Short slots pick exclusively from the current queue (deduped); Long slots search the full library.

Interaction model went through two live-feedback iterations before landing on the final minimal design (worth reading in full in `CLAUDE.md`'s Current Focus if touching `CalendarDayCell.jsx` again): icon buttons → a "..." kebab menu (found and fixed a real bug along the way — `IconButton`'s `className` prop replaces rather than appends to the base `tag-reset-button` class) → the current no-visible-buttons design, reusing this app's existing Ctrl+Click convention. Title click loads into Generator, Ctrl+Click changes the plan; status click directly confirms/clears the upload with no picker at all, Ctrl+Click opens the picker to set a specific different upload (drift). Also fixed a real CSS Grid bug (`.calendar-day-cell` needed `min-width:0` — a column with unwrapped long text was claiming more than its `1fr` share) and redesigned day-cell entries as a compact 2-line card instead of reusing `SavedEntryRow`'s horizontal layout, which was crushing titles to invisible in a ~180px cell.

Deliberately deferred to a later iteration: a paste-import parser for YouTube-generated text (needs a real sample first), a "suggested songs to add" feature, click-to-navigate-and-highlight from Shorts Queue/Generator to the day just added.

Verified at every step via live Playwright against the running dev server (isolated browser profile) — 6 test scripts covering settings persistence, grid rendering/nav, the full picker CRUD flow, all 4 statuses, queue sourcing/dedup, confirm/drift/revert, and empty+Ctrl+Click upload-logging. Zero console errors throughout. `eslint`/`vite build` clean at every commit.

**Storage cleanup: legacy-key backup/restore + `storageMigration.js` removed (2026-08-04).** First item in the PRIORITY TODO now done. `appBackup.js` no longer captures/restores the old standalone legacy keys (`LEGACY_KEYS`/`readLegacyData`/the `legacy` field on `buildAppBackup()`/the legacy write-back loop in `restoreAppBackup()` all dropped — 63 → 37 lines); backup/restore now only touch the unified `illegalMindGeneratorData` key. `storageMigration.js` (the one-shot preview/write migration utility) is deleted entirely, along with its DEV-only `window.previewUnifiedStorageMigration`/`writeUnifiedStorageMigration` exposure in `App.jsx` (and the now-unused `useEffect` import). Confirmed via grep before touching anything that each hook already does its own one-time fallback-and-migrate instead of relying on that utility — `useAppShellState.js`/`savedEntries.js`/`useSavedLibraryFilters.js`/`GeneratorPage.jsx`/`SavedLibrary.jsx` each check the unified key first and only fall back to *reading* (never writing) a legacy key when the unified value is empty; that per-hook fallback code is untouched, since it's the actual replacement mechanism, not part of this cleanup. The third PRIORITY TODO item (deleting the stale standalone keys from localStorage) needed no action — user confirmed via DevTools that none of the old keys (`savedEntries`, `shortsQueueByProject`, `tagOverrides`, `tagVisibilityOverrides`, `formData`, `panelVisibility`, `activePage`, `selectedProject`, `showSavedLibrary`, `hideQueueHidden`) exist in localStorage anymore. `eslint`/`vite build` clean. Storage-unification cleanup pass is now fully closed out — see Priority Todo removal in `CLAUDE.md`.

**Todo Statuses: reorderable, buttons + drag-and-drop (2026-07-30).** User asked for a way to reorder Todo statuses — `TodoPage.jsx` already builds its sections by mapping `todoStatuses` in array order, so reordering the array alone reorders the page, no other wiring needed. `ProjectSettingsTodo.jsx`'s Statuses editor previously used `TemplateGroupCard`'s built-in `templates`/`onUpdateTemplates` path (`HookTemplateEditor` — search box, no reorder concept); swapped for a hand-rolled row list (`TemplateGroupCard` used as a plain content shell instead) since order carries real meaning here, unlike every other array `HookTemplateEditor` normally edits (random-pick phrase pools). Shipped in two steps: (1) ↑/↓ buttons — `PhraseRow.jsx` gained optional `onMoveUp`/`onMoveDown`/`disabledMoveUp`/`disabledMoveDown` props, rendering a `MoveControls` pair in front of the field only when passed, so existing callers (Hook Blocks, Tag phrase editors) are unaffected. (2) User pushed back that buttons alone don't scale past a handful of items ("clicking 15 times to move the bottom one to the top?") — added `@dnd-kit` drag-and-drop on top, reusing the exact pattern `SortableActiveBlock.jsx` already established for Active Layout/Block Group reordering: a new `SortableStatusRow` wrapper (drag handle + the same `PhraseRow`), with the buttons kept as the precise/keyboard-accessible fallback rather than removed. `id` is `` `${status}::${index}` `` (value+index combined), not the bare status string — needed to stay unique even with two blank rows mid-add (e.g. "+ Add" clicked twice before either is typed into); safe despite not being a stable per-item identity across renders, since dnd-kit only needs the id to hold for the duration of one drag gesture (state doesn't change until drop, at which point everything remounts fresh). One small CSS addition: `.tag-phrase-row` added to the existing `.desc-block-wrapper > .tag-card, .desc-block-group` flex-fill rule. Verified live via Playwright: buttons — moved "Needs Re-record" above "Wishlist" with one ↓ click, confirmed the Todo page's section order changed to match, restored; drag — dragged "Needs Video" from the bottom straight to the top in one continuous gesture (real mouse move/down/up events, since dnd-kit's `PointerSensor` requires actual pointer movement to activate, not a single programmatic jump), confirmed Todo page order updated to match, restored; 0 console errors throughout both passes.

**Scroll-to-top on page switch (2026-07-30).** Small UX fix: navigating between pages left scroll position wherever it was on the previous page. Added `window.scrollTo(0, 0)` to the three places that represent a plain "switch to a different page with no specific target" action: `AppHeader.jsx`'s main nav tab buttons, `App.jsx`'s `loadEntryAndReturnToGenerator` (loading a saved entry from Tag Library/Shorts Queue/Todo jumps to Generator), and the "Open UIKit" button (Project Settings → General → Actions). Deliberately NOT added as a blanket `activePage`-change effect — this app has an extensive click-to-navigate system that scrolls to and highlights a *specific* element (a title, thumbnail, hook, tag, etc.), and a generic top-level scroll reset would have raced with and likely broken that. Scoped to exactly the three call sites that don't carry a target. Verified via live Playwright: two nav-tab switches both confirmed `scrollY` resets to 0; confirmed the click-to-navigate/highlight system is unaffected — a Ctrl+Click tag jump still lands on and scrolls to its specific highlighted element (non-zero `scrollY`), not overridden by the new top-reset code; 0 console errors.

**`hooks/useAppShellState.js` split (2026-07-30): 312 → 183 lines — strongest reduction in the whole refactor pass, eleventh file down.** Found a genuinely clean, large seam unlike the partial wins on `App.jsx`/`BlockGroups.jsx`: the 7 click-to-navigate "target" states (`tagLibrarySearchTarget`, `shortHooksTarget`, `titlesTarget`, `thumbnailsTarget`, `hashtagsTarget`, `blocksTarget`, `songOverrideTarget`) plus `activeProjectSettingsSection` (state + its persistence effect) plus every `open*`/`clear*` handler form one fully self-contained concern — "click-to-navigate target management" — touching only `setActivePage`/`setPanelVisibility`, both already available. Extracted verbatim to new `src/hooks/useNavigationTargets.js` (147 lines), taking those two setters as parameters and returning an object `useAppShellState.js` now spreads (`...navigationTargets`) into its own return — so the hook's *public* return shape is byte-identical, and `App.jsx` (the only consumer) needed zero changes. **One required, deliberate fix during the move, not just a pure copy**: `openSongOverride`'s `useCallback` had an empty `[]` dependency array in the original — safe there only because `setPanelVisibility` was a `useState` setter declared in the *same* component, which React can locally verify is stable. Once it became a parameter to a different file, React's exhaustive-deps lint correctly could no longer verify that locally and flagged a real compiler error ("Existing memoization could not be preserved"). Fixed by adding `setPanelVisibility` to the dependency array — behaviorally identical at runtime (the setter really is stable, since the caller passes its own real `useState` setter), but flagged here since it's the one place this "pure move" wasn't purely mechanical. **Verified via a broad live Playwright pass**, given this hook feeds nearly all cross-page state: all 6 pages load clean; every one of the 7 navigation targets exercised individually with real DOM/state assertions (not just page-load checks) — Tag Library (Ctrl+Click tag), Titles (both panel-nav and sourced-title click, exact template highlighted), Thumbnails, Hashtags, Short Hooks, Blocks (Groups → Broadcast), and the same-page Song Override target (Advanced Options re-expanded, field highlighted, highlight cleared on edit) — all landed correctly; `activeProjectSettingsSection` persistence confirmed surviving a full page reload; stale-target-clearing (a separate hook, `useStaleTargetClearing.js`) confirmed still correctly clearing a highlight after navigating away and back; 0 console errors or warnings across every step, including the specific dependency-array fix path. See Next Recommended Tasks.

**`App.jsx` split (2026-07-30): 351 → 300 lines — root component, tenth file down in the refactor pass.** Investigated `engine/placeholders.js` first (the user's alternative candidate) and concluded it resists a safe pure-move split: its token resolvers (`resolveLogNoteToken`/`resolveOperatorStatusToken`/`resolveTransformationToken`) call `fillPlaceholders`, but `fillPlaceholders` resolves tokens via the `REGISTRY` object that contains those same resolvers — a genuine mutual recursion at the file's center, not incidental organization. Splitting it means either a circular import (which this codebase already goes out of its way to avoid elsewhere — see `resolveOverrideText`'s duplication and `shortHookEligibility.js`'s dependency-free-leaf-module precedent) or dependency-injecting `fillPlaceholders` as a parameter (the `pooling.js` pattern), which changes signatures throughout — a real refactor, not a pure move, disproportionate to a file only 139 lines over. Recommended leaving it alone like `hookBlockOverrides.js`; user chose to try `App.jsx`/`useAppShellState.js` instead. `App.jsx` had two genuinely safe seams: two self-contained `useEffect` blocks that only touch already-passed-in state/setters, no cross-file coupling — extracted verbatim to new `src/hooks/useStaleTargetClearing.js` (46 lines, clears click-to-navigate highlight state when `activePage` leaves a target's home page) and `src/hooks/useBlockCollisionRepair.js` (33 lines, the self-healing customBlocks/hookBlocks key-collision repair). Also deduped a small thing while there: `(entry) => { handleLoadEntry(entry); setActivePage('generator'); }` was identically duplicated 3 times (passed to `TagLibraryPage`/`ShortsQueuePage`/`TodoPage`) — now one `loadEntryAndReturnToGenerator` defined once. **Honest about the outcome, discussed with the user upfront via `AskUserQuestion`**: this only gets `App.jsx` to 300 lines, not under 250 — the remaining bulk is the six-page routing JSX with heavy prop-threading, core orchestration inherent to being the root component (same situation as `UIKitPage.jsx`/`BlockGroups.jsx`), not something to force into an abstraction the codebase doesn't otherwise use. Separately noted, not touched: `App.jsx` still has the DEV-only `window.*StorageMigration` exposure CLAUDE.md's PRIORITY TODO already flags obsolete — left alone since that's explicitly filed under "storage cleanup, after a few stable sessions," a deferral already made deliberately, not something to override mid-refactor. **Verified via a broad live Playwright pass** (appropriate given this is the root component — every page routes through it): all 6 destinations (Generator/Tag Library/Shorts Queue/Todo/Project Settings/UIKit) load with real content; the `loadEntryAndReturnToGenerator` dedup confirmed working identically at all 3 call sites with a real saved entry; stale-target-clearing confirmed end-to-end (clicked Thumbnail → landed highlighted on Project Settings → Thumbnails; navigated away via main nav, back manually → highlight correctly gone); block-collision-repair confirmed silent (no spurious `console.warn`) across the whole session, as expected with no real collision in current data; Regenerate and project switching (Illegal Mind Covers ↔ Maxx Dee Covers) both confirmed working; 0 console errors or warnings throughout. See Next Recommended Tasks.

**`engine/titles/generateTitles.js` split (2026-07-30): 305 → 196 lines.** Second engine-file split in the pass, applying the same approach as `generateCustomBlocks.js` (see below). Initially looked like it had no clean seam (all its helpers are tightly wound around one shared function), but re-reading found one: `resolveRecordText` — used by *both* `pickTitles` (during the pick loop, for text/dedup) and `renderTitles` (the live-render phase) — plus its four support helpers (`buildGeneratedArtistShort`, `capitalizeFirst`, `resolveWrapping`, `resolveArtistSong`) form one coherent "resolve a picked record to display text" concern, distinct from the five helpers used only inside `pickTitles` for building candidate pools (`pickTransformation`, `buildTransformationVariations`, `shuffleArray`, `getTitleTemplateGroups`, `getWeightedTemplates`). Pure move, zero logic changes: the five extracted to new `src/engine/titles/resolveTitleRecord.js` (110 lines), imported back into `generateTitles.js` by both `pickTitles` and `renderTitles` — same cross-import shape as `resolveHookOverride`/`renderTextTemplate` in the prior split. Only one importer of `generateTitles.js` exists (`useGeneratedOutput.js`, using `pickTitles`/`renderTitles`), confirmed via grep before starting. Proposed via `AskUserQuestion` before touching code, same as the prior engine split. **Verified with the same seeded A/B rigor**: a Node harness (bundled with `rolldown`) checked out the pre-refactor commit into a `git worktree`, monkey-patched `Math.random` to a seeded PRNG, and ran `pickTitles`/`renderTitles` against both projects' real config across 6 scenario groups each (no tags / 1 tag / 2 tags / Shorts mode / hook-mixing with `useHooksForLongTitles` / a "pick once, render twice against different formData" live-substitution case) — **old and new code produced SHA256-identical output across all 12 runs**, with the hook-mixing scenario confirmed to actually exercise the `kind: 'hook'` branch (not just present-but-unused). Live Playwright confirmed the exact behavior most at risk from a bad move: typing into Artist then Song kept the same 5 title slots in the same order, only the substituted text changing — no reshuffle — plus Regenerate producing a genuinely different set, and Shorts mode rendering correctly; 0 console errors. Ninth file down in the large-file refactor pass. See Next Recommended Tasks.

**`engine/descriptions/generateCustomBlocks.js` split (2026-07-30): 325 → 201 lines.** First engine-file split in the pass, and the highest-stakes file touched so far — this is the "pick/render freeze" machinery CLAUDE.md documents at length as fragile (randomized picks frozen at pick-time, pure substitution at render-time, so live form edits don't re-roll). Pure move, zero logic changes: everything about hook-block-pool output + layout-key resolution + Block Groups (`pickHookBlockOutput`/`renderHookBlockOutput`/`resolveHookBlockOutput`, `pickLayoutHookBlocks`/`renderLayoutKey`/`resolveLayoutKey`, `renderBlockGroups`/`generateBlockGroups`) moved to new `src/engine/descriptions/resolveLayoutBlocks.js` (126 lines), which imports `resolveHookOverride`/`renderTextTemplate` back from `generateCustomBlocks.js` (the one cross-dependency; everything else about List/Text customBlocks + song-override resolution stayed put). Only two importers existed (`generateDescriptions.js`, `generateShortDescriptions.js`), both updated to pull from both files now. Confirmed via `AskUserQuestion` before touching code, given the risk tier. Verified with real rigor, not just lint/build: a Node A/B harness (bundled with `rolldown`) checked out the pre-refactor commit into a `git worktree`, monkey-patched `Math.random` to a seeded PRNG (same seed for old and new runs, for a true deterministic comparison rather than statistical), and ran `generateDescriptions`/`generateShortDescriptions` against both projects' real config across 7 scenarios each (no tags / 1 tag / 2 tags / song-scoped override / Shorts mode, etc.) — **old and new code produced byte-identical, SHA256-matched output across all 14 runs**, including Illegal Mind Covers' three Block Group segments (`broadcastBlock`/`logBlock`/`closingBlock` — the exact code that moved). Live Playwright on the running app confirmed Long/Shorts descriptions render correctly, Tag-toggle+Regenerate still works, a Story Block override still live-substitutes and reverts to a byte-identical frozen pick on clear, and Shorts mode renders correctly — 0 console errors. Also flagged, not fixed (out of scope): while reading `generateTitles.js` to compare candidates, spotted that `pickTitles` destructures a `hasEmpty` field from `resolveRecordText`'s return value that the function never actually returns — the empty-candidate filtering this drives may be dead code; worth a look separately. Eighth file down in the large-file refactor pass. See Next Recommended Tasks.

**`components/input/AdvancedDescriptionFields.jsx` split (2026-07-30): 265 → 57 lines.** Pure move, zero logic changes — two fully self-contained sub-components living in the same file were pulled out verbatim: `SongListBlockEditor` (the mini item-editor used inside a List-type song block override) → new `src/components/input/SongListBlockEditor.jsx` (52 lines); `SongBlockOverrideFields` (renders a per-song override field — text or list — for every song-scoped Text/List/Hook Block, plus the `songOverrideTarget` click-to-scroll effect) → new `src/components/input/SongBlockOverrideFields.jsx` (167 lines). The main file now only renders the "Additional Hashtags" input and `<SongBlockOverrideFields>`. Seventh file done in the large-file refactor pass — the last one in the "settings/form component" risk tier; remaining candidates are all either high-risk core files (`App.jsx`, `useAppShellState.js`) or engine files flagged for caution. Verified via live Playwright against the real Generator Advanced Options panel, since this spans the cross-file `songOverrideTarget` click-to-scroll wiring that could break silently on a dropped prop: Additional Hashtags input propagates live to the Hashtags output panel; a Story Block text override propagates live to the Descriptions panel with no save/regenerate needed; a temporary List-type song block (created in an isolated Playwright browser profile, not the real project data) correctly rendered via `SongListBlockEditor` with working add/remove; Reset (↺) correctly reverted an override and the Descriptions panel stopped showing it; clicking a song-overridden description line in the Descriptions panel correctly scrolled to and highlighted the matching Advanced Options field, confirming the cross-file wiring survived the extraction intact; 0 console errors throughout. See Next Recommended Tasks.

**`projectSettings/blocks/BlockGroups.jsx` split (2026-07-30): 261 → 234 lines.** Same pattern as the `HookBlocks.jsx`/`Placeholders.jsx` splits — the four patch-builder functions (`patchGroup`, `resetGroup`, `deleteGroup`, `addGroup`) that built override patch objects inline and called `updateProjectOverride` directly were replaced with calls to new pure functions in `src/utils/blockGroupOverrides.js` (59 lines: `patchGroupPatch`/`resetGroupPatch`/`deleteGroupPatch`/`addGroupPatch`/`isDynamicGroup`) that return the patch object — the component still calls `updateProjectOverride` and keeps the `window.confirm`/`isCore` delete guard itself. `AddChildList` (the per-group "add child" search list) stayed inline, matching the precedent of keeping small tab-specific sub-components (e.g. `AddPlaceholderRow`) local rather than extracting every small piece. Sixth file done in the large-file refactor pass — now under the 250-line threshold, though the reduction was more modest than prior files since most of this file's bulk is the drag-and-drop wiring and JSX, not duplicated patch logic. Verified via live Playwright against Illegal Mind Covers' real `blockGroups` config (31 assertions, 0 console errors): existing group cards render correctly; Scope/Target dropdown changes and renames landed correctly in `localStorage`'s `blockGroupOverrides` and persisted across reload; child reorder (move buttons), remove, and re-add via the Add-child list all landed correctly in the `children` array; Reset (↺) removed the entire override cleanly, correctly reverting the UI to defaults; a new test group's add/delete round-tripped through `customBlockGroups` with the exact expected shape; real project data confirmed left in its original state after testing. See Next Recommended Tasks.

**`projectSettings/blocks/Placeholders.jsx` split (2026-07-29): 274 → 164 lines.** Two seams: (1) `PlaceholderReference` (the "All Placeholders" reference card) was already a self-contained props-in/JSX-out component — extracted verbatim to new `blocks/PlaceholderReference.jsx` (60 lines), no logic changes. (2) The patch-builder functions (`patchPlaceholder`, `resetPlaceholder`, `deletePlaceholder`, `addPlaceholder`) plus pure helpers (`sourceToFieldValue`, `fieldValueToSource`, `generatePlaceholderKey`) — extracted to new `src/utils/placeholderOverrides.js` (88 lines), now returning patch objects instead of calling `updateProjectOverride` directly, matching `hookBlockOverrides.js`'s established convention (setters return patches; the component still calls `updateProjectOverride` and keeps the `window.confirm`/`isCore` guards itself, matching how `HookBlocks.jsx` does it). Fifth file done in the large-file refactor pass. Verified via live Playwright, since this changed the patch calling-convention (return-patch vs call-directly) and real project override data was at stake: reference card renders/expands/copies correctly; Count edit and Short Hooks Pool toggle both landed correctly in `localStorage.illegalMindGeneratorData.projectOverrides.<project>.description.placeholderOverrides` and persisted across a real page reload; Reset (↺) removed only the targeted key, leaving an unrelated override untouched (confirms scoped removal, not a blanket wipe); Add produced the exact documented `customPlaceholders` entry shape; Delete cleanly removed it; 0 console errors. See Next Recommended Tasks.

**`savedLibrary/SavedLibrary.jsx` split (2026-07-29): 337 → 161 lines.** Two real seams: (1) filter/sort state (search, sortBySignal, hideQueueHidden + its persistence, missingDataOnly) and the derived `filteredEntries`/`missingEntries`/`hasMissingData` — pure logic, no JSX — extracted to new `src/hooks/useSavedLibraryFilters.js` (107 lines). (2) The "Missing Data AI round-trip" feature (Copy AI Prompt / Paste AI Response, all its own local state) — fully self-contained — extracted to new `src/components/savedLibrary/MissingDataTools.jsx` (124 lines). One deliberate small layout change: the two AI-round-trip buttons used to render inline in the same flex row as the Sort/Hide/Missing-data checkboxes (`.filter-row`); they now render in their own `.button-row` directly below (reusing the existing class, no new CSS) — a cleaner grouping, confirmed live to look correct, not broken. Fourth file done in the large-file refactor pass. Verified via live Playwright: toggle/search/sort/count all correct; Hide Queue-Hidden filters and persists across a real page refresh (confirmed via raw `localStorage` read, not just UI); Missing Year/Genre filter and the new button-row layout render correctly; Copy AI Prompt confirmed via clipboard read; Paste AI Response confirmed via a real `localStorage` write to the target entry's `originalYear`/`originalGenre` (not just UI text); Export/Import buttons still present; 0 console errors. See Next Recommended Tasks.

**`useSavedEntries.js` split (2026-07-28): 400 → 208 lines.** New `src/utils/savedEntries.js` (220 lines): the already-standalone pure module functions moved verbatim, plus newly-extracted `buildEntryFromFormData`/`buildFormDataPatchFromEntry` (save/load field lists) and `loadInitialSavedEntriesByProject` (legacy-migration init logic). Verified via live Playwright against real `illegalMindGeneratorData`: save/load/delete/import-merge all correct, 0 console errors. Third file done in the large-file refactor pass — see Next Recommended Tasks.

**Large-file refactor pass started, two files down (2026-07-28).** User asked to begin splitting files over 250 lines app-wide. `blocks/HookBlocks.jsx` (497 → 125 lines): extracted the presentational `HookBlockEditor.jsx` and the ~20 pure getter/patch-builder functions into `src/utils/hookBlockOverrides.js` (331 lines, deliberately left long — a flat list of small independent functions, not tangled). `descriptions/LongDescriptionSettings.jsx`/`ShortsDescriptionSettings.jsx` (272/273 → 132/138): turned out to be near-duplicates, not just long — extracted the shared DnD/reorder logic into `useDescriptionLayoutActions.js` and the shared board JSX into `DescriptionLayoutBoard.jsx`, closing a sync-drift risk CLAUDE.md had already flagged. Remaining candidates tracked in Next Recommended Tasks below. See Recently Completed for full detail.

**`src/components/projectSettings/` reorganized into topic folders (2026-07-28).** 16 flat `ProjectSettings*.jsx` files sat alongside 5 existing topic subfolders (`blocks/`, `descriptions/`, `hooks/`, `lists/`, `titles/`) — reorganized so a tab's controller file lives in the same folder as the widgets it renders, generalizing the pattern `descriptions/` already partially had. 9 controllers moved into their matching folders; `hooks/` renamed to `shortHooks/` (was colliding in spirit with the real `src/hooks/`); the redundant `ProjectSettings` prefix dropped from filenames now namespaced by folder, kept on the 7 files still flat at the root. Pure structural refactor, zero behavior change. See Recently Completed for full detail.

**Blocks → Placeholders tab: "All Placeholders" reference list + narrower "Copy AI Prompt" button (2026-07-28).** The tab only ever listed the project's `{custom.*}` definitions — every other placeholder (`{artist}`, `{tags.*}`, `{links.*}`, `{num}`, etc.) was only discoverable via a `{`-autocomplete dropdown elsewhere. New `src/utils/placeholderReference.js` (`buildPlaceholderReference`/`buildPlaceholderPrompt`) plus a new collapsed-by-default `PlaceholderReference` card in `ProjectSettingsPlaceholders.jsx` lists all four groups (built-in, tag categories, this project's links, this project's custom placeholders) with short descriptions. The Copy button deliberately exports only built-in + tag-category tokens, not links/custom, per user feedback after the first pass copied everything. See Recently Completed for full detail.

**Shorts Description "Hook" line: title-suffix leak fixed, made a genuinely regular Hook Block via a new Custom Placeholder source (2026-07-25).** Two bugs reported together: generated Hook lines carried a Titles-only suffix (e.g. "... // [SIGNAL 07]"), and clicking "Hook →" in Active Layout dumped you on the whole top-level Short Hooks section instead of a scoped editor, because "Hook" was never a real `hookBlocks` entry — it read the shared `shortHookTypes` pool via a bespoke one-off branch in `generateShortDescriptions.js`. First pass gave "Hook" its own bespoke `hookBlocks` path (`path: 'shortHookPool'`) with a special no-template-editor UI — user saw it live and proposed a cleaner design instead: expose the pool as a `{placeholder}` token (reusing the already-shipped Custom Placeholders system) and make "Hook" a completely ordinary Hook Block whose default template is just that placeholder, so it's editable/addable like any other block. Implemented as the final design: `placeholders.js`'s `buildPlaceholderPool` gained a third source kind (`source.shortHookPool: true`, alongside the existing `tagField`/`hookBlockKey`) that pools the entire eligible `shortHookTypes` set via `getEligibleShortHookTypeEntries` (`src/engine/hooks/shortHookEligibility.js`, a dependency-free leaf module — needed to dodge a real import cycle: `hookBlockLookup.js` ← `placeholders.js` ← `generateShortHooks.js`). New `shortHooksPool` placeholder definition; `shortsHookLine` is now a totally standard `hookBlocks` entry (`path: 'shorts'`, `templateKey: 'hookLine'`) whose default template is literally `"{custom.shortHooksPool}"`. `hookBlockLookup.js`/`generateCustomBlocks.js`/`ProjectSettingsHookBlocks.jsx` needed **zero special-casing** and are back to their exact pre-session shape — the whole point of routing through the placeholder system. Deleted the special-cased `blockName === 'hook'` branch in `generateShortDescriptions.js`; the key now resolves through the same generic pipeline every other Hook Block uses, which structurally can't apply title-style wrapping — fixing both bugs with one change. `ProjectSettingsPlaceholders.jsx` gained a "Short Hooks pool" toggle. See Current Focus in `CLAUDE.md` for full detail (file paths, exact bug mechanics, verification).

**Per-tag "Copy from Project" action shipped (2026-07-25).** User asked for a per-tag dropdown + button (Tag Library → a tag's Basics tab) that pulls just *that* tag's data in from another project — distinct from the existing bulk "Sync Tags" control (copies every tag) and from "Duplicate tag" (creates a differently-named, empty-content copy). Clarified via `AskUserQuestion` that "copy and add, not override" meant additively merging (Set-union) the source project's title/thumbnail/hashtags/description/shortHooks into the *same* tag — reusing `syncProjectTags`'s exact `mergeTagData`/`mergeDescription`/`mergeShortHooks` machinery, scoped to one tag name instead of every override. New `copyTagFromProject(...)` in `useTagOverrides.js`; new dropdown+button in `TagBasicsTab.jsx` (reuses `FormSelect` and the existing `.tag-actions` CSS class — no new CSS), threaded through `TagLibraryPage.jsx` → `TagCard.jsx` → `TagEditor.jsx`. User renamed the button label from "Copy from Project" to "Sync" mid-review. Verified live via a throwaway Playwright script (scratchpad-only install): added a real hashtag to "Heavier" on Maxx Dee Covers, switched to Illegal Mind Covers, clicked the per-tag button, confirmed the hashtag plus shortHooks/title/thumbnail/description merged into Illegal Mind Covers' stored override and rendered live in its Hashtags tab; re-clicked to confirm dedup/no-op; confirmed bulk "Sync Tags" unaffected; 0 console errors. **Found, not fixed (pre-existing, out of scope):** `TagEditor.jsx`'s `activeTab` local state isn't reset when switching projects — since React reuses the same component instance for same-named tags across projects (`key={tag.name}`), whichever tab was last active (e.g. Hashtags) can carry over when the same tag is viewed under a different project. See Recently Completed for full detail.

**Fast tag creation via AI round-trip, "Add Tag" panel (2026-07-13).** User asked for a faster way to add a new transformation tag than the old `window.prompt()`-then-fill-5-tabs-by-hand flow. Modeled directly on the Saved Library "Copy AI Prompt"/"Paste AI Response" precedent — new `src/utils/tagPrompt.js` (`buildTagPrompt`/`parseTagResponse`, plain-text section-header format, one `SHORTHOOKS_<KEY>:` section per the project's real `shortHookTypes`) plus new `AddTagPanel.jsx` replacing `TagLibraryPage.jsx`'s old `handleCreateTag` prompt dialog. Data model needed zero changes — a new tag is just a `tagOverrides` entry, same as today. Leaving the paste box empty still creates a blank tag, no regression. Verified: 28-assertion Node smoke test, live Playwright (inline panel replaces the native dialog, a pasted fixture populates all 5 Tag Editor tabs correctly, the new tag is selectable in the Generator, quick no-AI creation still works, 0 console errors). **Same-day follow-up**: a real AI reply for "Melodic Hardcore" came back almost entirely genre-agnostic filler with placeholders barely used — tightened `buildTagPrompt` with an explicit anti-genericness instruction (naming the actual failure-mode phrases as bad examples), a hard "at least 4 of 6 phrases must include `{artist}`/`{song}`" requirement for Short Hooks, and per-hook-type placeholder examples now pulled live from that hook type's own config templates instead of a generic hint. **Second follow-up**: that blanket `{artist}`/`{song}` rule was itself too rigid — it fought the `progress` hook type's real angle (musician's own growth, e.g. `{years}`, not the covered song), so the AI just dropped placeholders for that section instead of reconciling the conflict. Fixed by showing up to 2 real example templates per section and letting each section's own examples dictate the placeholder pattern rather than forcing one universal pair. Also tightened `AddTagPanel.jsx`'s layout (Name/Category now share a `.form-row`, matching `BasicSongFields.jsx`'s existing side-by-side pattern) per user feedback — a first pass at spacing the Copy Prompt button shrank the gap above it, which the user immediately flagged as too cramped below it instead; fixed with `.add-tag-copy-row { margin-bottom: var(--space-6) }` to restore the panel's normal spacing rhythm on both sides of the button. See Recently Completed for full detail.

**Todo Settings tab built + new Shorts Queue settings tab (2026-07-13).** User asked to expose whatever's hardcoded around Todo and "the amount of cover in between similar songs" (Shorts Queue's duplicate-spacing logic), trusting scope to be decided — clarified via two questions first (keep the spacing rule's meaning as exact-song not same-artist; give Shorts Queue its own tab rather than folding into Todo Settings). **Todo Settings**: new `ProjectSettingsTodo.jsx`, one list editor for `todoStatuses`. Found and fixed a real bug while scoping: `TodoBulkAdd.jsx` hardcoded `status: 'Wishlist'` — renaming the first status would've silently broken bulk-add; now derives from `todoStatuses[0]`. Accepted, documented limitation: renaming/removing a status has no data migration for existing saved entries. **Shorts Queue settings**: a completely uncharted area (zero config, zero UI before this) — `useShortsQueue.js`'s hardcoded `QUEUE_LENGTH = 20` and spacing-window `.slice(-2)` are now config-driven (`projects.json`'s new `shortsQueue: {length, duplicateSpacing}`, defaults matching prior behavior exactly), new `ProjectSettingsShortsQueue.jsx` tab. Side benefit: `ShortsQueuePage.jsx` now actually uses its previously-unused `projectConfig` prop, resolving a pre-existing lint error. Verified via Node smoke test (spacing/length constraints hold at default and custom configs) plus live Playwright (13 assertions, 0 console errors). See Recently Completed for full detail.

**Artist/Song hashtag click-to-navigate + fixed stuck navigation-target highlights (2026-07-13).** Two follow-ups from the per-item hashtag nav work (below). (1) `#TestArtist`/`#TestSong`-style chips are now clickable — `generateHashtags.js` tags them `{type:'field', field:'artist'|'song'}`, `HashtagLink.jsx` reuses `onOpenSongOverride({blockKey: field})` verbatim (same mechanism Additional Hashtags already uses), `BasicSongFields.jsx` gained the matching id/highlight/scroll-effect/clear-on-edit treatment. (2) Fixed a real bug: every click-to-navigate highlight (block/title/thumbnail/hashtag/tag-library links) stayed stuck indefinitely once set, even after navigating to a totally different page, until a refresh — none of the `*Target` states were ever cleared by leaving their page, only by manual in-page interaction. New single `useEffect` in `App.jsx` clears each target once `activePage` no longer matches its home page; safe because every target-setting function already navigates to that page in the same handler, so a freshly-set target's page always matches and survives. Verified live via Playwright, 25 assertions, 0 console errors. See Recently Completed for full detail.

**Per-item click-to-navigate + hover border for Hashtags & YouTube Tags output (2026-07-13).** Follow-up to the Hashtags & YouTube Tags Project Settings tab (below) — user wanted the same click-to-navigate/hover-border treatment Descriptions and Thumbnails already have, applied per-hashtag/per-tag rather than just at the panel-title level. `generateHashtags.js` rewritten to return `{hashtags: Entry[], youtubeTags: Entry[]}` (`Entry = {text, source}`) instead of joined strings, verified behaviorally identical to the old dedup/cap logic via a 15-assertion Node smoke test. New shared `HashtagLink.jsx` renders each entry as a bordered `.hashtag-chip-link` (clickable) or plain text (no source at the time — artist/song-derived tags, made clickable in a same-day follow-up, see above). Three nav targets: tag-sourced (reused `onOpenSourceTag`, fixed a real gap where `TagEditor.jsx` never routed to the Hashtags tab), base-pool (new `hashtagsTarget`/`openHashtagsSearch` mirroring `thumbnailsTarget` exactly), and per-song custom hashtags (reused `openSongOverride` verbatim, just needed a DOM id + small scroll effect). Verified live via Playwright, 23 assertions, 0 console errors. See Recently Completed for full detail.

**Hashtags & YouTube Tags Project Settings tab built (2026-07-13).** The nav tab already existed but fell through to "Coming Soon" — `generateHashtags.js` read `config.hashtags`/`config.youtubetags` as hardcoded bare arrays with two literal caps (`MAX_HASHTAGS = 18`, YouTube tags `.slice(0, 20)`), no override mechanism at all. Reshaped both `projects.json` root keys to `{ base: [...], maxCount: N }` (same shape `thumbnail` uses; defaults preserve old behavior exactly). New `ProjectSettingsHashtags.jsx`, modeled directly on `ProjectSettingsThumbnails.jsx` — Generation card (two sliders + resets), Base Hashtags/Base YouTube Tags list editors. `buildResolvedProjectConfig.js` gained the matching nested-merge block. Per-tag hashtags (Tag Library) untouched — separate key, already wired. Follow-up: added panel-level click-to-navigate on the Generator's "HASHTAGS"/"YOUTUBE TAGS" output panel titles, reusing the exact same `onNavigate`/`CollapsiblePanel` mechanism Titles/Thumbnails/Descriptions already use — zero new state. Also fixed `TemplateGroupCard`'s `subtitle` styling (was reusing `.tag-category`, a class meant for short high-contrast labels, making long helper sentences read too loud) — new dedicated `.tag-card-subtitle` class, fixes every card's subtitle app-wide including Thumbnails' pre-existing ones. See Recently Completed for full detail.

**Transformation Tags: Ctrl+Click to open in Tag Library + hover tooltip (2026-07-13).** Ctrl/Cmd+Click a tag chip in the Generator's tag picker to jump to it in the Tag Library, reusing the existing `openTagLibrarySearch` navigation (same mechanism Titles/Hooks/Thumbnails already use). Threaded `onOpenSourceTag` through `App.jsx` → `GeneratorPage.jsx` → `InputForm.jsx` → `TransformationTagSelector.jsx`. Every chip also gained a hover tooltip for discoverability.

**Drag-and-drop reordering for Active Layout + Block Group children (2026-07-13).** First runtime dependency added beyond React — `@dnd-kit/core` + `@dnd-kit/sortable` + `@dnd-kit/utilities`, user's explicit choice over hand-rolled native HTML5 drag events, for touch/keyboard support. New shared `SortableActiveBlock.jsx` (drag handle + existing `MoveControls` + `BlockInfoCard`) is reused verbatim across `LongDescriptionSettings.jsx`, `ShortsDescriptionSettings.jsx`, and `ProjectSettingsBlockGroups.jsx`'s per-group children — both reorder mechanisms (drag and the ↑/↓ buttons) coexist everywhere. See Recently Completed for full detail.

**Saved Library: AI round-trip for missing Year/Genre data (2026-07-12).** Follow-up to "Copy AI Prompt" (below) — user asked if pasting an AI's reply back could auto-update the matching songs, and supplied a real 10-song response to test against. New `parseMissingDataResponse` (`src/utils/searchQuery.js`) parses `Artist - Song: Year, Genre1, Genre2` lines and matches each to a saved entry (case/whitespace/curly-quote tolerant), returning only `{matches, unmatched}` — the non-destructive "only fill empty fields" policy lives in `SavedLibrary.jsx`'s new `handleApplyPastedResponse`, not the parser. New "Paste AI Response" button + textarea, gated the same way as "Copy AI Prompt". Required threading a new `onUpdateEntry` prop through `App.jsx` → `GeneratorPage.jsx` → `SavedLibrary.jsx` (reusing the pre-existing `handleUpdateEntry` from `useSavedEntries.js`, previously only wired to Todo). Verified via direct Node smoke tests against the user's real pasted example (all 10 matched) and a non-destructive-fill case (pre-filled year preserved, only empty genre filled). See Recently Completed for full detail.

**Saved-entry data loss on Import — full fix, plus Saved/Tag Library and Generator polish (2026-07-12).** Round 1: user reported `originalYear`/`originalGenre` disappearing from saved songs — traced to `handleImportEntries` (Saved Library's JSON "Import Library" button) missing those fields (plus `artistShort`/`useCustomArtistShort`) from its own independent field list. Fixed, plus shipped double-click-to-copy a Google search string on empty Year/Genre fields in the Generator form, and red missing-data badges + a "Missing Year/Genre" filter checkbox in the Saved Library. Round 2 (same day, after real cross-device use): the actual root cause was deeper — Import replaced a matching existing entry *wholesale*, so an older/thinner export from another device silently wiped any field (notably Todo status) that was empty in the import but populated locally, even after the field-list fix. Fixed via a proper non-destructive field-by-field merge. Also shipped: Tag Library 3-column layout, a shown/total count in the Saved Library header, and Original Genre separator normalization (`/` and `and` become a comma on blur). See Recently Completed for full detail.

**Tag Editor: config-driven phrase tabs + override-merge centralization ("Phase 5" of the Custom Placeholders arc), shipped 2026-07-12.** Found 4-5 distinct tag-override-merge implementations, not the 2 the deferred rationale assumed — one (`mergeTagData`, used by "Sync Tags") had a genuine currently-shipping bug, silently dropping flat scalar fields (`label`/`category`/`visible`) when syncing created a brand-new tag. Fixed that, centralized the duplicate override-resolve merge (`buildTagExplorerData.js`/`buildResolvedProjectConfig.js`) into one shared `resolveTagOverride`, and extracted the 4 phrase-based Tag Editor tabs into shared config + one generic component, deleting 4 near-duplicate files. See Recently Completed for full detail.

**Broadcast Block and Log Notes are now real Block Groups with zero hand-written joins (2026-07-12).** Custom Placeholders Phase 4 shipped first (registry-ified `{fileId}`/`{operatorStatus}`, a `tagStatusLine` placeholder, `broadcastBlock` composed via a hand-written join in `generateDescriptions.js`) — then superseded same day by a better design the user found: a Group doesn't need a new child type to hold a Custom Placeholder, a trivial single-template Hook Block whose only content is `{custom.tagLogLine}`/`{custom.tagStatusLine}` works as an ordinary Group child. Both blocks are now produced entirely by the existing Group mechanism (`generateBlockGroups`, same as `closingBlock`), with a small generic addition (`groupCtxOverrides`) to preserve the `{tagLine}` correctness fix from Phase 3. Also added: click-to-navigate on Group children (was already supported by `BlockInfoCard`, just never wired), and 2 more wrapper-text variations for Illegal Mind's "Log · Format" pool. See Recently Completed for full detail.

**Custom Placeholders — Phases 0-3 shipped 2026-07-12.** User asked to (re)create a "Log Notes/Format Group" — investigation found Block Group's own Phase 2 plan (Generated child type) would have hit a real duplication bug (see the superseded plan note below), so a different, more general mechanism was designed instead: user-creatable `{custom.<key>}` placeholders, backed by a tag-scoped field pool and/or an existing hook block's pool. **This formally supersedes Block Group's "Phase 2 (Generated child type, migrate Log Notes)" and "Phase 3 (Broadcast Block)"** — see `C:\Users\Max\.claude\plans\you-write-too-much-spicy-crayon.md`, now annotated as superseded for those two phases.

---

## Recently Completed

- **Large-file refactor pass, eleven files down (2026-07-28 to 2026-07-30).** User asked to begin splitting files over 250 lines app-wide. Given the choice between the worst offender by line count (`pages/UIKitPage.jsx`, 748 — a component catalog, length inherent to its purpose) and a genuinely splittable file, started with the latter.
  - **`hooks/useAppShellState.js`: 312 → 183 lines (2026-07-30) — strongest reduction in the whole pass.** Unlike the partial wins on `App.jsx`/`BlockGroups.jsx`, this had one large, fully self-contained seam: the 7 click-to-navigate target states (`tagLibrarySearchTarget`, `shortHooksTarget`, `titlesTarget`, `thumbnailsTarget`, `hashtagsTarget`, `blocksTarget`, `songOverrideTarget`) plus `activeProjectSettingsSection` (state + its own persistence effect) plus every `open*`/`clear*` handler — one coherent "click-to-navigate target management" concern, touching only `setActivePage`/`setPanelVisibility`, both already available. Extracted verbatim to new `src/hooks/useNavigationTargets.js` (147 lines), taking those two setters as parameters and returning an object `useAppShellState.js` now spreads (`...navigationTargets`) into its own return — the hook's public return shape is unchanged, so `App.jsx` (the only consumer) needed zero edits. **One required, deliberate fix during the move — not a pure copy in this one spot**: `openSongOverride`'s `useCallback` had an empty `[]` dependency array in the original, safe there only because `setPanelVisibility` was a `useState` setter declared in the *same* component (React can locally verify that's stable). Once it became a cross-file parameter, React's exhaustive-deps lint correctly could no longer verify that and flagged a real compiler error — "Existing memoization could not be preserved." Fixed by adding `setPanelVisibility` to the dependency array; behaviorally identical at runtime (the setter genuinely is stable, since the caller passes its own real `useState` setter), but worth flagging as the one non-mechanical change in this split. **Verified via a broad live Playwright pass**, given this hook feeds nearly all cross-page state: all 6 pages load clean; every one of the 7 navigation targets exercised individually with real DOM/state assertions — Tag Library (Ctrl+Click tag → landed with tag found and expanded), Titles (both panel-nav and a sourced-title click, exact winning template highlighted), Thumbnails, Hashtags, Short Hooks, Blocks (Groups → Broadcast), and the same-page Song Override target (Advanced Options re-expanded, correct field's `<details>` opened and highlighted, highlight cleared once the field was edited) — every one landed correctly; `activeProjectSettingsSection` persistence confirmed surviving a full page reload (reopened on the same Project Settings tab); stale-target-clearing (a separate hook, `useStaleTargetClearing.js`, verified in the prior `App.jsx` session) confirmed still correctly clearing a highlight after navigating away via the main nav and back manually; 0 console errors or warnings across every step, including the specific dependency-array-fix code path.
  - **`App.jsx`: 351 → 300 lines (2026-07-30) — root component, honest partial reduction.** First investigated `engine/placeholders.js` (the other candidate offered) and concluded it resists a safe pure-move split for a structural reason: its token resolvers (`resolveLogNoteToken`/`resolveOperatorStatusToken`/`resolveTransformationToken`) call `fillPlaceholders`, which resolves tokens via the `REGISTRY` object containing those same resolvers — genuine mutual recursion, not incidental size. A split would need either a circular import (which this codebase already works hard to avoid elsewhere — `resolveOverrideText`'s intentional duplication, `shortHookEligibility.js`'s dependency-free-leaf-module precedent) or dependency-injecting `fillPlaceholders` as a parameter (the pattern `pooling.js` already uses to dodge exactly this), which changes signatures throughout — a real refactor, not a pure move, for a file only 139 lines over threshold. Recommended via `AskUserQuestion` leaving it alone (like `hookBlockOverrides.js`); user chose to try `App.jsx`/`useAppShellState.js` instead. `App.jsx` had two genuinely safe seams: two self-contained `useEffect` blocks touching only already-passed-in state/setters, zero cross-file coupling — extracted verbatim to new `src/hooks/useStaleTargetClearing.js` (46 lines) and `src/hooks/useBlockCollisionRepair.js` (33 lines). Also deduped a small repeated inline callback while there: `(entry) => { handleLoadEntry(entry); setActivePage('generator'); }` was identical across 3 call sites (`TagLibraryPage`/`ShortsQueuePage`/`TodoPage`'s `onLoadEntry` props) — now one `loadEntryAndReturnToGenerator` defined once. **Discussed upfront via `AskUserQuestion` that this wouldn't fully clear 250** — the remaining bulk is the six-page routing JSX with heavy prop-threading, core orchestration inherent to being the root component (same situation as `UIKitPage.jsx`/`BlockGroups.jsx`), not something to force into a new abstraction. Separately noted, not touched: `App.jsx` still has the DEV-only `window.*StorageMigration` exposure the PRIORITY TODO already calls obsolete — left alone since that's explicitly filed under "storage cleanup, after a few stable sessions," a deferral already made deliberately by the user, not something to override mid-refactor without asking. **Verified via a broad live Playwright pass**, appropriate given every page in the app routes through this file: all 6 destinations (Generator/Tag Library/Shorts Queue/Todo/Project Settings/UIKit) load with real content; `loadEntryAndReturnToGenerator` confirmed working identically at all 3 call sites with a real saved entry; stale-target-clearing confirmed end-to-end (clicked Thumbnail → landed highlighted on Project Settings → Thumbnails; navigated away via main nav, back manually → highlight correctly gone, not stuck); block-collision-repair confirmed silent — no spurious `console.warn` fired across the whole session, as expected with no real collision in current project data; Regenerate and project switching (Illegal Mind Covers ↔ Maxx Dee Covers) both confirmed working; 0 console errors or warnings throughout.
  - **`engine/titles/generateTitles.js`: 305 → 196 lines (2026-07-30) — second engine-file split, same approach as `generateCustomBlocks.js`.** Initial read suggested no clean seam (helpers tightly wound around one shared function) — re-reading found one anyway: `resolveRecordText`, used by both `pickTitles` (during the pick loop, for text/dedup) and `renderTitles` (the live-render phase), plus its four support helpers (`buildGeneratedArtistShort`, `capitalizeFirst`, `resolveWrapping`, `resolveArtistSong`) form a coherent "resolve a picked record to display text" concern — separable from the five helpers used only inside `pickTitles` for building candidate pools (`pickTransformation`, `buildTransformationVariations`, `shuffleArray`, `getTitleTemplateGroups`, `getWeightedTemplates`). Pure move, zero logic changes: the five extracted to new `src/engine/titles/resolveTitleRecord.js` (110 lines), imported back by both `pickTitles` and `renderTitles` — same cross-import shape `resolveHookOverride`/`renderTextTemplate` used in the prior split. Only one importer of `generateTitles.js` exists (`useGeneratedOutput.js`), confirmed via grep before starting. Proposed via `AskUserQuestion` before touching code, matching the prior engine split's process. **Verified with the same seeded A/B rigor**: a Node harness (bundled with `rolldown`) checked out the pre-refactor commit into a `git worktree`, monkey-patched `Math.random` to a seeded PRNG (identical seed old vs. new), and ran `pickTitles`/`renderTitles` against both projects' real config across 6 scenario groups each (no tags / 1 tag / 2 tags / Shorts mode / hook-mixing via `useHooksForLongTitles` / a "pick once, render twice against different formData" live-substitution case) — **old and new code produced SHA256-identical output across all 12 runs**, with the hook-mixing scenario confirmed to actually exercise the `kind: 'hook'` branch, not just sit there unused. Live Playwright confirmed the specific behavior most at risk from a bad move here: typing into Artist then Song kept the same 5 title slots in the same order, only the substituted text changing (no reshuffle — this is exactly what the original "Generator pick/render split, part 1" work fixed, so it's the most likely thing to silently regress); Regenerate still produced a genuinely different set; Shorts mode rendered correctly; 0 console errors.
  - **`engine/descriptions/generateCustomBlocks.js`: 325 → 201 lines (2026-07-30) — first engine-file split, and the highest-stakes file touched in this pass.** After the settings-tab/form-component tier was exhausted, the user confirmed via `AskUserQuestion` to continue into engine-file territory. This is the "pick/render freeze" machinery — CLAUDE.md documents at length how randomized template/phrase selection is frozen at pick-time and pure-substituted at render-time, specifically so live form edits reflect instantly without re-rolling; getting this subtly wrong (evaluation order, a dropped parameter, a wrong `ctx` closure) would silently change generation output with no lint/build error to catch it. Proposed the exact split plan to the user before touching code (same `AskUserQuestion` pattern used for `SavedLibrary.jsx`): everything about hook-block-pool output + layout-key resolution + Block Groups (`pickHookBlockOutput`/`renderHookBlockOutput`/`resolveHookBlockOutput`, `pickLayoutHookBlocks`/`renderLayoutKey`/`resolveLayoutKey`, `renderBlockGroups`/`generateBlockGroups`) moved verbatim to new `src/engine/descriptions/resolveLayoutBlocks.js` (126 lines), which imports `resolveHookOverride`/`renderTextTemplate` back from `generateCustomBlocks.js` — the one cross-dependency; the customBlocks/structured-block/song-override logic stayed in the original file. Only two files imported the moved functions (`generateDescriptions.js`, `generateShortDescriptions.js`), both updated to pull from both files now; two stale code comments referencing the old file location were also updated. **Verification went well beyond lint/build, matching the stakes**: a Node A/B harness (bundled with `rolldown`) checked out the pre-refactor commit into a `git worktree` (never touching the main working tree's uncommitted changes), monkey-patched `Math.random` to a seeded PRNG (identical seed for old and new runs, making this a true deterministic comparison rather than a statistical one), and ran `generateDescriptions`/`generateShortDescriptions` against both projects' real config across 7 scenarios each (no tags / 1 tag / 2 tags / a song-scoped Story Block override / a song-scoped Log Block override / Shorts mode / a second Shorts multi-tag scenario) — **old and new code produced byte-identical, SHA256-matched output across all 14 runs**, including Illegal Mind Covers' three Block Group segments (`broadcastBlock`/`logBlock`/`closingBlock` — the exact code that moved) confirmed to produce real, non-empty, varying text. Live Playwright on the running dev server confirmed Long/Shorts descriptions render with real content; a Transformation Tag toggle + Regenerate still correctly changes output; typing into the Story Block override still live-substitutes `{artist}` with no Regenerate needed, and clearing it reverts to byte-identical text matching the pre-override frozen pick (confirming the freeze/replay mechanism survived the move); Shorts mode renders its distinct variant blocks correctly; 0 console errors throughout. **Incidental finding, flagged not fixed**: while reading `generateTitles.js` as an alternative candidate before settling on this file, spotted that `pickTitles` destructures a `hasEmpty` field from `resolveRecordText`'s return value that the function never actually returns, meaning the empty-candidate filtering it drives (`if (hasEmpty) continue`) may be dead code — see Known Issues.
  - **`components/input/AdvancedDescriptionFields.jsx`: 265 → 57 lines (2026-07-30).** A pure move, not a rewrite — two fully self-contained sub-components already living in the same file were extracted verbatim, with zero logic changes: `SongListBlockEditor` (the mini item-editor used inside a List-type song block override — its own local `items` state, no dependency on parent scope) → new `src/components/input/SongListBlockEditor.jsx` (52 lines); `SongBlockOverrideFields` (renders a per-song override field, text or list, for every song-scoped Text/List/Hook Block, plus the `songOverrideTarget` click-to-scroll effect that opens/highlights the right field when a song-overridden description line is clicked elsewhere in the app) → new `src/components/input/SongBlockOverrideFields.jsx` (167 lines). The main file is now just the "Additional Hashtags" input plus a single `<SongBlockOverrideFields>` render call. This is the last file in the "settings/form component" risk tier from the original candidate list — every remaining file over 250 lines is either a high-risk core file (`App.jsx`, `useAppShellState.js`) or an engine file flagged "go carefully" from a recent heavy rework. Verified via live Playwright against the real Generator's Advanced Options panel — the one real risk in a pure move like this is a dropped prop silently breaking the cross-file `songOverrideTarget` wiring, so that was the focus: Additional Hashtags input propagates live to the Hashtags output panel; a Story Block text override propagates live to the Descriptions panel with no save/regenerate needed; a temporary List-type song block (created in an isolated Playwright browser profile, never touching the real project's data) rendered correctly via `SongListBlockEditor` with working add/remove; Reset (↺) correctly reverted an override, and the Descriptions panel immediately stopped showing it; clicking a song-overridden description line in the Descriptions panel correctly scrolled to and highlighted the matching Advanced Options field — confirming the `songOverrideTarget` wiring survived the file split intact; 0 console errors throughout.
  - **`projectSettings/blocks/BlockGroups.jsx`: 261 → 234 lines (2026-07-30).** Third application of the same patch-builder extraction pattern (after `HookBlocks.jsx` and `Placeholders.jsx`): `patchGroup`/`resetGroup`/`deleteGroup`/`addGroup` moved to new `src/utils/blockGroupOverrides.js` (59 lines: `patchGroupPatch`/`resetGroupPatch`/`deleteGroupPatch`/`addGroupPatch`/`isDynamicGroup`), returning patch objects instead of calling `updateProjectOverride` directly — the component still owns that call plus the `window.confirm`/`isCore` delete guard. `AddChildList` (per-group "add child" search list) stayed inline, matching the precedent of not extracting every small tab-specific sub-component. Modest reduction compared to prior files — most of this file's remaining bulk is drag-and-drop wiring (`@dnd-kit` sensors, `SortableActiveBlock` rendering) and JSX, not duplicated patch logic — but it's now under the 250-line threshold. Verified via live Playwright against Illegal Mind Covers' real `blockGroups` config (31 assertions, 0 console errors): existing group cards (Closing/Broadcast/Log Notes) render correctly; Scope/Target dropdown changes and renames landed correctly in `localStorage`'s `blockGroupOverrides` and persisted across reload; child reorder (move buttons), remove, and re-add via the Add-child list all correctly modified the `children` array; Reset (↺) removed the entire group override cleanly and the UI correctly reverted to defaults, including the reorder/remove/re-add from the same test session (expected, since they were part of the same override object); a new test group's add/delete round-tripped through `customBlockGroups` with the exact expected shape; final check confirmed real project data was left in its original state (no leftover overrides) after testing.
  - **`projectSettings/blocks/Placeholders.jsx`: 274 → 164 lines (2026-07-29).** Two seams: (1) `PlaceholderReference` (the collapsible "All Placeholders" reference card) was already a self-contained props-in/JSX-out component — extracted verbatim to new `blocks/PlaceholderReference.jsx` (60 lines), zero logic changes. (2) The patch-builder functions (`patchPlaceholder`/`resetPlaceholder`/`deletePlaceholder`/`addPlaceholder`) plus the pure helpers they depend on (`sourceToFieldValue`, `fieldValueToSource`, `generatePlaceholderKey`, `isDynamicPlaceholder`) — extracted to new `src/utils/placeholderOverrides.js` (88 lines), rewritten to *return* patch objects instead of calling `updateProjectOverride` directly, explicitly matching `hookBlockOverrides.js`'s established convention from the `HookBlocks.jsx` split (setters return patches; the calling component still owns the `updateProjectOverride` call plus the `window.confirm`/`isCore` delete guards, exactly as `HookBlocks.jsx` does). Verified via live Playwright, since the calling-convention change (return-patch vs call-directly) was the one place a subtle patch-shape bug could slip in and real project override data was at stake: the reference card renders/expands/copies correctly; a Count edit and a Short Hooks Pool toggle both landed correctly in `localStorage`'s `projectOverrides.<project>.description.placeholderOverrides` and persisted across a real page reload; Reset (↺) removed only the targeted key, leaving an unrelated override untouched (confirms scoped removal, not a blanket wipe); Add produced the exact documented `customPlaceholders` entry shape (`{key, label, source:{}, count:1, isCore:false}`); Delete cleanly removed it; 0 console errors throughout.
  - **`savedLibrary/SavedLibrary.jsx`: 337 → 161 lines (2026-07-29).** New `src/hooks/useSavedLibraryFilters.js` (107 lines) owns all filter/sort state (`search`, `sortBySignal`, `hideQueueHidden` + its unified-storage persistence effect, `missingDataOnly`) and the derived `filteredEntries`/`missingEntries`/`hasMissingData` — pure logic, previously interleaved with JSX in the same file. New `src/components/savedLibrary/MissingDataTools.jsx` (124 lines) owns the entire "Missing Data AI round-trip" feature (Copy AI Prompt button, Paste AI Response toggle + textarea + Apply/Cancel + result summary), a fully self-contained widget with its own private state (`promptCopied`, `showPasteBox`, `pasteText`, `applyResult`) — needed only `missingEntries`/`savedEntries`/`onUpdateEntry` as props. One deliberate small layout change made during the split: the two AI-round-trip buttons previously rendered inline within the same flex row as the Sort/Hide/Missing-data-only checkboxes (`.filter-row`); they now render in their own `.button-row` directly below that row instead (reusing the existing CSS class — no new styles) — a cleaner conceptual grouping (all "missing data" actions together), confirmed live to render correctly, not broken. Verified via live Playwright, beyond lint/build given real saved-song data was at stake: toggle/search/sort/shown-count all correct; Hide Queue-Hidden filters correctly and **persists across a real page refresh** (confirmed via raw `localStorage.getItem('illegalMindGeneratorData').ui.hideQueueHidden`, not just the UI checkbox state); Missing Year/Genre filter appears/filters correctly and the new button-row layout renders cleanly; Copy AI Prompt confirmed via clipboard read-back containing the missing song; Paste AI Response confirmed via a real `localStorage` write — pasted a fabricated valid response line, applied it, and the target entry's `originalYear`/`originalGenre` genuinely updated in storage (not just the on-screen summary text); Export/Import buttons still render; 0 console errors.
  - **`hooks/useSavedEntries.js`: 400 → 208 lines.** New `src/utils/savedEntries.js` (220 lines): the 6 module-scope functions that were already pure and standalone (`buildEntryId`, `normalizeEntryIds`, `mergeImportedEntry`, `toSlug`, plus private `isEmptyValue`/`preferNonEmpty`) moved verbatim — zero risk, they weren't even closures. Newly extracted: `buildEntryFromFormData` (from `handleSaveEntry`) and `buildFormDataPatchFromEntry` (from `handleLoadEntry`) — the exact two "explicit field list" concerns CLAUDE.md's Known Gotchas already documents as distinct from import-merge logic. Also extracted `loadInitialSavedEntriesByProject`, the `useState` lazy initializer's legacy-format migration (already routes through `storage.js`). The 4 tiny reducer-only handlers (`handleDeleteEntry`, `handleUpdateEntryTodo`, `handleAddEntries`, `handleUpdateEntry`) stayed inline, byte-identical. Verification went beyond lint/build given real saved-song data was at stake: live Playwright reading `illegalMindGeneratorData` directly (not just UI text matching) confirmed save, a second save, load (fields correctly repopulate from a *different* saved entry, not just round-tripping the same one), delete, and an import-merge case (a thinner re-imported record filled an empty field without wiping an existing populated one, a brand-new entry added fresh) — all correct, 0 console errors. One test-script false alarm along the way: loading an entry auto-collapses the Saved Library panel (real, pre-existing UX, not a refactor bug) — caused several rounds of "rows disappeared" confusion until traced to a screenshot.
  - **`blocks/HookBlocks.jsx`: 497 → 125 lines.** Two real seams found by reading the file, not an arbitrary chop at the midpoint: (1) `HookBlockEditor` — the presentational card body (override-type select, lines slider, max input, template list) — was a pure props-in/JSX-out sub-component with zero closures over parent state, extracted verbatim to `blocks/HookBlockEditor.jsx` (112 lines). (2) The ~20 getter/setter functions (`getTemplates`, `getScope`, `updateTemplates`, `resetBlock`, `addHookBlock`, `deleteHookBlock`, etc.) were pure closures over `projectConfig`/`projectSettingsOverrides`/`updateProjectOverride` with zero internal React state — extracted into `src/utils/hookBlockOverrides.js` (331 lines) as plain functions taking the relevant state explicitly and *returning* patch objects, rather than calling `updateProjectOverride` themselves — the component still owns that call, matching how every other Project Settings tab wires patches inline, and matching the architecture rule "storage in `src/utils`."
  - **`hookBlockOverrides.js` deliberately left over 250 lines.** It's ~20 small, independently-readable, almost-non-interdependent pure functions (only `getCountValue` calls `getMaxLines`) — long because it has many functions, not because anything is tangled. A further split (readers vs. patch-builders) would satisfy the line-count rule without adding real clarity, since both halves operate on the same state shape and are always imported together by the one caller. Discussed with the user before deciding to leave it.
  - **`descriptions/LongDescriptionSettings.jsx` (272 → 132) + `ShortsDescriptionSettings.jsx` (273 → 138) — turned out to be near-duplicates, not just long.** Reading both files revealed identical DnD sensor setup, `layoutIndex`/`addToLayout`/`removeFromLayout`/`moveBlock`/`handleDragEnd`/`resetOrder`, `getNavigateHandler`, and the entire two-column Available/Active JSX board — differing only in *where* each reads/writes the layout array (`templates.long` vs `templates.shorts`) and a couple of small config differences (Shorts has a count slider, different `KNOWN_*_BLOCK_META`, no `supportBlockConfig`). CLAUDE.md had already flagged this exact pair as a sync-drift risk in two earlier sessions (the drag-and-drop reordering work, and the Long-description-tooltip `getNavigateHandler` unification) — this refactor removes the risk at the source instead of continuing to work around it piecemeal.
  - **Extracted:** `descriptions/useDescriptionLayoutActions.js` (62 lines) — a hook owning the DnD sensors plus all six reorder/add/remove functions, parameterized by `{ activeKeys, defaultLayout, updateLayout }`. `descriptions/DescriptionLayoutBoard.jsx` (97 lines) — the shared mobile-tabs/header/two-column-DnD-board JSX; it now also owns `mobileTab` state internally, since nothing outside it ever read that state. `makeNavigateHandler` added to `src/utils/descriptionLayout.js` — the existing shared home for `resolveBlockSource`/`makeLayoutLabelResolver`/`buildHookBlockMaps`, a natural extension rather than a new module — to dedupe `getNavigateHandler` too (the two pages' only difference there was `supportBlockConfig`, now just a parameter). Each page keeps only what's genuinely different: its own `defaultLayout`/`activeKeys`/`availableKeys` computation and its own `updateLayout` patch-builder.
  - **Verification went beyond lint/build for both files**, given the real interactive surface touched (write paths, drag-and-drop, add/remove/reorder). `HookBlocks.jsx`: live Playwright confirmed a template line's full write path — added, edited, value committed on blur, landed correctly in `localStorage` — plus remove/reset and all 5 Blocks sub-tabs still rendering, 0 console errors. Description settings: live Playwright confirmed exact-count round-trips (a remove→re-add cycle went 7→6→7 active rows, not just "didn't crash"), move up/down changing order, Reset Order non-crashing, and all 10 Project Settings tabs + both Description sub-tabs still rendering, 0 console errors. No behavior change in either case.
  - **Remaining candidates tracked in Next Recommended Tasks below** — refresh the list via the `find ... | wc -l` command there before picking the next one, since line counts drift as other work touches these files.

- **`src/components/projectSettings/` reorganized into topic folders (2026-07-28).** User pointed at the VSCode file explorer — 16 flat `ProjectSettings*.jsx` files sitting alongside 5 existing topic subfolders (`blocks/`, `descriptions/`, `hooks/`, `lists/`, `titles/`) — and asked for a folder reorg.
  - **The rule.** Investigation found `descriptions/` already partially had the answer: its two sub-tab widgets (`LongDescriptionSettings.jsx`, `ShortsDescriptionSettings.jsx`) already lived inside it, with only the top `ProjectSettingsDescriptions.jsx` router left stranded flat at the root. Generalized that into one consistent rule: a tab's controller file moves into the same folder as the widgets it renders; a tab with no dedicated widgets (`General`, `Links`, `Thumbnails`, `Hashtags`, `Todo`, `ShortsQueue`) stays flat at `projectSettings/` root alongside `ProjectSettingsContent.jsx` (the master router).
  - **9 controllers moved.** `Blocks`/`TextBlocks`/`HookBlocks`/`BlockGroups`/`Placeholders` → `blocks/`; `Lists` → `lists/`; `Descriptions` → `descriptions/`; `Titles` → `titles/`; `ShortHooks` → a renamed `shortHooks/` (was `hooks/` — confusing next to the app's real `src/hooks/` React-hooks directory, fixed while touching it anyway since it was about to gain a second file).
  - **Prefix drop, confirmed via `AskUserQuestion`.** After the first move, the user asked whether to also drop the now-redundant `ProjectSettings` prefix once the folder already gives context. Confirmed: drop it for files moving into topic folders (file renamed + internal `export default function` name renamed to match, e.g. `blocks/Blocks.jsx` exports `Blocks`); kept it on the 7 files staying flat at the root, since for those it's the only namespacing they get and it matches how every other top-level page component is named elsewhere in the app (`GeneratorPage.jsx`, `TodoPage.jsx`, etc.).
  - **Blast radius confirmed small before starting.** An Explore-agent import-graph audit found only one import from outside the directory tree touches it at all (`ProjectSettingsPage.jsx` → `ProjectSettingsContent.jsx`, itself unchanged) — the other ~40 fixups were entirely internal `projectSettings/**` relative-path edits: sibling-reference path changes (a moved file referencing another moved file) plus depth-based `../` increments for every moved file's imports reaching outside the directory (one more `../` per level of new nesting). No barrel/`index.js` files introduced — confirmed via grep that none exist anywhere in this codebase area, so direct deep imports stayed the convention.
  - **Executed as 3 separately-verified steps**, matching the project's step-by-step preference: Blocks topic (5 files + router) → a prefix-drop rename pass on those same 5 files (once the naming question was resolved) → the remaining 4 single-file topics (Lists, Descriptions, Titles, ShortHooks) together. `eslint`+`vite build` run after each step; each left for review before continuing.
  - **Verification.** `eslint`/`vite build` clean throughout (same 4 pre-existing unrelated errors every time, nothing new). Live via Playwright against the running dev server: every one of the 10 top-level Project Settings tabs rendered real content; Blocks' 5 sub-tabs and Descriptions' 2 sub-tabs each confirmed rendering correctly in isolation; zero console errors anywhere. A whole-tree grep confirmed no stale `ProjectSettingsX`-named import survived the moves. Pure structural refactor — zero behavior changes, nothing in the app looks or works differently.

- **Blocks → Placeholders tab: "All Placeholders" reference list + "Copy AI Prompt" button (2026-07-28).** User wanted a visible list of every placeholder usable in the current project, not just the editable `{custom.*}` ones, plus a one-click way to copy a short-explanation version for pasting into an AI prompt.
  - **New `src/utils/placeholderReference.js`.** `buildPlaceholderReference(projectConfig)` returns `{ builtIn, tagCategories, links, custom }`. `builtIn` pairs `REGISTRY_TOKENS` (`src/engine/placeholders.js`) plus the two `SPECIAL_TOKENS` (`num`/`transformation`, excluded from the registry by design) against a new static `PLACEHOLDER_DESCRIPTIONS` map — one short sentence per token. `tagCategories` pairs `TAG_CATEGORY_PLACEHOLDERS` (`hookPlaceholders.js`) with a generated sentence resolved through `TAG_CATEGORY_ALIASES` (`descriptionTagHelpers.js`) so `{tags.lang}` correctly describes the "language" category rather than literally "lang" — a smoke test against real config caught this before it shipped wrong. `links`/`custom` are read live from `projectConfig.description.links`/`.placeholders`, so they always reflect the current project exactly (confirmed via Playwright: Illegal Mind Covers shows 4 links/3 custom, Maxx Dee Covers shows 9 links/2 custom, no cross-contamination).
  - **UI.** New `PlaceholderReference` component inside `ProjectSettingsPlaceholders.jsx` renders the four groups as plain `<ul>` lists inside a `TemplateGroupCard` used as a non-editable content shell (per its documented "omit `onUpdateTemplates`" usage) — no new UI primitive needed. Placed above the existing editable custom-placeholder `BlockEditorCard` list, `initialCollapsed` to match the tab's other collapsed-by-default cards, with the "Copy AI Prompt" button in `headerActions` so it's clickable without expanding.
  - **Copy button scope narrowed after user feedback.** The first pass had `buildPlaceholderPrompt(projectConfig)` copy all four groups; user asked for it to copy only `BUILT-IN`/`TAG CATEGORIES` — the tokens actually useful context for an AI writing new phrase templates, excluding the project-specific `PROJECT LINKS`/`CUSTOM` plumbing. Since neither remaining group depends on `projectConfig`, `buildPlaceholderPrompt` now takes no arguments. Follows the existing one-way `buildXPrompt` convention (`tagPrompt.js`, `searchQuery.js`) — no parse-back counterpart, this is reference text only.
  - **Verification.** A Node+esbuild smoke test against both projects' real `projects.json` (token well-formedness, non-empty descriptions, correct tag-category-alias text, section-omission for an empty group). `eslint`/`vite build` clean. Live via Playwright against the running dev server for both projects: card renders collapsed by default with the button still clickable; expanding shows all four groups with real per-project data; clipboard content after clicking Copy confirmed to include only `BUILT-IN:`/`TAG CATEGORIES:`, not `PROJECT LINKS:`/`CUSTOM:`; zero console errors throughout.

- **Per-tag "Copy from Project" action (2026-07-25).** User wanted a way to pull one specific tag's data in from another project without affecting every other tag (bulk "Sync Tags") or creating a separately-named copy ("Duplicate tag").
  - **Reused `syncProjectTags`'s merge pattern, scoped to one tag.** New `copyTagFromProject({tagName, sourceProjectId, targetProjectId, sourceBaseTags, targetBaseTags})` in `useTagOverrides.js` builds each side's effective tag via the same `buildEffective` helper (base + override, with `description`/`shortHooks` merged one level deep) `syncProjectTags` already uses, then calls the existing `mergeTagData` (Set-union merge) to combine them — no new merge semantics introduced, just a narrower entry point that touches one `tagName` instead of looping every override in the source project.
  - **UI.** `TagBasicsTab.jsx` gained a dropdown (built from the same `destinationProjects`/`otherProjects` list the bulk Sync control already computes, filtered to exclude the current project) + a button, both wrapped in the existing `.tag-actions` CSS class (already used by `TagSyncControls.jsx` — no new CSS needed) and placed directly below "Duplicate tag". Threaded `otherProjects`/`onCopyTagFromProject` props through `TagLibraryPage.jsx` → `TagCard.jsx` → `TagEditor.jsx` → `TagBasicsTab.jsx`. `TagLibraryPage.jsx`'s new `handleCopyTagFromProject(tagName, sourceProjectId)` builds `sourceBaseTags`/`targetBaseTags` from `projects[id]?.tags` (same pattern its existing `handleSyncTags` already uses) and shows a `window.alert` confirmation, matching the bulk sync control's existing UX.
  - **Naming clarified via `AskUserQuestion`.** First read of "copy and add, not override" suggested creating a new, differently-named tag (mirroring "Duplicate tag"). The user's actual answer reframed the ask entirely: both projects already share the same tag *name* via prior "Sync Tags" use, but hashtags/short hooks stayed empty after syncing — they wanted those fields to actually populate. "Copy and add" turned out to mean: merge into the *same* tag additively (add missing content, don't wipe local edits), not create a sibling tag.
  - **Verification — live via Playwright, real button clicks, real localStorage.** A collapsible `<details>` disclosure widget flaked when clicked mid-script (traced to a pre-existing, unrelated quirk — `TagEditor`'s `open={shouldOpenEditor(...)}` prop has no `onToggle` handler, so a manually-toggled-open native state can silently collapse on the next unrelated re-render); routed around it by forcing `.open = true` via `element.evaluate()` alongside real clicks for the actual interactions under test. Confirmed end-to-end: added `#TESTPLAYWRIGHTHASHTAG` to "Heavier" on Maxx Dee Covers through the real Hashtags tab UI, confirmed it landed in that project's stored override; switched to Illegal Mind Covers, confirmed the per-tag dropdown defaulted to (and listed only) "Maxx Dee Covers"; clicked the button, confirmed an alert fired and Illegal Mind Covers' "heavier" override gained the hashtag (plus title/thumbnail/description/shortHooks content merged in) both in localStorage and in the live Hashtags tab UI; clicked again and confirmed the array stayed at one entry (no duplicate); confirmed the bulk "Sync Tags" button still renders and is intact. Zero console errors throughout.
  - **Found, left alone (out of scope):** `TagEditor.jsx`'s `activeTab` state persists across a project switch for same-named tags (React reuses the component instance since `key={tag.name}` matches across both projects' card lists) — clicking into a non-Basics tab on one project's tag, then switching projects, can land on that same non-Basics tab for the other project's same-named tag instead of resetting to Basics. Not touched since it wasn't part of this ask; worth fixing if it causes real confusion.

- **Fast tag creation via AI round-trip, "Add Tag" panel (2026-07-13).** User wanted a fast way to add a new transformation tag (e.g. "Melodic Hardcore") — today's flow was a bare `window.prompt()` for a name, then filling every phrase by hand across the Tag Editor's 5 tabs one row at a time.
  - **Reused an existing precedent.** `src/utils/searchQuery.js`'s `buildMissingDataPrompt`/`parseMissingDataResponse` (Saved Library's "Copy AI Prompt"/"Paste AI Response") already established this exact shape of feature for a different domain — modeled the new one on it directly rather than inventing a new UX pattern.
  - **New `src/utils/tagPrompt.js`.** `buildTagPrompt(label, category, shortHookTypes)` builds a plain-text prompt with one section header per tag field — `TITLE:`, `THUMBNAIL:`, `DESCRIPTION_TECHNICAL:`, `DESCRIPTION_LOG:`, `DESCRIPTION_STATUS:`, `HASHTAGS:`, plus one `SHORTHOOKS_<KEY>:` per entry in the project's real `shortHookTypes` (read from `projectConfig`, not hardcoded — a project that adds/removes a hook type stays correct automatically) — asking for 4-6 phrases per section and mentioning `{artist}`/`{song}` as usable placeholders. `parseTagResponse(text, shortHookTypes)` is a line-based state machine: a line matching `SECTION_NAME:` opens a section, subsequent non-empty lines accumulate under it until the next header; anything outside a recognized section (AI preamble, sign-off, bad headers) collects into `unrecognized` rather than being silently dropped — mirrors `parseMissingDataResponse`'s `unmatched` array. Plain-text sections were chosen over JSON specifically to avoid AI-output JSON fragility (markdown fences, trailing commas), matching the codebase's existing precedent.
  - **Data model needed zero changes.** Confirmed via `buildResolvedProjectConfig.js`'s tag-override merge loop that a brand-new tag can be created purely as a `tagOverrides[projectId][tagName]` entry with no `projects.json` counterpart — the resolver injects it into `projectConfig.tags` even when no base tag exists, and the Tag Library derives its list from `Object.keys(projectConfig.tags)`. So `AddTagPanel` just builds the same override object shape `handleCreateTag`/`handleDuplicateTag` already built and calls `updateTagOverride` verbatim — no new storage, no new merge logic.
  - **New `src/components/tags/AddTagPanel.jsx`.** Inline panel (no modal primitive exists in this app) — Name + Category inputs (Category is free text with a `<datalist>` suggesting `getTagCategories(projectConfig)`'s existing values), "Copy AI Prompt" button (500ms "Copied ✔️" state, same pattern as `CopyButton`), an always-visible paste textarea, and Create Tag/Cancel. `TagLibraryPage.jsx`'s `handleCreateTag` changed from a self-contained prompt handler to `(tagName, overrideObject) => updateTagOverride(tagName, overrideObject)` — the panel now owns shape-construction, same division of responsibility `handleDuplicateTag` already had. The existing "+ Add tag" button (`TagActions.jsx`, unchanged) now opens the panel instead of triggering `window.prompt` directly.
  - **No regression to the manual path.** Leaving the paste textarea empty still creates a fully-empty, fully-editable tag exactly like the old `window.prompt` flow did.
  - **Deliberate non-goal.** Re-pasting a response into an *already-existing* tag to refill/refresh it — that needs a non-destructive "only fill empty fields" merge policy (like Saved Library's), a materially different feature from creation. `parseTagResponse` is generic enough that adding it later would be straightforward.
  - **Verification.** A 28-assertion Node smoke test covering prompt structure, well-formed responses, partial/missing sections, comma-joined hashtag lines, stray unrecognized headers/preamble/signoff text, and empty input. `eslint`/`vite build` clean (only pre-existing unrelated issues remain), NUL-byte sweep clean. Live via Playwright (0 console errors): the inline panel replaces the native dialog entirely (no `dialog` event fires); the copied prompt contains all 7 real `SHORTHOOKS_*` sections for Illegal Mind Covers and mentions the tag name/placeholders; a pasted fixture response correctly populates Basics/Titles/Descriptions/Short Hooks/Hashtags on the new tag card; the new tag is selectable in the Generator's Transformation Tags picker; the no-paste quick-create path still produces a blank editable tag, confirming no regression from the old `window.prompt` behavior.
  - **Same-day follow-up: prompt was too weak.** First real use (`label="Melodic Hardcore"`) exposed the problem directly — the user's actual AI reply came back almost entirely genre-agnostic filler ("More emotion. Same song.", "Pure Melodic Hardcore energy.", "Heavier heart", "Sing it loud") that could describe any genre, with `{artist}`/`{song}` placeholders used in barely 1 of 6 phrases per Short Hooks section despite the tag name being deliberately specific. Tightened `buildTagPrompt`: (1) a new anti-genericness paragraph naming the user's exact failure-mode phrases as bad examples and requiring every phrase to name an actual sonic/genre trait of the tag; (2) a hard requirement that at least 4 of the 6 phrases in *every* `SHORTHOOKS_*` section include `{artist}`/`{song}` directly, since those sections are read as complete video-hook sentences about one specific song, not abstract genre description; (3) each `SHORTHOOKS_<KEY>:` section's hint line now pulls a real example straight from that hook type's own `shortHookTypes[key].templates[0]` in `projects.json` (e.g. `"{artist} still hits different"` for Nostalgia) instead of a generic parenthetical — the app's own config data becomes the few-shot example, so it can't drift from what the app actually expects. `parseTagResponse` untouched — only the prompt wording changed, response format is identical. All 28 smoke-test assertions still pass.

- **Todo Settings tab + new Shorts Queue settings tab (2026-07-13).** User asked (loosely) to expose whatever's hardcoded around Todo and "the amount of cover in between similar songs," explicitly trusting scope decisions to be made — resolved two real design forks with `AskUserQuestion` before implementing (keep the queue's spacing rule as exact-song matching rather than extending to same-artist; give Shorts Queue its own new Project Settings tab rather than folding it into Todo Settings).
  - **Todo Settings.** New `ProjectSettingsTodo.jsx` — one `TemplateGroupCard` list editor bound to `projectConfig.todoStatuses`. `todoStatuses` is a flat top-level `projects.json` array, so the existing top-level spread in `buildResolvedProjectConfig.js`'s `mergeProjectOverrides()` already merges an override correctly — no new merge block needed. Reset reuses `resetProjectOverride(fieldName)` (already generic, already threaded to `ProjectSettingsContent.jsx` for `ProjectSettingsGeneral`, just needed passing to the new tab too). Routed via a new `activeSection === 'todo'` branch in `ProjectSettingsContent.jsx` — the tab id/label/summary function already existed in `projectSettingsSections.js` from an earlier session, just fell through to "Coming Soon" until now.
  - **Real bug found and fixed while scoping.** `TodoBulkAdd.jsx` hardcoded `status: 'Wishlist'` (plus UI copy "Bulk Add Wishlist Covers"/"Add to Wishlist") — renaming the first status via the new editor would've silently assigned bulk-added entries a status matching nothing in the configured list, making them invisible on the Todo page. `TodoPage.jsx` already received `todoStatuses` as a prop and passed it to sibling components, just not to `TodoBulkAdd` — fixed by threading it through; `TodoBulkAdd` now derives both the assigned status and its UI copy from `todoStatuses[0] || 'Wishlist'`.
  - **Accepted, documented limitation.** Renaming or removing a status has no migration for saved entries already using the old status string — they become invisible (filtered out of every section) rather than deleted, since `TemplateGroupCard`'s array editor has no rename-vs-add/remove distinction to hang automatic migration off of. Noted in `CLAUDE.md`'s Known Gotchas rather than built around, matching how the app documents similar rename risk elsewhere instead of over-engineering a migration system nobody asked for.
  - **Shorts Queue settings — a genuinely fresh area.** Unlike hashtags (which already had partial `projects.json` config wiring before its settings tab existed), `useShortsQueue.js` was 100% hardcoded module constants: `QUEUE_LENGTH = 20`, and `isTooClose`'s spacing check hardcoded to `queueIds.slice(-2)` (only ever compared against the last 2 queue slots, and only for an *exact* same-song match — no artist- or tag-level spacing exists today, confirmed by reading the function). Added `projects.json`'s `shortsQueue: { length: 20, duplicateSpacing: 2 }` to both projects (defaults exactly match the prior hardcoded behavior — zero behavior change until edited), a matching `shortsQueue` nested-merge block in `buildResolvedProjectConfig.js` (same shallow shape as `hashtags`/`youtubetags`), and reworked `useShortsQueue(projectId, savedEntries, queueConfig = {})` to accept a third param — the module-level pure functions `isTooClose`/`buildQueue`/`getValidReplacement` now take `spacing`/`queueLength` params instead of reading fixed constants. The `maxAttempts` safety-valve multipliers (`*50` in `buildQueue`, `*20` in `getValidReplacement`) stay as internal implementation details, deliberately not exposed as settings — they're a randomizer safety net, not user-facing behavior — but `buildQueue`'s now scales off the *resolved* `queueLength` instead of the old fixed constant, so a user-configured longer queue still gets a proportionally larger attempt budget.
  - **New tab UI.** `ProjectSettingsShortsQueue.jsx` mirrors `ProjectSettingsHashtags.jsx`'s "Generation" card exactly — two `LabelSliderRow`s ("Queue Length" min 5/max 50, "Duplicate Spacing" min 1/max 10), each with its own inline ↺ reset via the same `tag-card-label-row` + `IconButton` pattern. New `projectSettingsSections.js` entry (`id: 'shortsQueue'`) + summary function, new `activeSection === 'shortsQueue'` branch in `ProjectSettingsContent.jsx`.
  - **Side benefit.** `ShortsQueuePage.jsx` now actually uses its `projectConfig` prop (to pass `projectConfig.shortsQueue` into the hook) — this was previously received but unused, a pre-existing lint error (`'projectConfig' is defined but never used`) this change incidentally resolved, confirmed by the full-project `eslint` problem count dropping from 6 to 5. The page's empty-state copy ("Click Randomize to create a N-cover Shorts queue") now reflects the real configured length instead of a hardcoded "20".
  - **Verification.** A Node smoke test (reimplementing the pure functions, since the hook itself can't run outside React) confirmed: default config (20/2) reaches full length with the spacing constraint holding across 50 randomized trials; a tight 3-song pool with queue length 10 still respects spacing 2; a wider configured spacing of 5 is respected against a 30-song pool; a longer configured length of 40 is reached. `eslint`/`vite build` clean, NUL-byte sweep clean. Live via Playwright (13 assertions, 0 console errors): Todo Settings — rename propagates live to the Todo page's section headers and to Bulk Add's dynamic copy/status; reset reverts to the JSON defaults. Shorts Queue settings — a lowered Queue Length is respected by both the empty-state copy and an actual randomized queue's row count; reset reverts to 20/2.

- **Artist/Song hashtag click-to-navigate + fixed stuck navigation-target highlights (2026-07-13).** Two follow-ups reported together right after the per-item hashtag nav work shipped.
  - **Artist/Song chips clickable.** `generateHashtags.js`'s `dynamicEntries`/`dynamicYTEntries` (artist, song) now tag their entries `{type:'field', field:'artist'|'song'}` instead of `source: null`. The combined `` `${artist} ${song}` `` YouTube-tag-only entry stays `source: null` — doesn't map cleanly to one field. `HashtagLink.jsx` gained a `'field'` branch that reuses `onOpenSongOverride?.({blockKey: source.field})` verbatim — the exact same mechanism the Additional Hashtags field already uses, so no new prop was needed anywhere in the output-side plumbing. `BasicSongFields.jsx` gained `songOverrideTarget`/`clearSongOverrideTarget` props (one new hop — `InputForm.jsx` already had both, wasn't passing them to `BasicSongFields`), `id="song-override-artist"`/`"song-override-song"` on each field's `FormField` + conditional `tag-section--highlight`, a scroll-into-view `useEffect` mirroring `AdvancedDescriptionFields.jsx`'s existing one for `customHashtags`, and a `handleFieldChange(field)` wrapper around the existing `handleChange` prop that clears the target once its own field is edited (matching the "consumed on edit" convention).
  - **Stuck navigation-target highlights, real bug fixed.** User reported every click-to-navigate highlight (block/title/thumbnail/hashtag/tag-library links) stayed stuck indefinitely — persisting even after navigating to a completely different page — until a full refresh. Traced by reading every `setActivePage` call site in `useAppShellState.js`: `titlesTarget`/`thumbnailsTarget`/`hashtagsTarget`/`blocksTarget`/`shortHooksTarget` were only ever cleared by `ProjectSettingsPage.jsx`'s `handleSectionChange` (a manual section-tab click *within* Project Settings) — the main app nav (`AppHeader.jsx`, a bare `setActivePage(id)`) cleared nothing. Same gap for `tagLibrarySearchTarget` (only cleared by in-page search/filter interaction in `TagLibraryPage.jsx`) and `songOverrideTarget` (only cleared by editing the specific targeted field, never by leaving and returning to the Generator page). No existing "clear X when the user navigates away" precedent existed anywhere in the codebase. Fixed with one new `useEffect` in `App.jsx`, placed immediately after the `useAppShellState()` destructure: whenever `activePage` no longer matches a target group's home page, clear that group (`'projectSettings'` for the first five, `'tags'` for `tagLibrarySearchTarget`, `'generator'` for `songOverrideTarget`). Correct by construction — every `open*Search`/`openBlocksEditor`/`openTagLibrarySearch` function sets its own target *and* calls `setActivePage` to that target's own page in the same synchronous handler, so React batches both updates together and the effect always sees the matching `activePage` already in place for a freshly-set target, correctly skipping the clear. `ProjectSettingsPage.jsx`'s existing `handleSectionChange` is untouched and still needed — it covers switching tabs *within* Project Settings, where `activePage` never changes, a case this new effect doesn't handle.
  - **Verification.** `eslint`/`vite build` clean, NUL-byte sweep clean. Live via Playwright (25 assertions, 0 console errors): Artist/Song chips confirmed bordered + clickable with correct hover style and tooltip, clicking scrolls to and highlights the right field, typing clears the highlight; base-pool hashtag click → Project Settings → Generator → back to Project Settings via the main nav confirmed no stale highlight (lands on the default/last-manual section instead); tag-sourced hashtag click → Tag Library → Generator → back to Tag Library confirmed the stale search/highlight is gone; same-page Generator field highlight confirmed cleared after a Tag Library → Generator round-trip; manual in-Project-Settings tab switching (Titles → Thumbnail Templates) confirmed unaffected by the new effect.

- **Per-item click-to-navigate + hover border for Hashtags & YouTube Tags output (2026-07-13).** User asked to extend the same click-to-navigate + hover-border treatment Descriptions/Thumbnails already have to individual hashtags/tags, not just the panel title.
  - **Engine rewrite.** `generateHashtags.js` now returns `{hashtags: Entry[], youtubeTags: Entry[]}` (`Entry = {text, source}`) instead of joined strings. Rewrote around a shared `dedupEntries(entries, max)` helper (first-occurrence dedup + cap) — verified behaviorally identical to the old `[...new Set(arr)].filter(Boolean).slice(0, max)` via a 15-assertion Node smoke test against real `projects.json` data, including confirming the YouTube-tags branch's redundant inner `Set` was safe to drop (the final dedup already fully covers it — same membership, same order). Four `source` origins, identified by reading the generation logic itself: `{type:'base', card:'hashtagsBase'|'youtubeTagsBase', phrase}` (project preset pool), `{type:'tag', tagName, phrase}` (per-tag `hashtags` array entry, `phrase:null` for the bare-tag-name fallback), `{type:'override'}` (per-song `customHashtags`), and `source: null` (artist/song-derived, no source, plain text — same convention Titles/Thumbnails already use). Note: hashtags fall back to the tag's own name only when its `hashtags` array is empty, but YouTube tags include the bare tag name *unconditionally* on top of any configured phrases — a genuinely different rule between the two, both preserved exactly.
  - **New shared `HashtagLink.jsx`** (`src/components/output/`), reused by both `HashtagsPanel.jsx`/`YouTubeTagsPanel.jsx` (same "one component, multiple call sites" reasoning as this session's `SortableActiveBlock`), modeled directly on `DescriptionLineLink`/`ThumbnailNavLink`'s branch-on-`source.type` pattern. New `.hashtag-chip-link` CSS — inline-block, transparent border, amber border+glow on hover (same tokens `.description-block-link` uses, sized to a short token instead of a full-width paragraph block, since many short items flow inline in one wrapped line here, unlike Long description's paragraph blocks).
  - **Three nav targets, one genuinely new.** (1) Tag-sourced clicks reuse `onOpenSourceTag`/`openTagLibrarySearch` verbatim, but surfaced a real pre-existing gap: `TagEditor.jsx`'s `getInitialTab` never checked `sourceTarget.field === 'hashtags'` even though `'hashtags'` was already a registered `TAG_FIELD_TABS` entry — the tab auto-switch silently stayed on `'basics'`. Fixed with one added branch. (2) Base-pool clicks needed a new `hashtagsTarget`/`openHashtagsSearch`/`clearHashtagsTarget` triple in `useAppShellState.js`, built as an exact mirror of `thumbnailsTarget`'s three pieces, threaded through `App.jsx` → `GeneratorPage.jsx`/`ProjectSettingsPage.jsx` → `ProjectSettingsContent.jsx` → `ProjectSettingsHashtags.jsx` (gained `hashtagsTarget` prop + `highlightText` on its two base-pool cards, same pattern `ProjectSettingsThumbnails.jsx` uses for `words`/`fallbacks`). (3) Per-song custom-hashtag clicks reuse `openSongOverride({blockKey})` verbatim — already fully generic, zero new state — just needed a landing spot: `FormField.jsx` gained an optional `id` prop, the "Additional Hashtags" field got `id="song-override-customHashtags"` + the existing `tag-section--highlight` class (confirmed generic box styling, not `<details>`-specific, safe to reuse on a plain div) + its own small scroll-into-view `useEffect` in `AdvancedDescriptionFields.jsx` (separate from `SongBlockOverrideFields`'s own effect, since this field lives outside that subcomponent) + highlight-cleared-on-edit, matching `updateOverride`'s existing "consumed once edited" behavior.
  - **Copy footer fix.** `GeneratorResultsPanel.jsx`'s `renderCopyFooter` updated to join the new array shape (`.map(e => e.text).join(' ')`) for its `{hashtags}` placeholder substitution.
  - **Verification.** `eslint`/`vite build` clean, NUL-byte sweep clean. Live via Playwright (23 assertions, 0 console errors): hover border/tooltip on real-source chips vs. plain text on dynamic ones (confirmed via computed style + `title` attribute); base-pool clicks landing on and highlighting the correct card (hashtags vs. YouTube tags routed to genuinely different cards); tag-sourced clicks landing on the Hashtags tab specifically (confirming the `getInitialTab` fix, both the phrase-backed case and the tag-name-fallback case); per-song custom-hashtag clicks scrolling to and highlighting the Generator's own field, clearing on edit; both panels' Copy buttons still producing the correct joined string (space-separated hashtags, comma-separated YouTube tags).

- **Hashtags & YouTube Tags Project Settings tab (2026-07-13).** User asked to finally build the UI for what was fully hardcoded — the amount of displayed hashtags/tags, wired to the actual outputs.
  - **Config reshape.** `projects.json`'s `hashtags`/`youtubetags` root keys went from bare arrays to `{ base: [...], maxCount: N }` for both projects — `maxCount` defaults (18/18/20 for Illegal Mind hashtags/Maxx Dee hashtags/both projects' YouTube tags) exactly preserve the old hardcoded literals (`MAX_HASHTAGS = 18`, `.slice(0, 20)`) that lived in `generateHashtags.js`, so this is a zero-behavior-change reshape until a user actually edits something. Per-tag `tags.<category>.<tag>.hashtags`/`.excludeFromHashtags` is a separate, already-fully-wired key (Tag Library editor → `resolveTagOverride` → `buildResolvedProjectConfig.js`) — untouched.
  - **Engine.** `generateHashtags.js` now reads `config.hashtags?.base`/`config.hashtags?.maxCount ?? 18` and the youtubetags equivalent (`MAX_YOUTUBE_TAGS`, `?? 20`) instead of the two literals — everything else (per-tag pooling, dynamic artist/song tags, `customHashtags`) unchanged.
  - **Override merge.** `buildResolvedProjectConfig.js`'s `mergeProjectOverrides()` gained a `hashtags`/`youtubetags` nested-merge block, same one-level-deep shallow-merge shape as `thumbnail`'s (`{...projectConfig.hashtags, ...projectSettingsOverrides.hashtags}` — `base`/`maxCount` are both leaf values, no deeper nesting needed).
  - **New UI.** `ProjectSettingsHashtags.jsx`, directly modeled on `ProjectSettingsThumbnails.jsx`'s structure (`updateArray`/`resetArray` writing into `projectSettingsOverrides.hashtags`/`.youtubetags`, `tag-library--3col` grid of `TemplateGroupCard`s): a "Generation" card with two `LabelSliderRow`s (Max Hashtags, Max YouTube Tags), each with its own inline ↺ reset via a small `tag-card-label-row` + `IconButton` (same per-subsection-reset pattern Thumbnails' Patterns card already used for its Long/Shorts halves, since one card here holds two independent leaf fields rather than one); "Base Hashtags"/"Base YouTube Tags" list-editor cards (`templates`/`onUpdateTemplates`/`onReset`, `placeholders={[]}` since these are plain text, not templates). Routed via a new `activeSection === 'hashtags'` branch in `ProjectSettingsContent.jsx`. `projectSettingsSections.js`'s pre-existing summary function updated for the new `.base.length` shape.
  - **No per-item nav at this point** — `generateHashtags.js` still returned plain joined strings with no `source` object per hashtag/tag, so this tab had no `*Target`/highlight-on-click machinery at first. Superseded later the same day — see the per-item nav entry above.
  - **Follow-up — panel-level click-to-navigate.** User asked for the Generator's "HASHTAGS"/"YOUTUBE TAGS" output panel titles to link back to this new settings tab, same as Titles/Thumbnails/Descriptions already do. Traced the existing mechanism first (`CollapsiblePanel`'s `onNavigate` prop → `onNavigateToSettings(sectionId)` → `openProjectSettings` in `useAppShellState.js`, which is already the *minimal* version — just `setActiveProjectSettingsSection(section); setActivePage('projectSettings')`, no target/highlight state at all, unlike the separate `openTitlesSearch`/`openThumbnailsSearch`/etc. functions that back per-item nav). `onNavigateToSettings` was already threaded all the way down to `GeneratorResultsPanel.jsx` (used by the other three panels) — just needed passing to `HashtagsPanel`/`YouTubeTagsPanel` as a new prop, and each panel wiring `onNavigate={() => onNavigateToSettings('hashtags')}` into its own `CollapsiblePanel`. Zero new state anywhere.
  - **Subtitle color fix.** While building the Base Hashtags/Base YouTube Tags cards, the long descriptive subtitle sentences ("Always included, on top of tag-based and per-song hashtags.") read too bold/loud — traced to `TemplateGroupCard`'s `subtitle` prop reusing `.tag-category`, a CSS class also used for short, high-contrast category/section-header labels elsewhere (`TagHeader.jsx`'s tag category badge, several `<details>`/`<summary>` disclosure headers like "Transformation"/"Prefix / Suffix") — genuinely two different jobs sharing one class. Gave the subtitle its own `.tag-card-subtitle` class (same font-size/mono/uppercase, dimmer `--color-text-muted-3` instead of `--color-text-muted-2`) — fixes every `TemplateGroupCard` subtitle app-wide in one place, including the pre-existing ones in Thumbnail Templates (Words/Fallbacks/Generic Tag Templates), not just the new Hashtags cards.
  - **Verification.** `eslint`/`vite build` clean throughout, NUL-byte sweep clean. Live via Playwright against the dev server, in stages: (1) confirmed both cards render real data, no more "Coming Soon"; (2) dragged Max Hashtags to 6, regenerated, confirmed output hashtag count respected the new lower cap; (3) confirmed ↺ reset reverted the slider to 18; (4) added a test entry to Base Hashtags, regenerated, confirmed it appeared verbatim in output, then removed it with zero residue; (5) confirmed both panel titles now render as `panel-title--nav` buttons and correctly navigate to the Hashtags & YouTube Tags tab; (6) confirmed the subtitle color reads as a clear three-tier hierarchy (bright title → medium status line → dim caption) in both Hashtags and Thumbnail Templates. Zero console errors across every pass.

- **Transformation Tags: Ctrl+Click to open in Tag Library + hover tooltip (2026-07-13).** `TransformationTagSelector.jsx` (Generator's Advanced Options tag picker) — Ctrl+Click (or Cmd+Click) a tag chip to jump to it in the Tag Library. Reused the existing `openTagLibrarySearch`/`onOpenSourceTag` navigation mechanism verbatim (already used by Titles/Hooks/Thumbnails to switch page + filter/highlight a tag), no new navigation logic needed. Required threading `onOpenSourceTag` through `App.jsx` → `GeneratorPage.jsx` → `InputForm.jsx` → `TransformationTagSelector.jsx` — it previously only reached the Generator's output side (`GeneratorResultsPanel`), never the input-side tag picker. Every chip also gained a `title` tooltip (`"{category} — Ctrl+Click to open in Tag Library"`) since the shortcut has no other visual affordance. Plain click behavior (toggle tag) unchanged.

- **Drag-and-drop reordering for Active Layout + Block Group children (2026-07-13).** User asked for drag-and-drop in Description Settings' Active Layout, then to extend the same to Block Groups' children list.
  - **Dependency decision.** Chose `@dnd-kit/core` + `@dnd-kit/sortable` + `@dnd-kit/utilities` over hand-rolled native HTML5 drag events — explicitly confirmed with the user first, since this app had zero runtime dependencies beyond React until now. Picked for touch support, keyboard accessibility, and smoother UX; confirmed React 19 compatible (unlike the unmaintained `react-beautiful-dnd`). `npm audit --omit=dev` shows 0 production vulnerabilities after install.
  - **Shared component.** New `SortableActiveBlock.jsx` (`src/components/projectSettings/descriptions/`) wraps `useSortable` and renders a drag-handle (`⠿`) + the existing `MoveControls` + `BlockInfoCard`, unchanged. Listeners/attributes attach only to the handle, not the whole row, so dragging can't hijack clicks on the ↑/↓ buttons or the card's own nav/remove buttons. Both reorder mechanisms coexist everywhere — buttons remain the precise/keyboard-accessible fallback.
  - **Three call sites, one component.** `LongDescriptionSettings.jsx` and `ShortsDescriptionSettings.jsx` (CLAUDE.md already flagged these two as needing to stay in sync structurally — a shared component removes that risk for this piece) each wrap their Active Layout list in one `DndContext`/`SortableContext`, with `handleDragEnd` calling their own existing `updateLayout` via `arrayMove`. `ProjectSettingsBlockGroups.jsx` needed one `DndContext`/`SortableContext` *per group* (each group's children are an independent reorderable list) — `handleChildDragEnd(group, event)` computes `arrayMove` over that specific group's `children` array and patches via the existing `patchGroup`.
  - **Interaction details.** `PointerSensor` uses a 5px `activationConstraint` specifically so a plain click on a button inside the row isn't misread as a drag start. `KeyboardSensor` + `sortableKeyboardCoordinates` gives arrow-key reordering for free, once the handle has focus.
  - **Verification.** `eslint` clean on all touched/new files, `npx vite build` succeeds with the new dependency bundled (~4KB gzip added), NUL-byte sweep clean. Live: dragging with the mouse, the ↑/↓ buttons, nav/remove clicks, and reload-persistence all confirmed working in both Description Settings pages and Block Groups.

- **Saved Library: AI round-trip for missing Year/Genre data (2026-07-12).** Direct follow-up, same day, to "Copy AI Prompt" — the user asked whether pasting an AI's reply back into the app could auto-update the matching songs, and supplied a real 10-song response as a concrete test case.
  - **Parsing + matching.** New `parseMissingDataResponse(text, savedEntries)` in `src/utils/searchQuery.js` parses `Artist - Song: Year, Genre1, Genre2` lines (the exact format `buildMissingDataPrompt` asks the AI to reply in) via a small `parseResponseLine` helper (splits on first `:` then first ` - `, no complex regex needed since no artist/song in the real data contains either separator). Matches each parsed line to a saved entry using new `normalizeForMatch` (lowercase, trim, collapse whitespace, and fold curly quotes `''`/`""` to straight `'`/`"`) — needed because an AI's reply commonly uses typographic apostrophes ("Won't") even when the stored title uses a plain one, confirmed as a real mismatch case in the user's own test data ("I Won't Lie Down"). Deliberately returns only `{ matches, unmatched }` and does NOT decide what to overwrite — that's the caller's job.
  - **Non-destructive apply.** `SavedLibrary.jsx`'s new `handleApplyPastedResponse` owns the "don't clobber real data" policy: for each match, only sets `originalYear`/`originalGenre` if that field is currently empty on the entry, otherwise skips it — same principle as the Import merge fix's `preferNonEmpty`, applied here to a different data-entry path. Verified with a direct case: an entry with a pre-filled year kept it; only its empty genre field got filled from the pasted response.
  - **UI.** New "Paste AI Response" `button-secondary` next to "Copy AI Prompt" (same `hasMissingData` gating) reveals a `textarea` + Apply/Cancel (new `.saved-library-paste-box` CSS, a simple bordered panel). After Apply, shows a result summary (`Updated N songs. M already had data. Couldn't match K lines: ...`) so leftover/unmatched lines are visible for manual follow-up, then clears the textarea.
  - **Prop threading.** `onUpdateEntry` is new on `SavedLibrary.jsx` — threaded through `App.jsx` → `GeneratorPage.jsx` → `SavedLibrary.jsx`, reusing the `handleUpdateEntry` function `useSavedEntries.js` already exposed (previously only wired to the Todo page's `EntrySettings.jsx`).
  - **Verification.** Direct Node smoke tests (bypassing the browser) against the user's real 10-song pasted example — all 10 lines matched correctly, including the curly-quote case — plus the non-destructive-fill regression case above. `eslint` clean on every touched file (one pre-existing, unrelated `no-unused-vars` warning on `GeneratorPage.jsx`'s `handleRegenerate` was surfaced by linting this file for the first time this session — confirmed via `git show HEAD` to predate this change, left alone rather than doing unrelated cleanup).
  - See Known Gotchas in `CLAUDE.md` — `buildMissingDataPrompt`'s requested format and `parseResponseLine`'s parsing must be kept in sync, since a prompt-wording change with no matching parser update would silently send every pasted response to `unmatched`.

- **Saved-entry data loss on Import — full fix, plus Saved/Tag Library and Generator polish (2026-07-12).**
  - **Round 1 bug fix — missing fields.** `handleImportEntries` (`src/hooks/useSavedEntries.js`) — the Saved Library's JSON "Import Library" button, distinct from full app Backup/Restore — hand-builds its own normalized-entry object rather than reusing `handleSaveEntry`'s field list, and was missing `originalYear`, `originalGenre`, `artistShort`, `useCustomArtistShort` entirely. Live `formData` persistence, `handleSaveEntry`/`handleLoadEntry`, and full app Backup/Restore (`appBackup.js`) were all independently verified correct during investigation — not the source of the bug.
  - **Generator form — double-click-to-copy on empty Year/Genre.** New shared `src/utils/searchQuery.js` (`buildSearchQuery(kind, artist, song)`) builds `"{artist} - {song} release date"` / `"{artist} - {song} genre"`. `BasicSongFields.jsx`'s Year/Genre `<input>`s get an `onDoubleClick` handler, active only when the field is empty, that copies the query to clipboard and flashes a small "Copied ✔️" hint (`.field-copy-hint` CSS, new) for 500ms — mirrors `CopyButton`'s existing confirm-flash convention. This is the first double-click handler in the codebase; empty fields get a `title` tooltip for discoverability.
  - **Saved Library — red missing-data badges + filter checkbox.** `SavedEntryRow.jsx` gained an optional `badges` slot (plain ReactNode, same pattern as `middle`/`actions`, rendered inline after the tags/hidden badges in `.saved-entry-main`) — kept as a dumb slot so the shared row component stays free of clipboard logic. `SavedLibraryItem.jsx` renders a small `year`/`genre` badge (built on `IconButton`) for whichever field is empty on that entry; single click copies the same search string via `buildSearchQuery` and flashes "copied ✔️". New `.saved-entry-missing-badge` CSS reuses the existing `--color-*-danger` tokens (same ones `.tag-delete-button` uses) — first pass accidentally inherited native unstyled-button chrome (missing `background`/`border-width`/`display` resets), fixed to a proper small pill matching `.tag-chip`'s sizing/shape. `SavedLibrary.jsx` also gained a "Missing Year/Genre" filter checkbox in the existing filter row, mirroring the hand-rolled markup of the two sibling toggles already there ("Sort by Signal #"/"Hide Queue-Hidden") rather than switching to `ToggleField` mid-row; filters `filteredEntries` to `!entry.originalYear || !entry.originalGenre`. Not persisted (matches "Sort by Signal #"'s behavior in this file; only "Hide Queue-Hidden" persists here).
  - **Round 2 bug fix — the real root cause.** Reported same day after the user actually used the fixed Import for real cross-device syncing: a song's Todo status was wiped after importing a library exported from another device. Root cause was more fundamental than the missing-field gap — `handleImportEntries` replaced any existing entry with a matching id *wholesale* (imported entries placed first, matching existing ones filtered out), so any field that happened to be empty in the imported copy but populated locally got silently erased, even after Round 1's field-list fix. Fixed with a proper non-destructive merge: new `mergeImportedEntry(item, existing)` + `preferNonEmpty(importedValue, existingValue)` in `useSavedEntries.js` — every field merges independently, keeping the existing value whenever the imported one is empty/absent. `songBlockOverrides` is shallow-merged per sub-block key rather than swapped wholesale (an import touching only `storyBlock` can no longer erase an unrelated `logBlock` override). `useCustomArtistShort`/`excludeFromRandomizer` only take the imported value when that key is actually present in the source JSON (`!== undefined`), since `false` is a meaningful explicit value for both, not just "absent." Verified via direct Node smoke tests against the actual `mergeImportedEntry` logic.
  - **Tag Library — 3 columns instead of 4.** `TagLibraryPage.jsx`'s grid now applies the existing `.tag-library--3col` modifier class (`min-width: 400px` cards instead of the default `280px`) — that class already existed and was already used by 4 Project Settings pages' tag grids, just never applied to the actual Tag Library page itself. Pure reuse, no new CSS.
  - **Saved Library — shown/total count.** Header now reads `"Saved Library — {project} ({filteredEntries.length} / {savedEntries.length} shown)"`, live against the active search/filters. New `.saved-library-count` CSS overrides the panel title's uppercase/letter-spacing for just this span so it reads as a subtle count, not another heading.
  - **Original Genre — separator normalization on blur.** `normalizeGenreSeparators` in `BasicSongFields.jsx` converts `/` and `and` separators into `, ` (e.g. `"Skate Punk / Pop-Punk"` → `"Skate Punk, Pop-Punk"`) when the field loses focus. Protects a short exceptions list (`Rock and Roll`, `Rhythm and Blues`, `Drum and Bass`) so genuine "X and Y" genre names aren't split into two. Implementation note for future edits: the protected-phrase placeholder must use a non-whitespace sentinel, not a space-padded one — a space-padded placeholder breaks when the protected phrase lands at the very end of the string, since `.trim()` strips the padding before the restore step can match it. Verified via direct Node tests covering leading/middle/trailing positions.
  - See Known Gotchas in `CLAUDE.md` — `handleImportEntries`'s field list *and* its merge semantics are now documented together so a future new per-entry field gets the same non-destructive treatment, not just a field-list addition.
- **Tag Editor: config-driven phrase tabs + override-merge centralization (2026-07-12).** "Phase 5" of the Custom Placeholders arc, picked up after the deferred rationale turned out to undersell the real duplication (research verified against actual file contents, not guessed — found 4-5 distinct merge implementations, not 2).
  - **Step 1 — fixed a real, currently-shipping bug.** `mergeTagData` (`useTagOverrides.js`, used only by "Sync Tags") only ever spread the *destination* tag for flat scalar fields (`label`/`category`/`visible`/`excludeFromHashtags`/`excludeFromButIts`) — syncing a tag into a project where it didn't exist yet silently dropped the source tag's values for all of these, leaving them `undefined` (visibly: the tag's card would show the raw tag key instead of its real label, per `buildTagExplorerData.js`'s `label: registryTag?.label || tag` fallback). Fixed by spreading `...sourceTag` first as defaults, `...targetTag` second so an *existing* tag's own settings still win — the 5 explicitly-merged array/nested fields are unaffected by spread order either way. Verified via 2 Node smoke-test cases (new tag inherits source's fields; existing tag's own value isn't overwritten).
  - **Step 2 — centralized the override-resolve merge.** `buildTagExplorerData.js` (feeds what the Tag Editor UI shows) and `buildResolvedProjectConfig.js` (feeds what generation actually uses) each independently hand-wrote the identical one-level-deep "override wins" merge for `description`/`shortHooks` — a real drift risk CLAUDE.md's Known Gotchas already worries about for this exact field pair. Extracted into new `src/utils/resolveTagOverride.js`, both files now call it. Deliberately did *not* unify this with `mergeTagData`'s Set-union sync merge — semantically different on purpose (sync combines two tags' content pools, resolve lets the override win). `buildTagExplorerData.js`'s extra `description: undefined`-vs-`{}` guard (a stylistic difference from `buildResolvedProjectConfig.js`) was verified not load-bearing for any of today's real data (every tag in both projects' registries already has a `description` key) and kept local as an explicit `hasDescriptionSource` boolean rather than baked into the shared function, so the function doesn't diverge from the generation-critical path's simpler always-`{}` behavior. Verified via a before/after diff — bundled the pre-refactor and post-refactor versions of both functions, ran each against real `projects.json` data for both projects × 4 synthetic override fixtures (none/scalar-only/nested-only/brand-new-tag), diffed the serialized output. `buildResolvedProjectConfig.js` was byte-identical on every case. `buildTagExplorerData.js` had exactly one difference, confirmed harmless: `isRegistryDriven` changed from a JS `&&`-chain artifact (returns the raw `description` object, not a boolean, when all three checks pass) to a real boolean — grepped the whole repo, this field has zero consumers anywhere.
  - **Step 3 — config-driven phrase tabs.** The Tag Editor's 4 phrase-based tabs (Titles/Descriptions/Short Hooks/Hashtags) were 4 near-identical hand-written components wrapping one already-generic building block (`TagPhraseEditor.jsx`, confirmed fully data-driven via its `field`/`parentField`/`parentValue` props, no hardcoded field-name assumptions). Extracted their field-lists into `TAG_FIELD_TABS` (`src/utils/tagFieldTabs.js`) and built one generic `TagFieldTab.jsx`, deleting `TagTitlesTab.jsx`/`TagDescriptionsTab.jsx`/`TagShortHooksTab.jsx`/`TagHashtagsTab.jsx`. `TagBasicsTab.jsx` (discrete fixed fields — Label/Category/2 toggles/action buttons, not a phrase list) and `TagEditorTabs.jsx`'s 5-tab list stayed untouched — deliberately out of scope, nothing today needs a 6th tab. Field names preserved exactly (`title`, `thumbnail`, `technical`/`log`/`status`, the 6 hook types, `hashtags`), so all 4 external click-to-navigate call sites that build `sourceTarget` objects (`ThumbnailsPanel.jsx` → `{field:'thumbnail', phraseText}`, `GeneratedTitle.jsx`/`DescriptionsPanel.jsx`/`ShortHookTitles.jsx` → `{hookType, hookText}`) and `TagEditor.jsx`'s `getInitialTab` needed zero changes. New `isSourceMatch(sourceTarget, entry)` (`tagFieldTabs.js`, exported separately from `TagFieldTab.jsx` per the `react-refresh/only-export-components` lint rule) matches both real `sourceTarget` shapes uniformly across all 4 tabs — verified no false-positive matches exist given the real field/hookType vocabulary, confirmed via 6 smoke-test cases. A real, easy-to-miss regression risk caught during design: `TagTitlesTab.jsx`/`TagHashtagsTab.jsx` rendered with no wrapper div, `TagDescriptionsTab.jsx`/`TagShortHooksTab.jsx` wrapped in `<div className="tag-editor-nested-section">` (a real visible left-border + indent, `src/index.css:1590-1594`) — preserved via `entries.some(e => e.parentField)`, which happens to align exactly with today's per-tab wrapping. Short Hooks' search box + `buildHookPlaceholders(projectConfig)` autocomplete stay scoped to that one tab via an opt-in `searchable` prop, no new capability leaking into the other 3. `TagHashtagsTab`'s old top-level `tag.hashtags` read switched to `tag.maps.hashtags` — confirmed both were already `registryTag.hashtags || []` in `buildTagExplorerData.js`, byte-identical value, just makes the previously-dead `tag.maps.hashtags` field a real, exercised field consistent with the other 3 tabs. Verified live: title-sourced and hook-sourced click-navigation both still highlight correctly, search still filters, editing both a nested field (`description.log`) and a flat field (`hashtags`) still persists.
  - **Found but deliberately left alone:** a 5th, currently-harmless merge implementation (`src/utils/tagRegistry.js`'s `getResolvedTagRegistry`, a flat shallow merge used only to filter the tag selector by `.visible` — not buggy for its one caller, out of this phase's stated scope); `buildTagExplorerData.js` re-merging already-resolved `tagOverrides` a second time on top of `resolvedProjectConfig` (which already applied them) — idempotent, harmless, just worth knowing when reading the Step 2 diff; `isRegistryDriven` being dead code (computed, never read elsewhere — not removed in this phase).
- **Broadcast Block + Log Notes become real Block Groups, Custom Placeholders Phase 4 shipped then superseded (2026-07-12).**
  - **Phase 4 (shipped first, then replaced):** registry-ified `{fileId}`/`{operatorStatus}` in `placeholders.js` (mirrors `{logNote}`'s precedent; `{operatorStatus}` got a self-recursion guard since its pool is user-editable via Blocks → Hook Blocks). Added a `tagStatusLine` Custom Placeholder to Illegal Mind (Maxx Dee has no Broadcast Block at all — confirmed via its `layout` array). `generateDescriptions.js` composed `broadcastBlock` via `[resolveHookBlockOutput('broadcastHeader', ctx)?.text, resolveCustomPlaceholder('tagStatusLine', ctx, statusLineCount)].filter(Boolean).join('\n')`, with `statusLineCount` read dynamically from `hookBlockCounts.statusLines` rather than a static placeholder count — deliberately, so an existing "Broadcast · Status Lines" Lines-slider customization wouldn't silently reset (confirmed live in the app it was still at the default of 2 before this shipped). `generateBroadcastBlock.js` deleted. Verified via 14 Node/esbuild assertions against real `projects.json` data for both projects, including a simulated `hookBlockCounts.statusLines: 4` override producing exactly 4 status lines.
  - **The redesign:** user pushed back on the hand-written join still being "hardcoded" and found the real fix — Groups don't need a new child type to hold a Custom Placeholder. A Hook Block whose *only* template is `{custom.tagLogLine}` (or `{custom.tagStatusLine}`) can be an ordinary Group `{type:'block'}` child: "drop this candidate if the placeholder resolves empty" is exactly correct when the template has nothing else to lose, unlike embedding the token inside a wrapper template that also holds other content (the original reasoning for why a `{type:'generated'}` child type seemed necessary). Added two new one-template Hook Blocks — `tagLogLineWrapper` (both projects, template `{custom.tagLogLine}`) and `tagStatusLineWrapper` (Illegal Mind only, template `{custom.tagStatusLine}`) — and two new real `blockGroups` entries reusing the existing layout keys: `logBlock` (both projects, `scope:'song'`, children `logFormat` + `tagLogLineWrapper`) and `broadcastBlock` (Illegal Mind, `scope:'project'`, children `broadcastHeader` + `tagStatusLineWrapper`). `generateBlockGroups` (`generateCustomBlocks.js`) gained one small, generic, backward-compatible addition: an optional `groupCtxOverrides` param (keyed by group key, defaults to `{}`) — needed because Log Notes' wrapper references `{tagLine}`, which must resolve via `buildTagLine`'s result (the raw `tagLine` var), not `buildTagPhrase`'s (what the shared `ctx.tagLine` holds for every other group) — the two are genuinely different values, confirmed by reading `descriptionTagHelpers.js`. `generateDescriptions.js` now has **zero hand-written block-composition joins** — `broadcastBlock`/`logBlock` are produced entirely by the Group mechanism, same as `closingBlock` already was; `baseBlocks` no longer sets either key directly. Removed the now-redundant `descriptionLayoutKey: 'broadcastBlock'`/`'logBlock'` from `broadcastHeader`/`operatorStatuses`/`statusLines`/`logFormat`'s hookBlocks entries (same cleanup Closing Block's migration did for `closingSignal`/`philosophyLine`). **Side benefit:** click-nav/tooltip attribution for Broadcast is now more correct than before this session — it previously landed on `statusLines` via a last-wins collision quirk (3 hookBlocks entries sharing one `descriptionLayoutKey`, no self-named entry to prefer); now the Group correctly wins attribution, same precedent `blockGroupMaps` already established for Closing Block. Re-verified via 21 Node/esbuild assertions against real data for both projects (correct Group attribution, override still routes to the Generator field not the Group, no token leaks, no duplication — `FILE_` appears exactly once — `{tagLine}` still resolves via the correct source, randomness intact across 10 regenerations, Maxx Dee fully unaffected) plus live browser confirmation.
  - **Follow-up 1 — Groups' child rows gained click-to-navigate.** `BlockInfoCard` already supported an `onNavigate` prop (clicking the title jumps to the block's own editor, `→` arrow indicator) — it was just never wired for Group children. Added via the same `resolveBlockSource` dispatcher `LongDescriptionSettings.jsx`'s nav arrows already use, routed through the existing `openBlocksEditor` (newly threaded `ProjectSettingsContent.jsx` → `ProjectSettingsBlocks.jsx` → `ProjectSettingsBlockGroups.jsx` — previously only reached the Descriptions tab). Same-page navigation (Groups sub-tab → Hooks sub-tab, both within Blocks), confirmed harmless since `openBlocksEditor` re-setting `activePage` to the page you're already on is a no-op.
  - **Follow-up 2 — Illegal Mind's "Log · Format" pool gained 2 more wrapper-text variations** (`[SIGNAL_LOG]`/`[ARCHIVE_ENTRY]` openers, matching `broadcastHeader`'s existing multi-variant style) after the user noticed it only ever showed one fixed wrapper — it was always a genuine Hook Block (a pool), just happened to have only one template so far, unlike Broadcast Header which already had several. Verified all 3 variants rotate correctly across 20 regenerations.
- **Custom Placeholders — Tag Short Hooks autocomplete (2026-07-12).** Closed out Phase 2's deferred piece: threaded `projectConfig` through `TagLibraryPage.jsx` → `TagCard.jsx` → `TagEditor.jsx` → `TagShortHooksTab.jsx` (none previously needed it) so `buildHookPlaceholders(projectConfig)` works there too, same as the Hook Blocks/Text Blocks editors. Phase 2 is now fully complete.
- **Custom Placeholders — Phases 0-3 (2026-07-12).** New engine capability: user-creatable `{custom.<key>}` placeholder tokens, usable in any template alongside `{artist}`/`{tags.*}`/etc. Full design + verification in `C:\Users\Max\.claude\plans\thatnk-you-i-have-ticklish-fern.md`.
  - **Phase 0 (pure refactor, zero behavior change):** the same "pool candidate strings, fill placeholders, filter empty, pick N" logic was duplicated 4 separate times — `resolveHookBlockOutput` (Hook Blocks), `generateBroadcastBlock.js` (tag status pool), `generateTechnicalBlock.js` Stage 2 (remainder pool), `generateShortHooks.js` (already-resolved hook pool). Extracted into new `src/engine/pooling.js` (`pickRandomLines` + `resolvePooledOutput`) and `src/engine/hookBlockLookup.js` (`findHookBlockByKey` + `resolveHookBlockTemplates`, moved verbatim) — both deliberately dependency-free (`resolvePooledOutput` takes `fillPlaceholders` as a parameter, not an import) so `placeholders.js` can import them without a circular dependency. All 4 call sites migrated, signatures unchanged, output byte-identical — verified via regeneration, no observable difference.
  - **Phase 1 (data model + resolver, no UI yet):** `resolveCustomPlaceholder(key, ctx)` added directly inside `placeholders.js` (same precedent as `{transformation}`'s `resolveTransformationToken` — needs to call `fillPlaceholders` on each pooled candidate, so it can't live in a separate importing-back file). Wired into `DYNAMIC_PATTERNS` as `{custom.<key>}`. Config shape mirrors `blockGroups`/`customBlockGroups`/`blockGroupOverrides` exactly: `description.placeholders` (JSON defaults) + `description.placeholderOverrides[key]` (per-key patch) + `description.customPlaceholders` (user-created array), merged in `buildResolvedProjectConfig.js` with a block structurally identical to the existing `blockGroups` merge. A placeholder's `source` is `{ tagParentField?, tagField?, hookBlockKey? }` — `tagParentField`/`tagField` mirrors `TagPhraseEditor`'s own prop names (pools that field across every selected tag), `hookBlockKey` merges in an existing hook block's template pool via the already-existing `resolveHookBlockTemplates`. Unknown/stale keys resolve to `''` (not `undefined`) so a stale `{custom.x}` token behaves like a legitimately-empty pool (drops its candidate template) rather than leaking literal token text into output. No collision-namespace checking needed against hookBlocks/customBlocks/blockGroups — placeholder keys are addressed as `{custom.<key>}` tokens, a genuinely separate namespace. Verified via Node + esbuild smoke tests (both the resolver and the config-merge logic — 17 assertions total) before any UI existed.
  - **Phase 2 (UI):** new `ProjectSettingsPlaceholders.jsx` (Blocks → Placeholders, 5th `BLOCK_TYPE_SUBTABS` entry) — mirrors `ProjectSettingsBlockGroups.jsx`'s shell but simpler: Tag field source dropdown (one combined dropdown covering `description.technical/log/status` + `shortHooks.<hookType>`, not two cascading selects), optional Hook Block pool dropdown, plain Count input. `BlockEditorCard`'s Scope/Target selects made conditional (`onScopeChange`/`onTargetChange` now optional) since placeholders have neither concept — existing callers unaffected. `hookPlaceholders.js` gained `buildHookPlaceholders(projectConfig)` (static `HOOK_PLACEHOLDERS` export unchanged) — wired into Hook Blocks editor, Text Blocks editor, and Song Override fields (`AdvancedDescriptionFields.jsx`) so `{custom.*}` shows in autocomplete there. **Tag Short Hooks editor's autocomplete deliberately deferred** — `TagShortHooksTab.jsx` doesn't receive `projectConfig` and the chain to thread it through (`TagLibraryPage.jsx` → `TagCard.jsx` → `TagEditor.jsx` → `TagShortHooksTab.jsx`) needs 3 new props in components that don't otherwise need `projectConfig` — bigger than the rest of Phase 2, punted to a future pass. **Found and fixed a real, unrelated pre-existing bug while testing:** the lock/unlock (`isCore`/`onToggleCore`) button only ever worked for List/Text blocks (`StructuredListEditor.jsx`/`TextBlockEditor.jsx`) — Hook Blocks and Block Groups' user-created entries had a visibly rendered lock icon wired to nothing. Fixed consistently across Hook Blocks, Groups, and the new Placeholders tab (isCore stored on the entry itself in `customHookBlocks`/`customBlockGroups`/`customPlaceholders`, toggle + delete-guard added to all three).
  - **Two real pre-existing placeholder-resolution bugs found and fixed while testing Phase 2, unrelated to Custom Placeholders specifically (would affect `{artist}`/`{song}`/anything):** (1) Log Notes' song override (`resolveLogNoteToken`, `placeholders.js`) returned the raw override text verbatim (`if (override) return override;`) instead of running it through `fillPlaceholders` — any placeholder typed into that override field showed up literally unresolved in generated output. Fixed with a self-recursion guard (`_resolvingLogNote`, same pattern as `{transformation}`'s guard) for the pathological case of `{logNote}` referencing itself. (2) List block items (`renderStructuredBlock`, `generateCustomBlocks.js`) never resolved any placeholder in `item.text`/`item.label`/`block.title` at all — only `item.link` got partial substitution (and only for `{links.*}`, via a separate regex). Affected every List block, project-level or song-overridden (Gear Used, Playlists, Support Block), not just new Custom Placeholder usage. Fixed by threading `ctx` (`{formData, projectConfig, tagLine}`) into `renderStructuredBlock` (signature change, 4 call sites updated: 2 in `generateCustomBlocks.js`'s `renderCustomBlock`, 1 in `generateCustomBlocks.js`'s `generateCustomBlocks` for `supportBlock`, 1 in `generateShortDescriptions.js`) and resolving each item through `fillPlaceholders`. Both verified via Node smoke tests (8 assertions).
  - **Phase 3 (migrate Log Notes, delete `generateLogBlock.js`):** found the `tagLogLine` (per-tag `description.log`, pooled across selected tags) piece can't simply be embedded as `{custom.tagLogLine}` inside the existing "Log · Format" wrapper templates — both projects have only ONE wrapper template each, so embedding the token would make the *entire* wrapper vanish whenever a selected tag has no log phrases (any placeholder resolving empty drops its whole candidate template, the existing global rule), and adding a second token-free fallback template would introduce ~50% *random* omission of the tag line even when one is genuinely available — a real regression from today's "always append when present" behavior. Kept the wrapper and the tag line as two independently-resolved pieces, joined in `generateDescriptions.js` (`[resolveHookBlockOutput('logFormat', logCtx)?.text, fillPlaceholders('{custom.tagLogLine}', logCtx).text].filter(Boolean).join('\n')`) — byte-identical output to before, no randomness introduced, `generateLogBlock.js` (27 lines of bespoke per-tag pooling) still fully deleted since the tag-line pooling itself now goes through the shared `resolveCustomPlaceholder` machinery instead of its own one-off flatMap. One subtlety caught before shipping: `generateLogBlock.js`'s internal ctx used `buildTagLine`'s result for `{tagLine}` (used by Illegal Mind's wrapper), but the rest of `generateDescriptions.js` uses `buildTagPhrase`'s result for the shared `ctx.tagLine` — a genuinely different value (random 3-tags-with-fallback vs. deterministic comma-joined-all). Naively reusing the shared `ctx` would have silently changed what `{tagLine}` resolves to; a separate `logCtx = { ...ctx, tagLine }` (the real one) preserves it exactly. Verified end-to-end against **real `projects.json` data for both projects** (not synthetic fixtures) via a Node + esbuild smoke test — 16 assertions covering: tag-with-log-lines (wrapper + tag line both present), no-tags-selected (wrapper-only, no dangling artifact), song override (replaces `{logNote}` only, tag line still independently appended), tag-with-no-log-lines (no blank-line artifact).
- **Block Group Phase 1 follow-up: Add Child search, Log Notes override-nav fix, unified clearable search inputs** (2026-07-11) — three small fixes done right after Phase 1 shipped. (1) Groups tab's "+ add child" list (`ProjectSettingsBlockGroups.jsx`) was a flat unfiltered list of every addable block (~14-15 rows per project) — extracted a new `AddChildList` subcomponent (owns its own `searchText` state, since it renders inside a `blockGroups.map()` and can't call `useState` directly there) that filters by `getBlockKeyLabel(key)`, mirroring `HookTemplateEditor.jsx`'s existing search pattern exactly (same `form-input` class, same lowercase-includes filter). (2) Real bug: clicking a generated Log Notes line with an active song override navigated to Project Settings → Blocks → Hook Blocks instead of back to the Generator's own Advanced Options override field. Root cause: `generateDescriptions.js`'s `overridden` flag had a hardcoded `blockName !== 'logBlock'` exclusion, added when the override-click-nav feature first shipped (see the "song-overridden description blocks" entry below, Round 1) on the reasoning that Log Notes' override only fills `{logNote}` inside a still-pool-picked template — but that reasoning doesn't hold from the user's perspective: the override does meaningfully change the visible output, so it should route to where it's actually set, same as every other song-scoped block. Fixed by removing the exclusion — `isSongOverrideActive(songOverrides, blockName)` alone now decides `overridden` for every block, no special case. (3) User noticed Tag Library → Short Hooks' search box has a native "×" clear button that most other search boxes in the app lack — turned out to be nothing but the native browser behavior of `<input type="search">` vs the default `type="text"`; audited every search-filter input in the app (7 total) and added `type="search"` to the ones missing it (`TagControls.jsx`, `SavedLibrary.jsx`, `LinksRegistryEditor.jsx`, `ProjectSettingsLists.jsx`, `ProjectSettingsTextBlocks.jsx`, `HookTemplateEditor.jsx`, plus the new Groups search from (1)) — no new component or CSS, purely a one-attribute-per-file addition matching the two that already had it (`TransformationTagSelector.jsx`, `TagShortHooksTab.jsx`).
- **Block Group — Phase 1: generic mechanism, validated on Closing Block** (2026-07-11) — first of three phases from the approved plan (`C:\Users\Max\.claude\plans\you-write-too-much-spicy-crayon.md`); see that file for full design rationale. Data model: `description.blockGroups: [{ key, label, scope, target, children: [{ type: 'block', key }] }]` — JSON-default groups live in `projects.json` (mirrors `hookBlocks`), edited via a new `blockGroupOverrides[key]` patch map, user-created ones via a new `customBlockGroups` array (mirrors `customHookBlocks`) — both merged in `buildResolvedProjectConfig.js`, deliberately never written to `description.blockGroups` directly since that field is only ever shallow-spread. Engine: new `resolveLayoutKey`/`generateBlockGroups` in `generateCustomBlocks.js` — `resolveLayoutKey` is the old inline per-key resolution logic from `generateDescriptions.js`'s layout loop, extracted so both the top-level loop and group-children resolution can't drift apart; `generateBlockGroups` resolves each group's children through it and joins tight with `\n`. `{num}` (used by Closing Signal) is now set once via `ctx.overrides.num` in `generateDescriptions.js` instead of the old post-hoc `.replace()`, so it resolves correctly wherever a template using it renders, including standalone as a Group child. `resolveHookBlockOutput`/`resolveHookBlockTemplates` (`generateCustomBlocks.js`) gained a "prefer the hook block whose own `key` matches" lookup (via new `findHookBlockByKey`) before falling back to the existing `descriptionLayoutKey ?? key` match — this is what lets a Group child address one specific sibling (`closingSignal`) even when another sibling used to share its `descriptionLayoutKey` (`philosophyLine`, both previously `'closingBlock'`); same self-named-entry precedent `buildHookBlockMaps` already established for `logBlock`. Nav/tooltips: new `buildBlockGroupMaps` (`descriptionLayout.js`) — `resolveBlockSource` checks it *before* the hook-block-layout-key branch (a Group can legitimately reuse an existing layout key, as Closing Block does), and both label resolvers (`makeLayoutLabelResolver`, `makeBlockKeyLabelResolver`) now check a `blockGroupLabelMap` with top priority so a Group's own label wins over its children's. Exclusivity: `LongDescriptionSettings.jsx`/`ShortsDescriptionSettings.jsx` exclude any `blockGroups[].children[].key` from their independently-addable Available lists (a block assigned to a Group is no longer independently placeable at the top level, though still fully editable wherever it normally lives — e.g. the Hook Blocks tab). New UI: `ProjectSettingsBlockGroups.jsx` (Blocks → Groups sub-tab, auto-registered via `BLOCK_TYPE_SUBTABS.group`) — same JSON-default-vs-dynamic split as `ProjectSettingsHookBlocks.jsx`, reuses `BlockEditorCard`/`BlockInfoCard`/`MoveControls`/`AddBlockForm` (no new primitives). `resolveCustomBlockCollisions` (self-healing collision repair) and all "Add block" forms' `existingKeys` now also include `blockGroups` keys — a 4-way collision namespace instead of 3. Migrated `closingBlock` in `projects.json` (Illegal Mind only — Maxx Dee has no closing content) to a Group reusing the existing `'closingBlock'` layout key (no saved-layout migration needed); removed the now-redundant `descriptionLayoutKey: 'closingBlock'` from `closingSignal`/`philosophyLine`'s hookBlocks entries (the Group now owns that layout key; those two are self-named going forward, and the exclusivity filter — not layout-key coincidence — is what now keeps them out of the top-level Available list). Verified: a direct engine smoke test (Node + esbuild bundle, bypassing the browser) confirmed the Closing segment joins both children with `{num}` filled and reports `source: { blockType: 'group', blockKey: 'closingBlock' }`, and that Maxx Dee (no `blockGroups`) is byte-for-byte unaffected. Then verified live in the running dev server via a throwaway Playwright script (installed to the session scratchpad only): Blocks → Groups shows one "Closing" card (2 children, reorder/remove controls, correct "+ add child" picker excluding both current children); Hook Blocks tab still lists `Closing · Signal`/`Closing · Philosophy` independently editable; Descriptions → Long layout shows exactly one "Closing →" card (not two) and clicking it navigates to and auto-expands the Groups tab's card; generated output contains both `[SIGNAL ...]` and `// ...` lines under one segment whose tooltip reads `"Blocks → Groups → Closing"`; zero console errors throughout. `npx vite build` and full-project `eslint` both clean (pre-existing unrelated lint errors in untouched files noted, not introduced by this work).
- **Exposed the hidden `templates.long.logBlock` wrapper template + relabeled the misgrouped Tag Line blocks** (2026-07-10) — follow-up question after the collision-overwrite fix above: user asked whether the "Modification: ..." line in a Log Notes segment was hardcoded, and why clicking an overridden Log Notes segment still pointed at the Hook Blocks "Log · Notes" card. Traced precisely: `generateLogBlock.js` joins two independently-sourced pieces — (1) `baseLogBlock`, a wrapping template (`templates.long.logBlock`, e.g. `"Production Note:\n {logNote}"` for Maxx Dee, `"[LOG_DATA]\nReconstruction Phase: {tagLine}.\nOperator Note: {logNote}"` for Illegal Mind) with `{logNote}` filled from either the song override or the "Log · Notes" pool (`templates.long.logNotes`); and (2) `tagLogBlock`, a random line from the *selected tag's own* `description.log` array (e.g. the "Hardcore Punk" tag's `"Modification: Signal pushed into hardcore punk intensity."`) — **not hardcoded**, editable per-tag in Tag Library → [tag] → Descriptions tab, just not yet attributed by the description tooltip (the known, documented block-level-not-per-line limitation). The wrapping template (1), however, *was* genuinely hardcoded — no hookBlocks entry anywhere referenced `templateKey: 'logBlock'`, so it had zero UI exposure; the existing "Log · Notes" card only ever edited the fallback pool (`logNotes`), never the wrapper — which is why the wrapper's content ("Production Note:" prefix, `{tagLine}` for Illegal Mind) was invisible and uneditable regardless of override state. Fixed: added a new hookBlocks entry per project — `{ key: 'logFormat', label: 'Log · Format', path: 'long', templateKey: 'logBlock' }` — making the wrapper a normal editable Hook Block. No `descriptionLayoutKey` given (deliberately not joined to the `'logBlock'` collision group — it has no layout slot of its own, same category as the Tag Line blocks below; navigation for Log Notes segments is unchanged, still points at "Log · Notes"). Separately, confirmed `tagLineFallbacks`/`tagLineTemplates` (feeding the shared `{tagLine}` placeholder used across many templates, not a standalone paragraph — correctly absent from the Descriptions layout builder, by design) were mislabeled "Log · Tag Fallbacks"/"Log · Tag Templates" despite having nothing specifically to do with Log Notes — relabeled to "Tag Line · Fallbacks"/"Tag Line · Templates" in both projects. Config-only change (`projects.json`), no code touched.
- **Fix: colliding customBlocks silently overwrote hook block output in generation (not just Settings-tab navigation) + auto-repair** (2026-07-10) — follow-up to the key-collision-prevention fix (below): the user reported that "Intro · Hook" (3 templates) never actually varies in generated Long descriptions — always the same static "Hey Wastelanders!" line. Traced to a real, more severe bug than the earlier navigation-orphaning one: `generateDescriptions.js`'s `blocks` object is built as `{ broadcastBlock, introBlock, storyBlock, technicalBlock, logBlock, closingBlock, supportBlock, ...renderedCustomBlocks }` — `...renderedCustomBlocks` is spread *last*, so a `customBlocks` entry whose key collides with any of the explicitly-computed keys **silently overwrites it in the actual `blocks` map used for generation**, not just in Settings-tab display. Since "Intro-block" (a Text block, key `introBlock`, static content) collided with `introHook`'s `descriptionLayoutKey: 'introBlock'`, its static text always won over the Hook Block's 3-template random pick — with zero errors or indication anywhere. (Log · Notes was independently confirmed correct — it reads its own pool with no colliding customBlocks entry; the earlier round's "Log · Tag Templates" issue was a separate, already-fixed Settings-tab label/nav bug, not a generation bug.) Fixed properly this time — user explicitly asked for automatic repair, not another "go delete and recreate it" — via `resolveCustomBlockCollisions(projectSettingsOverrides, hookBlocks)` (`customBlocks.js`): detects any `customBlocks` key colliding with a hook block's `key`/`descriptionLayoutKey`, and for **Project-scoped** ones, renames the key (suffixed, e.g. `introBlock` → `introBlock2`) and moves any matching `blockLabelOverrides` entry to the new key — all in one patch object ready for `updateProjectOverride`. Runs as a `useEffect` in `App.jsx`, keyed on `[projectSettingsOverrides, resolvedProjectConfig, updateProjectOverride]`; self-stabilizes after one repair pass since the next run finds no more collisions. **Deliberately does *not* touch the saved `layout` array** — a colliding customBlocks key can never win its own slot in `layout` (the `dynamicBlockKeys` filter excludes any key already in `defaultLayout`, which every hook-block layout key is), so any `layout` array entry matching the old key unambiguously belongs to the *hook block's* slot, not the renamed customBlocks entry; remapping it would have stolen the hook block's layout position. The renamed block simply becomes available (not auto-added) in the Descriptions layout builder, same as any newly created block. **Song-scoped** colliding blocks are deliberately *not* auto-renamed — returned in a `skipped` list with a `console.warn` instead — because a safe rename here can't also migrate whatever per-song override data might exist across `formData`/every saved entry, which this function has no access to; that case needs a human. A `console.warn` also fires for successful renames, so this stays visible/debuggable rather than being a silent background mutation of the user's saved data.
- **Block system: fixed a real key-collision gap, unified storyBlock/logBlock onto the dynamic hook-block path** (2026-07-10) — user flagged that Project Settings → Blocks / Descriptions / Generator's Advanced Options don't always agree on which blocks exist or where they're reachable, and that some of the Generator's override fields were hardcoded rather than config-driven. Investigation (via a research-only Explore pass, no guessing) confirmed four separate root causes:
  1. **Key-collision gap.** All three "Add block" forms (`ProjectSettingsTextBlocks.jsx`, `ProjectSettingsLists.jsx`, `ProjectSettingsHookBlocks.jsx`) call the shared `generateBlockKey(name, existingKeys)` (`AddBlockForm.jsx` → `customBlocks.js`), but each passed an incomplete `existingKeys` set — Text/List forms only checked `Object.keys(customBlocks)`, never the Hook Blocks layout-key namespace (`key` *and* `descriptionLayoutKey`); the Hook Blocks form checked hook-block `key`s but not `descriptionLayoutKey` either. Confirmed real: a Text block named "Intro Block" generated key `introBlock`, silently colliding with the Hook block `introHook`'s `descriptionLayoutKey: 'introBlock'` — since `resolveBlockSource`/`makeLayoutLabelResolver` (`descriptionLayout.js`) check the hook-layout-key namespace *before* `customBlocks`, and `LongDescriptionSettings.jsx`'s `dynamicBlockKeys` filter excludes any customBlocks key already in `defaultLayout` (which `'introBlock'` is), the Text block became permanently orphaned — editable in Blocks → Text Blocks, but structurally unreachable from the Descriptions layout builder, with no error anywhere. Fixed by building one combined `existingKeys` set (`customBlocks` keys + hook blocks' `key`s + hook blocks' `descriptionLayoutKey`s) in all three callers. (Existing already-colliding data, e.g. the reported "Intro-block", is auto-repaired by a follow-up fix — see the newer entry above.)
  2. **Scope-toggle default mismatch, not a storage-path bug.** Both `ProjectSettingsHookBlocks.jsx`'s Scope dropdown and `AdvancedDescriptionFields.jsx`'s override-field visibility check read/write the identical path (`description.templates.long.phraseBlockScopes[key]`) — confirmed no mismatch there. The real bug: when no explicit scope had ever been set, the dropdown *displayed* `'project'` (its own arbitrary UI default) while the Advanced panel *treated* `storyBlock`/`logBlock` as `'song'` by default (a hardcoded key-name check, "were always song-scoped before phraseBlockScopes existed"). So the two sides silently disagreed until a user explicitly touched the dropdown once.
  3. **`PHRASE_BLOCK_OVERRIDES` was genuinely hardcoded**, exactly as flagged: `storyBlock`/`logBlock` had their own dedicated array + render loop in `AdvancedDescriptionFields.jsx`, explicitly excluded from the generic `songScopeHookBlocks` dynamic loop via a `hardcodedPhraseKeys` set — despite being ordinary `hookBlocks` config entries everywhere else (Hook Blocks tab, Descriptions layout, click-to-navigate already treated them generically).
  4. **"Cover Summary" (stale layout entry) was confirmed as saved-data cruft, not a code bug** — `coverSummaryBlock` has zero references anywhere in current `projects.json`/`src/` (a prior cleanup removed it from JSON defaults + `KNOWN_BLOCK_META`), but a project's already-persisted `projectSettingsOverrides.description.templates.long.layout` array is used verbatim forever, with nothing pruning unknown/stale keys out of it. No code fix — user removes it via the existing "×" button on that card.

  **Fixes shipped for #1–#3** (², ⁴ needed no code): all three Add forms now build the combined `existingKeys` set (see #1). Added an optional `defaultScope` field to hookBlocks config entries in `projects.json` — `storyBlock`/`logBlock` set `"defaultScope": "song"` (both projects, replacing the vestigial unread `"scope": true` legacy flag) since that's their real historical default; every other hook block implicitly defaults to `'project'`. Both `ProjectSettingsHookBlocks.jsx`'s `getScope(key, defaultScope)` and `AdvancedDescriptionFields.jsx`'s `songScopeHookBlocks` filter now resolve scope with the exact same precedence — `phraseBlockScopes[key] ?? defaultScope ?? 'project'` — closing the mismatch generically instead of patching the two hardcoded keys. With that default in place, `PHRASE_BLOCK_OVERRIDES` and `hardcodedPhraseKeys` were deleted outright (net −47 lines in `AdvancedDescriptionFields.jsx`) — `storyBlock`/`logBlock` now flow through the same `songScopeHookBlocks` render loop as any other song-scoped hook block. Verified safe: `overrideKey = block.descriptionLayoutKey ?? block.key` resolves to the same `'storyBlock'`/`'logBlock'` strings the hardcoded path always used, so existing saved `formData.songBlockOverrides` needs no migration; they also gain the reset-to-default icon, the full `HOOK_PLACEHOLDERS` autocomplete (was a narrower hardcoded 3-token list), and the Override Type (textarea/one-line) control — parity gains, not regressions, aside from the Story field's textarea shrinking from a fixed `rows={5}` to the generic path's `rows={3}`.
- **Fix: song-overridden description blocks — misattribution, a real hook-block collision, and click-to-scroll** (2026-07-10) — reported right after the Long description tooltip feature (below) shipped, in three rounds:
  - **Round 1 — override misattribution.** A block with an active per-song override (e.g. Story Block, overridden via the Generator's Advanced Options panel) still tooltipped/linked to "Blocks → Hook Blocks → Story Block," landing on the project-default template card, which has nothing to do with the override text actually shown. Root cause: `resolveBlockSource()` only read *config shape* (is this key a hook/list/text block?), blind to whether a song override had replaced that pool's output. New `isSongOverrideActive(songOverrides, blockKey)` in `generateCustomBlocks.js` mirrors `renderCustomBlock`/`resolveHookOverride`'s exact truthiness. `resolveBlockSource` gained an `overridden` param (default `false` — Long/Shorts *settings* pages' nav-arrow callers don't pass it, since Project Settings always means the project pool); when true it returns `{ type: 'override', blockKey }` — deliberately the *layout* key, not the resolved Hook Blocks config key, since `formData.songBlockOverrides` is keyed by layout key and can differ from a hook block's own config key (e.g. `introBlock` vs `introHook`). Both engines pass this per-segment (`generateDescriptions.js`'s `longDescriptionSegments` loop; `generateShortDescriptions.js`'s text-block and generic-catch-all branches). `logBlock` is excluded — its override only fills the `{logNote}` placeholder *inside* a still-pool-picked template, so the pool stays the accurate source. Shorts' list-block branch didn't apply song overrides at all — fixed 2026-08-29, see Current Focus.
  - **Round 2 — a real config collision the tooltip surfaced.** User reported a *different* mismatch: a `logBlock` segment (label "Production Note") tooltipped to "Log · Tag Templates" instead of "Log · Notes," and the generated text visibly matched a Log · Notes template. Cause: three `hookBlocks` entries (`logBlock`, `tagLineFallbacks`, `tagLineTemplates`) share `descriptionLayoutKey: 'logBlock'`, but `generateLogBlock.js` only ever reads the pool behind the `logBlock` entry (`templateKey: 'logNotes'`) — `tagLineFallbacks`/`tagLineTemplates` are unused by it entirely. `buildHookBlockMaps`'s plain last-one-wins collision resolution was landing on `tagLineTemplates` (array order), a pool with zero relation to the actual content — not just imprecise like `broadcastBlock`/`closingBlock`'s multi-pool merges (those "last wins" picks are at least real contributors), but flatly wrong. Fixed generically: `buildHookBlockMaps` now prefers the hookBlocks entry whose own `key` equals the shared layout key (a "self-named" entry, e.g. `logBlock`'s own entry) when one exists; groups with no self-named entry (`broadcastBlock`, `closingBlock`) keep the unchanged last-one-wins fallback. This also fixed the pre-existing (and previously unnoticed) `LongDescriptionSettings.jsx` nav-arrow for `logBlock`, which shared the same bug — the Descriptions tab's own "Log Block" arrow was silently pointing at the wrong Hook Blocks card before this change too.
  - **Round 3 — user wanted overrides clickable after all.** Initially shipped `type: 'override'` as tooltip-only (no `onClick`) per an explicit choice — but after seeing it live, the user asked for it to actually lead somewhere and highlight it. Since the override lives inline in the Generator's own Advanced Options panel (same page as the description, not Project Settings), "leading" means scroll-to + auto-expand + highlight, not cross-page navigation. New `songOverrideTarget`/`openSongOverride`/`clearSongOverrideTarget` in `useAppShellState.js` (same `{blockKey}`-target shape as `blocksTarget` etc., but same-page — no `setActivePage` call; `openSongOverride` also force-opens `panelVisibility.advanced`). Threaded `App.jsx` → `GeneratorPage.jsx` → both `InputForm.jsx`/`AdvancedDescriptionFields.jsx` (consumer: auto-open + scroll + highlight) and `GeneratorResultsPanel.jsx`/`DescriptionsPanel.jsx` (producer: `onClick` on the `'override'` segment). `AdvancedDescriptionFields.jsx`'s `SongBlockOverrideFields` gained a stable `id="song-override-{key}"` per field across all three of its override-rendering loops (hardcoded `PHRASE_BLOCK_OVERRIDES`, dynamic List/Text `customBlocks`, dynamic `songScopeHookBlocks`), a `useEffect` that does `el.open = true; el.scrollIntoView(...)` when `songOverrideTarget` changes (plain `getElementById` lookup, simpler than threading refs through three separate `.map()` calls), and a `tag-section--highlight` class (new CSS, same amber-glow tokens as `.description-block-link`) applied while that field is the current target. The highlight is "consumed" — cleared — the moment the user actually edits or resets that specific field (`updateOverride`/`clearOverride`), not on a timer.
- **Long description block tooltips + glowing-border hover** (2026-07-10) — extends the Shorts-only source tooltip/click-nav feature (below) to Long descriptions, which previously rendered as one flat unsegmented string. Granularity is block-level (one segment per `layout` entry), not per-line within a merged block — `broadcastBlock`/`technicalBlock`/`logBlock` are each built by `generateBroadcastBlock.js`/`generateTechnicalBlock.js`/`generateLogBlock.js` merging several tag-specific and project-level pools into one multi-line paragraph, and re-architecting those three engine files for per-line attribution was judged out of proportion to the ask — this matches the same granularity `LongDescriptionSettings.jsx`'s existing nav arrows already use, and mirrors how Shorts itself first shipped block-level nav before a later pass added exact-template highlighting. New `resolveBlockSource(blockKey, { hookBlockMaps, customBlocks, supportBlockConfig })` in `descriptionLayout.js` is the single place this block→source resolution now lives — extracted from `LongDescriptionSettings.jsx`'s `getNavigateHandler` (which had it hardcoded) so the engine and the UI's nav arrows can't drift apart; `ShortsDescriptionSettings.jsx`'s near-identical `getNavigateHandler` was refactored onto the same function for the same reason (its `key === 'hook'` → Shorts-hook-pool special case stays first, unrelated to block resolution). `generateDescriptions.js` now builds `longDescriptionSegments` (`{text, source}[]`) in the same `.map()` pass that used to just produce the joined `longDescription` string, so the two can never disagree. `DescriptionsPanel.jsx`'s `DescriptionLineLink` gained a `className` prop (default `'description-line-link'`, Shorts' existing inline style) so Long can pass a new `'description-block-link'` class — same tooltip/label/click logic, since Long segments only ever hit the existing `source.type === 'block'` branch (Long doesn't consume the `shortHooks` pool the way Shorts' "hook" slot does). New CSS: `.description-block-link` is `display:block` with a transparent resting border that turns into an amber glow (`border-color` + `box-shadow`, same `--color-border-hover`/`--color-shadow-accent`/`--radius-terminal` tokens as `.button-primary`/`.tag-used`) on hover — a full bordered box reads better for paragraph-length blocks than Shorts' inline text-shadow-only treatment. `copyText` stays wired to the flat `longDescription` string, unaffected by the segmented display. Known limitation: blocks built from multiple merged hook-block pools with no self-named entry (`broadcastBlock`, `closingBlock` — see "Hook Blocks vs description layout keys" in Architectural Notes below) resolve to whichever pool's key `layoutKeyToBlockKey` picks (last one wins) — identical to what clicking that block's existing nav arrow in the Descriptions tab already did before this change. (`logBlock` no longer has this limitation — see Round 2 above.)
- **Shorts description line tooltips + `BLOCK_TYPE_SUBTABS` bug fix** (2026-07-10) — hovering a generated Shorts description line now shows a native `title=` tooltip describing its source, matching the convention `GeneratedTitle.jsx`/`ThumbnailsPanel.jsx` already use (`Blocks → Hook Blocks → Story Log: "template text"` for block-sourced lines; `{tagName} ({hookType}): "text"` for tag-sourced hooks; `Project preset ({hookType}): "text"` for base-preset hooks). Block-sourced tooltips need a human-readable, override-aware block label — added `makeBlockKeyLabelResolver` to `descriptionLayout.js`, a sibling to the existing `makeLayoutLabelResolver` but keyed by *config* key instead of *layout-slot* key (`generateShortDescriptions.js`'s `source.blockKey` is already resolved to the config key by the time it reaches the UI, so the layout-key resolver's `layoutKeyToBlockKey` translation doesn't apply). `projectConfig`/`projectSettingsOverrides` threaded down through `App.jsx` → `GeneratorPage.jsx` → `GeneratorResultsPanel.jsx` → `DescriptionsPanel.jsx` to build it. Found and fixed a real bug along the way: the click-to-navigate code computed `subTab: source.blockType === 'hook' ? 'hooks' : source.blockType`, sending `subTab: 'list'` for list blocks — but `ProjectSettingsBlocks.jsx`'s actual subtab id is `'lists'` (plural), so a list-block-sourced line's click navigated to Project Settings but matched none of the three subtab branches, rendering an empty Blocks panel. Fixed by extracting the `blockType` → `{subTab, label}` mapping into one shared constant, `BLOCK_TYPE_SUBTABS` in `customBlocks.js` — both `ProjectSettingsBlocks.jsx`'s `SubTabNav` tabs array and `DescriptionsPanel.jsx`'s tooltip/nav logic now derive from it instead of each keeping its own copy.
- **Fix: Hooks toggle duplicated the Shorts suffix into Long titles** (2026-07-09) — reported with a screenshot showing Long titles like `[SIGNAL 11] // Does I Won't Lie Down still hold up? // [SIGNAL 11]` when the Titles panel's Hooks checkbox (mixes shorts hooks into Long title candidates) was on — a trailing `[SIGNAL 11]` leaking in that shouldn't be there. Root cause: `generateShortHooks.js` (line ~86-91) bakes the Shorts `prefix`/`suffix` directly into each hook object's `.text` field before returning the hook pool — correct for its primary consumers (`ShortHooksPanel.jsx`/`ShortHookTitles.jsx` for the Shorts titles candidates panel, Shorts-mode titles in `TitlesPanel.jsx`, and `generateShortDescriptions.js`'s "hook" block, all of which want the Shorts-wrapped text). But `useGeneratedOutput.js` generates this hook pool once and reuses the *same array* as an input to `generateTitles.js`, whose `buildHookTitles()` was also reading `hook.text` and wrapping it a second time with the *Long* prefix/suffix — so a hook-derived Long title ended up as `longPrefix + (shortsPrefix + coreText + shortsSuffix) + longSuffix`, trapping the Shorts suffix inside. Fix: `generateShortHooks.js` now also stores the pre-wrap filled text in a new `rawText` field (`{...hook, rawText: hook.text, text: wrapped}`); `generateTitles.js`'s `buildHookTitles` uses `hook.rawText` instead of `hook.text`. No other consumer changed — they all correctly want the Shorts-wrapped `.text`, unaffected by this fix. Verified with a headless Playwright browser against the running dev server: with Hooks on, no Long title contains a doubled `[SIGNAL ...]` marker; a hook-derived title now reads cleanly as `[SIGNAL XX] // Most underrated Face to Face song? // Illegal Mind Rework` (Long prefix/suffix only); Shorts mode still generates normally; no console errors.
- **Fix: tag `shortHooks`/`description` silently dropped during resolution — reported as "Sync Tags doesn't sync hooks between projects"** (2026-07-09) — user reproduced with the shared "Hardcore Punk" tag showing 0 phrases in every Short Hooks category (Nostalgia/Emotion/Transformation/Discussion/Musician/Progress) in Maxx Dee Covers, despite `projects.json` having real phrases for it there. Investigation started with `syncProjectTags`/`mergeTagData`/`mergeShortHooks` in `useTagOverrides.js` — that logic already unions `shortHooks` per hook-type correctly (verified by extracting and running the merge functions directly against real project data), so the sync action itself wasn't the bug. The actual defect was downstream, in the two places that combine a tag's base `projects.json` config with its stored `tagOverrides` entry for real use: `buildResolvedProjectConfig.js` (~line 91-102, feeds `generateShortHooks.js:35` — this is a real generation-quality bug, not just cosmetic, since it silently narrows the actual Shorts Hook pool used in generation) and `buildTagExplorerData.js` (~line 18-25, feeds `TagShortHooksTab.jsx`'s displayed counts via `tag.maps.shortHooks` — exactly what the screenshot showed as "(0)"). Both did a flat `{...baseTag, ...override}` spread for `shortHooks`, unlike `description` which `buildResolvedProjectConfig.js` already deep-merges explicitly (`buildTagExplorerData.js` had neither merged). In a plain object spread, a key only gets overwritten if it's *present* on the override — so any tag override that happens to carry a `shortHooks` key covering only some hook-types (from a Tag Editor edit, or any override-writing path) would silently wipe every category the override didn't include, both in the editor display and in the resolved config the generator engine actually reads. Fix: both files now deep-merge `shortHooks` per hook-type one level deep — override's key wins if present, base's key is kept otherwise — matching the exact pattern `description` already used in `buildResolvedProjectConfig.js`; `buildTagExplorerData.js` got the same treatment for `description` too, with a regression guard (`baseTag.description || tagOverride.description ? {...} : undefined`) so tags with genuinely no description at all still resolve to `undefined` rather than a falsely-truthy `{}` that would've broken `isRegistryDriven`/`hasMissingMappings`/`existsIn.description`'s existing truthy checks. `title`/`thumbnail`/`hashtags` are flat arrays with no sub-keys (and `TagPhraseEditor`'s `buildUpdate` already writes them as full-array replacements by design), so they correctly stay flat-spread — not touched. `useTagOverrides.js` itself needed no changes. Verified end-to-end with a headless Playwright browser against the running dev server (installed only in the session scratchpad): Maxx Dee's "Hardcore Punk" now shows real counts (Nostalgia 4, Emotion 4, Transformation 6, Discussion 4, Musician 4, Progress 4) matching `projects.json`, with the actual phrase text confirmed by expanding a category; Illegal Mind Covers' version of the same tag still correctly shows all zeros (it genuinely has no base `shortHooks` there — not a regression); and clicking the real Sync Tags button (Illegal Mind → Maxx Dee) leaves Maxx Dee's rich data unchanged before/after, confirming sync no longer wipes it. Related but out-of-scope finding, flagged not fixed: `handleDuplicateTag` (`TagLibraryPage.jsx:62-76`) initializes a duplicated tag's `title`/`thumbnail`/`hashtags`/`description` as empty rather than copied from the source tag, despite this file's changelog previously describing "Tag duplicate button — duplicates a tag's full override data."
- **Titles panel: Uppercase toggle (functional) + Hooks moved to a checkbox** (2026-07-09) — two-round follow-up to user feedback on the Titles panel's options row. Round 1: moved the "Hooks: ON/OFF" pill out of `CollapsiblePanel`'s `headerExtra` header slot into a new `.titles-options-row` `<div>` inside the panel body, above the generated titles list (`TitlesPanel.jsx`); added an "Uppercase" `ToggleField` alongside it. New `titleUppercase` boolean added to `useAppShellState.js` (mirrors `panelVisibility`'s init/persist pattern exactly — reads/writes `illegalMindGeneratorData.ui.titleUppercase`), threaded through `App.jsx` → `GeneratorPage.jsx` → `GeneratorResultsPanel.jsx` → `TitlesPanel.jsx`. Initial implementation applied uppercase via a CSS `text-transform: uppercase` class on the title-list wrapper. Round 2 (user tested it): that only changed the visual — `CopyButton` still copied normal-case text, and the user also asked for Hooks to become a checkbox to match Uppercase visually. Fixed both: `GeneratedTitle.jsx` and `ShortHookTitles.jsx` now accept an `uppercase` prop and do a real `title.text.toUpperCase()` / `hookText.toUpperCase()` transform, used for *both* the displayed label and `CopyButton`'s `text` prop — verified via a headless-browser clipboard read that Copy output actually matches the uppercased display. The CSS-only `.title-uppercase` rule and its wrapper `className` were removed as dead code once the real transform landed. This surfaced a latent bug in `ShortHookTitles.jsx`: its `onOpenSourceTag`/`onOpenSourceHook` click handlers fell back to the display `hookText` (now possibly uppercased) when `hook.sourceText` was empty, which would have fed uppercased text into source-navigation matching — fixed by introducing a separate `rawHookText` used only for those fallbacks, keeping `hookText` (display/copy) and `rawHookText` (navigation) independent. The Hooks pill button was replaced with a second `ToggleField` (static "Hooks" label instead of the old dynamic "Hooks: ON"/"Hooks: OFF" text — the checkbox state itself now communicates on/off); `ToggleField.jsx` gained an optional `title` prop (forwarded to the wrapping `<label>`) so the "Mix shorts hooks into long title candidates" tooltip survived the pill→checkbox conversion. Uppercase applies in both Long and Shorts modes (pure display/copy concern); Hooks stays Long-only (`!isShorts`), unchanged from before. Verified end-to-end with a headless Playwright browser against the running dev server (installed only in the session scratchpad, not added to the project): checkbox presence/absence per mode, real uppercase in DOM text and clipboard, Hooks toggle still mixes shorts hooks into long titles, and `ui.titleUppercase` persists across reload.
- **Exact-template/item highlighting for Shorts description navigation** (2026-07-05) — follow-up to the block-level navigation below: clicking a description line now also scrolls to and highlights the *specific* template/item that was picked, not just the right block editor card. `resolveHookBlockOutput` (`generateCustomBlocks.js`) now returns `{ text, template }` — `template` is the winning template's raw text, only set when exactly one line was picked (no single "winner" when a hook block outputs multiple lines). `renderStructuredBlock` now returns `{ text, pickedItem }` — `pickedItem` only set for `displayMode: 'random'` list blocks (`'all'` mode has no single winner either). Fixed a latent bug this surfaced along the way: the old plain-string return meant `renderStructuredBlock(...)` was always truthy as an object even when its rendered text was empty, so a list block's "did anything render" checks in `generateShortDescriptions.js` were silently always true — now correctly checks the extracted `.text`. All 4 external call sites of these two functions (Long's `generateDescriptions.js`, and 3 internal callers within `generateCustomBlocks.js` itself) were updated to extract `.text`, with zero behavior change for Long since it never consumes the new metadata. `blocksTarget` (`useAppShellState.js`) gained a `highlightText` field, threaded through `ProjectSettingsBlocks.jsx` → `ProjectSettingsHookBlocks.jsx` (as `highlightText`, a string) / `ProjectSettingsLists.jsx` (as `highlightItem`, the raw item object — list items have no stable id, `_id` is a client-only React key stripped before save, so matching is by content equality on `label`/`text`/`link`). `HookTemplateEditor.jsx`'s existing `highlightText` handling now also prefills its search box (`setSearchText(highlightText)`) — collapses long template lists down to just the matching row instead of relying on scroll position alone; this improves the existing Titles/Thumbnails navigation too, not just this new path, since they share the same component. `ListItemRow.jsx` converted to `forwardRef` with a `highlighted` prop (mirroring `PhraseRow`'s exact pattern) — new `.links-editor-row--highlight` CSS matches `.tag-phrase-row--highlight`'s box-shadow/border emphasis. Text blocks are excluded — a Text block is one field, nothing to disambiguate.
- **Shorts description line → source navigation** (2026-07-05) — clicking a line in a generated Shorts description now jumps to whatever block/tag produced it, reusing the exact navigation machinery Titles/Thumbnails already had rather than building new infrastructure. Hover shows a CRT-style glow (no permanent color change, unlike `.generated-pair-link`) via new `.description-line-link` CSS. Long descriptions are untouched — this pass is Shorts-only. Engine: `generateShortDescriptions.js` now returns `{ shortDescriptions, shortDescriptionSegments }` instead of a bare string array — each block gets tagged with a UI-agnostic `source` descriptor (`{ type: 'block', blockKey, blockType }` for list/text/hook blocks, resolved through `buildHookBlockMaps`'s `layoutKeyToBlockKey` for hook-block layout-key-vs-config-key mismatches like `coverLine`→`shortsHeader`; or the hook object itself — `{ sourceType, sourceTag, hookType, sourceText }` — for the `'hook'` block, since `generateShortHooks.js`'s hook objects already carry that shape for Titles and were previously being discarded down to `.text` here). `generateDescriptions.js`/`useGeneratedOutput.js` pass the new field through unchanged. Wiring: `openBlocksEditor` (`useAppShellState.js`) now also calls `setActivePage('projectSettings')`, matching its four siblings (`openTagLibrarySearch`/`openThumbnailsSearch`/`openTitlesSearch`/`openShortHooksSearch`) — previously it only worked when already on the Project Settings page (its only caller, the Descriptions layout tab's block nav arrows); a same-page call is a no-op so existing behavior is unaffected. `onOpenSourceTag`/`onOpenSourceHook`/`onOpenBlocksEditor` threaded from `App.jsx` through `GeneratorPage.jsx` → `GeneratorResultsPanel.jsx` → `DescriptionsPanel.jsx`. UI: `OutputItem.jsx` gained a backward-compatible `children` prop (`{children ?? text}`) so `DescriptionsPanel` can render segmented content instead of a plain string — `copyText` stays wired to the flat joined string, completely decoupled from the segmented display, so Copy output is unaffected. New inline `DescriptionLineLink` in `DescriptionsPanel.jsx` mirrors the existing `TitleNavLink`/`ThumbnailNavLink` per-panel branching pattern (no shared abstraction introduced, matching that precedent) — falls back to a plain inert `<span>` when a segment has no source (the legacy `shortsConfig[blockName]` fallback, which predates the Hook Blocks system and has no dedicated editor). Granularity is per-block, not per-exact-template — clicking opens the right block's editor card but doesn't highlight which specific template text was picked; that's flagged as a follow-up, not this pass.
- **CRT design polish round 3: remaining sizing/spacing fixes + a real CSS bug** (2026-07-05, `src/index.css` + `InputFormActions.jsx` + `UIKitPage.jsx`) — `.button-primary`/`.button-secondary` (Save, Clear Form, Regenerate, Randomize, Sync Tags) gained `font-size: var(--font-size-toggle)`, same gap as round 2's other controls. `InputFormActions.jsx` merged the Save button and Clear Form button onto one row with the project select (was two stacked rows) — `.input-form-actions .tag-actions` gained a scoped `flex-wrap: wrap` fallback for narrow sidebar widths. `.toggle-row` (every `ToggleField` checkbox) got an 8px left inset so it doesn't sit flush with field labels above it. `.subtab-nav button` was silently rendering at the wrong size because it referenced `--font-size-sm`, a token that was never defined in `:root` — repointed to the real `--font-size-toggle` token. `.form-select` gained the same `font-size: var(--font-size-toggle)` fix as round 2's other mono controls (it got the font-family/uppercase treatment then but not the size fix). Two free-text inputs got scoped (not global) size reductions to 0.9rem: `.tag-edit-fields .form-input` (Tag Editor's Label field) and `.tag-phrase-row .form-input` (the shared `PhraseRow` used by both `HookTemplateEditor` and `TagPhraseEditor` — affects Tag Editor phrase lists and Project Settings hook/title template editors identically). `.tag-map-status span` (the "Title: ✓ / Thumbnail: ✓ / Description: ✓" badges in Tag Info) was sharpened + mono'd to match everything else, closing out the last visibly rounded holdout. **Real bug found and fixed:** `.tag-section[open]`/`.tag-editor-section[open]`'s `::before` marker used `content: '\2212 '` (CSS hex escape for U+2212 minus, followed by a space) — per the CSS spec, a whitespace character immediately after a hex escape is consumed as the escape's terminator and never appears in the actual content, so the open-state marker rendered with no trailing space while the closed-state `'+ '` (a literal character, no escape) rendered correctly spaced. Fixed by using the literal `−` character directly instead of the hex escape. `UIKitPage.jsx`'s `tag-chip` section description ("Pill-style toggle") was updated to reflect the sharpened shape — the rest of the catalog's descriptions were already accurate since they're behavior/usage-focused, not visual-detail claims, and UIKitPage renders the real components so restyling shows up automatically.
- **CRT design polish round 2: uppercase-mono controls, disclosure marker unification** (2026-07-05, `src/index.css` + `TemplateGroupCard.jsx` + `BlockEditorCard.jsx`) — further live-review pass after round 1's button/pill sharpening. (1) Uppercase + `--font-mono` applied to `.button-primary`/`.button-secondary`, `.copy-button`, `.tag-chip`, `.tag-filters button`, and `.form-select` (the last one deliberately excludes `.form-input`/`.form-textarea`, which stay sans-serif for free-text legibility) — closes the loop so every discrete-choice/action control reads as terminal chrome, not just nav/panels. Native `<select>` open-list styling is a known, accepted browser limitation (can't restyle OS-native popup chrome without a custom dropdown component — out of scope). (2) `.tag-chip`, `.tag-filters button`, `.tag-status`, and `.copy-button` also gained `font-size: var(--font-size-toggle)` (12px) — all four inherited the base `1rem` and read oversized once made uppercase/mono. (3) `.tag-status` (shared "N phrases"/"N saved"/slider-value badge — used by `TemplateGroupCard`, `TagHeader`, `LabelSliderRow`, `ProjectSettingsHookBlocks`, `BlockEditorCard`) now matches `.tag-category`'s mono/uppercase treatment instead of being plain sans, since the two often stack directly in the same header block. (4) Unified every disclosure widget onto the same `+`/`−` marker convention already used by `ToggleButton`: `.tag-section`/`.tag-editor-section` (native `<details><summary>`, used by `AdvancedDescriptionFields`/`TagEditor`/`TagDetails`/`HookTemplateEditor`/`BulkTextarea`/`TagPhraseEditor`) hide the native triangle marker (`list-style: none` + `::-webkit-details-marker { display: none }`) and add a `::before` `+`/`−` character keyed off the `[open]` attribute; `TemplateGroupCard.jsx` and `BlockEditorCard.jsx`'s custom `▶`/`▼` collapse icons (`.tag-card-collapse-icon`) were swapped to literal `+`/`−` characters for the same reason — no triangle markers remain anywhere in the codebase.
- **CRT design refinement: buttons & pills** (2026-07-05, `src/index.css` only) — reviewing the CRT atmosphere pass holistically surfaced two gaps it deliberately left alone: `.toggle-button` (the "+ SHOW"/"− HIDE" disclosure buttons) had no explicit `font-size`, so it inherited the base `1rem` and read oversized next to 12px mono labels — fixed by adding `font-size: var(--font-size-toggle)` (12px, matching the existing `.toggle-button.compact` variant). Separately, nav tabs, tag filter chips, radio options, transformation tag chips, and primary/secondary buttons all still used `--radius-pill`/`--radius-md` (fully rounded) while panels/cards were sharpened to `--radius-terminal` — now switched to `--radius-terminal` too (`.button-primary`/`.button-secondary`, `.app-backup-controls .button-secondary`, `.radio-option`, `.toggle-button`, `.toggle-button.compact`, `.app-menu-pages button` ×2 rules, `.tag-filters button`, `.tag-chip`) for a consistent sharp/rectangular look app-wide. `.copy-button` (already `--radius-sm`) and `.tag-map-status span` (a non-interactive status badge, not a button) were left as-is.
- **Fallout terminal CRT atmosphere pass** (2026-07-05, `src/index.css` only) — the app's stated visual identity is a Fallout/New Vegas terminal look, but the Tag Library screen didn't read that way: zero CRT atmosphere existed (no scanlines/vignette/glow/flicker), corners were rounded almost everywhere, and the prior day's Input form refresh had deliberately moved UI chrome to sans-serif/lighter borders — pulling away from the terminal feel. Rather than reverting that refresh, this pass adds the terminal identity around it, entirely through global CSS on shared primitives (`.panel`, `.tag-card`/`BlockEditorCard`, `FormField`'s `.form-label`, `CollapsiblePanel`'s `.panel-title`, `AppHeader` nav, `SubTabNav`) so it propagates app-wide with no per-page edits: (1) `body::before`/`::after` fixed, `pointer-events: none` overlay — scanlines via `repeating-linear-gradient` + `mix-blend-mode: overlay`, vignette via inset `box-shadow`, subtle `crt-flicker` keyframe animation disabled under `prefers-reduced-motion: reduce`; (2) monospace (`--font-mono`) reintroduced on headers/nav/labels only (`.app-title`, `.panel-title`, `.form-label`, `.tag-category`, `.tag-card-header h3`, `.tag-selector-label`, `.subtab-nav button`, `.app-menu-pages button`) — inputs/buttons/selects stay sans-serif, unchanged from the prior refresh; (3) new `--radius-terminal: 2px` token applied to `.panel`/`.tag-card`/`.terminal-block`/`.advanced-panel-content`/`.desc-generation-settings` (including the `@media (max-width: 640px)` override) to read as sharp terminal windows, while `--radius-sm`/`--radius-pill`-based buttons/chips/inputs/nav pills stay rounded; (4) terminal HUD corner-bracket accents (`::before`/`::after` two-sided borders) added to `.panel`/`.tag-card` top corners, plus intensified amber `box-shadow` glow on `.button-primary`, `.tag-chip.active`, the active nav tab, and `.tag-used` (composited with the existing inset panel shadow so it doesn't get clobbered). `BlockEditorCard` needed no separate edit since it already reuses `.tag-card` directly.
- **Input form visual refresh** (2026-07-05) — the Generator's Input form read as "bulky/90s programmer UI." Fixed in two batches:
  - *Structural cleanup* (`BasicSongFields.jsx`, `QueueSettings.jsx`, `TodoFields.jsx`, `TransformationTagSelector.jsx`, `FormField.jsx`, `ToggleField.jsx`): removed redundant double `.form-group` nesting (`FormField` already renders its own); replaced two hand-rolled checkboxes with the existing `ToggleField` component — this also fixed a latent bug in `QueueSettings.jsx` where `.toggle-checkbox` was applied to the `<label>` instead of the `<input>`. `ToggleField` itself was changed from uncontrolled (`defaultChecked`) to controlled (`checked`) — it was documented as intentionally uncontrolled with "use `ToggleInputRow` for controlled needs," but `ToggleInputRow` actually requires a text input alongside the checkbox (built for prefix/suffix-style toggles) and isn't a drop-in bare checkbox+label alternative. Nothing depended on the uncontrolled behavior, and `BasicSongFields`' "Use custom artist short" checkbox needs to resync when a different saved entry loads into the same mounted form, so controlled is strictly more correct. `FormField` gained an optional `className` prop (backward-compatible) to let the Artist/toggle/Artist-Short cluster carry a scoped spacing-tightening class without reintroducing a wrapper div. Todo and Transformation Tags converted from native `<details>` to `ToggleButton` + local `useState`, matching the Advanced Options pattern already used in the same form (and `CollapsiblePanel` used elsewhere) — `.tag-section`/`.tag-selector-details` CSS classes kept in place (shared by other components) since only the disclosure *mechanism* changed, not the class names.
  - *Global CSS* (`index.css`) — three changes affecting every page, not just Generator: (1) `.form-label` de-emphasized to 12px normal-case (new `--font-size-field-label` token; existing `--font-size-label` untouched since 6 other rules still use it) so field labels read subordinate to section headers like `.panel-title`. (2) Monospace font removed from the base `button, input, select, textarea` rule (now `--font-ui`) — `.terminal-block`/`.output-text`/`.prompt-block`/`.output-item`/`.links-registry-key*`/`.placeholder-suggestion` all set their own explicit mono font already, so generated output and code-like fields are unaffected; this creates an intentional visual split between "your input" (sans) and "generated output" (mono). (3) `.form-input`/`.form-select`/`.form-textarea` moved from a full 1px box border to a bottom-border-only resting state (`border: 1px solid transparent` + `border-bottom: 1px solid var(--color-border-soft)`) with the full border reappearing on focus (existing `:focus`/`.tag-phrase-row--highlight` rules already set `border-color` directly, so they work unchanged); `.tag-chip` moved from border-based to background-based hover/active states. `.radio-option`/`.toggle-button` borders intentionally left untouched (flagged as a possible fast-follow, not bundled in). Color palette (dark amber/brown) unchanged throughout — this was an execution refinement, not a re-theme.
- **Placeholder engine consolidated into a shared registry, empty-valued lines dropped** (2026-07-04/05) — `src/engine/placeholders.js` replaced three separately hand-maintained `.replace()` chains (hooks, titles, descriptions) with one resolver registry (`fillPlaceholders`/`pickViableTemplate`). Any candidate template/hook/line whose placeholders would render with an empty value (e.g. `{tags.genre}` with no genre-category tag selected, or the `transformationBlock` pool itself) is now dropped entirely before picking — including a special case where `resolveTransformationToken` bypassed this filtering by doing a raw random-index pick, since `{transformation}` fills a *nested* template. Wired into `generateShortHooks.js`, `generateTitles.js`, `generateCustomBlocks.js` (`resolveHookBlockOutput`), `generateBroadcastBlock.js`, `generateTechnicalBlock.js`, `generateLogBlock.js`. `HOOK_PLACEHOLDERS` (UI autocomplete) now derives from the registry's own keys (`REGISTRY_TOKENS`) instead of a hand-typed list.
- **Save-to-project dropdown** (2026-07-04) — `InputFormActions.jsx` can now save the current entry to any project, not just the active one (`handleSaveEntry(targetProjectId)` in `useSavedEntries.js`, defaults to the active project). UI matches `TagSyncControls`' `.tag-actions` pattern (bare select + button, no label) rather than a labeled form-field row — deliberately *not* reusing `TagSyncControls` itself despite the visual similarity, since its prop names/semantics are bound to tag-sync (pulling data between projects), not save (just persisting the current entry) — reusing it would have been misleading. Resets to the active project via `key={projectId}` on remount.
- **Tag-category placeholders (`{tags.*}`)** (2026-07-04) — new placeholders `{tags.era}`, `{tags.genre}`, `{tags.intent}`, `{tags.mood}`, `{tags.lang}`, `{tags.energy}`, `{tags.production}`, `{tags.tempo}`, one per tag `category` value (`{tags.lang}` aliases the `language` category — chosen for readability, mirrors the existing `{links.key}` naming pattern). When multiple selected tags share a category, one is picked at random. Shared resolver `resolveTagCategoryPlaceholders()` added to `descriptionTagHelpers.js`, wired into all three template-filling engines: `fillHookTemplate` (`generateShortHooks.js`, needed `projectConfig` threaded through — it didn't receive it before), `renderTextTemplate`/`resolveTransformation` (`generateCustomBlocks.js`), and `fillTemplate`/`buildGenericTitles` (`generateTitles.js`, needed `transformationTags`/`projectConfig` added to its `values`). Autocomplete lists (`HOOK_PLACEHOLDERS`, `TextBlockEditor.jsx`, `AdvancedDescriptionFields.jsx`) all pull from a new shared `TAG_CATEGORY_PLACEHOLDERS` constant in `hookPlaceholders.js` so the three lists can't drift out of sync. Note: `{tags.lang}` only resolves for the `hebrew`/`russian` tags — the only two tags carrying the `language` category in either project.
- **Title output polish** (2026-07-04) — removed the `"Title:"` label prefix from generated titles (`GeneratedTitle.jsx`); every generated title now has its first letter capitalized regardless of source (transformation-phrase titles, generic titles, and hook-sourced titles all previously could start lowercase when a template began with a placeholder value like `{originalGenre}` or a tag-category placeholder). New `capitalizeFirst()` helper in `generateTitles.js`, applied to the base title before prefix/suffix wrapping in all three title builders.
- **Hook blocks honor their configured Lines count** (2026-07-04) — previously only `statusLines` and `technicalLines` (hardcoded in `generateBroadcastBlock.js`/`generateTechnicalBlock.js`) actually picked more than one random line per generation; every other hook block — including user-created custom ones — always output exactly one random line no matter what its "Lines" slider/max was set to in Project Settings → Blocks → Hook Blocks. New shared `pickRandomLines(pool, count)` and `resolveHookBlockOutput(layoutKey, projectConfig)` in `generateCustomBlocks.js` fix this generically (reads `hookBlockCounts`/`hookBlockMaxLines`/`countDefault`/`countMax` for any block key) and are now used by the generic hook-block resolution path in both `generateDescriptions.js` (Long) and `generateShortDescriptions.js` (Shorts). `statusLines`/`technicalLines` refactored to use the same `pickRandomLines` helper instead of their own inline shuffle+slice, so there's one implementation of "pick N random lines" project-wide.
- **Shorts Queue stores id references, not entry snapshots** (2026-07-04) — the queue used to freeze full entry snapshots at randomize time, so editing a queued song's tags/notes afterward showed stale data until the next full randomize (the same class of bug as the stale-`savedEntries`-snapshot incident noted in CLAUDE.md, but in the shorts queue instead). `useShortsQueue.js`: `getCoverId` now prefers `entry.id` (falls back to the old `artist::song` composite key for legacy data); the queue state (`queueIds`) stores ids only; `queue` is derived by resolving each id against live `savedEntries` on every render, so downstream edits always show up. `normalizeQueueEntry` auto-migrates old snapshot-shaped queues (detected via `typeof item === 'object'`) to id-shape on load — no manual migration step. `ShortsQueuePage.jsx` skips rendering a slot when its id no longer resolves to any saved entry (e.g. the entry was deleted).
- **Tag Editor sub-tabs fixed** (2026-07-02) — `TagEditorTabs.jsx`'s pill-chip buttons (visually identical to filter chips elsewhere, and prone to wrapping into a broken multi-row grid at narrow tag-card widths — a container-width issue, not a viewport one) replaced with the existing `SubTabNav` component. `SubTabNav`'s CSS (`.subtab-nav`) now scrolls horizontally instead of wrapping (`overflow-x: auto`, `flex-wrap: nowrap`), fixing this for all 6 `SubTabNav` usages, not just Tag Editor. Dead `.tag-editor-tabs` CSS removed.
- **Thumbnails split into own Generator panel + source nav** (2026-07-02) — `GeneratedTitlePair.jsx` renamed to `GeneratedTitle.jsx` (title-only, no more embedded "Thumb:" row). New `ThumbnailsPanel.jsx` shows thumbnails independently via `CollapsiblePanel`, visible for both Long and Shorts (the engine already generated Shorts-flavored thumbnails, just discarded before — see `generateThumbnails.js`'s `patterns.shorts` pool). `generateThumbnails.js` now returns `{ text, source }` objects instead of plain strings, tracking which `words`/`fallbacks`/`genericTagTemplates` entry or per-tag `thumbnail` phrase produced each one. Clicking a thumbnail navigates to its source: new `thumbnailsTarget`/`openThumbnailsSearch`/`clearThumbnailsTarget` (mirrors `titlesTarget`) for Project Settings → Thumbnail Templates, or the existing `onOpenSourceTag`/`openTagLibrarySearch` (now wired through `TagEditor.jsx`/`TagTitlesTab.jsx` for `field: 'thumbnail'`/`'title'`) for tag-sourced phrases. `TemplateGroupCard` now auto-expands when `highlightText` is set, via a derived `isCollapsed = collapsed && !highlightText` (not `setState` in a `useEffect` — the project's ESLint config forbids that) — fixes a latent gap that also affected the existing Shorts Hooks nav. Thumbnail count is now its own "Generation" slider (`thumbnail.count`, 1–10, added to both projects' `projects.json`) instead of being tied to `titles.length`.
- **`originalGenre` Stage 2 — faithful hooks** (2026-07-02) — new `original` Shorts Hook type added to both projects' `projectOverrides.shortHookTypes` (`requiresGenre: true`, no `excludeForFaithful`), templates: `"The best {originalGenre} song of {year}"`, `"{originalGenre} perfection since {year}"`, `"Still the greatest {originalGenre} track from {year}"`. Content-only, no engine changes — uses the existing `requiresGenre` filter in `generateShortHooks.js`.
- **Maxx Dee `transformation` hook type — genre wording added** (2026-07-02) — it already had `requiresGenre: true` with no genre-referencing templates; added `"{originalGenre} turned {primaryTag}"` and `"From {originalGenre} to {primaryTag} in one cover"` so the flag is justified. Content-only.
- **`originalGenre` Stage 3 — contrast hooks engine** — `requiresGenre`/`excludeForFaithful` flags added to `generateShortHooks.js` hook-type filtering; a `contrast` Shorts Hook type added to both projects (`excludeForFaithful: true`, `requiresGenre: true`, templates using `{originalGenre}` + `{primaryTag}`, e.g. `"A {originalGenre} classic, but {primaryTag}"`). Fires only for non-faithful tags when a genre is set. (Commit `63ce426`, previously undocumented here.)
- **Hook Blocks scope sub-option + short desc placeholder fixes** — `resolveHookOverride` (`generateCustomBlocks.js`) handles string-or-array song overrides for all description engines. `ProjectSettingsHookBlocks` gained an Override Type dropdown (Textarea/One-line) per song-scoped hook block. `generateShortDescriptions` catch-all now uses `renderTextTemplate`, fixing `{transformation}`/`{originalGenre}`/`{year}`/`{links.*}` not substituting in Shorts descriptions. New `LabelSelectRow` UI primitive.
- **`{transformation}` capitalization + coverLine placeholder fix** — `resolveTransformation` capitalizes the first letter; Shorts `coverLine` now uses `renderTextTemplate` so all placeholders resolve.
- **Sticky header transparency** — `AppHeader` fades from transparent to the page background once scrolled past the top, via an `IntersectionObserver` sentinel.
- **Titles/Descriptions panel headers navigate to Project Settings** — clicking either panel title in the Generator jumps to the matching Project Settings section, via `onNavigateToSettings`/`openProjectSettings`.
- **Shorts title count respects the slider** — removed a hardcoded 5-title limit in `TitlesPanel`; `projectConfig.title?.count` now flows through.
- **Project Settings → Thumbnail Templates built** — new `src/components/projectSettings/ProjectSettingsThumbnails.jsx`, wired into `ProjectSettingsContent.jsx`. 5 cards (`tag-library tag-library--3col` grid, `TemplateGroupCard`): Generation (count slider), Words, Fallbacks, Generic Tag Templates, and a combined Patterns card (Long + Shorts as two `HookTemplateEditor noWrapper` instances in one card). Edits `thumbnail.count`/`.words`/`.fallbacks`/`.genericTagTemplates`/`.patterns.{long,shorts}` — the exact keys `generateThumbnails.js` reads; `buildResolvedProjectConfig.js` already merged these correctly, no engine changes needed. Added `placeholders` prop to `HookTemplateEditor`/`TemplateGroupCard` (default `HOOK_PLACEHOLDERS`, backward compatible) so Thumbnail fields can pass `[]` (words/fallbacks — no substitution at all in the engine) or the new `THUMBNAIL_TAG_PLACEHOLDER` (`{tag}` only, for Generic Tag Templates) instead of the misleading full hook-placeholder list. Click-to-source nav on generated thumbnails was added in a later pass (see above).
- **`CollapsiblePanel` extracted** — `HashtagsPanel`, `YouTubeTagsPanel`, `TitlesPanel`, `ShortHooksPanel`, `DescriptionsPanel` each hand-rolled the same `panel`/`panel-collapsed` + `panel-header` + `ToggleButton` shell. Now a shared `src/components/ui/CollapsiblePanel.jsx` (`label, visible, onToggle, onNavigate?, headerExtra?, children`). `onNavigate` covers Titles/Descriptions' clickable nav-title; `headerExtra` covers Titles' "Hooks: ON/OFF" toggle. Plain bare `.panel` usage (`ProjectSettingsPage`, `GeneratorPage`, `GeneratorResultsPanel`, `SavedLibrary`) is untouched — different, still-valid usage.
- **`OutputItem` extracted** — `HashtagsPanel`, `YouTubeTagsPanel`, `DescriptionsPanel` each hand-rolled `output-item terminal-block > text + CopyButton` (DescriptionsPanel had it twice, for Long and Shorts). Now a shared `src/components/ui/OutputItem.jsx`. `DescriptionsPanel` passes `textClassName={undefined}` + `textStyle={{whiteSpace:'pre-line'}}` to preserve its existing (deliberately different from the `output-text` default) visual style exactly — not unified.
- **UIKit audit: `CopyButton`/`FormField` documented** — both are real, actively-used components (`CopyButton` in every output panel; `FormField` in 7 files) that were completely missing from the UIKit catalog and CLAUDE.md. UIKitPage's Buttons tab now demos the real `CopyButton` instead of static markup; Inputs tab demos `FormField` alongside the raw `form-group` CSS reference.
- **`SavedEntryRow` extracted** — `SavedLibraryItem`, `ShortsQueueItem`, `TodoItem` each hand-rolled the same `saved-entry-row terminal-block` shape (signal number + artist/song title button + tags). Now a shared `src/components/ui/SavedEntryRow.jsx` with `middle`/`actions`/`actionsClassName` slots for the parts that differ. UIKit's "saved-entry-row pattern" Data Displays entry now demos the real component instead of duplicated markup.
- **Project Settings → General uses `TemplateGroupCard`** — Project Info / Actions / Backup cards now reuse `TemplateGroupCard` (made its template-list body conditional on `onUpdateTemplates` being passed, so it also works as a plain card shell) instead of hand-rolled `tag-card` markup. Actions card gained an "Open UIKit" button; `uikit` removed from the top `AppHeader` nav tabs, now reachable only from here via `onOpenUIKit`.
- **`originalGenre` field — Stage 1 complete** — `originalGenre` added to `defaultFormData` and generator input (below Signal Number / Year). `{originalGenre}` placeholder wired into `fillHookTemplate` (`generateShortHooks.js`), `fillTemplate` + `buildGenericTitles` (`generateTitles.js`), `renderTextTemplate` (`generateCustomBlocks.js`). Added to `HOOK_PLACEHOLDERS`, `TextBlockEditor` placeholder list, and `AdvancedDescriptionFields` text placeholder list. Also backfilled `{year}` in both editor placeholder lists (was in engine but missing from autocomplete).
- **Save/load audit + fixes** — `originalYear`, `originalGenre`, `useCustomArtistShort`, `artistShort` were missing from both `handleSaveEntry` and `handleLoadEntry` in `useSavedEntries.js` — now all persisted per entry. `useInputFormLogic` auto-fill effect guarded: only fills `artistShort` when the field is empty, preserving loaded/custom values.
- **Fix `useCustomArtistShort` in Long titles** — `buildTransformationTitles` and `buildGenericTitles` now use `artistShortFinal` when `useCustomArtistShort && artistShort` (not only in Shorts mode). `fillHookTemplate` also respects it so hook-sourced Long titles show the custom short.
- **Sticky AppHeader** — new `AppHeader.jsx` replaces deleted `AppMenu.jsx`. Contains nav (looped from `PAGE_LABELS`), project selector, and page title (`activePage` + `projectConfig.name`). Accepts `actions` prop for page-specific buttons (Regenerate on Generator page). `position: sticky; top: 0`. Removed `<h1 className="app-title">` from all 6 pages and `panel-header` wrappers where they only held the title. Mobile: `.app-header-title` stacks + Regenerate goes full-width.
- **`descriptionLayout.js` utility** — `buildHookBlockMaps(hookBlocks)` and `makeLayoutLabelResolver(...)` extracted from `LongDescriptionSettings` and `ShortsDescriptionSettings`. Both files had identical derivations for `allHookBlockLayoutKeys`, `hookBlockLabelMap`, `layoutKeyToBlockKey`, and `getLayoutBlockLabel`. Now shared from `src/utils/descriptionLayout.js`.
- **`detectItemType` extracted to `customBlocks.js`** — was duplicated in `AdvancedDescriptionFields.jsx` and `StructuredListEditor.jsx`. Now a shared export alongside `isListBlock`/`isTextBlock`.
- **`TitleGenerationCard` reset button** — replaced raw `<button className="tag-reset-button">` with `IconButton`.
- **Comments session** — documented non-obvious WHY in: `AdvancedDescriptionFields` (detectItemType fallback, phraseBlockScopes `?? 'song'` default, key-swap remount trick), `BasicSongFields` (song dedup asymmetry), `LongDescriptionSettings` (layoutIndex Infinity sentinel, findLastIndex insertion logic, supportBlock special case), `StructuredListEditor` (linkSuggestionsId scope, `_id` client key + strip-before-save), `LinksRegistryEditor` (isUserAdded/isOverridden states, --full class, onBlur skip-if-unchanged).
- **Generic / no-tags title mode** — `buildGenericTitles()` in `generateTitles.js`. Templates configurable from Project Settings → Titles → Generation card. Merged into the title pool alongside transformation and hook titles.
- **Title count slider** — `LabelSliderRow` in `TitleGenerationCard`, `count` field in title config, range 1–10.
- **Block cleanup (Maxx Dee)** — removed phantom `coverSummaryBlock` from Long layout + `KNOWN_BLOCK_META`; removed unused `tagLineFallbacks` hookBlock entry; added deduplication in `buildResolvedProjectConfig` to suppress stale `customHookBlocks` entries that duplicate JSON-defined blocks.
- **Fix blocks with no nav target** — `renovationBlock` (Maxx Dee Long) converted to a Text block in `customBlocks` (`scope: 'song'`, `target: 'long'`) so it has a nav arrow → Text Blocks tab and a per-song override field in the Generator. Illegal Mind Shorts layout key changed `"header"` → `"coverLine"` to match the `shortsHeader` hookBlock's `descriptionLayoutKey` — now navigable and consistent with Maxx Dee. `hook` in both projects' Shorts layouts now navigates to Shorts Hooks: `openShortHooksSearch` threaded from `App.jsx` down through `ProjectSettingsPage` → `ProjectSettingsContent` → `ProjectSettingsDescriptions` → `ShortsDescriptionSettings` as `onNavigateToShortHooks`; `getNavigateHandler` handles `'hook'` key as a special case.
- **Block renaming** — inline ✏ icon in `BlockEditorCard` header. Click to enter rename mode; Enter/blur saves, Escape cancels. Storage: `description.blockLabelOverrides[key]` for JSON-default List/Text blocks; `description.hookBlockLabelOverrides[key]` for JSON-default Hook blocks; direct `blockData.name` / `customHookBlocks[i].label` for user-created blocks. `hasOverride` and `onReset` updated to cover label overrides. `LongDescriptionSettings` and `ShortsDescriptionSettings` gained `getLayoutBlockLabel()` helper that checks all override paths so renamed labels appear in description layout cards immediately.
- **Description layout parity** — Available and Active Layout columns are now equal width (`1fr 1fr`). Available blocks render as `BlockInfoCard` cards (same style as Active), with `→` nav arrow and `+` add button. `BlockInfoCard` gained `onAdd` prop. Both column labels moved into a shared `desc-layout-header` row above the grid so the first card in each column aligns at the same Y. New CSS: `.desc-layout-header`, `.desc-col-label`. Navigation logic extracted into `getNavigateHandler()` shared by both `renderAvailableBlock` and `renderActiveBlock` in Long/Shorts settings components.
- **UIKit page** — living component catalog at `/uikit` nav route. 5 tabs covering all reusable primitives.
- **Docs folder** — `architecture.md`, `data-model.md`, `graph-report.md`, `project-overview.md`, `roadmap.md` added.
- **Description layout builder** — both Long and Shorts. Two-column: Available palette + Active Layout. Mobile stacks to sub-tabs. Block cards are `BlockInfoCard` with nav arrows pointing to editors.
- **Hook Blocks sub-tab** — config-driven from `description.hookBlocks` in `projects.json`. Target (Long/Shorts/Both) and Scope (Project/Song) per block. Lines slider. Phrase editor via `HookTemplateEditor noWrapper`.
- **Dynamic Hook Block creation** — users can add/delete custom hook blocks from the Blocks tab. Stored in `description.customHookBlocks`. Resolved into `projectConfig.description.hookBlocks` by `buildResolvedProjectConfig`.
- **Block nav with auto-expand** — clicking `→` on a `BlockInfoCard` in the Descriptions tab navigates to the block's editor and auto-expands it. Hook blocks → Blocks → Hook Blocks; List blocks → Blocks → Lists; Text blocks → Blocks → Text Blocks.
- **`hookBlockTargets` wiring** — hook blocks appear in the Shorts layout palette based on their `target` field (`'shorts'` or `'both'`).
- **`BlockEditorCard` extraction** — shared card shell (collapse, Scope/Target selects, `BlockActions`) used by all three block type editors.
- **Song block overrides** — `storyBlock` and `logBlock` migrated to `songBlockOverrides`. Override fields appear in the generator's Advanced panel when scope is `'song'`.
- **`customGear` → `gearBlock`** — dynamic song-scoped List block with mini list editor in the generator.
- **`customCta` → `customCtaBlock`** — Text block, `scope: 'song'`, per-song override field in generator.
- **`PlaceholderField`** — unified input/textarea with `{placeholder}` autocomplete. Used in hooks, text blocks, title prefix/suffix, bulk textarea.
- **`IconButton` / `MoveControls` extraction** — all icon-only and reorder buttons unified.
- **Tag duplicate button** — duplicates a tag's full override data in the Tag Library.
- **Tag sync fix** — sync now reads raw state, not registry; A-Z sort fixed.
- **Layout widening** — output column is now 3fr (was 1fr) vs 2fr input column.
- **Tag label fix** — duplicated tags (e.g. `verse_copy_copy_copy`) now show their label in descriptions. `buildTagLine`/`buildTagPhrase` resolve `projectConfig.tags[key].label` instead of using the raw key.
- **`{year}` placeholder** — `originalYear` field added to `formData` and generator input (side by side with Signal Number). `renderTextTemplate` now substitutes `{year}` in text block templates. Hook templates already supported it via `fillHookTemplate`.
- **Graphify knowledge graph** — `graphify-out/graph.json` built (510 nodes, 980 edges). Use `graphify query "..."` for codebase questions instead of grepping files. `graphify update .` after code changes (AST-only, no token cost).
- **Backup moved to Project Settings → General** — `AppBackupControls` removed from `AppMenu`, now lives at the bottom of `ProjectSettingsGeneral`. Added description text. Project ID and Project Name are now side by side (`.form-row`), Project ID uses a disabled `form-input` to match height.

---

## In Progress

**Block Group — 4th block type.** Phase 1 (Closing Block, 2026-07-11) plus two more real Groups added 2026-07-12 via the Custom Placeholders redesign — `logBlock` (both projects) and `broadcastBlock` (Illegal Mind) — see Recently Completed. These weren't built via the original plan's Phase 2/3 (Generated child type) — that approach is **superseded, not scheduled**; a Custom Placeholder wrapped in a trivial single-template Hook Block turned out to work as an ordinary Group child instead. Full original plan (Phase 2/3 sections now annotated as superseded): `C:\Users\Max\.claude\plans\you-write-too-much-spicy-crayon.md`.

**Custom Placeholders.** Phases 0-4 all shipped 2026-07-12 (Phase 4 itself superseded same day by the Group-based redesign — see Recently Completed), Phase 2's deferred piece (Tag Short Hooks editor autocomplete) also closed out same day, and **Phase 5 (Tag Editor config-driven tabs + override-merge centralization) shipped 2026-07-12 too** — see Recently Completed. The full arc is now complete. Full plan: `C:\Users\Max\.claude\plans\thatnk-you-i-have-ticklish-fern.md`.

**Generation V2 — SHELVED 2026-09-02. Do NOT resume.** The composition-engine direction (fragments → grammar → sentences from 1–3 selected tags, tag-combination system, "AI brain") was designed in full 2026-09-02, simulated against 7 real covers, and **rejected by the user**: the output ("Song — slower and heavier") is metadata converted to English, worse than the current generator's handwritten hook/title library. Full history + why: `C:\Users\Max\.claude\plans\quiet-static-between-two-true-tags.md` (its top now carries a SHELVED banner). **Keep for a later separate pass:** the confirmed bugs F1–F7 (esp. F4 `{originalGenre}` random-split, F6 `{decade}` "Classic" leak), decisions D1–D6, and 3 non-sentence-building ideas for improving the *existing* library (authored combination hook sets · claim-strength + re-ranking rules · combination-aware placeholder resolution + conditional firing). Also still-true live bug: Maxx Dee's `project_overrides.shortHookTypes.progress` override dropped `excludeForFaithful`, so `progress` fires on faithful non-re-records. None of this is scheduled.

**Two small additions replacing V2 — SHIPPED + live-tested 2026-09-02, CLOSED OUT. Current generator architecture untouched.** Commits: `dc66a0a` (Step 1 persistence), `cb745b5` (Step 2 pool), `466975a` (Step 3 editor + section), `ca13b00` (Step 4 AI prompt), `6c1ba31` (prompt tightening), `1f4d96e` (click-to-nav from output), `81b4686` (no trailing period), `fff42ef` (editor search box + Copy button moved inline with +Bulk). Follow-on work is the **separate "Reusable AI prompt authoring helpers" task** (see Next Recommended Tasks §5) — do not extend this feature further; generalize the pattern there instead.
- **Cover-specific Short Hooks — `formData.coverShortHooks: string[]`, a FLAT list (NOT categorized), attached to one saved cover.** Extra hooks with info unique to that cover (personal story, why it was covered, an unusual recording/arrangement/song-part detail, an anecdote, an in-joke). NOT `customTitleCandidates` (a discarded earlier misunderstanding). Storage: `jsonb` column `cover_short_hooks` on `saved_entries` DEFAULT `'[]'` (migration `0007`, **applied to Neon**), `coverShortHooks: []` in `src/constants/defaultFormData.js`, added to the 3 field-list touch points — `src/utils/savedEntries.js` save+load mappers, `server/savedEntryMerge.js` `mergeImportedEntry` (`preferNonEmpty(Array.isArray(...) ? ... : [], existing?.coverShortHooks)`, same as `transformationTags`), `server/routes/savedEntries.js` `rowToEntry`+`upsertEntry` (17 cols, `$17::jsonb`). No new endpoint, no `appBackup.js` change. **Generation:** `generateShortHooks.js` `pickShortHooks` appends one internal `{ type: 'cover', label: 'Cover-Specific', hooks: pickRandomLines(viable, 2) }` group after the eligible-type groups — pool = the flat list, no eligibility filters, same `fillPlaceholders` viability check + `pickRandomLines(_, 2)`, `sourceType: 'cover'`. Flows into `mixedRecords` + `buildHookTitles` like base/tag hooks. No `useGeneratedOutput.js` dep change (PICK memo re-runs on `entryLoadToken`; mid-session edits apply on next Regenerate). **Click-to-navigate (added 2026-09-02):** a cover hook in the Titles / Short Hooks output is a clickable `NavLinkButton` (cover branch in `GeneratedTitle.jsx` + `ShortHookTitles.jsx`) → new same-page target `coverHookTarget`/`openCoverHook`/`clearCoverHookTarget` in `useNavigationTargets.js` (force-opens `panelVisibility.coverHooks` + `input`, like `openSongOverride`), threaded `App.jsx`→`GeneratorPage.jsx`→`GeneratorResultsPanel.jsx`→`TitlesPanel.jsx` (output side) and →`InputForm.jsx`→`CoverShortHooksEditor.jsx` (landing side, passes the raw hook text as `TagPhraseEditor`'s `highlightText` → scroll+highlight the row, consumed on edit). New `generator` branch in `useStaleTargetClearing.js` clears it on nav-away. `sourceHook.sourceText` / `hook.sourceText` carries the raw unfilled template (verified preserved through both `renderShortHooks` and `buildHookTitles`), which is the exact `phrase === highlightText` match key. **UI:** own collapsible section in `InputForm.jsx` — a `ToggleButton` "+ Show Cover-Specific Hooks (N)" below the Advanced Options toggle, above `<InputFormActions>`, wrapped in `.cover-hooks-section` (`margin-top: var(--space-4)`, `index.css`, so the two toggle borders don't touch — `ToggleButton` itself untouched). New `coverHooks: false` in `defaultPanelVisibility`. New `src/components/input/CoverShortHooksEditor.jsx` renders `<TagPhraseEditor noWrapper searchable actionsSlot={<Copy AI Prompt>}>` (**`TagPhraseEditor` gained 3 optional props**: `noWrapper` — skip the `<details>`/`<summary>` shell, matching `HookTemplateEditor.noWrapper`; `searchable` — own search box above the list, filters which rows render by *real index* so edit/remove stay correct; `actionsSlot` — extra control in the +Add/+Bulk row via `AddBulkRow`'s new `extra` prop. All default off, so the ~15 tag-editor call sites are unaffected) with a formData adapter (`onUpdateTag={(_, u) => setFormData(p => ({...p, ...u}))}`, `parentField` undefined → `buildUpdate` returns `{ coverShortHooks: [...] }`). Add/edit/delete/bulk + `buildHookPlaceholders` autocomplete + search, all reused. The "Copy AI Prompt" button (`button-secondary`, 500ms "Copied ✔️", disabled + `data-tooltip` until artist+song set) sits inline next to +Bulk.
- **AI prompt for cover-specific Short Hooks** — `src/utils/coverPrompt.js` → `buildCoverHookPrompt(formData, projectConfig)`, one-way (no parse-back, no category parser), same convention as `tagPrompt.js`. Assembles: new per-project `promptContext` string (added to both projects in `projects.json`, near `name`); artist/song/`originalYear`/`originalGenre`; selected tags + `category`/`label`; resolved transformation phrase via `buildTagPhrase`; `signalNumber`/`artistShort` (if `useCustomArtistShort`)/`customHashtags`; Story/Renovation/Log-Notes text (`songBlockOverrides.storyBlock`/`.renovationBlock`/`.logBlock` + legacy `customStory`/`customLogNote`) plus a generic sweep of any other non-empty `songBlockOverrides` string (title-cased key); a compact optional `buildHookPlaceholders` list. Empty fields are omitted. Assignment text (tightened 2026-09-02 after first real use): ONE FLAT LIST of 8-12 hooks specific to THIS cover, one per line, no numbering/categories; **short (usually 5-12 words), one punchy thought not an explanatory sentence; compress Story/Log ideas into hooks, don't summarize; do NOT invent facts (only reference a section/instrument/arrangement decision/anecdote/reason if the CONTEXT supports it); never put signal numbers / hashtags / channel metadata in a hook (context only)**; explicitly NOT generic transformation lines ("X but heavier" etc.) — those come from the tag/global system. Three illustrative-tone/length examples included. Pastes straight into the editor's Bulk box. **UI:** "Copy AI Prompt" button in a `.button-row` above the editor in `CoverShortHooksEditor.jsx`, 500ms "Copied ✔️", disabled with a `data-tooltip` until artist+song are set. Verified via rolldown smoke test (full + empty-fields output) + `eslint`/`vite build` clean.

---

**(historical, superseded by the SHELVED note above) Hook/Title generation quality — investigation trail.** Full plan: `C:\Users\Max\.claude\plans\quiet-static-between-two-true-tags.md`.
- **Stage 0's blocker is gone** — the Neon migration means live override data is read/writable via the API; the "no live channel, use export/import" conclusion is superseded. Get fresh data from Neon (`GET /tag-overrides` / `/project-overrides`) each stage. `buildResolvedProjectConfig` still merges bootstrap `projects.json` + overrides with **overrides winning wholesale at the category level**, so `projects.json` edits are invisible for overridden fields — fix those via the API.
- **Stage 1 A1 + A2 shipped** (commit `c0748a8`, engine-verified): removed the `"This chorus still hits"` leak from Maxx Dee's base `emotion` pool, and the dead `{releaseyear}` token (both projects' `projects.json` + Illegal Mind's live Neon `chorus` tag override). B1–B4 (generic filler, hardcoded `"2026"`) deferred to Stage 5.
- **Stages 2 + 3 investigated** (no code changed): traced the engine + ran it 250×/cover against live Neon data for 5 real Maxx Dee covers. Key mechanism: **there is no composition step** — per hook category the engine pools base + each selected tag's phrases and picks 2 at random, so every hook describes exactly one tag; `{transformation}`/`{primaryTag}`/`{tags.<cat>}`/`{originalGenre}`/`{decade}` each collapse to one fact. 7 confirmed failure classes F1–F7 (no nuance; hardcoded "heavier"/"faster" firing regardless of tags; Progress dominating with re-record implications; `contrast` the only partial composer; identical generic filler across all covers; `{decade}` "Classic" leak; `{tags.genre}` fix only helps genre tags).
- **Decisions D1–D6 recorded as direction** (design, not code): **D1** real composition (combine several selected facts into one truthful hook), **D2** intensity from tag *relationships* (Faithful+Heavier ⇒ "somewhat heavier") — investigate before adding a manual field, **D3** Progress eligible only when `re-recorded` selected, **D4** explicit selected-tag priority/order (likely drag-and-drop reorder of selected tags) — design first, **D5** investigate deriving decade/age/`fresh`-`nostalgic`-`classic` from `originalYear` (inspect era-tag usage first), **D6** `contrast` = original-genre→transformed-genre only (Faithful+Heavier is NOT contrast).
- **Two new ideas recorded:** (A) a per-song AI-generated title/hook pool — user free-text + structured tag data → AI → ~10-20 song-specific candidates as an extra pool (distinct from correction #6's "no stale prose as input" — this is fresh curated candidates, a separate pool; storage = new `saved_entries` field/endpoint); (B) a deliberately-designed AI-generation prompt/template explaining placeholders, tag/hook categories, tag priority, faithful-vs-transformative intent, forbidden assumptions (Punk ≠ faster, Metal ≠ heavier), factual-truth + clickability requirements — reusable for both reusable templates and idea (A).
- **A full "generic composition model" (fragments / `compositionGrammar` / declarative `compositionRules` / `composeCandidates.js` / subset enumeration / order-weighting / `{fromGenre}`→`{toGenre}` / `ageClasses`) was designed 2026-09-02, simulated via a throwaway `v2sim.mjs` against 7 real covers, and REJECTED same day** — see the SHELVED note at the top of this section and the plan file's SHELVED banner + "(historical, shelved)" section for the complete design and the reasoning. Nothing from it is being implemented.

**Persistence — localStorage → Neon/PostgreSQL, all architecture decisions resolved + a dependency-audited 14-step migration sequence written 2026-08-31, still nothing implemented.** Surfaced as a side effect of the Hook/Title investigation above (Stage 0's "Claude can't see live data" finding), but the actual driving problem is bigger and predates that investigation: the user works across multiple PCs, and the only sync mechanism today is manual export → send the JSON through Telegram → import on the other machine — this is the thing being eliminated, not an AI-convenience nicety.

**Decided (2026-08-31):** localStorage holds only temporary/device-specific state (`ui`, and the Generator's in-progress `formData`); Postgres is the sole source of truth for everything persistent — **5 live domains**: `savedEntries` incl. Todo, `tagOverrides`, `shortsQueues`, `uploadCalendar`, `projectOverrides`. (`tagVisibilityOverrides` was originally listed as a 6th domain — dropped 2026-08-31, confirmed dead legacy state, see the Phase B renumber note below; it is not migrated.) `projects.json` stays code, untouched. No offline-first sync for v1 — the app requires connectivity to persist. Backup/export is demoted to a safety net only, no longer the cross-PC transport. **Schema is relational** (real per-entity tables, not one JSONB blob) — explicit user call: willing to accept more upfront migration work rather than build a second migration later. Auth is a static API key/shared secret (env var per PC). Conflict handling is plain last-write-wins, no detection — the user only ever works from one PC at a time in practice, so this is deliberately not over-built.

**Migration execution constraint (hard, governs every future step):** small, independent, resumable steps only — matches the user's real working pattern (frequently stops abruptly, resumes later, sometimes from the other PC). Every step: one small logical unit → fully verified → this plan file + `current-context.md` updated with exactly what changed/tested/remains/next → committed → safe non-ambiguous stopping point. Old localStorage data is never deleted/transformed until the full migration is verified complete.

**Dependency audit (2026-08-31, real code, not guessed):** the only real cross-domain references are `shortsQueues`→`savedEntries` and `uploadCalendar`→`savedEntries` (both via entry ids); `tagOverrides`/`tagVisibilityOverrides`/`projectOverrides` depend on nothing. Produced a verified 14-step sequence (Step 0-13), split into **Phase A** (Steps 0-6: build schema+API for all 6 domains, zero changes to the running app — pure additive infra, trivially safe to pause anywhere) and **Phase B** (originally Steps 7-12; **renumbered to Steps 7-11 + a final cleanup Step 12 on 2026-08-31** — see the Phase B renumber note below). Phase B cuts over one domain at a time: `tagOverrides` → `projectOverrides` → `savedEntries` → `shortsQueueItems` → `uploadCalendarSlots`, the last two ordered after `savedEntries` per the real FK dependency — then **Step 12**, final cleanup, deliberately held open pending a real soak period, not scheduled immediately. (The original plan opened with a `tagVisibilityOverrides` pilot cutover; deleted — dead legacy state, see below.) Two small sub-decisions flagged inline (not blocking): whether `tag_overrides`/`project_overrides` store their dynamic/config-driven nested content as a JSONB payload column within an otherwise-real relational row (proposed, awaiting confirmation before Steps 2/3 are built), and where the API layer physically runs (this app has never been deployed anywhere — no hosting config exists in the repo today).

**Every architecture decision is resolved. Step 0 (DB + API foundation) is done and verified, 2026-08-31 — Step 1 is next.** The two items flagged when the sequence was first presented are both closed: the JSONB-payload-column reading of "relational" (real rows for stable entities like saved songs/tags/queue slots/calendar slots; JSONB only for genuinely dynamic, user-extensible nested config like hook block templates/custom placeholders — explicitly not one giant blob recreating localStorage) was approved as proposed; API hosting is decided as **a local Node/Express server run per-PC** (not deployed/hosted anywhere for v1) — started together with the Vite dev server via one `npm run dev` command from the same repo, both PCs' local API processes connecting to the same shared remote Neon database.

**Step 0, what actually shipped:** a real Neon project (`illegal-mind-generator`, org "Maxx", `eu-central-1`/Frankfurt — closer to the user in Israel than the US default) with a `dev` branch (Auto-delete explicitly turned off — it needs to persist indefinitely, not the default "after 1 day"), never touching the default/production branch. Declined Neon's own "set up with your coding agent" auto-flow (would install a CLI + MCP server + broad account access) in favor of manually copying one connection string — keeps Claude's access scoped to exactly the `dev` branch, nothing more. New `server/` directory: `index.js` (Express app + `/health`), `authMiddleware.js` (static-API-key check via `Authorization: Bearer`), `db.js` (lazy `pg` pool — works correctly whether or not `DATABASE_URL` is set yet, no code change needed either way). `server/.env` (git-ignored, one per machine, `server/.env.example` committed as the template) holds a generated random API key + the real Neon `dev`-branch connection string. `npm run dev` now runs both `vite` and the new API server together via `concurrently`. Verified: unauthenticated/wrong-key requests correctly 401; correct key with no DB configured yet correctly returns `db: "not configured"`; after the Neon branch was created, the same endpoint correctly returned `db: "connected"` against the real database. `npm audit --omit=dev` clean (0 production vulnerabilities). **Not yet done:** the second PC's own `server/.env` hasn't been set up or tested — small, non-blocking gap, worth closing before treating "both PCs verified" as true.

**Step 1, what actually shipped:** a minimal, dependency-free migration runner (`server/migrate.js` + `npm run migrate` + numbered `.sql` files in `server/migrations/`, tracked via a `schema_migrations` table — re-running is a safe no-op for already-applied files, reused for every remaining Phase A step) applied `tag_visibility_overrides (project_id, tag_name, visible)` to the real Neon `dev` branch; new `GET`/`PUT /tag-visibility-overrides` endpoints (returning `{ [tagName]: boolean }`, the exact shape localStorage already has, so the eventual hook swap needs no reshaping). `eslint.config.js` gained a Node-globals block scoped to `server/**/*.js` (was failing on bare `process` before — the original config only had browser globals). All 9 endpoint checks passed against the real `dev` branch (missing-param 400, empty-project 200/`{}`, upsert, in-place update via `ON CONFLICT`, cross-project isolation, unauthenticated 401, missing-field 400); test data cleaned up after. `eslint`/`vite build` both clean. One environment gotcha worth remembering: background node processes from Bash tool calls don't reliably die via `kill $!` on this Windows/Git-Bash setup — use `netstat`+`taskkill //PID` instead for future steps' manual server verification.

**Step 2, what actually shipped:** `tag_overrides (project_id, tag_name, label, category, visible, is_custom, payload jsonb)` applied to the real Neon `dev` branch; `GET`/`PUT`/`DELETE /tag-overrides` for single-tag CRUD (`PUT` does a genuine partial merge — scalar columns via `COALESCE`, `payload` merged one level deep via Postgres's `||` operator, matching `updateTagOverride`'s `{...prev, ...updates}` exactly) plus `POST /tag-overrides/sync` and `POST /tag-overrides/copy-tag`, both transactional server-side ports of `syncProjectTags`/`copyTagFromProject`. **One deliberate, documented deviation**: the merge algorithm (`mergeTagData`/`mergeDescription`/`mergeShortHooks`) was *copied* into a new server-only `server/tagMerge.js` rather than imported from `useTagOverrides.js`, specifically so `src/` stayed genuinely untouched this step — flagged as a temporary duplicate to consolidate when Step 8 touches that hook anyway. New `server/projectsConfig.js` reads `src/config/projects.json` directly (same bootstrap data, second runtime, no duplication). Verified against the real `dev` branch with real project/tag data (`illegalMindCovers`/`maxxDeeCovers`): CRUD + partial-merge semantics, sync (confirmed correctly merged 21 base tags, and confirmed a subtle-but-correct result — Maxx Dee's own `heavier.visible: false` from `projects.json` correctly won over the synced-in source value, per `mergeTagData`'s target-wins-for-scalars rule), copy-tag, and same-project-rejected validation on both. Test rows cleaned up after. `eslint`/`vite build` clean (same 4 pre-existing unrelated errors).

**Step 3, what actually shipped:** `project_overrides (project_id primary key, settings jsonb)` on the real Neon `dev` branch; `GET`/`PATCH`/`DELETE /:field /project-overrides` (PATCH uses Postgres's `||` jsonb operator — same top-level shallow-merge semantics as `updateProjectOverride`, single upsert, no read-modify-write needed) plus `PUT /project-overrides/short-hook-types` (a genuine cross-project write, matches `syncHookTypesToProject`). Verified via a full endpoint pass (confirmed PATCH does a wholesale top-level key *replace*, not a deep merge — matches the real semantics exactly) and a dedicated `buildResolvedProjectConfig` round-trip regression check: a representative settings object (thumbnail config, todoStatuses, nested `description.templates.long.customBlocks`, blockGroups) sent through the real API and read back produces identical resolved config either way. One real, harmless finding along the way: Postgres's `jsonb` type doesn't preserve original object key order (well-documented, unrelated to data integrity) — the verification script's first pass used strict string equality and failed on this; fixed with a canonical/order-independent comparison, all 7 checks then passed. Test data cleaned up after. `eslint`/`vite build` clean.

**Standing rule added this session**: before any Phase B step imports real user data into Neon, stop and ask for a fresh Backup export taken at that moment — never reuse an older export already inspected.

**Step 4, what actually shipped:** `saved_entries` on the real Neon `dev` branch — every field from CLAUDE.md's documented save/load list as a real column (Todo split into `todo_status`/`todo_notes`), `transformation_tags`/`song_block_overrides` as jsonb. **Composite primary key `(project_id, id)`, not `id` alone** — caught before writing the migration: `buildEntryId(artist, song)` doesn't include `project_id`, so the same artist+song pair can legitimately exist as separate entries in both Illegal Mind Covers and Maxx Dee Covers, a real (not hypothetical) case for this app. 7 endpoints on `server/routes/savedEntries.js`, each a direct port of one `useSavedEntries.js` function (`GET` list, `PUT /:id` wholesale save, `PATCH /:id` partial update, `PATCH /:id/todo`, `DELETE /:id`, `POST /bulk`, `POST /import`). `server/savedEntryMerge.js` is the same deliberate-temporary-duplication pattern as Step 2 — `mergeImportedEntry` and friends copied from `src/utils/savedEntries.js`, to consolidate at Step 10.

Verified against the real `dev` branch: full CRUD pass including confirming `PATCH` only touches the fields it's given; bulk-add's intra-batch dedup; and **the critical regression check** — CLAUDE.md documents a real past data-loss bug where importing a thin/older export wiped fields like Todo status that weren't in the import. Reproduced the exact scenario: imported a thin record (only `originalYear`) against an entry with a real Todo status/hashtags/song-block-override/tags already set, confirmed the new field applied while every untouched field survived completely intact — then confirmed a field the import *did* provide correctly won. One real mid-verification catch, not a server bug: an early manual test used a hand-typed URL id instead of the actual computed `buildEntryId` value, creating a duplicate row — redone correctly, confirming the server's own endpoints were consistent with each other the whole time. Test data cleaned up after. `eslint`/`vite build` clean.

**Step 5, what actually shipped:** `shorts_queue_items (project_id, position, saved_entry_id)` on the real Neon `dev` branch — real rows with a real position column, not an array, and a **composite** foreign key `(project_id, saved_entry_id) → saved_entries(project_id, id)` (a plain single-column FK, as the plan originally drafted before Step 4's PK correction, wouldn't even have been valid — caught and fixed while writing this step's migration). `GET`/`PUT`/`POST /mark-uploaded` on `server/routes/shortsQueue.js`; `server/shortsQueueMerge.js` is the now-familiar temporary-duplication pattern (`getValidReplacement`/`isTooClose`/etc. copied from `useShortsQueue.js`, to consolidate at Step 11). The randomization pick itself deliberately stays client-side for now — `PUT` just durably stores whatever ordered list it's given.

Verified against the real `dev` branch with 5 real seeded `saved_entries`: empty state, validation, auth, and — the main point of this step — a `PUT` containing one bogus id alongside real ones was rejected with a genuine Postgres FK-violation error, and a follow-up GET confirmed the queue was still fully empty, not partially written (the whole transaction rolled back cleanly). A valid `PUT` and a `POST /mark-uploaded` (remove + replacement-append) both worked correctly; one result looked surprising at first (the replacement matched an entry already earlier in the queue) but tracing the real spacing algorithm confirmed it's correct — `isTooClose` only checks the most recent `spacing` positions, not the whole queue, exactly matching the original client logic. Test data cleaned up after. `eslint`/`vite build` clean.

**Step 6, what actually shipped — closes out Phase A entirely:** `upload_calendar_slots (project_id, iso_date, video_type, planned_entry_id, uploaded_entry_id)` on the real Neon `dev` branch, primary key the same 3-column combo as today's `slotKey`, both entry-id columns nullable with the same composite-FK pattern as Step 5. **A real timezone footgun was fixed at the driver level before it could ever cause a bug**: `pg` returns Postgres `DATE` columns as JS `Date` objects by default, and converting those back to a date string later can silently shift by a day depending on local timezone — fixed globally in `server/db.js` (`types.setTypeParser(1082, ...)`, OID 1082 = date) so every date column, this one and any future one, passes through as the exact string Postgres sends, no `Date` conversion anywhere. `GET`/`PUT /slot`/`DELETE /slot` on `server/routes/uploadCalendar.js` — `PUT` is a read-merge-write patch matching `patchSlot`'s real behavior exactly, including its auto-delete-the-row-when-both-fields-go-empty rule (not just "set both null and leave an empty row").

Verified against the real `dev` branch: empty state, validation, auth; the date round-tripped exactly (`"2026-09-01"` in, `"2026-09-01"` out — the timezone fix confirmed, not assumed); **a real drift case** (`plannedEntryId` and `uploadedEntryId` set to two different entries on the same slot, both correctly coexisting — exactly what `deriveSlotStatus` needs to detect "uploaded-drift"); a partial clear leaving the other field intact, then clearing both — confirmed the row was auto-deleted, not left empty; a bogus entry id correctly FK-rejected with nothing written. Test data cleaned up after. `eslint`/`vite build` clean.

**Phase A (Steps 0-6) is now fully complete** — all 6 domain tables and endpoints exist and are verified against the real database, with zero changes to `src/` across the entire phase; the running app remains 100% localStorage-based.

**Phase B renumbered 2026-08-31 — `tagVisibilityOverrides` is confirmed dead legacy state, not migrated.** At the start of Phase B (fresh Backup export in hand), `grep` confirmed `src/hooks/useTagVisibilityOverrides.js` has **zero importers anywhere** in `src/`; nothing reads the `tagVisibilityOverrides` storage field (only `storage.js` declares it `{}`; `appBackup.js` doesn't name it, it only rides the wholesale blob stringify); no tag-resolution file touches it. The data is also **provably stale** — the export's `tagVisibilityOverrides.illegalMindCovers.heavier = false` contradicts the authoritative `tagOverrides.illegalMindCovers.heavier.visible = true` ("Heavier" is in fact shown). This matches the long-standing "dead code" note under Architectural Notes / Known Issues below. **User's call:** the old Step 7 pilot cutover of this domain is **deleted** — those 4 legacy rows are not imported into Postgres. The unused `tag_visibility_overrides` table + endpoints (built in Step 1), the dead hook, and the storage field are all removed in **Step 12 (final cleanup)** instead. `tag_overrides` becomes the new **Step 7** and the real first live cutover; old Steps 8-13 renumber down to 7-12. Phase B is now 5 live-domain cutovers (Steps 7-11) + cleanup (Step 12), not 6. Migration stays incremental/checkpointed exactly as before.

**Step 7 (`tag_overrides` cutover) — code shipped 2026-08-31 in two commits on `main`; live UI verification is user-owned and pending.** Sub-decisions were settled with the user first: (a) transport/auth = **Vite dev-server proxy injects `Authorization`** (`/api/*` → `localhost:8787`, key from `server/.env` via `dotenv` in Node context, never in the client bundle); (b) `src/`'s tag-merge helpers **deleted** — `server/tagMerge.js` is the sole copy.
- **Commit A `ac1cc7f`** (inert scaffolding + import, no app behavior change): `vite.config.js` proxy; new `src/utils/apiClient.js` (`apiGet`/`Put`/`Post`/`Delete` + `ApiError` + in-flight GET de-dupe); new `server/imports/0007_tag_overrides.js`; `.gitignore` excludes `illegal-mind-generator-backup-*.json`. **Imported 40 `tag_overrides` rows** (26 `illegalMindCovers` + 14 `maxxDeeCovers`) into the Neon `dev` branch — verified every row canonical-deep-equal to the backup export, column/payload split correct, and `buildResolvedProjectConfig` byte-identical from the API vs the localStorage blob for both projects.
- **Commit B `ef0617c`** (the hook swap): `useTagOverrides.js` no longer touches localStorage — `GET` on mount/project-switch, `loading`/`error` **derived** from a `result.projectId !== projectId` mismatch (no synchronous setState in the effect — that lint rule is an error here; first attempt tripped it, reworked). Optimistic shallow-merge write → reconcile with server row; silent `reload()` resync on failure. `syncProjectTags`/`copyTagFromProject` → `POST /tag-overrides/sync|copy-tag` (merge is server-side; `*BaseTags` args dropped). Dead `mergeTagData`/`mergeDescription`/`mergeShortHooks`/`mergeUniqueArray` deleted from `src/`. `App.jsx` holds first render + every project switch behind a `.app-loading` gate (retry screen on load failure) so generation / Tag Library never flash base-only tags. `TagLibraryPage.jsx` Sync/Copy handlers are async with success + failure toasts. Two `useTagOverrides` instances kept; `apiClient` GET de-dupe coalesces their concurrent fetch. `eslint`/`vite build` clean (same 4 pre-existing unrelated errors).
- **Pending:** ⚠️ **restart the Vite dev server** (picks up the new `vite.config.js` proxy), then the live checklist in the plan's Step 7 card (edit phrase persists via `PUT /api/tag-overrides`, visibility toggle, reset, Sync, Copy-tag, project switching, Generator tag picker, `localStorage` blob untouched). **Rollback if it fails:** `git revert ef0617c` → localStorage-only tags again, the untouched blob still authoritative.
**Step 8 (`project_overrides` cutover) — code shipped 2026-08-31 in two commits on `main`; live UI verification user-owned and pending.** Used the same 2026-08-31 export (user re-confirmed no app data changed).
- **Commit A `4ea16a5`**: new `server/imports/0008_project_overrides.js` (`PATCH /project-overrides` per project). Imported 2 rows (`illegalMindCovers` 6 keys, `maxxDeeCovers` 9 keys) — verified `settings` deep-equal the export and `buildResolvedProjectConfig` round-trip byte-identical (localStorage blob vs API).
- **Commit B `5668624`**: `useProjectOverrides.js` → API, identical shape to `useTagOverrides` (GET on mount/switch, derived loading/error, optimistic `updateProjectOverride` PATCH + reconcile, `resetProjectOverride` DELETE, `syncHookTypesToProject` PUT). `apiClient.js` gained `apiPatch`. `App.jsx` load gate + retry now cover both async hooks. Every Project Settings tab keeps its call signatures — untouched. Automated: GET/PATCH/DELETE contract exercised against the real `dev` branch, row left byte-identical to the export. `eslint`/`build` clean.
- **Pending:** live pass — Thumbnail/Hashtags slider + base-entry edits persist across reload; hook block template edit; reset a field; sync hook types to the other project; project switching; Generator output unchanged for a fixed input; `localStorage.projectOverrides` untouched. **Rollback:** `git revert 5668624`.
- **Next:** Step 9 — `saved_entries` cutover (the big one; endpoints + `savedEntryMerge.js` built in Step 4).

**Step 9 (`saved_entries` cutover) — code shipped 2026-08-31 in two commits on `main`; live UI verification user-owned and pending.** Used a **fresh** export (`illegal-mind-generator-backup-2026-08-31 (1).json`, 13:14) — not Steps 7-8's.
- **Commit A `485213e`**: `server/imports/0009_saved_entries.js` drives `POST /saved-entries/import` (NOT a raw per-row `PUT` — an audit found 13 `illegalMindCovers` entries with legacy `customStory`/`customLogNote` and no matching `songBlockOverrides` key that a raw PUT would have silently dropped; `/import`'s `mergeImportedEntry` folds them into `song_block_overrides.storyBlock`/`.logBlock`). Imported **68** entries (17 + 51). Verified: exact counts; all 68 rows canonical-deep-equal to `mergeImportedEntry(exportEntry, undefined)` — byte-identical to what the app's own Import Library produces; only session-only keys (`videoType`/`changesMade`/`extraVibeNote`) dropped; legacy folding + Todo + tags + songBlockOverrides confirmed.
- **Commit B `0855f70`**: `useSavedEntries.js` → API, same async shape as the other two hooks (optimistic writes + reconcile, silent `reload()` on failure; `handleImportEntries`/`handleAddEntries` use the server merge/bulk endpoints and take the returned full list). `src/utils/savedEntries.js` cleaned up (−126 lines): deleted `mergeImportedEntry`/`preferNonEmpty`/`isEmptyValue`/`normalizeEntryIds`/`loadInitialSavedEntriesByProject` + the `storage` import; kept `buildEntryId`/`toSlug`/`buildEntryFromFormData`/`buildFormDataPatchFromEntry`. `server/savedEntryMerge.js` is now the sole home of the import-merge logic. `App.jsx` load gate + retry cover all three async hooks. Every consuming page (Generator/Saved Library/Shorts Queue/Calendar/Todo) untouched. Automated: PUT/PATCH/PATCH-todo/DELETE/bulk contract exercised on the real `dev` branch, imported rows left byte-identical afterward. `eslint`/`build` clean.
- **Live QA passed** (2026-08-31) — Saved Entries / Todo working correctly from Neon.

**`shortsQueues` will NOT be migrated — stays local (user decision, 2026-08-31).** The Shorts Queue is a randomized working list regenerable on demand from the Saved Library + `excludeFromRandomizer`; the exact queue/order is explicitly not worth syncing between PCs. It's now classified as transient/device-specific state alongside `ui` and `generator.formData`. `useShortsQueue.js` is untouched (keeps localStorage). The old Step 10 (`shorts_queue_items` cutover) is **skipped, nothing imported**. The Phase A `shorts_queue_items` table + `/shorts-queue` endpoints + `server/shortsQueueMerge.js` become dead infra — torn down in final cleanup (now Step 11) alongside the `tag_visibility_overrides` teardown. Renumber: old Step 11 (`upload_calendar_slots`) → **Step 10** (the last live cutover); old Step 12 (final cleanup) → **Step 11**.

**Step 10 (`upload_calendar_slots` cutover) — code shipped 2026-08-31 in two commits on `main`; live UI verification user-owned and pending.** Fresh export `illegal-mind-generator-backup-2026-08-31 (2).json` (13:53).
- **Commit A `d2d0be1`**: `server/imports/0010_upload_calendar_slots.js` (`PUT /upload-calendar/slot` per slot). FK pre-check clean (0 orphans). Imported **77** slots (41 maxxDeeCovers + 36 illegalMindCovers) — verified: exact counts; all 77 `{plannedEntryId, uploadedEntryId}` byte-identical to the export per slotKey; `DATE` round-trips as exact string (Step 6 driver fix).
- **Commit B `f23120b`**: `useUploadCalendar.js` → API, same async shape as the other hooks. GET slot-array → `{slotKey: {...}}` map; optimistic `patchSlot` (merge + delete-when-both-null) → `PUT /slot` → reconcile; `removeSlot` → `DELETE /slot`; `addToNextOpenSlot` stays sync-returning with a `loading` guard. Pure exports (`getMonthGrid`/`deriveSlotStatus`/`findNextOpenSlot`/`buildSlotKey`) unchanged. `CalendarPage.jsx` shows "Loading calendar…" / retry; Generator/ShortsQueue untouched; no `App.jsx` change (per-page hook). Automated: PUT/DELETE/FK-rejection contract exercised on the real `dev` branch, 77 rows left byte-identical afterward. `eslint`/`build` clean.
- **Pending:** live pass — Calendar loads with existing slots; derived statuses right; plan/confirm/clear/Ctrl+Click-change-plan/drag a slot persists across reload; Add-to-Calendar from Generator/ShortsQueue; Import-from-YouTube; project switching; `localStorage.uploadCalendar` untouched. **Rollback:** `git revert f23120b`.

**Migration progress: all 4 migrated live domains code-complete (`tag_overrides` ✅ live-verified, `project_overrides` ✅ live-verified, `saved_entries` ✅ live-verified, `upload_calendar_slots` ⏳ live-QA pending). After Step 10 sign-off → real-world soak period across both PCs, then Step 11 (final cleanup: dead-localStorage removal for the 4 domains + `tag_visibility_overrides` & `shorts_queue_items` teardown + Backup/Restore repoint + CLAUDE.md/AGENTS.md Storage Architecture rewrite). Deliberately staying local: `shortsQueues`, `ui`, `generator.formData`.**

**Soak period — SECOND-PC SETUP IS PENDING and is part of the soak (do not start Step 11 until it's done).** **Update 2026-08-31 (later same day):** the main desktop PC's own `server/.env` did not actually exist yet either when checked this session (despite Phase A/B's Neon verification work having happened from this codebase) — it was hand-created this session: `DATABASE_URL` from the Neon console's `dev` branch (project `illegal-mind-generator`, org "Maxx"), and a freshly generated `API_KEY` (no prior key existed anywhere to reuse). `npm run dev` confirmed `/health` → `db: "connected"`, and the user visually confirmed real data round-tripping with the **work laptop** (their actual second/other machine) — i.e. the main desktop and the laptop are NOT yet both connected; only the main desktop is, using data the laptop had previously written via some other path. **The work laptop still needs the full setup below, and it needs the exact same `API_KEY` now sitting in the main desktop's `server/.env`** (copy it over, don't regenerate — a mismatched key just 401s). Note: an AI session's local memory (Claude Code's own per-machine memory store) does NOT sync between PCs — only this file (`docs/current-context.md`), tracked in git, is guaranteed to be visible on both machines. Don't rely on an assistant "remembering" this across a device change; rely on this doc.

**`server/.env` is git-ignored, so it does NOT arrive via `git pull`** — it must be hand-created on the second PC with the same `API_KEY` and same Neon **dev-branch** `DATABASE_URL` as the main PC. The user has said they will not remember the procedure: **when a future session hears "I'm on the other PC" / "I'm on the laptop" / "help me connect this machine to the new DB", recognize the situation and walk through it interactively, one small step at a time.** Full step list is in the plan file's "Soak period — Second-PC setup" section: pull code → `npm install` → create `server/.env` → set `API_KEY` (copied from the main PC, not regenerated) + dev-branch `DATABASE_URL` + `PORT` → `npm run dev` → `/health` check → confirm the app loads the SAME shared Neon data as the main PC → confirm a write round-trips → confirm local-only `ui`/`formData`/`shortsQueues` stay per-device (expected to differ, not a bug).

Full detail for every decision and all steps is in `C:\Users\Max\.claude\plans\one-signal-many-terminals.md`. **Read that file in full before resuming this thread.** The Hook/Title investigation above stays paused until this persistence work reaches a stable point (at minimum, through Step 11 — full cutover of all five live domains).

---

## Next Recommended Tasks

1. **Audit findings investigation queue** (in progress, started 2026-08-29) — a general project review surfaced 15 items (below), not yet triaged. **Process, explicitly requested by the user — do not deviate:** this is an investigation queue, not an implementation plan. Go through items **one at a time only** — do not batch, do not auto-fix/refactor/delete/redesign anything. For each item: (1) inspect the current code and verify the issue still exists, (2) explain the current behavior in practical product terms, (3) give a concrete example of when the user would encounter it while using the app, (4) categorize as bug / UX improvement / technical debt / worth-checking, (5) **stop and wait** for the user's decision: fix / keep as-is / postpone / remove from the list. Some unusual behavior may be intentional given how the user actually uses the app — never assume. One item (`customHashtags`) was already reviewed this way in a prior pass and the user chose to keep it as-is (parked, working). `docs/roadmap.md` is explicitly excluded from this queue — a separate roadmap-rewrite session is planned first (establish what the app is/how it's used/what's finished vs evolving, then rewrite `roadmap.md` from scratch rather than patching it).

   Queue, in order:
   1. ~~TagEditor: `<details open={shouldOpenEditor(...)}>` has no `onToggle`~~ — **resolved, not a bug**: empirically verified (jsdom + this project's exact React 19.2.5) that a same-value re-render does not reset a manually-toggled `<details>` — React only writes the `open` attribute when the computed value actually changes. Dropped from the queue.
   2. ~~TagEditor: `activeTab` local state not reset when switching projects~~ — dropped by the user without investigation: "I'm not interested in those small things... there's a big Tag Library rework we'll need to discuss later." Folded into that future rework conversation (see item 3 in this same list, "Tag Library card feels too complex") rather than treated as a standalone fix.
   3. ~~Todo status badges in Saved Library~~ — **shipped 2026-08-29**, see Current Focus. Went through one live-feedback round on badge styling (bracketed metadata look, not a bordered action button) before landing.
   4. ~~Saved Library Todo filtering~~ — **removed from the queue 2026-08-29**, not built. User's reasoning on reflection: this would duplicate a job the Todo tab already does better (sectioned by status), while the real need — cross-referencing a song's Todo state from Saved Library — is already solved by the badge/jump-link (item 3, above). Kept as a design note, not just a dropped task: badges/links for lightweight cross-referencing, don't rebuild a page's own organizational view somewhere else.
   5. Shorts descriptions tag awareness — pull tag-specific phrases into Shorts description blocks (long-standing open item, unscoped).
   6. `coverLabel` settings UI — only editable by hand-editing `projects.json`.
   7. Calendar: click-to-navigate-and-highlight to the day just added — `addToNextOpenSlot` call sites already return `{isoDate, videoType}`, cheap to wire via `useNavigationTargets.js`.
   8. Titles' "mix hooks into Long titles" isn't on the live-refresh path (`generateTitles.js:154-159`) — every other output panel live-updates on Artist/Song edits without re-picking; this one still needs Regenerate.
   9. Long description block-nav can't distinguish a block's contributing pieces (e.g. Log Notes = wrapper Hook Block + `{custom.tagLogLine}` placeholder, joined) — needs real design work, not scoped.
   10. Calendar: "suggested songs to add" — still deferred, unscoped.
   11. `useTagVisibilityOverrides.js` — confirmed fully dead code, zero importers anywhere outside its own file. **Re-confirmed 2026-08-31 at Phase B start, and its data confirmed stale** (contradicts `tagOverrides.visible`) — the persistence migration will NOT migrate this domain; the hook, the `tag_visibility_overrides` table/endpoints, and the `tagVisibilityOverrides` storage field are all removed in migration Step 12 (final cleanup). Until then, the storage field stays for data-shape compatibility.
   12. `ProjectSettingsContent.jsx`'s "Coming Soon" fallback is unreachable — all 11 registered sections in `projectSettingsSections.js` have a matching branch now.
   13. `generateTitles.js`/`pooling.js`'s `hasEmpty` destructure — `resolveRecordText` never actually returns `hasEmpty`, so `if (hasEmpty) continue` never fires. Unconfirmed whether this causes a visible bug (see Known Issues below).
   14. Two direct `updateAppStorage` call sites bypass the hook layer (`SavedLibrary.jsx`, `GeneratorPage.jsx`) — acknowledged tech debt.
   15. CSS refactor — consolidate `index.css`, clean up class naming. Broad, lowest urgency.

2. **Upload Calendar Phase 2** (in progress) — Phase 1 fully shipped (2026-08-05, all 7 steps). First Phase 2 sub-feature, the "Import from YouTube" paste-import parser, is now also fully shipped (2026-08-05, see Current Focus) — including two same-day follow-up fixes (stale-plan-drift resolution, song-title fallback matching + confirmation UI). Plan file: `C:\Users\Max\.claude\plans\yes-let-s-do-this-prancy-shell.md`. Remaining Phase 2 sub-features, still unscoped: a "suggested songs to add" feature; click-to-navigate-and-highlight from Shorts Queue/Generator to the specific day just added (both `addToNextOpenSlot` call sites already return the assigned `{isoDate, videoType}`, so this is cheap to add whenever picked up — follow the existing `useNavigationTargets.js` convention); flagging a *scheduled* day that has nothing planned/uploaded once its date passes (today's model only flags a day that had an unconfirmed plan). Original Phase 1 plan file (for the deferral rationale): `C:\Users\Max\.claude\plans\i-think-the-time-wild-floyd.md`.
3. ~~**Tag Library card feels too complex**~~ (2026-07-02) — **shipped 2026-09-03 as the Tag card drill-down IA rework, see Current Focus.** Flat 6-tab editor → `Tag → content overview → content type → pool editor` drill; Basics behind an "Edit Basics" button; Short Hook categories config-driven from `projectConfig.shortHookTypes` (Contrast now editable). Absorbed queue items 1.1 (`<details>` `onToggle` — verified a non-issue) and 1.2 (`activeTab` project-switch carryover — fixed by the `${projectId}:${tag.name}` card key).
4. **Large-file refactor pass, started 2026-07-28, now effectively complete** — user asked to start splitting files over 250 lines. Done (see Recently Completed): `blocks/HookBlocks.jsx` (497 → 125), the `descriptions/Long`/`ShortsDescriptionSettings.jsx` pair (272/273 → 132/138, deduped — they were near-duplicates), `hooks/useSavedEntries.js` (400 → 208), `savedLibrary/SavedLibrary.jsx` (337 → 161), `projectSettings/blocks/Placeholders.jsx` (274 → 164), `projectSettings/blocks/BlockGroups.jsx` (261 → 234), `components/input/AdvancedDescriptionFields.jsx` (265 → 57), `engine/descriptions/generateCustomBlocks.js` (325 → 201), `engine/titles/generateTitles.js` (305 → 196), `App.jsx` (351 → 300, a partial/honest reduction, still flagged below), and `hooks/useAppShellState.js` (312 → 183, cleared the threshold). **Only `App.jsx` (300 lines) remains flagged, and it's judged a permanent partial-reduction case** — like `pages/UIKitPage.jsx`, its remaining bulk is the inherent six-page routing JSX with heavy prop-threading, not something to force into an abstraction the codebase doesn't otherwise use; both safe seams it had were already extracted. `engine/placeholders.js` and `src/utils/hookBlockOverrides.js` are both deliberately left alone — the former has a genuine mutual recursion (token resolvers call `fillPlaceholders`, which resolves tokens via the `REGISTRY` those same resolvers populate) that blocks a pure-move split, the latter is a flat list of independent pure functions. Don't re-flag either without a materially different approach. Re-run `find src -type f \( -name "*.jsx" -o -name "*.js" \) -not -path "*/node_modules/*" -exec wc -l {} \; | sort -rn | awk '$1 > 250'` to refresh the list below if resuming this pass — but at this point every remaining candidate has already been investigated and is either done, permanently accepted as partial, or deliberately skipped:

   | File | Lines | Notes |
   |---|---|---|
   | `pages/UIKitPage.jsx` | 748 | Component catalog/demo page — likely leave alone, length is inherent to cataloging many components |
   | `engine/placeholders.js` | 389 | Deliberately left alone — see above, don't re-flag without a different approach |
   | `src/utils/hookBlockOverrides.js` | 331 | Deliberately left alone — flat list of independent pure functions, don't re-flag |
   | `App.jsx` | 300 | Root component — two safe seams already extracted (see Recently Completed); remaining bulk is inherent six-page routing JSX, permanent partial-reduction case like `UIKitPage.jsx` |

5. ~~**Reusable "Copy AI Prompt" authoring helper for phrase-pool editors**~~ — **DONE, shipped 2026-09-03** (see Current Focus for full detail). Built as `src/utils/authorPrompt.js` (skeleton: `buildAuthorPrompt` + helpers + `RULE`) + `src/utils/authorPromptContexts.js` (four adapters: `coverShortHooksContext` / `tagShortHooksContext` / `globalShortHookContext` / `titlePoolContext`, each with a `buildX*Prompt` wrapper) + `src/components/ui/CopyPromptButton.jsx`. Wired into Cover-Specific Short Hooks, Tag Short Hooks (per category), global Short Hook types, and Title template pools. **`coverPrompt.js` is now a 2-line re-export.** New per-tag `promptContext` field feeds the tag adapter (Tag Basics; rides in the override `payload` jsonb; all 51 tags populated). Key decisions: no `{transformation}` in AI-generated suggestions (repetitive), `{primaryTag}` is type-unsafe so a "PLACEHOLDER TYPES" section teaches the AI to use typed `{tags.*}` tokens instead. **Not built (deliberate):** Hook Blocks (mostly lore blocks, not hook pools — could be added per-block later), Title prefix/suffix (single-value fields, not candidate pools), DRYing the two near-identical "PLACEHOLDER TYPES" strings, wiring `promptContext` into `AddTagPanel`. 5 tag `promptContext` values flagged for user review.

---

## Known Issues

### Storage cleanup debt

- `useTagVisibilityOverrides.js` writes to a `tagVisibilityOverrides` field that nothing reads back through the hook interface — effectively dead (re-confirmed 2026-08-31, data also confirmed stale). Scheduled for removal in persistence-migration Step 12 (hook + `tag_visibility_overrides` table/endpoints + the storage field). Until then, the field inside the unified storage object stays for data-shape compatibility.

### `coverLabel` not editable from UI
Shorts Description uses a `coverLabel` block that is only configurable by hand-editing `projects.json`. No settings field exists.

### `customHashtags` is hardcoded
The per-song custom hashtag field in `AdvancedDescriptionFields.jsx` is still a raw `formData.customHashtags` field, not part of the block override system. It predates the block system and needs separate design work.

### `generateTitles.js`'s empty-candidate filtering may be dead code
Spotted 2026-07-30 while scoping the large-file refactor pass (not fixed — out of scope for that task). `pickTitles` destructures `{ text, hasEmpty }` from `resolveRecordText(...)`'s return value and does `if (hasEmpty) continue;` to skip a transformation-record candidate whose placeholders resolved empty — but `resolveRecordText` only ever returns `{ text, sourceHook, sourceTemplate }`, never a `hasEmpty` field, so `hasEmpty` is always `undefined` and the `continue` never fires. Same pattern in the generic-templates branch (`.filter((r) => !r.hasEmpty)` is always `true`). Unconfirmed whether this actually causes a visible bug (a template with an always-empty placeholder combination might just render with a gap instead of being excluded) — needs investigation before deciding whether/how to fix.

### Long description block-level click-nav can't distinguish a block's contributing pieces
Raised 2026-07-12 after the Log Notes/Custom Placeholders work: the Log Notes segment in a generated Long description is really two independently-resolved pieces joined together (the "Log · Format" wrapper Hook Block + the `{custom.tagLogLine}` placeholder's pooled tag text) — see Custom Placeholders in Recently Completed. Clicking the segment today routes to one place at block-level granularity (a pre-existing limitation, not new — see the Long description tooltip work's "Known limitation" note further down this file), so the tag-sourced "Modification: ..."-style line has no click-through to either its config (Blocks → Placeholders) or its real text source (the contributing tag's Descriptions tab in Tag Library). Shorts descriptions already have real per-line source tracking (`shortDescriptionSegments`); Long does not. Not scoped or planned yet — would need real design (research the existing Shorts per-line mechanism as precedent) before touching code.

---

## Important Files

| File | Why it matters |
|------|---------------|
| `src/config/projects.json` | Base config for both projects. All generation and block structure starts here. |
| `src/utils/buildResolvedProjectConfig.js` | Merges `projects.json` + `tagOverrides` + `projectSettingsOverrides` → resolved config. Called on every render. |
| `src/utils/customBlocks.js` | Shared utilities for all block types: `isListBlock`, `isTextBlock`, `detectItemType`, `getBlockLabel`, `generateBlockKey`, `SCOPE_OPTIONS`, `TARGET_OPTIONS`, `BLOCK_TYPE_SUBTABS` (blockType → Blocks-tab subTab id/label, single source of truth — don't hand-roll this mapping), `resolveCustomBlockCollisions` (self-healing repair for customBlocks keys that collide with a hook block's key/layout-key, called from `App.jsx` on every project-config resolve). All block editors import from here. |
| `src/utils/descriptionLayout.js` | Shared helpers for description layout/label/source resolution: `buildHookBlockMaps(hookBlocks)`, `makeLayoutLabelResolver(...)` (layout-slot-key based labels, used by `LongDescriptionSettings`/`ShortsDescriptionSettings`), `makeBlockKeyLabelResolver(...)` (config-key based labels, used by `DescriptionsPanel`'s source tooltips), `resolveBlockSource(blockKey, {..., overridden})` (layout key → a `'block'`- or `'override'`-typed source descriptor — the single place both the Descriptions settings nav arrows and both engines' segment-building loops resolve a block's source; `overridden` param, default `false`, comes from `isSongOverrideActive()` in `generateCustomBlocks.js`). Import here before adding anything to Long/Shorts description settings or block-source labeling. |
| `src/utils/authorPromptContexts.js` | All four "Copy AI Prompt" authoring adapters (Cover / Tag Short Hooks / global Short Hooks / Title pools) + their `buildX*Prompt` wrappers. Assembled by `src/utils/authorPrompt.js`. Shared rules: no `{transformation}` in new AI suggestions, `{primaryTag}` type-safety. Keep the four in sync if placeholder semantics change. |
| `src/utils/tagContentSections.js` | Pure model for the Tag Library card drill-down: overview sections + live `tag.maps` count selectors, `getShortHookCategories(projectConfig)` (config-driven, incl. Contrast), `getInitialView`/`parentOf`/`resolvePoolEditorProps` nav helpers, `pickTagPhrasePlaceholders`, `isSourceMatch`. Consumed by `TagEditor.jsx`. |
| `src/config/contentSetupNav.js` | Pure nav model for Content Setup (the 4-section, ex-"Project Settings" workspace): `CONTENT_SETUP_SECTIONS`, `resolveContentSetupTarget` (legacy section ids + `onNavigateToSettings` strings + `blocksTarget.subTab` → `{section, leaf}`), `getSection`/`getSectionLeaves`. Consumed by `ProjectSettingsPage.jsx`. |
| `src/utils/storage.js` | Single storage API. All reads/writes go through `readAppStorage` / `updateAppStorage`. |
| `src/utils/appBackup.js` | Export/import. The safety net for all storage work. Must stay reliable. |
| `src/hooks/useProjectOverrides.js` | All project settings overrides (descriptions, blocks, titles, links, etc.) live here. |
| `src/hooks/useAppShellState.js` | Top-level UI state: activePage, formData, panelVisibility, navigation targets (tagLibrarySearchTarget, etc.). |
| `src/components/AppHeader.jsx` | Sticky app header: nav tabs (looped from `PAGE_LABELS`), project selector, page title, optional `actions` slot. Replaces deleted `AppMenu.jsx`. |
| `src/components/projectSettings/descriptions/LongDescriptionSettings.jsx` | Description layout builder for Long descriptions. Block nav wiring lives here. |
| `src/components/projectSettings/descriptions/ShortsDescriptionSettings.jsx` | Same for Shorts. |
| `src/components/projectSettings/blocks/HookBlocks.jsx` | Content Setup › Descriptions › Hook Blocks leaf. Config-driven from `projectConfig.description.hookBlocks` (+ patch helpers in `src/utils/hookBlockOverrides.js`). |
| `src/components/projectSettings/blocks/BlockEditorCard.jsx` | Shared card chrome for all block type editors. |
| `src/components/ui/BlockInfoCard.jsx` | Read-only card in description layout builder. `onNavigate` sets the `→` arrow. |
| `src/engine/descriptions/` | Long and Shorts description generation. Reads resolved config and formData only — no React, no storage. |
| `src/pages/UIKitPage.jsx` | Living component catalog. Check here before building any new UI pattern. |

---

## Architectural Notes

**Config-driven, not code-driven.** `projects.json` is the bootstrap template. All real per-project configuration lives in localStorage overrides merged by `buildResolvedProjectConfig`. Engine and UI consume the resolved config — never the raw JSON directly.

**Storage is a single key.** All live app state reads/writes `illegalMindGeneratorData`. The old standalone keys (`savedEntries`, `shortsQueueByProject`, `tagOverrides`, etc.) are inert. Do not write new code targeting them. `tagVisibilityOverrides` as a *field inside* the unified object must stay for data-shape compatibility.

**Pages orchestrate, components render, engine generates.** No generation logic in components. No UI logic in engine files. Storage access only through hooks or `storage.js` utils — the two known exceptions (`SavedLibrary.jsx` and `GeneratorPage.jsx` calling `updateAppStorage` directly) are acknowledged tech debt.

**`longPrefix` vs `prefix`.** Illegal Mind uses `longPrefix` (legacy). Engine reads `config.title?.prefix || config.title?.longPrefix`. Do not consolidate — both keys must stay.

**Block collision detection.** `generateBlockKey` must check all `customBlocks` keys regardless of block type (List and Text blocks share one key namespace) *and* every hook block's layout-key namespace (`key` and `descriptionLayoutKey`) — three separate namespaces that all funnel into one shared `layout` array. All three "Add block" forms (`ProjectSettingsTextBlocks.jsx`, `ProjectSettingsLists.jsx`, `ProjectSettingsHookBlocks.jsx`) build this combined `existingKeys` set before calling `generateBlockKey`. Missing any of the three produces a *silent* collision, not an error — a fixed real case (2026-07-10): a Text block named "Intro Block" generated key `introBlock`, colliding with the Hook block `introHook`'s `descriptionLayoutKey: 'introBlock'`. Since `resolveBlockSource`/`makeLayoutLabelResolver` (`descriptionLayout.js`) both check the hook-layout-key namespace *before* `customBlocks`, and `LongDescriptionSettings.jsx`'s `dynamicBlockKeys` filter excludes any customBlocks key already in `defaultLayout` (which `'introBlock'` is), the Text block became permanently orphaned — fully editable in Blocks → Text Blocks, but unreachable from the Descriptions layout builder in either column, with no error anywhere. If you hit an "existing block invisible in the Descriptions tab" report, check for exactly this key collision first.

**Hook Blocks vs description layout keys.** A hook block's `key` is its config/storage key; `descriptionLayoutKey` is its key in the layout array (may differ, e.g. `introHook` → `introBlock`). Both are needed. `layoutKeyToBlockKey` (and the other hook block maps) are derived by `buildHookBlockMaps()` in `src/utils/descriptionLayout.js`, shared by both Long and Shorts settings.

**Song-scoped block overrides.** Song-scoped Text/List/Hook blocks get override fields in the generator's Advanced panel. Overrides stored in `formData.songBlockOverrides[blockKey]`. Wins over project-default template at engine time via `getEffectiveSongOverrides`. Legacy fields (`customCta`, `customStory`, `customLogNote`) are seeded into `songBlockOverrides` on entry load in `useSavedEntries`.

**Backup is the safety net.** Any storage change must be tested with export/import. The backup format is version 3: `{ version, exportedAt, data: {unified storage}, legacy: {old keys} }`.

---

## AI Notes

**Reuse before creating:**
- `IconButton` — every small action button (reset, remove, move, labeled). Default class `tag-reset-button`; pass `button-secondary` for bordered look.
- `TemplateGroupCard` — any collapsible card with a template list and reset button. `ShortHookCard` is a thin adapter over it.
- `BlockEditorCard` — card shell for block editors (collapse, scope/target selects, actions). All three block type editors use it.
- `BlockInfoCard` — read-only card in description layouts. Set `onNavigate` for the `→` arrow; omit for non-interactive generated blocks.
- `SubTabNav` — lightweight underline tab nav. Use inside pages for sub-sections.
- `PlaceholderField` — any input that should support `{placeholder}` autocomplete. Don't build a custom autocomplete.
- `MoveControls` — up/down reorder. Don't hand-roll `IconButton` pairs for this.
- `HookTemplateEditor` — searchable phrase list with add/bulk. Always use this inside `TemplateGroupCard`, not directly in feature components.
- `AppHeader` — the sticky app header. Do not add `<h1 className="app-title">` in individual pages; titles live here, derived from `activePage` + `projectConfig.name`.

**Do not duplicate:**
- `customBlocks.js` — single source of truth for block utilities (`isListBlock`, `isTextBlock`, `detectItemType`, `getBlockLabel`, etc.) and the `BLOCK_TYPE_SUBTABS` blockType→subTab/label map. All block editors import from here.
- `descriptionLayout.js` — shared helpers for both description layout builders (`buildHookBlockMaps`, `makeLayoutLabelResolver`) plus `makeBlockKeyLabelResolver` for config-key-based label lookups (e.g. from a `source.blockKey`) and `resolveBlockSource` for block key → source descriptor (used by both Long/Shorts settings nav arrows and the Long/Shorts engines' segment attribution). Do not re-derive these maps or this branching inline.
- `storage.js` — all reads/writes go through here. Don't call `localStorage` directly.
- Description layout logic — `LongDescriptionSettings` and `ShortsDescriptionSettings` share the same structural pattern. Keep them in sync when changing either.
- `descriptionTagHelpers.js`'s `resolveTagCategoryPlaceholders()` — the only place `{tags.<category>}` tokens are substituted. Used by `generateShortHooks.js`, `generateCustomBlocks.js`, and `generateTitles.js`. Keep `hookPlaceholders.js`'s `TAG_CATEGORY_PLACEHOLDERS` in sync with `TAG_CATEGORY_ALIASES` if categories change.
- `generateCustomBlocks.js`'s `pickRandomLines()`/`resolveHookBlockOutput()` — the only place that resolves a hook block's final multi-line output (count-aware). Don't hand-roll shuffle+slice for a new multi-line block; extend `resolveHookBlockOutput` or the block's `hookBlockCounts` config instead.

**Side-effect-sensitive areas:**
- `buildResolvedProjectConfig` — runs on every render. Changes here affect every page simultaneously.
- Tag overrides — editing a tag immediately changes titles, descriptions, hooks, and hashtags. Test the full generator output after tag system changes.
- `appBackup.js` — must export the complete `illegalMindGeneratorData` object. Any new top-level storage key must be added here or it won't survive backup/restore.
- Description layout arrays — stored in `projectSettingsOverrides.description.layout` (Long) and `projectSettingsOverrides.description.shortsLayout` (Shorts). If you add a new block key to `projects.json`, it won't appear in existing user layouts until the user adds it from the Available palette.
- `hookBlocks` config shape — each entry needs `key`, `label`, `path`, `templateKey`. `descriptionLayoutKey` only when layout key differs from `key`. Don't add entries without checking all three places: config resolution, engine template reading, and the description layout builder.

**UIKit page** — navigate to it in the app (`/uikit`) to see live interactive examples of every UI primitive before building anything new.

---

## Session Summary

The app is functionally complete for its core feature set. Recent sessions fixed a shorts-queue staleness bug, consolidated placeholder resolution into a shared engine registry (`src/engine/placeholders.js`) with empty-valued-line filtering, added tag-category placeholders (`{tags.*}`), polished title output, added a save-to-any-project dropdown, and shipped a visual refresh of the Generator's Input form (structural markup cleanup + de-emphasized labels + sans-serif UI chrome + lighter input/chip borders, global CSS but scoped to execution details — color palette unchanged). This paragraph is stale — see Current Focus for what's actually current; a proper rewrite is planned as part of the upcoming roadmap session (see Next Recommended Tasks item 1's note on `docs/roadmap.md`).
