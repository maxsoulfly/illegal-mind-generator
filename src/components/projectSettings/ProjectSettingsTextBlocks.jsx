// Placeholder for the Text block type (single paragraph, optional
// {placeholders}) — create/edit/delete UI not built yet. Existing Text
// blocks like mixingCtaBlock are still plain strings in customBlocks.
export default function ProjectSettingsTextBlocks() {
  return (
    <p className="tag-summary">
      Text block editing is coming soon. Existing text blocks (e.g. Mixing
      CTA) are still active in descriptions but not yet editable here.
    </p>
  );
}
