# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Server secrets (Neon connection string, API key) — one server/.env per
# machine, never committed. server/.env.example documents the required keys.
server/.env

# Graphify knowledge graph (regeneratable outputs)
graphify-out/graph.html
graphify-out/cache/
# Graphify machine-specific and temp files
graphify-out/.graphify_python
graphify-out/.graphify_root
graphify-out/.graphify_uncached.txt
# Graphify dated backup directories (created by graphify update)
graphify-out/20*/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local to-do / checklist
_checklist.md

# Backup exports (real user data) — used as one-time import sources during the
# localStorage -> Postgres migration; never committed.
illegal-mind-generator-backup-*.json
